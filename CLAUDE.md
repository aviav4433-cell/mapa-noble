# MAPA-NOBLE ERP — הקשר קבוע לסשן עבודה

קרא קובץ זה במלואו לפני כל שינוי. עודכן: 26.07.2026 (סשן תכנון, אחרי B47).
**כל המספרים כאן נספרו מהקוד החי** — אם עבר זמן, ספור שוב. הקוד גובר על כל מסמך.

## מה המערכת

מערכת ERP לשני עסקים: MAPA (השכרת מפות וטקסטיל לאירועים) ו-NOBLE (מכבסה תעשייתית).
הבעלים: אבי. אינו מתכנת — כל הנחיה אליו פשוטה, צעד-אחר-צעד, בעברית.
פרוטוקול העבודה המחייב: `WORKING_PROTOCOL.md` בידע הפרויקט. ההיקף לכל batch: `BUILD_PLAN.md`.

## ארכיטקטורה (נכון ל-v4.37-B47 ממשק / v4.35-B45 שרת)

- **שרת:** Google Apps Script, קובץ יחיד. **נמצא בריפו בשם `קוד שרת.txt`** (6,579 שורות).
  משיכה: `raw.githubusercontent.com/aviav4433-cell/mapa-noble/main/%D7%A7%D7%95%D7%93%20%D7%A9%D7%A8%D7%AA.txt`
  נקודת כניסה יחידה `doPost` → `route`.
- **נתונים:** Google Sheets — **55 טבלאות**. הגדרת העמודות באובייקט `TABLES` בראש קובץ השרת.
  `setupDatabase` הוא נתיב יצירת הטבלאות היחיד; מורץ פעם אחת אחרי כל שינוי סכימה.
- **ממשק:** `index.html` יחיד (9,782 שורות), עברית RTL, GitHub Pages. **29 מסכים** במערך `VIEWS`
  (כולל `superadmin` המוצג רק ל-IS_SUPER_ADMIN).
- **פעולות שרת:** 134 מקרים ב-`route`, מהם 27 ב-`READ_ONLY_ACTIONS` (בלי LockService ובלי audit).
- **ריפו:** `aviav4433-cell/mapa-noble`, ענף `main`. אתר: https://aviav4433-cell.github.io/mapa-noble/
- **Endpoint:** משתנה `API_URL` בראש הסקריפט ב-index.html. אסור לשנות אותו.

## שלושה הקשרי ריצה (כל נגיעה ב-DB נבדקת בשלושתם)

1. **צוות** — כניסה רגילה, `DB` מלא.
2. **פורטל לקוח** — `?portal=1`, `DB = null`.
3. **חנות ציבורית** — `?shop=1`, `DB = null`, `SHOP` מלא.

## פרוטוקול תקשורת ממשק↔שרת (קריטי — כאן נולדו באגים)

- `POST` עם גוף JSON: `{ action, payload, token?, password?, user? }`.
- **הטוקן ברמה העליונה** (`body.token`); השרת מקבל גם `payload.token` (תאימות). אל תשבור.
- כניסה: `login` עם `{ username, password }`. סשן 6 שעות ב-CacheService, מכיל `{ employee_id, role, name }` — `name` נחוץ לבדיקות ההרשאה.
- "זכור אותי": `rememberLogin` / `loginByRemember` (hash ב-ScriptProperties).
- `route` בודק הרשאות לפי **שם** עובד (`user`), לא id. פעולות מנהל דורשות עובד פעיל בתפקיד 'מנהל'.
- **טקסטים ארוכים (G1):** `getAll` קוצץ שדות ארוכים (notes.text, tasks.details, meetings.protocol, task_comments.text) ל-300 תווים עם דגל `_trunc:1`. שליפה מלאה: `getFullTexts {table, ids}`. **אסור לפתוח טופס עריכה בלי `ensureFullTexts` קודם** — שמירה תדרוס את המקור.
- **B38 — סינון שדות רגישים:** `b38ScrubEmployees` מסיר `pin` ו-`password_hash` מכל payload יוצא; במקומם `has_password`/`has_pin`. המספר האישי (`pin`) נשלח **למנהל בלבד**. אימות מספר אישי של מנהל — `b38VerifyManagerPin` **בשרת בלבד**, עם הגבלת 5 ניסיונות.

## אבטחה — חוקים שאסור להפר

1. סיסמאות עובדים רק כ-hash: `sha256WithSalt(password, employee_id)`. לעולם לא טקסט גלוי.
2. המשתנה `PASSWORD` בראש קובץ השרת — שריד שהוחלף למחרוזת אקראית. אל תבנה עליו.
3. אין פונקציות עזר עם סיסמאות גלויות; אין סיסמאות/טוקנים בקבצי הריפו.
4. אין מספרי כרטיס אשראי ב-DB — token מחברת הסליקה בלבד. credentials של ספק סליקה — ScriptProperties בלבד (הכרעה S4).
5. פריסת Web App: הרצה "אני", גישה "כל אחד". האבטחה במנגנון הטוקנים.
6. תפקיד שאינו מנהל לעולם לא דורס שדות `pin`/`password_hash`.

## כללי כספים ותאריכים

- כספים במנוע התשלום (B2) — **באגורות** (integer); המרות דרך `fromAg`/`toAg`.
- **כלל מע"מ (B45):** כל סכום נקלט ומוצג **נטו**, עם הכולל בכתב קטן לצידו. חריג יחיד: מסמך חשבונית/קבלה.
- **תאריכים:** השוואות תאריך בממשק תמיד ב-`ymdLocal()` (זמן ישראל), לעולם לא `toISOString()` (UTC). שדות תאריך מהגיליון מנורמלים ב-`dYmd()` לפני השוואה.
- **גבול יום (הכרעת אבי, 26.07.2026):** יום עסקים = יום קלנדרי מ-00:00. **אין** "שעת תחילת יום" קבועה בשום חישוב. "תחילת יום עבודה" בדוחות ניצולת נגזרת מהאירוע הראשון בפועל (הפעלת מכונה ראשונה / נוכחות ראשונה).

## פריסה — התהליך הנכון

- `index.html` → commit ל-main → Pages מתעדכן תוך דקות → רענון קשיח (Ctrl+Shift+R).
- קובץ שרת → אבי מדביק קובץ **מלא** בעורך Apps Script → שמירה → פריסה ← ניהול פריסות ← **עריכת הפריסה הקיימת** ← גרסה חדשה ← פרוס. **לעולם לא פריסה חדשה.**
- שינוי סכימה → לומר לאבי במפורש "יש להריץ setupDatabase". אין שינוי → לומר "אין שינוי סכימה".
- canary בפורמט `v4.XX-B##` על מסך הכניסה (שורה ~430 ב-index.html) + בכותרת קובץ השרת.

## מלכודות ידועות

- **PDF (עד תיקון B46):** `invoiceSaveToDrive` משתמש ב-`Drive.Files.insert` — **Drive API v2 כשירות מתקדם**. אם השירות לא מופעל / מופעל כ-v3, הקריאה נכשלת בשקט וה-fallback שומר **HTML** במקום PDF. כל מסמך שיוצא ללקוח נראה כזבל. תיקון מלא ב-BUILD_PLAN סעיף B46.
- עמודת `pin` בטבלת העובדים **בשימוש פעיל** מאז B36 (שעון נוכחות, אימות מנהל). מסמכים ישנים שקראו לה "שריד" — שגויים.
- **תפריט B47:** דסקטופ ≥900px — שורת תפריט אופקית sticky עם חלוניות (`NAV_DD`); מובייל — אקורדיון (`NAV_OPEN`). שני משתנים נפרדים בכוונה. פריטי הקטגוריות תמיד ב-DOM (עטיפת `.gmenu`). נהג — סרגל טאבים תחתון (`DRIVER_TAB_ORDER`), `renderNav` יוצא מוקדם. אל תיגע בלי הוראה מפורשת.
- דפוס אריח ציוד אחיד: `.eqtile`/`.eqgrid` (עגלות ומכונות, `rAssets`). אריחים חדשים — באותו דפוס, לא דפוס שלישי.
- קבצים שאבי מעלה לצ'אט עלולים להיקטע — ודא סיום ב-`</html>` / פונקציה שלמה. אם קטוע — משוך מהריפו.
- אל תסמוך על מסמכים לגבי מצב הקוד — קרא את הקוד החי. כבר קרה פעמיים שמסמך תיאר גרסה שלא נמסרה (B46 "הישן").
- פעולה חדשה שכותבת לגיליון — לעולם לא ל-`READ_ONLY_ACTIONS`.
- הזמנות כביסה עם הובלה (`delivery_fee > 0`) — כיוון המשלוחים **הפוך** מהזמנת השכרה (איסוף בתאריך התחלה, אספקה בתאריך חזרה).
- משלוח שנוצר אוטומטית — בלי נהג; לא מופיע במסלולי נהגים עד שיבוץ. חובה בלוק "⚠ טרם שובץ נהג".
- מפרט חסר = עצירה מלאה. לא בונים מהשערה.
