# MAPA-NOBLE — תור המשימות המאוחד

נכתב: 29.07.2026 · עודכן **18.08.2026 — B79 נמסר (`WASH-23ג` חלק א', ציר המשלוחים) · חלק ב' נספר ולא תוקן** · canary בייצור: **v4.79-B79** (ממשק ושרת) · האצווה הבאה שתיבנה: **B80 = `WASH-23ג` חלק ב'**

> ⚠⚠ **תיקון תיעוד — 18.08.2026.** הקובץ הזה **לא עודכן ב-B78**: חלק 11 החזיק
> עדיין את ה-OPENER של B78, והכותרת אמרה שהאצווה הבאה היא B78. `PROGRESS.md`
> כן עודכן. B78 (`WASH-23ב` — payroll · tasks · purchases · deliveryTrips)
> **נמסר ונפרס**, 601 בדיקות. הפער תוקן כאן. ⛔ לקח: קובץ מתגלגל שלא הוחזר
> לריפו מייצר OPENER שגוי לצ'אט הבא — לוודא בפתיחת כל סשן שחלק 11 תואם ל-canary.

> ⭐ **`WASH-22` (17.08.2026):** B75 סגר את ציר ה**סוג**. אותה מחלקת כשל
> חיה בציר ה**סטטוס** — `RESERVING_STATUSES.indexOf(o.status)` ·
> `B40_OUT_STATUSES`/`B40_RESERVED_STATUSES` · רשימות סטטוס ביומן ובהתראות.
> **הזמנה מאושרת שהסטטוס שלה בגיליון נושא רווח קשיח אינה משריינת מלאי.**
> ⛔ B75 לא טיפל בו בכוונה: איחוד שני הצירים באצווה אחת היה מכפיל את שטח
> הבדיקה על המלאי, ו-`M6` מחייב עצירה. **מוקצה ל-B76.**

> ## ⚠ שינוי מבני — 30.07.2026
> **הכלל "פריט אחד לצ'אט" בוטל. הכלל החדש: "אצווה אחת לצ'אט, ואצווה יכולה להכיל כמה פריטים."**
> 16 הפריטים שנותרו קובצו ל-**8 אצוות** לפי אזור קוד, לא לפי גודל. הכללים המחייבים
> למיזוג — חלק 9א. **אין לפצל אצווה מאוחדת בחזרה לפריטים בלי הכרעת אבי.**

**מקורות שמוזגו לכאן — שלושתם, במלואם:**
1. "מצאי מלא של פריטים פתוחים" (סיום B56) — ACT · DEC · BLD · BLK · Backlog
2. "תוספת להיקף סשן האבחון" — T1–T4 + כלל השלמות
3. `PROGRESS.md` בריפו — סעיפים "פתוח לטיפול", "ממתין לאימות אבי", "מפרטים ממתינים"

**המסמך הזה מחליף את שלושתם כמקור האמת להיקף הבא.**
`BUILD_PLAN.md` (B46–B53) **הושלם במלואו** ואינו מקור להיקף חדש.

> ## ✅ הכרעות אבי — 29.07.2026, משולבות במסמך
> **1. סדר הבנייה:** `PERF-01` (אבחון האיטיות) נבנה ראשון. → חלק 9
> **2. סרגל הצד — `DEC-05` נסגרה בכן:** מותר לקפל קטגוריות שאינן פעילות.
> **הכרעת N3 מתעדכנת ל-N3א.** לשון אבי: *"לא להחזיר לאיך שהיה אלא לשדרג
> שיהיה יותר יעיל ומכובד."* → כלומר **אסור לחזור לאקורדיון הישן** — נדרש
> דפוס חדש ומקצועי. פירוט מלא ב-`T2`.
> **3. תצוגת מחיר — `DEC-01` נסגרה בכולל מע"מ ראשי** בחנות ובפורטל.
> נגזרת אצווה חדשה: **`BLD-12`**.

---

## איך משתמשים במסמך הזה

- **מיקום הקובץ: שורש הריפו** — `aviav4433-cell/mapa-noble/TASK_QUEUE.md`.
  קובץ **מתגלגל**, בדיוק כמו `PROGRESS.md`: הצ'אט מושך אותו ב-`curl`, מסמן ✅ ליד
  הפריט שבנה, ומחזיר את הקובץ **כולו**. אבי דורס את הישן.
  ⚠ **`BUILD_PLAN.md` הוסר מידע הפרויקט** — הוא הושלם (B46–B53) ומסמך זה מחליף אותו.
- **אצווה אחת לצ'אט. אצווה = פריט אחד או כמה פריטים מקובצים.** צ'אט מקבל OPENER עם
  רשימת המזהים של האצווה שלו (למשל `T3` + `T4`), בונה את כולם, מוסר פעם אחת, נגמר.
  **מיזוג נוסף או פיצול — רק לפי חלק 9א.**
- **כל מזהה קבוע לנצח.** אין להמציא מזהים חדשים לפריט קיים; פריט חדש מקבל את המספר הבא ברצף.
- כשפריט נסגר — מסמנים ✅ ליד המזהה ומשאירים אותו במסמך. **לא מוחקים.**
- פריטי **ACT** אינם אצוות — הם לחיצות של אבי במערכת שכבר פרוסה.
- פריטי **DEC** חוסמים בנייה עד שתתקבל הכרעה.

---
---

# חלק 1 — חוקי-על מחייבים

**כל צ'אט קורא את החלק הזה לפני שהוא נוגע בכלום. הוא גובר על כל מסמך אחר פרט ל-`WORKING_PROTOCOL.md`.**

## R1 — אבי אינו מייצא בסיס נתונים. לעולם.
אין לבקש ייצוא גיליון, CSV, צילום מסך של טבלה או "תבדוק מה יש לך ב-X".
**מבטל את הכלל ב-`WORKING_PROTOCOL.md` חלק 5.**
במקומו: כשצריך לדעת מה יש בנתונים — **בונים פעולת אבחון `READ_ONLY`** ומציגים על המסך.
תיקון נתונים חייב להיות **מגלה-בעצמו ואידמפוטנטי, עם תצוגה מקדימה לפני ביצוע.**

## R2 — כלל השלמות: אין מפרט בלי מפת חיבורים כתובה
לכל פיצ'ר, תיקון או שינוי — **גם של שורה אחת** — המפרט חייב לכלול סעיף בשם
**"מפת חיבורים"** עם תשובה מפורשת לשש השאלות. **שאלה בלי תשובה = המפרט פסול.**

1. **איפה הנתון נשמר?** שם הטבלה והעמודות ב-`TABLES` בקוד השרת. שדה שאינו קיים — לומר זאת
   במפורש ולציין ששינוי סכימה מחייב `setupDatabase`. אסור להסתפק ב"יישמר בטבלה מתאימה".
2. **מי כותב אותו?** כל מסך, טופס ופעולת שרת שיוצרים או מעדכנים אותו.
3. **מי קורא אותו?** `grep` מלא, **ורשימה כתובה של הצרכנים בתוך המפרט.** אין "כנראה אף אחד".
4. **לאן הוא זורם?** כל סיכום, דוח, מסך ומדד שמושפעים. **ואם מדובר בכסף** — תשובה מפורשת
   לכל אחת: האם נוצר חיוב? האם הוא נכנס לספר החיובים (`b54Ledger`)? האם נוצרת חשבונית או
   קבלה? האם זו הוצאה (`expenses`)? האם היא מקזזת רווח בדוח הכנסות מול הוצאות? האם היא
   נכנסת לתזרים (B43)? האם היא נוגעת במסגרת האשראי (S5)? האם חל עליה כלל המע"מ (B45)?
   **כל תשובה "לא" חייבת נימוק של שורה.**
5. **מה קורה במצבי הקצה?** אין נתונים · הנתון נמחק · הרשומה בוטלה · לחיצה כפולה · כשל באמצע.
6. **מי רואה את זה?** חמשת התפקידים (מנהל · משרד · מכבסה · עובד רצפה · נהג)
   וארבעת הקשרי הריצה (צוות · פורטל · חנות · **קיוסק**).

## R3 — כלל אי-ההתחמקות
אסור לסגור פריט במשפטים האלה או בכל ניסוח שקול:
*"מחוץ להיקף" · "אפשר להוסיף בהמשך" · "נניח שקיים" · "כנראה מטופל במקום אחר" ·
"לא נבדק אבל סביר ש..." · "המשתמש יוכל להזין ידנית".*
עצירה מותרת **רק** מארבע הסיבות ב-`WORKING_PROTOCOL` חלק 4א: התנגשות בין צ'אטים ·
שינוי סכימה · הרחבת היקף מהותית · סיכון אמיתי לשבירה.
כשעוצרים — לומר **איזו** מהארבע, **למה**, ו**באיזו אצווה** זה כן ייכנס.
עצירה בלי שלושת אלה אינה עצירה, היא דילוג.

## R4 — כלל אי-הפגיעה: רשימת המנגנונים החסינים
שום תיקון לא פוגע במנגנון קיים, בקישור או בנתון. **אין לגעת:**
`API_URL` · מנוע האשראי והתשלום · ספר החיובים (`b54Ledger`) · חישוב השכר והנוכחות
(`clockCore`, `attEff`, `buildPayrollForMonth`) · כלל המע"מ B45 · `allowedViews` ו-`navGroupOf`/`navQuickKeys` ·
`DRIVER_TAB_ORDER` · טוקן הקיוסק ומנגנון B52 · פריסת הניווט של B53
(`#shell`, `navIsSide`, `NAV_MODE`, `nav_quick`) · `NAV_DD`/`.gmenu` (משמשים 900–1099px) ·
האקורדיון של המובייל · `READ_ONLY_ACTIONS` — **פעולה שכותבת לגיליון לעולם לא נכנסת לשם.**
**כל אצווה מגיעה עם בדיקות אוטומטיות שמוכיחות תוצאה זהה לפני ואחרי בכל מה שלא היה אמור להשתנות.**

⚠ **תיקון שמות — B61, 30.07.2026 (R9).** חמישה שמות ברשימה הזו לא היו קיימים בקוד.
שומר אוטומטי שנכתב על שם שגוי נכשל לנצח, ולכן הם תוקנו מול הקוד החי:

| שם ישן ושגוי | השם האמיתי | איפה |
|---|---|---|
| `navFilter` | `allowedViews` · `navGroupOf` · `navQuickKeys` | ממשק |
| `b2CreditUsedAg` | קיים בשם הזה — **בשרת בלבד**, לא בממשק | שרת |
| `b58Prefetch` | קיים בשם הזה — **בשרת בלבד** (שורה 947), לא בממשק | שרת |
| `B52_KIOSK_ACTIONS` | קיים בשם הזה — **בשרת בלבד**, לא בממשק | שרת |
| `batchGet` · `buildPayroll` | `Sheets.Spreadsheets.Values.batchGet` (שירות מתקדם) · `buildPayrollForMonth` | שרת |

**השומרים האוטומטיים לשמות האלה יושבים ב-`חלק t01 ב-tests.js` וב-`t04-server.test.js`.**

⚠ **תוספת B62, 31.07.2026 — שלושה מנגנונים חדשים נכנסים לרשימת החסינים:**

| מנגנון | מה אסור | איפה השומר |
|---|---|---|
| `b62NavPins` · `b62NavPinsSave` · `b62NavPinned` · `b62NavPin` · `B62_PINS_KEY` | הכלל היחיד של סרגל הצד: **פתוחה אם נעוצה או אם המסך הנוכחי בתוכה.** אין להוסיף מצב שני ואין להחזיר אקורדיון | חלק `t06` |
| `.qchip { touch-action:none }` | הסרתו הופכת את גרירת הקיצורים ללא-עובדת במגע, בלי ששום בדיקה לוגית תיפול | חלק `t06` |
| `b62OrderActions` | כרטיס ההזמנה: **פעולה ראשית אחת גלויה + ⋯**. אין להחזיר שורה שטוחה | חלק `t06` |

⛔ **`NAV_QUICK_MAX` הוא 10 ו-`NAV_QUICK_LABEL_MAX` הוא 6.** מעל סף התוויות
השורה העליונה עוברת ל-`qmini` (אייקון בלבד). **אין להסתיר קיצור** — רק תווית.

⚠⚠ **תוספת B63, 31.07.2026 — פריסת הניווט השתנתה מהיסוד. קרא לפני שאתה נוגע בניווט:**

| מה | הכלל | השומר |
|---|---|---|
| **התפריט הראשי הוא השורה העליונה** מ-900px ומעלה — דפוס B47 (`NAV_DD`/`.gmenu`) | **כל 29 המסכים חייבים להיות נגישים ממנו.** אין להחזיר סרגל צד שמחזיק את כל המסכים | חלק `t06` |
| **`#nav` יושב מחוץ ל-`#shell`, ו-`#rail` בתוכו** | החזרת `#nav` לתוך `#shell` שוברת את שורת התפריט לרוחב מלא | חלק `t06` |
| `renderRail` · `b63RailGroups` · `NAV_RAIL_DEFAULT` · `NAV_RAIL_MAX` · המפתח `nav_rail` | ⛔ **הרצועה היא קיצור דרך ולא הרשאה.** היא נגזרת מ-`allowedViews`; אסור שתסתיר מסך מותר ואסור שתציג מסך אסור. **שני הכיוונים נבדקים** | חלק `t06` |
| ⛔ `b62NavPin` ומשפחתו — **הוסרו ב-B63a בהוראת אבי** | הרצועה חזרה לדפוס שלפני B62: כותרת = תווית, כל המסכים גלויים תמיד. **אין להחזיר קיפול, נעיצה או `mn_nav_pins`** | חלק `t06` |
| `nav_rail` ברשימת מפתחות `saveSettings` | הסרתו מוחקת את הבחירה בכל רענון | חלק `t06` |

⛔ **`navMode` · `navIsSide` · `navIsTop` · `NAV_MODE` לא שינו משמעות ולא ערך.**
`'side'` = "רחב מספיק לרצועה" (≥1100px), `'top'` = ≥900px, `'mob'` = מתחת.
**מתחת ל-900px ובמצב נהג לא השתנה דבר** — `#nav` שם `position:fixed`.

⚠ **תוספת B64, 31.07.2026 — ארבעה מנגנונים חדשים נכנסים לרשימת החסינים:**

| מנגנון | מה אסור | איפה השומר |
|---|---|---|
| `b64FexpDue` · `b64RecvDue` · `b64MoneyVisible` | ⛔ **מועדי תשלום ביומן הם נגזרת, לא ישות.** אסור ליצור טבלה או רשומה כספית משלהם. `b64FexpDue` נשען על `fexpOpen`/`fexpRemain` (מקור התזרים B43) ו-`b64RecvDue` על `b54LedgerFE` (מקור דוח גיל החובות). ⛔ כסף ביומן — מנהל ומשרד בלבד | חלק `t07` |
| `b64DayCreateHtml` | ⛔ **אסור טופס שני לאותה ישות.** יצירה מהיומן פותחת את `newOrderForm`/`taskForm`/`meetingForm`/`futureExpForm` הקיימים, עם פרמטר `iso` **אופציונלי ובסוף** — כדי שהקריאות הישנות לא יישברו. ⛔ טסט/ביטוח/טיפול רכב מוצגים ואינם ניתנים ליצירה | חלק `t07` |
| `b12Gross` · `b12Net` · `b12PriceInline` · `b12PriceStack` | ⛔ **החריג הוא החנות והפורטל בלבד.** אסור להשתמש בהם בשום מסך פנימי — כלל B45 (נטו ראשי) נשאר בכל שאר המערכת. ⛔ **תצוגה בלבד**: הסכום לשרת נשאר נטו באגורות. ⛔ מסגרת האשראי והחוב הקודם בפורטל נשארים נטו — אלה מספרי המנוע | חלק `t07` · חלק `t03` |
| `b02ToggleProv` · `b02CanProv` · `b02ProvidersHtml` | ⛔ **השבתה אינה מחיקה.** הרשומה נשארת בטבלה וההיסטוריה שלה שמורה; `b42Providers()` הוא שמסנן אותה מהבוררים. הקריאה שולחת את **כל** השדות כדי שעדכון חלקי לא ירוקן עמודה בגיליון. מנהל ומשרד בלבד | חלק `t07` |

⛔ **הרשימה הזו לא הוסיפה מסך.** אנשי המקצוע יושבים בתוך `purchasing` —
**29 המסכים נשארו 29**, ויש בדיקה ב-`t06` שנכשלת אם מישהו יוסיף `providers`
ל-`VIEWS`.

⚠ **תוספת B64a, 01.08.2026 — נרמול ערכי טקסט מהגיליון:**

| מנגנון | מה אסור | איפה השומר |
|---|---|---|
| `sVal` · `sPick` | ⛔ **חייבים להישאר זהים תו-בתו בין `index.html` לבין `קוד שרת.txt`** — יש בדיקה שמשווה את גוף הפונקציה בשני הקבצים ונכשלת אם הם התפצלו. ⛔ **נרמול להשוואה בלבד** — הערך הנשמר בגיליון אינו משתנה. ⛔ **אסור להחיל על כסף או על מספרים** — שם ההשוואה מספרית, ויש בדיקה שסורקת זאת | חלק `t08` |
| הודעת "שלב לא מוכר" | ⛔ אסור לאחד אותה חזרה עם "המשימה כבר בשלב מוכן". אלה **שני מצבים שונים לגמרי**, ואיחודם הוא שהחביא את הבאג המקורי חודשים. ההודעה חייבת להכיל את הערך הבעייתי ואת מזהה הרשומה | חלק `t08` |
| הגנת המיקוד ב-`b58AfterRefresh` | ⛔ אסור להסיר את בדיקת `activeElement`. בורר או שדה **בתוך מסך** (להבדיל ממודל) נמחק ברינדור אוטומטי, יחד עם טקסט שכבר הוקלד | חלק `t08` |

⚠ **הכלל הכללי שנלמד:** כל ערך שנקרא מגיליון ומושווה מול רשימה קבועה
עובר `sVal`/`sPick` — בדיוק כמו שכל תאריך מגיליון עובר `dYmd` מאז B38b.
**כשל ההשוואה הזה שקט**: אין שגיאה, אין חריגה, פשוט מסך שלא מגיב.

⚠ **תוספת B70, 08.08.2026 — ארבעה מנגנונים חדשים נכנסים לרשימת החסינים:**

| מנגנון | מה אסור | איפה השומר |
|---|---|---|
| `nobleWeighRollup` | ⛔⛔ **הסיכום מצטבר.** אסור להחזיר מפתח שדורס לפי עגלה. הדפוס `lastByCart[...]` **מחק חיובים בשקט** ויש בדיקה שסורקת את קוד השרת ונכשלת אם הוא חוזר. כל שקילה תקפה נספרת פעם אחת | חלק `t16-b70-wash03-srv` |
| `b70Weighs` · `b70Seq` | ⛔ **המספור (01/02/03) נגזר מהיומן ואינו נשמר בשום עמודה.** אסור ליצור שדה "סיבוב". `seq` סופר שקילות בלבד — תיקון יורש את מספר השורה שהוא מתקן | חלק `t16-b70-wash03-srv` · `t16b-b70-wash03-ui` |
| `nobleWeighFix` · `B70_FIX_EVENT` · `laundry_events.ref_id` | ⛔⛔ **מסלול התיקון היחיד לשקילה.** מנהל בלבד דרך `b38VerifyManagerPin`, **סיבה חובה**, שורה חדשה עם `ref_id`. ⛔ `laundry_events` הוא append-only — אסור לגעת בשורה קיימת ואסור `writeTable('laundry_events')`. ⛔ המחיר מוקפא באירוע המקורי — תיקון לא מחיל מחירון חדש | חלק `t16-b70-wash03-srv` |
| `b70CartNeedsWeigh` | ⛔ **סיום מחזור אחרי השקילה האחרונה = העגלה טרם נשקלה.** בלי זה כביסה נוסעת ללקוח בלי חיוב. אסור להחזיר את `nobleMarkReady` לשאלה "נשקלה אי פעם" | חלק `t16-b70-wash03-srv` |

⚠⚠ **תוספת B71, 09.08.2026 — הכסף במסלול הכביסה. ארבעה מנגנונים חדשים:**

| מנגנון | מה אסור | איפה השומר |
|---|---|---|
| `w10Cent` · `w10MulAg` · `w10PctAg` · `fromAg` | ⛔⛔ **כל חשבון כסף במסלול הכביסה נעשה באגורות שלמות (integer)**, וההמרה לשקלים נעשית **פעם אחת בלבד**, בזמן הכתיבה. `w10Cent` קיימת משום ש-`Math.round(v*100)` לבדו מפיל אגורה על חצי אגורה בגלל הייצוג הבינארי | חלק `t17-b71-wash10-srv` |
| ארבע נקודות ה-`nRound2` הכספיות | ⛔⛔ **אסור להחזיר** את `nRound2(net * price)` · `nRound2(charge)` · `nRound2(sub * VAT_RATE)` · `nRound2(sub + vat)`. יש בדיקה שסורקת את קוד השרת ונכשלת על כל אחת מהן. כל אחת היא **נקודת נטישת אגורה מדודה** | חלק `t17-b71-wash10-srv` |
| `toAg` | ⛔⛔ **אין לגעת בו לעולם.** הוא משרת את מנוע האשראי, את `b54Ledger` ואת `b48BalancesAg` — שינוי שלו מזיז יתרות היסטוריות בכל המערכת (R4/R6). יש בדיקה שמשווה את גופו תו-בתו | חלק `t17-b71-wash10-srv` |
| הסה"כ בחשבונית המכבסה | ⛔ **`total` הוא חיבור מדויק של `subtotal` ו-`vat` באגורות**, ולא עיגול שלישי עצמאי. ⛔ כלל B45 לא השתנה: נטו ראשי, כולל מע"מ לצידו | חלק `t17-b71-wash10-srv` |

⛔ **`nRound2` עצמה נשארת בקוד ומשרתת קבלות, תשלומים ומשקלים** (שורות 2796 · 2808 · 3635 · 7100 · 7532 · 7548 · 7756 · 8328). אלה אינם מסלול הכביסה ו**מוגנים ב-R4** (מנוע התשלום). העברתם לאגורות שלמות היא אצווה בפני עצמה, לא תוספת.

⛔ **משקל אינו כסף** (הכרעת אבי 09.08.2026): `nRound2(gross - tare)` לא נגע ולא ייגע במסגרת WASH-10 — שינוי שלו מזיז משקלים מוצגים ב-10 גרם.

⚠ **תוספת B67, 03.08.2026 — שלושה מנגנונים חדשים נכנסים לרשימת החסינים:**

| מנגנון | מה אסור | איפה השומר |
|---|---|---|
| `b67PayOneRow` | ⛔ **מסלול הכתיבה היחיד לתשלום שכר.** גם `case 'payPayroll'` וגם `b67PayEmployee` עוברים דרכו. יש בדיקה שסופרת את `db.payrollPayments.push(` בקוד השרת ונכשלת אם יש יותר מאחד — כלומר אם נוצרה לוגיקת תשלום שנייה | חלק `t11-b67-srv` |
| `B67_WRITE_DEFER` · `b67FlushWrites` | ⛔ **הדגל נדלק אך ורק בתוך `b67PayEmployee`, בתוך `try/finally`.** דגל שנשאר דלוק גורם לכל כתיבה עתידית להיעלם **בשקט**. שתי בדיקות מוודאות שהוא `null` אחרי הצלחה ואחרי כשל. ⛔ `b67FlushWrites` חייבת לכבות את הדגל **לפני** הכתיבה, אחרת היא צוברת את עצמה | חלק `t11-b67-srv` |
| `b67EmpView` · `b67DraftCardHtml` · `b67DraftRows` | ⛔⛔ **טיוטות נגזרות ואינן נכתבות ל-`futureExpenses`.** הרגע שטיוטה נכנסת לטבלה היא נספרת בתחזית התזרים כהתחייבות אמיתית. ⛔ `finPayrollTotal` ו-`fexpOpen` לא משנים את ערכם — יש בדיקה שמוודאת ששניהם מחזירים אפס בזמן שסיכום הטיוטות מציג 8,000. ⛔ **השיוך לחודשים שקוף** (הכרעת אבי): אין מסך אישור לפני התשלום ואין כפתור תשלום בשורת הטבלה — שתי בדיקות נכשלות אם מישהו יוסיף. ⛔ לנהג: אין תשלום, אין סיכום טיוטות, ושם העובד אינו לחיץ | חלק `t11-b67-ui` |

⛔ **טופס התשלום לרשומה בודדת שבתוך `payrollDetail` נשאר במקומו (R8).**
אין לבנות טופס תשלום שלישי ואין להסיר את הקיים.

⚠⚠ **תוספת B81, 18.08.2026 — מחלקת `WASH-23` נסגרה. תשע הרשימות האחרונות:**

| מנגנון | מה אסור | איפה השומר |
|---|---|---|
| `UNIT_BUSY` · `UNIT_DEL_DFLT` · `UNIT_STATUSES` | ⛔⛔ **שער מחיקת היחידות.** `deleteUnits` חוסם `'בשימוש'`/`'בכביסה'`/`'אצל לקוח'` **וגם כל סטטוס לא מוכר**. ⛔ סטטוס **ריק** נשאר ניתן למחיקה — אין להפוך אותו לחוסם. יש בדיקה על **שני** הצדדים. ⛔ `UNIT_STATUSES` זהה תו-בתו בשני הקבצים | חלק `t27-b81-srv` |
| `FAULT_STATUSES` · `FAULT_DFLT` | ⛔ תקלה לא מוכרת/ריקה = `'פתוחה'`. הפיכת הכיוון מחזירה את הבאג שבו **לא ניתן היה לסגור תקלה לעולם**. ⛔⛔ `R5` — אין לאחד רשימת תקלות רכב ומכבסה | חלק `t27-b81-srv` · `t27-b81-ui` |
| `CASE_STATUSES` · `CASE_DFLT` · `CART_STATUSES` · `CART_DFLT` | ⛔ לא מוכר/ריק = **תפוס** (`'משויך'`/`'בשימוש'`). ⛔⛔ **`carts.condition` הוא שדה התקלות ואינו `status`** — `R5`/B56. יש בדיקה שנכשלת על ערבוב | חלק `t27-b81-srv` |
| `INTAKE_DFLT` · `NOBLE_STAGES` · `B49D_CLOSED_INTAKE` · `W23G_SHIPPED` | ⛔ קליטה לא מוכרת/ריקה = `'התקבל'` (**פתוחה**, נשארת על לוח הכביסה). ⛔ **אין ליצור `INTAKE_STATUSES`** — `NOBLE_STAGES` היא הרשימה, בשני הקבצים (`R8`). ⛔ `W23G_SHIPPED` מוגדרת **פעם אחת** בשרת; הליטרל `['במשלוח','נמסר']` לא יחזור | חלק `t27-b81-srv` |
| `NOTIF_STATUSES` · `TCOMMENT_STATUSES` · `VISIT_STATUSES` · `MSG_STATUSES` · `MSG_OPEN` | ⛔⛔ **לארבעתן אין dflt במכוון.** בהתראות dflt היה או שולח פעמיים או מסמן כנשלח משהו שלא נשלח; ב-`TCOMMENT_STATUSES` הערך `''` הוא **חוקי** (`VALID_CS`) ולא לכלוך — אין להסיר אותו מהרשימה | חלק `t27-b81-srv` |
| ⛔⛔ `p.status` ב-`setUnitStatus` וב-`commentSetStatus` | **קלט מהמשתמש, לא ערך מהגיליון.** עובר ולידציה מול `UNIT_STATUSES`/`VALID_CS` ו**אסור לנרמל אותו** — `'פנוי '` מהמשתמש חייב להידחות. יש בדיקה | חלק `t27-b81-srv` |
| ⛔ הדפוס הידני של `w25Usable` | `!sVal(m.status) || sVal(m.status)==='פעילה'` **לא יחזור** לבורר המכונות — זה דפוס שני לאותו כלל (`R8`). סורק נכשל עליו | חלק `t27-b81-ui` |

⛔⛔ **הלקח החוזר, בפעם השנייה ברציפות:** בקרה נגדית שבה בדיקה **עוברת**
מול הקוד הישן פירושה שהבדיקה **אינה מודדת כלום**. ב-B81 שתי בדיקות
נשענו על `w24Stat` לבדו ועברו מול הקוד הישן; הן הועברו לקוד החי
(`b10ApprovalsInbox` ושער `b49dAddCarts`). **בדיקה שנשענת רק על עוזר
טהור היא שומר קבועים, לא מדידה של תיקון.**

## R5 — הכרעת B56-א: הפרדה מלאה בין תקלות רכב לתקלות מכבסה
לשון אבי: *"אין קשר בין תקלות של רכב לתקלות של מכונות כביסה."*
מסך `assets` מסונן ל-`['מכונה','עגלה']`, מסך `fleet` ל-`['רכב']`, דרך `b42ScopeTypes`/`b42InScope`.
**אין להציג רשימת תקלות מאוחדת בשום מסך.**

## R6 — תלות הכסף המשולשת (B48/B54)
`b48BalancesAg` (שרת) · `custBalance` (ממשק) · `b2CreditUsedAg` (מנוע האשראי) —
**שלושתם חייבים להחזיר אותו מספר.** בדיקת השוויון האוטומטית היא **חובה בכל אצווה שנוגעת בכסף.**

## R7 — כלל בדיקות ה-DOM (מ-B48a)
כל בדיקת ניווט או קליק **מדספאטצ'ת אירוע DOM אמיתי** (`MouseEvent`/`change` עם `bubbles:true`).
ב-jsdom במצב `outside-only` מטפלי `on*` בתגית אינם מקומפלים — יש לחברם כמאזינים אמיתיים
לפני הדיספאטץ' (עוזר `wire()`). **קריאה ישירה ל-handler אינה קבילה.**

## R8 — נקודות ההרחבה הקיימות (אין לבנות דפוס שני)
| מה | המנגנון | איך מרחיבים |
|---|---|---|
| דוח חדש | `REPORTS_CATALOG` / `REPORT_RENDERERS` / `repParamsForm` | רשומה בקטלוג + פונקציית render. **בלי לגעת ב-`rReports`** |
| ייצוא טבלה | `exportCsv(filename, headers, rows)` | `b50Reg(key, filename, headers, rows)` + `b50ExportBtn(key)`. **אין לשרשר נתונים ל-`onclick`** |
| תפריט פעולות משני | `moreMenu(items)`, `.more-wrap`, `toggleMore`, `closeMore` | להשתמש בקיים. **אין ליצור דפוס שני** |
| פעולת קיוסק | `B52_KIOSK_ACTIONS` (רשימה סגורה) | הוספה לרשימה בלבד |

## R10 — כלל הנסיעות: לצמצם את **מספר** הבקשות, לא רק את משקלן
נוסח רשמית ב-B58 מתוך המדידה שהוכיחה אותו (הוא צוטט ב-`PERF-02` לפני שנכתב כאן).
**שתי עובדות מדודות שכל אצווה מחויבת להן:**
1. **כל נסיעה לגיליון עולה ~215 מ״ש, בלי קשר לגודל הטבלה.** `invoices` — 3 שורות,
   1.3 KB, **1.1 שניות**. מערכת שנוסעת 55 פעם לכל בקשה תהיה איטית גם עם טבלה של
   3 שורות, ומהירה עם מיליון שורות אם תיסע פעם אחת.
2. **רצפת התקורה של Apps Script היא ~2.2 שניות לכל בקשת HTTP**, גם לזעירה
   (`checkVersion`). לכן שתי בקשות קטנות גרועות מבקשה אחת גדולה.

**מה זה מחייב בפועל:**
- **מסך חדש שולף ממוקד** — רק מה שהוא צריך, ולא נשען על כך ש-`getAll` יביא הכול.
- **קריאות מרובות לגיליון מתאחדות לקריאה אחת** (`b58Prefetch`/`batchGet`), ולא
  מתפזרות ל-`readTable` נפרד לכל טבלה.
- **polling ורענון אוטומטי מצמצמים בקשות**, לא רק בייטים. לשונית ברקע אינה שולחת.
- ⚠ **אין לשכתב מסכים קיימים בגלל הכלל הזה.** תיקון ① של B58 ייתר את הדחיפות.
  הכלל מחייב **מה שנבנה מעכשיו**.

## R11 — כלל האימות. אין ניחוש. אין הערכה. בלי חריגים.
**נוסח בהוראת אבי, 30.07.2026, אחרי כשלון B60 בייצור.**

כל תיקון, כל אבחנה וכל טענה על התנהגות המערכת נשענים **אך ורק על בדיקה
שבוצעה בפועל** — הרצה · `grep` בקוד החי · בדיקה אוטומטית · או פעולת אבחון
`READ_ONLY` שרצה על המערכת האמיתית.

**אסור בהחלט:** *"כנראה"* · *"סביר ש"* · *"אני די בטוח"* · *"זה נובע מ"* בלי
הוכחה · *"בדרך כלל זה קורה כש"* · לסגור פריט על סמך היגיון בלבד.

**כשאי אפשר לאמת — זו אינה רשות לנחש. זו חובה לבנות את הכלי שיאמת**, למסור
אותו, ולומר במפורש: *"לא אומת. זה מה שנבנה כדי לאמת."*

**כל טענה שנמסרת לאבי נושאת תווית:**
✅ **אומת** — ואיך אומת · ⚠ **לא אומת** — ומה נבנה כדי לאמת.

⚠ **סביבת בדיקה שאינה זהה לייצור אינה אימות.** הלקח שהוליד את הכלל: B60 עבר
**60 בדיקות אוטומטיות ב-jsdom** ונכשל אצל אבי בלחיצה השנייה. בדיקה שעברה
ונכשלה בייצור מוכיחה **שהבדיקה לא כיסתה** — לא שהקוד היה תקין.
מסקנה מעשית: כל מנגנון שתלוי בהתנהגות דפדפן אמיתי (היסטוריה, מצלמה, הדפסה,
מגע) מחייב **פעולת אבחון שרצה אצל אבי**, ולא רק בדיקה בסביבה מדומה.

✅ **B61 מימש את זה כתשתית קבועה (30.07.2026, הכרעת אבי "ב'"):**

| שכבה | מה מכסה | מי מריץ | מתי |
|---|---|---|---|
| **1 · `tests.js` בריפו** | לוגיקה · הרשאות · חישוב · מבנה · שומרי קוד מקור | **הצ'אט** | לפני כל מסירה |
| **2 · כרטיס "בדיקה עצמית"** | היסטוריה · אחסון · מצלמה · הדפסה · מגע · אזור זמן · פריסה | **אבי, לחיצה אחת** | אחרי כל עדכון גרסה |

⛔ **מעכשיו: אצווה שנוגעת ביכולת דפדפן ואינה מוסיפה טענה לשכבה 2 — לא נמסרה.**
המקום: `b61Tests()` ב-`index.html`. הבדיקות של הכרטיס עצמו: `חלק t05`.

## R12 — כלל התקשורת: מלא בתוכן, קצר בניסוח
**נוסח בהוראת אבי, 16.08.2026.**

הצ'אט מספק **את כל המידע הנדרש — ולא מילה מעבר לו.**
אורך אינו הוכחה לעומק, והוא עולה קרדיטים וזמן קריאה.

**בשאלות לאבי:**
- **הודעה אחת מרוכזת** לכל ההכרעות. לא שאלה-ואז-שאלה.
- כל שאלה: **שורת רקע אחת · אפשרויות ממוספרות · המלצה מסומנת ⭐**.
- ⛔ אין להסביר את הקוד כדי להצדיק את השאלה. אבי מכריע עסקית.
- תשובה מתקבלת בפורמט קצר (`1ב 2א`) — ואם אבי כותב "לפי המלצתך", זו הכרעה מלאה.

**במסירה בסוף אצווה — חמישה חלקים, בסדר הזה, ולא יותר:**
1. **התוצאה** — מה תוקן, במשפט עד שלושה. + canary · מספר בדיקות · שינוי סכימה כן/לא.
2. **הממצא שלא היה צפוי** — רק אם היה. ⛔ אין למלא אותו בכוח.
3. **מה אבי עושה עכשיו** — רשימה ממוספרת של פעולות בפועל. לא הסברים.
4. **מה נשאר פתוח** — מזהה + לאיזו אצווה הוקצה. שורה לכל אחד.
5. **מכתב מקדים לצ'אט הבא** — ה-OPENER, להעתקה כלשונו.

⛔ **אסור במסירה:** חזרה על מה שנכתב באמצע הסשן · הסבר איך הקוד עובד ·
תיאור תהליך העבודה · טבלאות של פרטים שכבר בקבצים.
**הפירוט המלא חי ב-`PROGRESS.md`. המסירה בצ'אט היא תקציר מנהלים.**

⚠ **הכלל אינו חל על `PROGRESS.md` ועל `TASK_QUEUE.md`** — שם הפירוט
המלא הוא הערך, והם נקראים על ידי הצ'אט הבא ולא על ידי אבי.

## R9 — מפרט תכנון הוא כוונה, לא מציאות
ארבעה לקחים מצטברים (B48 · B49d · B50 · B52): סעיפי "מצב קיים" במסמכי מפרט תיארו קוד
שהתיישן, טריגר שאינו קיים, מדד "חדש" שהיה זהה לקיים, ומנגנון (`checkVersion`/`data_version`)
שעושה משהו אחר ממה שהמפרט הניח.
**תמיד לאמת את העוגנים ואת מה שהפונקציה עושה בפועל מול הקוד החי — ולעצור מול אבי כשנמצאת סתירה.**

---
---

# חלק 2 — קטגוריה ACT: פעולות של אבי בלבד

**אלה אינן אצוות.** הקוד קיים ופרוס; הפריט ממתין ללחיצה או להזנה.

| מזהה | מה | איפה | למה | חומרה |
|---|---|---|---|---|
| **ACT-20** | **הרצת "בדיקה עצמית בדפדפן" — לחיצה אחת** | יומן פעולות ← *בדיקה עצמית בדפדפן (B61)* ← **▶ הרץ בדיקה עצמית** | ⛔ **חוסם את B62.** ירוק ⇦ אין מה לעשות. אדום ⇦ **📋 העתק תוצאה** והדבקה בצ׳אט הבא. **הפעם הראשונה שיהיו נתונים על המכשיר של אבי:** דפדפן · רוחב מסך בפועל · מגע · מצב ניווט · אזור זמן. B62 עוסק כולו בנקודות שבירה ובסרגל צד ונבנה היום על **הנחה** | 🔴 |
| **ACT-06** | הרצת `setupDatabase` — **אם טרם בוצעה מאז B49f** | עורך Apps Script | `deliveries.trip_confirmed_by` · `deliveries.trip_auto_cancel`. ההרצה מכסה גם את B49b/B49c/B49e/B54/B55 | 🔴 |
| **ACT-07** ✅ | **בוצע — Drive API מופעל** (אומת 29.07.2026) | Apps Script ← שירותים | ⚠ השירות כבר היה ברשימה. ניסיון הוספה שני יוצר שגיאת כפילות במניפסט — הפתרון: **לא לשמור, לרענן את הדף** | — |
| **ACT-08** | הרצת "תיקון מסמכים שנשמרו כ-HTML" | הגדרות | **רק אחרי ACT-06 ו-ACT-07.** ממיר רטרואקטיבית מסמכים ישנים | 🔴 |
| **ACT-01** | הרצת ניקוי הסיסמאות מ-`audit_log` | יומן פעולות ← 🧹 | סיסמאות היסטוריות מלפני B36 בטקסט גלוי. נבנה ב-B51, **מעולם לא הורץ**. תצוגה מקדימה קודם; ריצה שנייה = 0 | 🔴 |
| **ACT-09** | אימות B54/B55 — הכסף | הגדרות ← "בדיקת נתוני חיובים" · כספים ← לשונית "חיובים" · הפורטל | ספר החיובים ופורטל הלקוח נבנו ולא נראו על נתוני אמת. **זה כסף** | 🔴 |
| **ACT-04** | אימות B56 | תשע הבדיקות ב-HANDOFF של B56 | בפרט: תקלת רכב לא מופיעה במסך המכבסה · הזמנת כביסה נסגרת במסירה | 🔴 |
| **ACT-10** | אימות B52 — בורר העובד בשעון | נוכחות ← בחירת עובד אחר ← החתמה | זה הבאג ששויך למנהל במקום לעובד. לוודא שהשם שמופיע הוא הנבחר | 🔴 |
| **ACT-11** | אימות B49f — "נסיעות שבוטלו בקליטה" | משלוחים · שלושת מסלולי הקליטה | השאלה "האם הנהג ביצע את הנסיעה בפועל?" בשער הקליטה | 🔴 |
| **ACT-03** | מילוי מספר אישי (`pin`) לעובדים חסרים | כרטיסי עובדים (האבחון בהגדרות מציג רשימה) | עובד בלי מספר אישי אינו יכול להחתים בקיוסק | 🟡 |
| **ACT-05** | אימות B53 — ששת המסכים הרחבים | פריטים · הזמנות · כספים · שכר · יומן פעולות · דוחות | הסרגל גזל 228px ואיש לא בדק גלישה אופקית בדפדפן אמיתי. **חוסם את BLD-05** | 🟡 |
| **ACT-12** | אימות B52 קיוסק · B49d · B49e | מיתוג והגדרות ← טאבלט קיוסק · "ממתין לקליטה" · אופן מסירה · חתימה מכרטיס ההזמנה | ⚠ החתימה — **חובה לבדוק על הטאבלט של הדלפק**, לא במחשב | 🟡 |
| **ACT-13** | אימות B50/B51 | ארבעת הדוחות החדשים · ⬇ ייצוא · יומן פעולות בעימוד | ⚠ לוודא שה-CSV נפתח באקסל בעברית תקינה (BOM) ושהסכומים ניתנים לסכימה | 🟢 |
| **ACT-02** ✅ | **מוחלף** — מדידת משקל הנתונים | יומן פעולות ← 📊 | ✅ **התייתר: B57 מודד את זה ועוד.** מוחלף ב-**`ACT-15`** | — |
| **ACT-16** ✅ | **בוצע** — שירות Sheets מופעל בעורך Apps Script | Apps Script ← שירותים | תיקון ① של B58 נבנה עליו. ⚠ אם יתברר שאינו מופעל בפועל — כרטיס האבחון של B58 יציג "שירות Sheets מופעל: לא", והמערכת פשוט תמשיך במסלול הישן בלי להישבר | — |
| **ACT-17** ✅ | **הורץ 30.07.2026 — ומצא תקלה. תוקן ב-B58a** | יומן פעולות ← "קריאה מהירה (B58 · PERF-02)" | 51/55 זהות; `tasks` · `task_comments` · `meetings` · `credit_profiles` נבדלו בעמודות תאריך. **המסלול המהיר כובה לבד ואף נתון לא נפגע** — בדיוק מה שהמנגנון נבנה בשבילו. נמדד: 486 מ״ש מול 13.3 שנ׳ = **פי 27.3** | — |
| **ACT-19** ✅ | **בוצע 30.07.2026 19:07 — עבר.** "זהות מלאה: 55 מתוך 55 טבלאות זהות ביט-אחר-ביט. המסלול המהיר פעיל" | — | `batchGet` **553 מ״ש** מול **12.8 שנ׳** = **פי 23.2**. **PERF-02 סגור** | — |
| **ACT-18** ✅ | **בוצע 30.07.2026 — קו הבסיס החדש נמדד** | — | `getAll` מלא מהדפדפן: **1.6 שנ׳** (היה ~14). מטען 49.2 KB · בניית מטען 44 מ״ש · פענוח 1 מ״ש · רינדור 9 מ״ש. ⚠ **רצפת התקורה של Apps Script: 2.7 שנ׳ לבקשה — הגענו אליה** | — |
| **ACT-15** ✅ | **בוצע 29.07.2026 20:41** — אבחון הביצועים הורץ על נתוני בדיקה | יומן פעולות ← אבחון ביצועים | המספרים התקבלו. `PERF-02` כבר אינו חסום. ⚠ **להריץ שוב אחרי מעבר לנתוני אמת** — קו הבסיס הנוכחי הוא של מערכת ריקה | — |
| **ACT-14** | אימות ותק: B46 · B48 · B49 · B49b · B49c | פריטי האימות מה-HANDOFF של כל אצווה | פתוחים מזה חמש אצוות | 🟢 |

> **הערה:** `ACT-02` התייתר — `b57PerfProbe` מודד את משקל הנתונים **וגם** את הזמנים
> ואת המטמון. במקומו נכנס **`ACT-15`** (בוצע ✅).
> **`ACT-17` · `ACT-18` · `ACT-19` — כולם בוצעו ✅. `PERF-02` סגור סופית על נתוני אמת.**
> ⛔ **`ACT-20` חוסם את הצ'אט הבא (B62).** לחיצה אחת של אבי על "הרץ בדיקה עצמית".
> בלעדיה B62 ייבנה על הנחה לגבי גודל המסך והמכשיר — בדיוק מה ש-R11 אוסר.

---
---

# חלק 3 — קטגוריה DEC: הכרעות עסקיות שחוסמות בנייה

**אין כאן קוד לכתוב עד שאבי מכריע.**

## ✅ הוכרעו 29.07.2026

| מזהה | ההכרעה שהתקבלה | מה נגזר ממנה |
|---|---|---|
| **DEC-01** ✅ | **כולל מע"מ כמספר ראשי** בחנות ובפורטל (נטו בכתב קטן לצידו) | אצווה חדשה **`BLD-12`**. ⚠ **כלל B45 נשאר על כנו בכל שאר המערכת** — החריג חל על החנות והפורטל בלבד |
| **DEC-05** ✅ | **כן — מותר לקפל קטגוריות שאינן פעילות בסרגל הצד.** הכרעת N3 מתעדכנת ל-**N3א** | מבטל את החסימה על **`T2`**. ⚠ **אבי: "לא להחזיר לאיך שהיה אלא לשדרג שיהיה יותר יעיל ומכובד"** — אסור לחזור לאקורדיון הישן |

## ⏳ עדיין פתוחות

| מזהה | ההכרעה | הרקע | חוסם |
|---|---|---|---|
| **DEC-02** ✅ | תיקון נוכחות היסטורית מבאג B37 | ✅ **הוכרע 09.08.2026 — אין לתקן אחורה.** נימוק אבי: כל הנתונים ההיסטוריים הם נתוני בדיקה ואינם ייצור אמיתי. ⛔ **אין אצווה. אין קוד. סגור.** ⚠ ההכרעה תקפה לנתונים שעד 09.08.2026 בלבד — נוכחות שתירשם מכאן והלאה היא נתון אמיתי | — |
| **DEC-03** ✅ | קליטה שנקשרה להזמנה אחרי שהופקה חשבונית | ✅ **הוכרע 09.08.2026 — לחסום במקור.** ⛔ אין לקלוט כביסה על הזמנה שכבר נסגרה. **המצב לא ייווצר, ולכן אין צורך בחשבונית מתקנת ולא בזיכוי.** ⭐ נגזרת: איחוד הזמנות יישאר אפשרי **לצרכים תפעוליים בלבד** — ⛔ **אסור שיהיה לו אפקט חשבונאי**, ואסור שיזיז סכום שכבר יצא במסמך ללקוח. ⚠ **נדרשת אצווה** — ראה `WASH-16` | — |
| **DEC-04** ✅ | הפקת חשבוניות סלקטיבית בהצהרת תשלום | ✅ **הוכרע 09.08.2026 — ברירת מחדל: החוב הישן ביותר.** כל תשלום משויך אוטומטית לחוב הפתוח הוותיק ביותר, ⭐ **ולעובד תינתן אפשרות לבחור חשבונית מסוימת** במקום זאת. ⚠ הדפוס זהה ל-`b67PayOneRow` בשכר (B67) — **אין לבנות דפוס שני (R8)**. `BLD-06` נפתח | **BLD-06** |

---
---

# חלק 4 — תקלות פתוחות שדווחו על ידי אבי

## PERF-01 ✅ — איטיות המערכת · אבחון לפני תיקון · **בוצע ב-B57, 29.07.2026**

**מה נמסר:** מסך *יומן פעולות* ← כרטיס **“אבחון ביצועים (B57 · PERF-01)”**
(מנהל בלבד) + פעולת שרת `b57PerfProbe` (`READ_ONLY`, מנהל בלבד).
מוצג על המסך, בלי ייצוא ובלי לבקש מאבי נתון (R1):
משקל וזמן קריאה **לכל טבלה**, זמן לכל שלב (שליפה · העברה · רינדור),
שיעור פגיעות המטמון, ומקצב דריסת המטמון.

**⚠ האבחון לא האיץ דבר, במכוון. התיקון הוא `PERF-02` = B58.**

### ששת הממצאים שהוכחו מהקוד החי (לא ניחוש, לא מדידה — קריאה)

| # | ממצא | מה זה אומר |
|---|---|---|
| 1 | `getAll` קורא **55 מתוך 59** גיליונות בכל החטאת מטמון | `SLIM_POLICY` מפילה 4 בלבד. `stock_moves` · `package_moves` · `fault_events` · `attendance` · `payroll` **לא נחתכו** — אומת |
| 2 | **המטמון נמחק בכל כתיבה במערכת** | מפתח `getAllCached` = `'getAll_payload_' + data_version`, ו-`route` קורא `bumpDataVersion()` אחרי כל פעולה שאינה `READ_ONLY`. אורך החיים האפקטיבי = הפער בין כתיבות, לא TTL 120 שנ׳. **חשד B12 מאומת** |
| 3 | **ייתכן שהמטמון מעולם לא אחסן דבר** | `cache.put` עטוף ב-`catch` ריק; ל-`CacheService` תקרה של ~100KB לערך יחיד. **שאלה בינארית שאי אפשר להכריע מקריאת קוד** — האבחון מנסה לאחסן את המטען האמיתי ומדווח הצלחה/כישלון |
| 4 | `enterApp()` ממתין ל-`getAll` מלא לפני שמוצג משהו | ובמסלול טוקן-הסשן **אין אפילו הודעת “טוען”**. ← זו ההכרעה על `T4` |
| 5 | **כל לקוח מחובר מושך `getAll` מלא כל 30 שניות** ביום עבודה פעיל | `pollDataVersion` מושך שלם בכל שינוי `data_version`; בשילוב עם ממצא 2, בזמן עבודה זה משתנה כל הזמן |
| 6 | **הנהג משלם הכי הרבה ואינו נהנה מהמטמון** | `getDriverFilteredPayload` בונה `getAllPayload` **מלא** ואז מסנן, ואינו עובר ב-`getAllCached` כלל (מכוון, B17). כל רענון = בנייה מאפס |

### ✅ ההכרעה שנדרשה על `T4` — **פיצול, לא איחוד**

אבי דיווח שני תסמינים במשפט אחד. בקוד הם שני דברים שונים לחלוטין:

- **“רענון מוציא מהמערכת” = תסמין של PERF-01.** אומת: `sessionStorage.mn_token`
  שורד רענון · `autoLogin()` משחזר · `sessionStorage.mn_view` מחזיר לאותו מסך ·
  `idleLock` **אינו יכול** לירות ברענון כי `LAST_ACTIVITY` מאותחל בטעינת הדף.
  המערכת אינה זורקת את אבי — היא מציגה לו את מסך הכניסה, בשקט, כל עוד `getAll` רץ.
  **עבר ל-`PERF-02`.**
- **“הקודם מוציא מהאתר” = באג נפרד ואמיתי.** `grep` על `pushState`/`popstate`
  בכל `index.html` מחזיר **מופע אחד** — `history.replaceState` של הקיוסק.
  המנגנון מעולם לא נבנה. לביצועים אין לזה קשר.

**⚠ לכן התור אינו מתקצר ל-17.** `T4` נשאר אצווה, אך היקפו נחתך בכ-חצי —
רק היסטוריית דפדפן. → ראה `T4` המעודכן.

### ⚠ מה אבי חייב לעשות לפני B58

**להריץ *יומן פעולות ← אבחון ביצועים ← “הרץ אבחון מלא”* בשעת עבודה רגילה**
(לא בערב, לא בשבת — המספרים צריכים לשקף עומס אמיתי), ולהעביר את המספרים
לצ'אט של B58. **בלעדיהם `PERF-02` הוא ניחוש, ו-B58 עוצר.**
מומלץ ללחוץ “↺ אפס מוני מטמון” בבוקר, ולהריץ את האבחון אחרי כמה שעות עבודה.

---

## PERF-02 ✅ — תיקון האיטיות · **בוצע ב-B58, 29.07.2026**

**מה נמסר.** ארבעה תיקונים, שניים תשתיתיים ושניים בממשק. פירוט מלא בקטע B58
ב-`PROGRESS.md`; כאן התמצית ומה שנשאר פתוח.

| # | מה נבנה | הוכחה |
|---|---|---|
| ① | `b58Prefetch` — `Sheets.Spreadsheets.Values.batchGet` קורא את כל הטווחים. `readTable`/`loadDb` לא השתנו כלפי שאר הקוד. **B58a: שתי קריאות בשני ייצוגים**, כדי לזהות תא-תאריך בוודאות | נמדד אצל אבי: **486 מ״ש מול 13.3 שנ׳ = פי 27.3**. נמדד בבדיקה: 55 `getValues` → **2 `batchGet` + 0 `getValues`** |
| ② | גרסת מטמון לכל טבלה (`b58_table_versions`). `bumpDataVersion` ו-`checkVersion` שמרו על סמנטיקה זהה; נוסף שדה `tables` בלבד | כתיבה ל-`orders` מסמנת רק אותה; `settings` לא מסומנת |
| ③ | חיווי "טוען נתונים…" ב-`enterApp` → מכסה את **שני** מסלולי `autoLogin` | jsdom: החיווי גלוי בזמן ההמתנה, מוסתר בהצלחה/בכשל/בנפילת רשת |
| ④ | `pollDataVersion` מושך רק טבלאות שהשתנו (`b58Tables`) ואינו שולח דבר כשהלשונית ברקע | לשונית מוסתרת = אפס בקשות; `visibilitychange` אמיתי מפעיל בדיקה מיידית |

**80 בדיקות אוטומטיות, כולן עברו** — 47 שרת (`vm.createContext`) + 33 ממשק (jsdom, R7).
**הבדיקה המרכזית עברה: מטען `getAll` זהה ביט-אחר-ביט לפני ואחרי, לכל אחד
מחמשת התפקידים.** R6 נבדק: `b48BalancesAg` מחזיר תוצאה זהה.

**⚠ שלוש רמות נפילה חיננית, כולן נבדקו ומחזירות מטען זהה:**
שירות Sheets אינו מופעל · `batchGet` זורק · אי-התאמה בין המסלולים.

**⚠ מה שאי אפשר היה לבדוק מהצ'אט — ולכן נבנה לתוך המערכת:**
אין דרך להוכיח מכאן ש-`batchGet` מחזיר בדיוק את מה ש-`getValues` מחזיר על
הגיליון האמיתי. לכן **המערכת מריצה את ההשוואה לבד פעם ב-6 שעות**, ואי-התאמה
מכבה את המסלול המהיר אוטומטית. בנוסף נבנה כפתור אבחון ידני → **`ACT-17`**.

**⚠ B58a (30.07.2026) — תיקון שנספח אחרי `ACT-17`.**
האבחון על נתוני אמת מצא **51/55 זהות**; ארבע טבלאות עם עמודות תאריך נבדלו
(`tasks` · `task_comments` · `meetings` · `credit_profiles`), עם **מספר שורות
זהה** — כלומר ההבדל היה בתוך תא. השורש: `getValues` מחזיר תא-תאריך כ-`Date`
ו-`readTable` הופך ל-`yyyy-MM-dd`, בעוד `FORMATTED_STRING` החזיר את מחרוזת
התצוגה של הגיליון. **התיקון: קריאה בשני ייצוגים** (`SERIAL_NUMBER` +
`FORMATTED_STRING`) — ההפרש ביניהן מזהה תא-תאריך **בוודאות**, בלי לנחש לפי
תבנית טקסט. נוסף: האבחון מצביע על **התא המדויק**, וזהות מלאה מפעילה מחדש
את המסלול המהיר לבד. **97 בדיקות, כולל רגרסיה שמשחזרת את ארבע הטבלאות של אבי.**

**✅ PERF-02 סגור סופית (30.07.2026, 19:07).** `ACT-19` אישר **זהות מלאה
55/55 והמסלול המהיר פעיל**; `ACT-18` נתן את קו הבסיס החדש:

| מדד | לפני | אחרי |
|---|---|---|
| `getAll` מלא מהדפדפן | ~14 שנ׳ | **1.6 שנ׳** |
| קריאת כל הגיליונות | 12.8 שנ׳ (55 נסיעות) | **553 מ״ש** (`batchGet`) |

**⚠ הממצא המחייב לכל אצווה הבאה: הגענו לרצפת הפלטפורמה.**
קריאה שנייה מיד אחרי הראשונה — 1.7 שנ׳, כמעט זהה. **רצפת התקורה של
Apps Script נמדדה אצל אבי ב-2.7 שנ׳ לכל בקשה.** בניית המטען 44 מ״ש ·
סריאליזציה 0 · פענוח 1 מ״ש · רינדור 9 מ״ש. **אין יותר מה לסחוט מהמשקל —
כל שיפור עתידי חייב להיות צמצום מספר הבקשות. זה בדיוק `R10`.**

**ממצא שתועד ולא נפתחה עליו אצווה:** פגיעות מטמון **26.7%** · **47.4%
מהפערים בין כתיבות קצרים מה-TTL (120 שנ׳)** — כתיבה דורסת את המטמון הגלובלי
לפני שהספיק לשרת. **כבר לא מזיק** (החטאה = 1.6 שנ׳ במקום 14), ובדיוק בשביל
זה נבנה המטמון פר-טבלה של B58 ②. חוזרים רק אם מדידה תראה שזה בצוואר.

**`BLD-11` נדחה במכוון** — ראה למטה.

---

## PERF-02 — ההיקף כפי שנקבע לפני הבנייה (נשמר לתיעוד)

**קלט חובה:** תצלום מסך אחד של פלט האבחון. **בלעדיו — עצירה** (R3: העצירה
היא מסוג “מפרט חסר”, הפריט חוזר ל-B58 ברגע שהמספר קיים).

### ✅ המספרים התקבלו — ההיקף אינו עץ הכרעה עוד. הוא ידוע.

**מדידה: 29.07.2026 20:41, על נתוני בדיקה (המערכת עדיין ריקה).**

| מה נמדד | הערך | מה זה אומר |
|---|---|---|
| **קריאת 55 הגיליונות** | **11.9 שנ׳** | 🔴 **זה כל הסיפור** |
| בניית המטען | 8 מ"ש | אפסי |
| סריאליזציה | 0 מ"ש | אפסי |
| פענוח בדפדפן | 1 מ"ש | אפסי |
| רינדור | 6 מ"ש | אפסי |
| **סך המטען הנשלח** | **40.7 KB** | ⚠ **המערכת ריקה. הנפח אינו הבעיה** |
| רצפת רשת (`checkVersion`) | **2.2 שנ׳** | תקורת Apps Script לכל בקשה, בלי קשר לנתונים |
| `getAll` בפגיעת מטמון | 2.2 שנ׳ | = רק התקורה |
| `getAll` בהחטאת מטמון | **~14 שנ׳** | 11.9 קריאה + 2.2 תקורה |

### ⚠ שלוש השערות B57 שהמדידה **הפריכה** — R9

1. **❌ “המטמון אולי מעולם לא אחסן דבר” — הופרך.**
   `fits_limit = true` · 0 כישלונות אחסון · שיעור פגיעות 66.7%.
   **המטמון עובד מצוין.** אין צורך בפיצול נתחים, ואין צורך ב-`cache.putAll`.
2. **⚠ “המטמון נדרס בכל כתיבה” — נכון מכנית, אך אינו הצוואר.**
   חציון הפער בין כתיבות: **166 שנ׳** — גדול מ-TTL של 120. רק 46.1% מהחלונות נדרסים.
   (⚠ על נתוני אמת עם כמה משתמשים המספר הזה יחמיר משמעותית.)
3. **❌ “הנפח הוא הבעיה” — הופרך לחלוטין.** 40.7 KB סך הכול.
   הטבלה הכבדה במערכת היא `audit_log` (229 שורות) — **והיא כבר לא נשלחת**, בזכות B51.

### 🔴 השורש האמיתי: **מספר הקריאות, לא כמות הנתונים**

ההוכחה בשורה אחת מתוך הטבלה:

> **`invoices` — 3 שורות · 1.3 KB · זמן קריאה: 1.1 שניות.**

וכך כל הטבלאות: 127–680 מ"ש **לכל טבלה, בלי קשר לגודלה.**
כל `readTable` הוא `getRange().getValues()` = **נסיעה נפרדת לשרתי Google.**
**55 נסיעות × ~215 מ"ש = 11.9 שניות.** הנתונים עצמם אפסיים.

**זו בדיוק הנקודה מ-R10:** מערכת שנוסעת 55 פעם לכל בקשה תהיה איטית גם עם
טבלה של 3 שורות — ומהירה עם מיליון שורות אם תיסע פעם אחת.

### ההיקף של B58 — שני תיקונים, שניהם תשתיתיים

**① קריאה אחת במקום 55 — העיקר.**
מעבר ל-**Advanced Sheets Service**: `Sheets.Spreadsheets.Values.batchGet`
קורא **את כל הטווחים בקריאת HTTP אחת.**
`loadDb`/`readTable` נשארים בחוץ כלפי כל שאר הקוד — משנים רק את המימוש מתחתם,
כך ש-`db.orders` ממשיך לעבוד בדיוק אותו דבר.
**צפי: 11.9 שנ׳ → ~1 שנ׳.**
⚠ **דורש הפעלת שירות Sheets ב-Apps Script** — ולכן `ACT-16`.
⚠ **זהירות מיוחדת במניפסט**: אבי כבר נתקל בכפילות `Drive` ב-`appsscript.json`.
   להפעיל דרך *שירותים ← +* בלבד, ולוודא שאין רשומה כפולה.
⚠ **נפילה חיננית חובה:** אם שירות Sheets אינו זמין — ליפול חזרה ל-`readTable`
   הקיים, בלי לשבור כלום.

**② מפתח מטמון לכל טבלה, במקום `data_version` אחד גלובלי.**
היום כתיבה להזמנות מוחקת גם את `settings` ואת `employees`.
**⚠ נוגע ב-`bumpDataVersion` (מנגנון B7)** — ולכן חובה בדיקת שוויון מלאה,
ו-`checkVersion` חייב להמשיך להחזיר בדיוק את אותה סמנטיקה כלפי הממשק.

### ⛔ מה **לא** נכנס ל-B58 — והסיבה

- **snapshot ליתרות מלאי (`BLD-11`/B59):** ⚠ **נדחה. הוא מיותר כרגע.**
  `stock_moves` שוקלת אפס והקריאה שלה אינה בעשרים הכבדות. R3: זו אינה התחמקות —
  זו החלטה מדודה. **חוזרים אליה רק אם מדידה עתידית תראה שהיא נכנסה לצוואר.**
- **חיתוך נוסף ב-`SLIM_POLICY`:** מיותר. 40.7 KB.
- **שליפה ממוקדת לכל מסך (R10 סעיף 1):** נכון עקרונית, אבל **תיקון ① מייתר
  את הדחיפות שלו.** נשאר ב-`R10` ככלל מחייב לכל מסך **חדש**, ולא כשכתוב עכשיו.

---

### שני פריטים שנכנסים ל-PERF-02 **בכל מקרה**, בלי תלות במספרים

1. **חיווי “טוען” במקום מסך הכניסה** (חצי הרענון של `T4`).
   `enterApp()` יציג מצב טעינה מפורש לפני `await api('getAll')` — **בשני
   המסלולים** של `autoLogin()`, כולל מסלול טוקן-הסשן שהיום שותק לגמרי.
   ⚠ **אסור לגעת ב-`IDLE_LIMIT_MS` ובנעילת B7** — שני מנגנונים נפרדים.
2. **`pollDataVersion` לא ימשוך `getAll` מלא בכל bump.** היום כל לקוח מושך את
   כל בסיס הנתונים כל 30 שניות ביום עבודה פעיל. **⚠ חייב להישאר נכון:**
   נתון שהשתנה במסך אחד חייב להופיע במסך אחר. הכרעה מוצגת לאבי.
   ⚠ **המדידה חידדה את זה:** רצפת התקורה של Apps Script היא **2.2 שניות לכל
   בקשה**, גם ל-`checkVersion` הזעיר. כלומר גם אחרי תיקון ①, כל בקשה עולה
   2.2 שניות. **המסקנה: לצמצם את מספר הבקשות, לא רק את משקלן.**

### מפת חיבורים (R2) — `PERF-02`

1. **איפה הנתון נשמר?** **אין שדה חדש ואין טבלה חדשה. אין שינוי סכימה,
   אין להריץ `setupDatabase`.** מפתחות המטמון חיים ב-`CacheService` בלבד (נדיפים).
2. **מי כותב אותו?** אף מסך. `getAllCached` · `bumpDataVersion` · `pollDataVersion`
   בלבד — שלוש נקודות, כולן תשתית.
3. **מי קורא אותו?** `getAll` (כל התפקידים) · `getDriverFilteredPayload` (נהג) ·
   `enterApp` · `pollDataVersion` · `enterOfflineIfPossible`. **רשימה סגורה — `grep` בוצע.**
4. **לאן הוא זורם? ⚠ כסף:** `getAll` מזין את `custBalance` ואת `b2CreditUsedAg`
   בממשק. **לכן R6 חל במלואו:** `b48BalancesAg` · `custBalance` · `b2CreditUsedAg`
   חייבים להחזיר אותו מספר לפני ואחרי. אין חיוב חדש · אין כניסה ל-`b54Ledger` ·
   אין חשבונית או קבלה · אינה הוצאה · אינה נוגעת ברווח · אינה נוגעת בתזרים B43 ·
   אינה נוגעת במסגרת האשראי S5 · כלל המע"מ B45 אינו רלוונטי —
   **הסיבה לכל “לא”: PERF-02 משנה מתי ואיך הנתון מגיע, ולא ולו ספרה אחת בתוכנו.**
5. **מצבי קצה?** מטמון ריק · מטמון חלקי (חלק מהנתחים פג) · כתיבה בדיוק בין
   שתי קריאות · שני משתמשים מרעננים יחד · רשת נופלת באמצע · מטען שגדל מעל
   התקרה אחרי הפיצול. **כל אחד מהם דורש בדיקה כתובה ב-B58.**
6. **מי רואה את זה?** כל חמשת התפקידים וכל ארבעת הקשרי הריצה.
   ⚠ הפורטל והחנות אינם עוברים ב-`getAll` (`DB=null`) — לוודא שלא נשברו.
   ⚠ **הקיוסק אינו מושך `getAll` כלל** — לוודא שהוא נשאר כך.

**R4 — מה אסור לגעת בו ב-B58:** `API_URL` · מנוע האשראי · `b54Ledger` ·
`clockCore`/`attEff`/`buildPayroll` · `READ_ONLY_ACTIONS` (רק קריאות) ·
`B52_KIOSK_ACTIONS` · פריסת B53. **וכל אצווה מגיעה עם בדיקות שמוכיחות
מטען זהה לפני ואחרי** — זו הבדיקה המרכזית של PERF-02.

---

## PERF-01 — ההיקף המקורי (נשמר לתיעוד)

**מצב:** ניתוח האיטיות הוגדר כהיקף אך **מעולם לא בוצע ואינו מתועד ב-`PROGRESS.md`.**
**האצווה הראשונה שלו היא אבחון, לא תיקון.**

**מה כבר ידוע (מהקוד):**
- B51 קיצץ את `getAll` לפי `SLIM_POLICY`, אבל **`stock_moves`, `package_moves`,
  `fault_events`, `attendance`, `payroll` לא נחתכו** — הן צומחות ללא גבול.
- `getAllCached` (B12) מטמון 120 שניות; המפתח כולל `data_version`, **ו-`data_version`
  מתעדכן בכל כתיבה במערכת** — כלומר במערכת פעילה המטמון נדרס תדיר. זו חשודה מרכזית.
- `enterApp()` מושך `getAll` מלא לפני הצגת המסך.

**חובה באצווה:** כלל R1 — פעולת אבחון `READ_ONLY` שמציגה על המסך: משקל לכל טבלה,
זמן ריצה לכל שלב (שליפה · העברה · רינדור), ושיעור פגיעות המטמון. **בלי ניחושים.**
**קו בסיס:** ACT-02 יספק את המספר לפני האצווה.

**⚠ מיזוג T4:** ראה `T4` — ייתכן ש"רענון מוציא מהמערכת" הוא תסמין של האיטיות הזו.
**PERF-01 מכריע בשאלה הזו, ו-T4 לא נבנה לפניו.**

---

## T1 — לחיצה על יום ביומן אינה מאפשרת ליצור כלום 🟡 · **✅ בוצע ב-B64 (31.07.2026)**

> **✅ נסגר.** `calEvents()` נמנתה במלואה (7 סוגי אירועים, ואפס מועדי תשלום),
> ולחיצה על יום פותחת יצירה של הזמנה · משימה · פגישה · יום לתשלום, עם
> התאריך מאוכלס. **אפס טפסים חדשים** — ארבעת הטפסים הקיימים קיבלו פרמטר
> `iso` אופציונלי בסוף.
> ⛔ **"יום לתשלום" = הוצאה עתידית** (`futureExpForm`), ונכנס לתזרים B43.
> **מועד פירעון של לקוח מוצג ואינו נוצר** — הוא נגזר מ-`b54LedgerFE`, אותו
> מקור של דוח גיל החובות; יצירה ידנית הייתה מייצרת שני מספרים סותרים
> לאותו חוב. ⛔ טסט/ביטוח/טיפול רכב — מוצגים ואינם ניתנים ליצירה (הכרעת
> אבי; אלה שדות בכרטיס הרכב). ⛔ כסף ביומן — מנהל ומשרד בלבד.

**אבי:** *"לחיצה על יום ביומן אמורה לפתוח הוספת אירוע/פגישה/אסיפה/יום לתשלום ועוד. זה לא עובד."*

**אומת בקוד:** `openDay(iso)` פותחת מודל **קריאה בלבד** שמציג אירועים קיימים ליום.
אין בה שום פעולת יצירה. מקור האירועים: `calEvents()`.

**מה נדרש:** מסלול יצירה מהיום שנלחץ, כשהתאריך כבר מאוכלס.

**דרישות מחייבות למפרט (מעבר ל-R2):**
- לעבור על `calEvents()` ולמנות **כל** סוג אירוע שהיומן מציג — **מה שהיומן מראה הוא מה
  שהוא צריך לדעת ליצור.** לכל סוג: לאיזו טבלה נכתב, ואיזה טופס קיים כבר
  (`meetingForm`, טופס משימה, טופס הזמנה).
- **להשתמש בטפסים הקיימים. אסור לכתוב טופס שני לאותה ישות.**
- אם "יום לתשלום" פירושו מועד פירעון — **תשובה מפורשת** איך הוא מתחבר לספר החיובים
  (`b54Ledger`), לתזרים (B43) ולדוח גיל החובות. **אין ליצור ישות כספית חדשה בלי לחבר אותה.**

---

## T2 — הניווט וסרגלי הפעולות עמוסים ולא מקצועיים 🟡 · **✅ בוצע ב-B62 (31.07.2026)**

> **✅ נבנה. הכרעת אבי: חלופה ב' — קיפול + נעיצה.**
>
> **המספר שהכריע (חושב מה-CSS החי מול המדידה של אבי, חלון 1422×650, 29 מסכים):**
> 1278px של תוכן בתוך 596px זמינים — **פי 2.14**. 15 מסכים דרשו גלילה.
>
> **הכלל שנבנה, אחד ויחיד:** *קטגוריה פתוחה אם היא נעוצה או אם המסך הנוכחי
> בתוכה.* לחיצה על שורת הכותרת = נעיצה/שחרור. נשמר ב-`localStorage`
> (`mn_nav_pins`) — שורד מעבר מסך, רענון וסגירת דפדפן. **אפס בקשות שרת.**
> ההנפשה ב-`grid-template-rows` עם החלפת מחלקה **בלי `renderNav`** —
> ולכן אין קפיצת פריסה. אין חצים ואין אקורדיון. ⛔ שום מסך לא נמחק מה-DOM.
>
> **⚠ R9 — המפרט למטה הצביע על המקום הלא נכון.** הוא דרש למפות את כל
> ה-`section-title`. נסרקו כל 38: **ל-31 מהם אפס כפתורים, ולשאר 1–2.**
> העומס האמיתי הוא בכרטיסים: `openDelivery` 16 · `driverStopCard` 15 ·
> `openOrder` 14 — **ושניים מהשלושה כבר מיישמים את הדפוס שהמפרט ביקש.**
> ב-B62 טופלו `openOrder` (`b62OrderActions`) והשורה העליונה. אף כפתור לא
> הוסר ואף תנאי הרשאה לא נחלש.
>
> **⚠ נותר — ראה `T2ב` בחלק 5.** חמישה מסכים נוספים עם צפיפות כפתורים
> בינונית לא טופלו: `doScan` · `itemForm` · `rShelves` · `loadApprovals` ·
> `floorRenderInfo`. **המיפוי הכתוב שלהם הוא בדיוק מה ש-T2 דרש והוא הועבר
> לשם** — זו אינה דחייה בלי מפרט.

<details><summary>הניסוח והמפרט המקוריים — לתיעוד</summary>


**אבי:** *"תפריט הצד והתפריט העליון קצת ילדותיים והכל פתוח מדי. כל תצוגה מרובת כפתורים,
צריך לארגן את זה מקצועי יותר ולהעביר כפתורים ודברים מסוימים מה'פרונט' לתת-תפריט/3 נקודות."*

**זו בקשה עיצובית — ולכן ההכרעות מוצגות לאבי לפני בנייה, לא אחריה.**

**אומת:** דפוס תפריט שלוש נקודות **כבר קיים** — `moreMenu(items)`, `.more-wrap`,
`toggleMore`, `closeMore`. **אין ליצור דפוס שני** (R8).

**מה המפרט חייב לכלול:**
- מיפוי כתוב של **כל** `section-title` במערכת: כמה כפתורים בו, איזה הוא הפעולה הראשית
  (נשארת גלויה) ואיזה משני (עובר ל-⋯). **הכלל: פעולה ראשית אחת גלויה לכל מסך, השאר בתפריט.**
- **בסרגל הצד — `DEC-05` הוכרעה 29.07.2026: כן, מותר לקפל קטגוריות שאינן פעילות.**
  **הכרעת N3 מתעדכנת ל-N3א:** הסרגל נשאר קבוע ופתוח ברוחב קבוע (N3 לא בוטלה),
  אבל **קטגוריה שאינה פעילה רשאית להיות מכווצת.**
  ⚠ **מגבלה מפורשת של אבי — קרא אותה פעמיים:**
  *"לא להחזיר לאיך שהיה אלא לשדרג שיהיה יותר יעיל ומכובד."*
  **אסור** להחזיר את האקורדיון הישן (`NAV_OPEN`/`toggleNavPanel` — הוא ממשיך לשמש
  את המובייל בלבד ו-R4 אוסר לגעת בו). נדרש **דפוס חדש** שעומד בשלושה תנאים:
  (א) **יעיל** — מעבר בין מסכים בלחיצה אחת, גם בין קטגוריות. הקטגוריה של המסך הנוכחי
  פתוחה תמיד ואוטומטית; קיפול לא יוצר "לחפש ואז ללחוץ" כמו בתפריט B47 שאבי דחה.
  (ב) **מכובד** — מעבר רך, בלי קפיצות פריסה, בלי חצים ילדותיים.
  (ג) **זוכר** — מצב הקיפול נשמר בין מסכים ובין רענונים (⚠ מתואם עם הכלל של `T3`
  על מצב מסך — קיפול הוא מצב **שנשמר במכוון**, לא מצב שמתאפס).
  להציג לאבי 2–3 חלופות לדפוס עצמו לפני בנייה.
- 2–3 חלופות ממוספרות עם המלצה מסומנת, לפי `WORKING_PROTOCOL` חלק 4.
- **אסור שכפתור ייעלם.** כל פעולה שעוברת ל-⋯ נשארת נגישה, ואף הרשאה לא משתנה בדרך.

</details>

---

## T3 — אי אפשר לחזור לקטלוג הדוחות בלחיצה על "דוחות" 🟡 · **✅ בוצע ב-B60 (30.07.2026)**

**אבי:** *"אם אני נכנס לדוח מתוך 'דוחות' ואחר כך רוצה לחזור, אני צריך ללחוץ על לחצן ייעודי.
לחיצה שוב על 'דוחות' לא מנווטת לתצוגה ראשית."*

**אומת ומאובחן במלואו:** `REPORT_ID` הוא משתנה מצב גלובלי. `repBack()` מאפס אותו — **`go()` לא.**
לכן `go('reports')` מרנדר מחדש את אותו דוח.

**⚠ אסור לתקן רק את `reports`.** המפרט חייב לענות: **אילו עוד מסכים מחזיקים מצב פנימי
שאינו מתאפס בכניסה חוזרת?** (טאבים נבחרים, מסננים, כרטיס פתוח, `REPORT_PARAMS`).
`grep` על משתני מצב ברמת המודול → **כלל אחד כתוב:** מתי מצב מסך מתאפס ומתי הוא נשמר במכוון.
אחרת נתקן מסך אחד ונשאיר חמישה שבורים באותה דרך.

### ✅ מה נבנה ב-B60

**הסקירה שנדרשה בוצעה:** `grep` על כ-70 משתני מצב ברמת המודול + מעבר על 29
פונקציות ה-render. הממצא: **`rReports` הוא היחיד שמחליף את כל המסך בתת-מסך
לפי משתנה מודול.** כל שאר המסכים מבוססי טאבים גלויים, שבהם למשתמש יש תמיד
דרך לצאת. אבל נמצאו **שתי מלכודות מאותה מחלקה** שהיו נשארות: הקליטה ברצפת
הייצור נשארה קשורה להזמנה ישנה, ותעודת החזרה נשארה פתוחה בזיכרון.

**הכלל שנכתב — שלוש מחלקות. כל משתנה מצב שייך לאחת מהן:**

| מחלקה | מה זה | מה קורה ב-`go()` |
|---|---|---|
| **א' — תת-מסך / סשן קשור לרשומה** | `REPORT_ID` · `REPORT_PARAMS` · `FLOOR_INTAKE_ORDER` · `FLOOR_INTAKE_CARTS` · `FLOOR_BC` · `FLOOR_INFO` · `B49F_DECL` · `RET` · `RET_CAM` | **מתאפס בכל כניסה למסך**, כולל לחיצה חוזרת על אותו מסך. חריג יחיד: `go(v,{keep:1})` מנקודת דריל-דאון |
| **ב' — העדפת תצוגה** | טאבים (`FIN_TAB` · `ORD_TAB` · `DELIV_TAB` · `DRV_TAB` · `BI_TAB` · `ATT_TAB` · `FLEET_TAB` · `ASSET_TAB` · `FLOOR_TAB` · `CUST_TAB` · `EMP_TAB`) · מסננים (`AUDIT_FILTER` · `B54_F` · `B43_RANGE` · `FEXP_*` · `ITEMS_WH` · `SHELF_Q` · `COL_MONTH` · `B42_CUT` · `B42_CUT_SCOPE` · `UNITS_SHOW_DEL` · `NOBLE_REP_DAYS` · `B49F_DAYS` · `ATT_Y`/`ATT_M` · `CAL_Y`/`CAL_M` · `BI_DATA` · `B51_STATS`) | **נשמרת בכוונה.** המשתמש בחר אותה, היא גלויה על המסך, ויש דרך גלויה לשנותה |
| **ג' — הקשר מודל** | `CONFIRM_CB` · `MGR_DEL_CB` · `SIG_CTX` · `TAG_CTX` · `MENTION_CTX` · `PAY_CTX` · `COLLECT_CTX` · `MEET_DRAFT` · `B59_TASK` · `B34_PICKS` · `B44_OSCAN` · `B35_ST` · `B35_CNT` · `B46_PDF_WARN_NEXT` | **`go()` אינו נוגע בו כלל** — מאותחל בכל פתיחת מודל |

⚠ **חריג מפורש שנרשם בקוד: `UNDO_PENDING` לעולם אינו מתאפס.** הוא מחזיק פעולה
שממתינה חמש שניות לשליחה לשרת; איפוסו יבטל שליחה שהמשתמש כבר אישר.

**איפה זה יושב:** `B60_VIEW_RESET` ב-`index.html` — טבלה אחת, ליד `go()`.
**אין רשימה שנייה שתתיישן.** מסך חדש עם מצב מחלקה א' מוסיף שורה שם וזהו.

**סיפוח שנעשה (WORKING_PROTOCOL חלק 4א):** `goFin(tab)` הציב `VIEW='finance'`
ישירות ועקף את `go()` לגמרי — ולכן לא נשמר ב-`mn_view` (רענון לא החזיר לכספים)
ולא נכנס להיסטוריה. הועבר לעבור דרך `go()`. `FIN_TAB` הוא מחלקה ב' ולכן נשמר.

**מה במכוון לא שונה:** שלושת מסלולי המצלמה (`RET_CAM` · סריקה · `FLOOR_CAM`)
מציבים `VIEW` ישירות. הם **אינם ניווט** — הם חזרה למסך שהמשתמש כבר עובד בו,
אחרי שכיסוי המצלמה החליף אותו זמנית. המרתם ל-`go()` הייתה מוחקת את הקשר
הקליטה בדיוק ברגע שבו נסרק הברקוד. **זו הכרעה, לא השמטה.**

---

## T5 — קידום שלב בכביסה לא עובר לשלב שנבחר 🔴 · **✅ בוצע ב-B59 (30.07.2026)**

**אבי:** *"בכביסות, אם לא סורקים את העגלה ואת המכונה אלא מקדמים שלב על ידי
התוכנה — אם אני בוחר שלב שלא לפי הסדר (לדוגמא אחרי כביסה מיד גיהוץ או אריזה)
זה לא עובר לשם ישר. יש אפשרות שמדלגים על שלבים. תתקן שלפי השלב שנבחר זה יעבור."*

**השורש שאומת (R9).** לא היה במערכת בורר שלבים כלל — מה שנראה כמוהו הוא
**בורר המכונה**, שכל אפשרות בו מסמנת את שלב המכונה בסוגריים.
`advanceForm` חישבה `STAGES[idx+1]`, `doAdvance` שלחה עגלה+מכונה בלבד,
ו-`advanceLaundry` קבעה `toStage = STAGES[idx + 1]` קשיח.

**הכרעות אבי שנרשמו ב-B59:**
1. **דילוג שלבים מותר** — **מהפך את לשון "אין יותר דילוג" מ-B40**, שנגעה בשלבים.
   **דרישת העגלה והמכונה של B40 נשארת במלואה** — היא לא הופכה.
2. **חזרה אחורה מותרת** (כביסה חוזרת), מסומנת ביומן.
3. **"באריזה" אינו נעשה עמודה בלוח** — מכונת אריזה נחשבת מתאימה
   ל-"בגיהוץ וקיפול". אין שינוי סכימה ואין שינוי בערכים הנשמרים.
4. **משימה ב"מוכן" אינה נפתחת מחדש** — העגלה שוחררה ו-`done_date` נרשם.

**⚠ פתוח להכרעת אבי — `DEC-06`:** האם רצוי להוסיף עמודת "באריזה" מלאה
ללוח הכביסה (חמש עמודות ← שש)? ב-B59 נבחרה הדרך הבטוחה (מיפוי), כדי
שלא לגעת בערכים שכבר שמורים בגיליון. הוספת עמודה נוגעת ב-`STAGES`,
בדוח ביצועי המכבסה ובסטטוס הקליטה — אצווה נפרדת, לא תוספת אגב.

---

## T4 — "הקודם" מוציא מהאתר 🟡 · **✅ בוצע ב-B60 (30.07.2026)** · הוכרע ופוצל ב-B57

> **✅ הכרעת B57 (29.07.2026): שני תסמינים, שני דברים שונים.**
> **חצי אחד — “רענון מוציא” — הוא תסמין של PERF-01 ו‎עבר ל-`PERF-02`.**
> אומת בקוד: `mn_token` שורד רענון · `autoLogin()` משחזר · `mn_view` מחזיר
> לאותו מסך · `idleLock` אינו יכול לירות ברענון (`LAST_ACTIVITY` מאותחל בטעינה).
> אבי אינו נזרק — הוא רואה את מסך הכניסה, בשקט וללא הודעה, כל עוד `getAll` רץ.
> **חצי שני — “הקודם מוציא מהאתר” — באג נפרד ואמיתי שנשאר כאן.**
> `grep` על `pushState`/`popstate` מחזיר מופע אחד בכל `index.html`:
> `history.replaceState` של הקיוסק. המנגנון מעולם לא נבנה.
> **התור לא התקצר ל-17. `T4` נשאר, בהיקף מצומצם.**

**ההיקף שנותר ל-`T4`:** היסטוריית דפדפן בלבד — `pushState`/`popstate`.
כל מה שנוגע לרענון, ל-`idleLock` ולחיווי הטעינה **הוצא מכאן ל-`PERF-02`.**

### ✅ מה נבנה ב-B60

**אומת מחדש מול הקוד החי (R9):** `grep` על `pushState`/`popstate` החזיר מופע
אחד בכל `index.html` — `history.replaceState` של הקיוסק. **המנגנון אכן מעולם
לא נבנה.** נבנה עכשיו:

| מצב | ההתנהגות החדשה |
|---|---|
| "הקודם" ממסך שנוסע אליו | חוזר למסך הקודם. **לא יוצא מהאתר** |
| "הקודם" כשמודל פתוח | **סוגר את המודל** ונשאר במסך |
| "הקודם" בעוגן הבסיס (מסך ראשון, בלי ניווט) | יציאה אמיתית מהאתר, כמו בכל אתר. אין לכידת משתמש |
| "הקודם" למסך שאינו מותר לתפקיד | לא מזיז. המשתמש נשאר במקומו |
| לחיצה חוזרת על אותו מסך בתפריט | `replaceState` — ההיסטוריה לא מתנפחת |
| דריל-דאון (`goReport` · `b49dGoIntake`) | `pushState` גם כשהמסך זהה, כדי ש"הקודם" יחזיר לקטלוג |
| לחיצה על שם האתר (`goHome`) | כבר עבדה קודם — עכשיו גם נכנסת להיסטוריה |

**ארבעת הגבולות שנשמרו — הם התשובה ל"מה קורה בפורטל, בחנות ובקיוסק":**
1. **הכתובת עצמה לא משתנה לעולם.** `pushState` מקבל `location.href` כמו שהוא.
   זה מה ששומר על `?kiosk=1` · `?portal=1` · `?shop=1` — ובפרט על
   `history.replaceState` של הקיוסק (B52), שמנקה את `kt` מהכתובת. **אין התנגשות.**
2. **המנגנון פעיל אך ורק בהקשר צוות וכשהמערכת פתוחה בפועל.** `b60HistCtx()`
   מחזיר `false` בפורטל, בחנות, בקיוסק ובמסך הכניסה, ואז אף מאזין אינו עושה דבר.
   "הקודם" בפורטל ובחנות ממשיך להתנהג בדיוק כמו היום — הם מסך יחיד ואין להם ניווט.
3. **נעילת B7 מכבה את המנגנון מיידית** — `idleLock` מסתיר את `#app`. אין נגיעה
   ב-`IDLE_LIMIT_MS`, ואין החלשה של הנעילה. חלון 10 הדקות של אבי ונעילת
   האבטחה נשארו **שני מנגנונים נפרדים**, בדיוק כפי שנדרש.
4. **`closeModal` לא שונה, ואין קריאה ל-`history.back()` בשום מקום** מלבד
   עוגן הבסיס. חמישים אתרי הקריאה מהדפוס `closeModal();go('x')` ממשיכים כמו שהם.

**נקודת החיכוך בין `T3` ל-`T4`** — שני הפריטים אכן יכלו לבטל זה את זה, בשתי
נקודות מדויקות, ושתיהן נפתרו בדגלים מפורשים ב-`go(v, o)`:
- `o.keep` — דריל-דאון: המצב הוגדר לפני הקריאה ואסור לאפס אותו (T3), והרשומה
  בהיסטוריה נדחפת גם כשהמסך זהה (T4).
- `o.fromHistory` — הניווט הגיע מ"הקודם": מאפסים מצב מסך כרגיל, אבל **לא**
  דוחפים רשומה חדשה, אחרת כל לחיצת "הקודם" הייתה יוצרת לולאה.

**התוצאה הרצויה:** "הקודם" מתוך דוח פתוח מחזיר **לקטלוג הדוחות** — כלומר כלל
האיפוס של `T3` חל גם על ניווט היסטוריה. יש בדיקה ייעודית לזה.

**R10:** אפס בקשות שרת נוספו. הכל בצד הלקוח.

---

## T4 — הניסוח המקורי (נשמר לתיעוד)

**אבי:** *"לחיצה על 'הקודם' בדפדפן מוציאה לגמרי מהאתר לתצוגת כניסה. שלחיצה על 'הקודם'
תחזיר למקום הקודם שהיית בו. אותו דבר רענון. תתקן שככל והפעולה האחרונה בוצעה ב-10 הדקות
האחרונות, רענון יחזיר לאותו דף, ושלחיצה על שם האתר (ימין למעלה) תיקח לעמוד הבית."*

**מה שאומת בקוד:**
- **אין שום אינטגרציה עם היסטוריית הדפדפן.** אין `pushState`, אין מאזין `popstate`.
  `go()` שומר את המסך ב-`sessionStorage` בלבד. "הקודם" יוצא מהאתר — **זה מנגנון שלא נבנה מעולם.**
- **רענון:** `autoLogin()` **כן** משחזר סשן מ-`sessionStorage`, ו-`sessionStorage.mn_view`
  **כן** מחזיר לאותו מסך. הכוונה כבר בקוד.
- `goHome()` (לחיצה על הלוגו) **כן** קורא `go('dash')` כשהמסך מותר.

**⚠ השערה שחובה לאמת או לפסול לפני כתיבת מפרט:** ייתכן שרענון אינו מוציא באמת, אלא
ש-`enterApp()` מושך `getAll` מלא — וכל עוד המערכת איטית, אבי רואה את מסך הכניסה עשרות
שניות ומסיק שנזרק. **אם זו הסיבה — T4 הוא תסמין של PERF-01, והתיקון הנכון הוא סדר אחר:
קודם ביצועים, ואז חיווי "טוען..." במקום מסך כניסה.**
לבדוק גם: האם `idleLock` יורה מוקדם מדי, והאם `mn_token` שורד רענון בפועל.

**מה שהמפרט חייב לכלול בכל מקרה:**
- `pushState`/`popstate` כך ש"הקודם" מחזיר מסך ולא מוציא — **ובמפורש: מה קורה ל"הקודם"
  בפורטל, בחנות ובקיוסק.** ⚠ הקיוסק מנקה את הכתובת ב-`history.replaceState` — **אסור לשבור.**
- מודל פתוח + "הקודם" = **סגירת המודל**, לא יציאה מהמסך.
- חלון 10 הדקות של אבי מול `IDLE_LIMIT_MS` הקיים — **שני מנגנונים נפרדים.
  אסור שהתיקון יחליש את נעילת האבטחה של B7.**

---
---

# חלק 5 — קטגוריה BLD: אצוות מוכנות לבנייה

## קטנים — אצווה אחת, סיכון נמוך

| מזהה | מה | היקף | סכימה | הערות |
|---|---|---|---|---|
| **BLD-01** ✅ | **ריפוי הזמנות כביסה היסטוריות** — בוצע ב-B65 | ממשק+שרת | אין | `bld01Diagnose` (`READ_ONLY`) ← תצוגה מקדימה על המסך ← `bld01Heal` (כתיבה, מנהל/משרד בלבד). ⛔ **הריפוי אינו מכיל לוגיקה משלו** — הוא קורא ל-`b56CloseLaundryReturnLeg` ו-`b56CloseLaundryOrder`, ולכן אין מסלול תיקון שני שיכול להתפצל מ-B56. אידמפוטנטי: יש בדיקה שמשווה `JSON.stringify` של המסד לפני ואחרי הרצה שנייה. ⛔ אין נגיעה בכסף — `B54_SKIP_ORDER` מחריג רק בוטלה/טיוטה/הצעת מחיר |
| **BLD-14** ✅ | **טופס הזמנה שמשנה צורה לפי הסוג** — בוצע ב-B65 (דיווח אבי באמצע הסשן) | ממשק | אין | בורר "סוג" היה `<select>` בלי `onchange`. ⚠ R9 — **"מסירה והחזרה ע"י הלקוח" כבר היה בנוי ב-B49e**; מה שחסר היה השם הנכון. `b49eLegLabel` נשאר מקור האמת היחיד לתוויות. ⛔ החלפת התוויות בטקסט בלבד — רינדור מחדש היה מוחק ערכים שהוקלדו. `presetType` אופציונלי ובסוף (כלל B64) |
| **BLD-15** ✅ | **שורות בהזמנת כביסה הן אומדן ולא חובה** — בוצע ב-B66 | שרת+ממשק | אין | `approveOrder` חסמה **כל** הזמנה בלי `orderLines`, בסתירה ל-C5 שמצהיר ששורות כביסה "אינן כסף ואינן נספרות" — המערכת דרשה נתון שהיא עצמה מתעלמת ממנו. התנאי מותנה עכשיו ב-`sPick(o.type, ORDER_TYPES)`. ⛔ **השכרה בלי שורות נשארת חסומה** · ⛔ **סוג לא מוכר או ריק מקבל את ההתנהגות המחמירה** · ⛔ **מקטע הפריטים לא הוסתר** — יש בדיקה שנכשלת אם מישהו יסתיר את "+ הוסף פריט". **⭐ באג שני שנתפס באותן עשר שורות:** `o.type === 'השכרה'` בהשוואה מדויקת — סוג עם רווח **דילג על בדיקת המלאי כולה בשקט**. תוקן. ✅ `deliverOrder` · `b54RawOrderTotalAg` · `availableQty` · חסימת התשלום B2/D2 — כולם נבדקו ולא נפגעו |
| **BLD-13** 🔴 | **תכנון עומס מכבסה** — טון ליום, מחשבון זמינות, גלילת עודף, אישור ללקוח | ממשק+שרת+פורטל | **יש** | בקשת אבי 01.08.2026. ⛔ **מפרט חובה לפני קוד (R2)** — המנוע נוגע בהזמנות, בהחזרות, ביומן, ברצפת הייצור ובפורטל. **מה שכבר אומת בקוד:** `items.weight_kg` קיים ✅ · `settings` היא מפתח/ערך ולכן "טון ליום" אינו שינוי סכימה ✅ · ⛔ **ל-`orders` אין עמודת משקל** — אומדן משקל בהזמנה **דורש `setupDatabase`**. **שתי מלכודות שזוהו:** גלילת העודף היא חישוב רקורסיבי על פני ימים וכשל בו **שקט** · האישור ללקוח יושב בפורטל שבו `DB=null`, וכל קריאה ל-`setting()` תקרוס שם |
| **BLD-02** ✅ | **כפתור השבתת ספק שירות** — בוצע ב-B64 | ממשק | אין | ⚠ R9 — `active` **והפעולה בשרת** (`b42SaveProvider` עם `id`) היו קיימים מ-B42; מה שחסר היה **מסך**: אנשי המקצוע הופיעו רק בתוך הבורר. נבנתה רשימה בתוך **רכש וספקים** ולא מסך חדש — 29 המסכים נשארו 29. מנהל ומשרד בלבד. ⛔ השבתה אינה מחיקה: `b42Providers()` כבר מסנן, המושבת נעלם מהבוררים ונשאר בהיסטוריה |
| **BLD-03** ✅ | **קיוסק לשני טאבלטים ומעלה** — בוצע ב-B66 | ממשק+שרת | אין (ScriptProperties) | `kiosk_token_hash` היה מפתח בודד ורישום שני **דרס את הראשון בשקט**. עכשיו `kiosk_token_hash_<n>` עם שם ידידותי, עד 10 טאבלטים. ⛔ **מרחיבים ולא מחליפים (R4)** — `B52_TOKEN_KEY` נשאר, והטאבלט הרשום ממשיך לעבוד כמכשיר 0 בלי רישום מחדש. ⛔ **ביטול תמיד פר-מכשיר**; ביטול בלי מזהה מוחזר כשגיאה ואינו מוחק כלום. ⛔ **hash לעולם לא יוצא ללקוח** — `b52KioskStatus` ממפה. ⛔ `B52_KIOSK_ACTIONS` לא נפתחה — יש בדיקה. ⚠ הרישום והביטול הוצאו מה-`switch` ל-`b52RegisterDevice`/`b52RevokeDevice` כדי שיהיו ניתנים לבדיקה |
| **BLD-04** ✅ | **סידור קיצורי הדרך בגרירה** — בוצע ב-B62 | ממשק | אין | Pointer Events (לא HTML5 drag — אינו נורה במגע) · `touch-action:none` · `insertBefore` בלי רינדור מחדש · שני חצים בכל שבב כמסלול חלופי · תקרה 6→10 עם `qmini` אוטומטי |
| **BLD-05** | **הורדת נקודת השבירה של הסרגל ל-1024px** | ממשק | אין | 🔴 **ירד מ-B62 → B63 → B64 → B65 → B66 → B67 → B68. ב-B67 לא הודבק בלוק בדיקה עצמית ולכן לא סופח, בדיוק לפי ה-OPENER. ב-B66 אבי ביקש לדלג במפורש; `BLD-03` ו-`BLD-15` נבנו בכל מקרה.** ✅ הסיבה נמצאה ב-B65, והיא לא הייתה אצל אבי:** `b61Text` העתיק **רק בדיקות שנכשלו**, ולכן כשהפריסה נכנסה — והיא נכנסה — השורה עם המספר לא נכנסה לבלוק שהודבק לצ'אט. בנוסף, המדידה כיסתה את המסך שפתוח בלבד, והכרטיס יושב ביומן פעולות. **שתיהן תוקנו ב-B65:** `b65RecordWidth` מודדת פסיבית אחרי כל רינדור ושומרת ב-`localStorage`, והמספר נכנס לבלוק ההעתקה **תמיד**. ⛔ הטענה החדשה **נכשלת בכוונה** עד שנמדדו כל ששת המסכים — מספר חלקי גרוע ממספר חסר. ⚠ B63 שינה את החשבון: הרצועה 206px (במקום 228px) ונעלמת לגמרי כשלא נבחרה קטגוריה. ⛔ **אם המספר יאמר שהמערכת צריכה יותר מ-1024px — אל תוריד את הנקודה. דווח לאבי ותן לו את המספר. זו התשובה הנכונה, לא כישלון** |
| **T2ב** ◐ | **המשך T2 — סרגלי פעולות** — **בוצע חלקית ב-B64** | ממשק | אין | ✅ `doScan` (יחידה · עגלה · מדף) ו-`loadApprovals` (אישור גלוי, דחייה ב-⋯) עברו לדפוס `b62OrderActions`. ⛔ **שלושת הנותרים נסגרים בזאת ולא יבוצעו** — ⚠ R9: הספירות 11/11/10 **נספרו ולא נקראו**. `itemForm` — כפתורי "＋ הוסף" צמודים כל אחד לשדה שלו (דפוס B39); ⋯ ינתק אותם מהשדה · `rShelves` — רצועת אייקונים `.rowact` קיימת ודחוסה · `floorRenderInfo` — מסך רצפת ייצור, כפתורים גדולים ברוחב מלא לעבודה בכפפות; ⋯ שם מזיק. **אין שם צפיפות לתקן** |
| **BLD-06** ✅ | **שיוך תשלום: החוב הישן ביותר כברירת מחדל + בחירת חשבונית מסוימת** — בוצע ב-B82 | שרת+ממשק | אין | ⚠⚠ **`R9` שינה את צורת הפריט: מחצית ממנו כבר הייתה בנויה.** `b2ProcessPayment` מקצה FIFO מהישן לחדש **מאז `D15`** (ההערה בקוד: *"הקצאה FIFO — הישן קודם"*), ומסך הגבייה במשרד כבר גובה מול מסמך בודד — `collectPayForm(cid, docKind, docId)` עם כפתור "גבה" בכל שורה ב-`collectCustModal`. ⛔⛔ **מה שכן היה חסר התברר כבאג ולא כפיצ'ר:** `b55EnsureDocsForDebt` הפיקה חשבונית ל**כל** החוב הפתוח — כל הזמנה בלי חשבונית פעילה **וכל** החיובים הידניים — **בלי קשר לסכום שהתקבל**; לקוח שחייב 2,360 ₪ והעביר 1,180 ₪ קיבל מסמכי מס על מלוא החוב. **התיקון:** `b82ParseTarget`/`b82DebtSort` (זהות תו-בתו בשני הקבצים) · `p.debt_target` ב-`b2ProcessPayment` שרק **מצמצם** את רשימת `debts` ואינו מסלול כתיבה שני (`R8`) · `opts.target`/`opts.capAg` ב-`b55EnsureDocsForDebt`, ⛔ **בלי `opts` ההתנהגות זהה למה שהייתה** · בורר "שיוך התשלום" במסך אימות התשלום, בנוי מ-`collectDocRows` הקיימת. ⛔ `R6` אומת בשלושת המקורות לפני ההפקה, אחרי הפקה סלקטיבית ואחרי גבייה ממוקדת | ✅ **שוחרר — `DEC-04` הוכרע 09.08.2026.** ברירת המחדל היא **החוב הפתוח הוותיק ביותר**, ולעובד תינתן אפשרות לבחור חשבונית מסוימת במקומה. ⛔ **אין לבנות דפוס שני (R8)** — `b67PayOneRow` כבר עושה בדיוק את זה בשכר, ו-`b54Alloc` כבר מקצה לפי סדר דטרמיניסטי. ⛔ **R6** — `b54Ledger`, `b48BalancesAg` ו-`b2CreditUsedAg` חייבים להחזיר אותו מספר אחרי השינוי. ⚠ `b55EnsureDocsForDebt` מפיק היום ל**כל** החוב הפתוח — זו הנקודה שמשתנה |
| **BLD-12** ✅ | **מחיר כולל מע"מ כמספר ראשי בחנות ובפורטל** — בוצע ב-B64 | ממשק | אין | `grep` העלה 8 אתרי מחיר בשני ההקשרים; 6 הוחלפו, 2 כבר היו נכונים. ⛔ **תצוגה בלבד** — הסכום לשרת נשאר נטו באגורות, ויש בדיקה שסורקת את `shopSubmit`/`portalSubmit` ונכשלת אם מישהו ישלח ברוטו. ⛔ כלל B45 נשאר בכל שאר המערכת (R4) — נבדק ש-`b45Money` לא התהפך ושעוזרי `b12*` לא חלחלו למסך הכספים. ⚠ הכרעת אבי: **מסגרת האשראי והחוב הקודם בפורטל נשארים נטו** ומתויגים במפורש — אלה מספרי המנוע |

## בינוניים

| מזהה | מה | היקף | סכימה | הערות |
|---|---|---|---|---|
| **BLD-08** ✅ | **`tests.js` בריפו + בדיקה עצמית בדפדפן** — בוצע ב-**B61**, 30.07.2026 | תשתית | אין | **87 בדיקות, 0 כשלים.** `harness.js` · `run.js` · 5 קבצי בדיקה · מניעת ריקבון דרך `requires` · `node --check` קודם לכול. `node_modules` ו-`package.json` **אינם** בריפו. **+ שכבה 2 בהכרעת אבי:** כרטיס "בדיקה עצמית" ביומן פעולות (מנהל בלבד, אפס בקשות שרת). ⚠ ממתין להרצה אחת של אבי |
| **BLD-07** | **תחזוקת רכב מלאה — שלב ב'** | ממשק+שרת | **יש** | (א) יומן ביקורי מוסך — `technician_visits` נשאר במכבסה בלבד בהכרעת B56; (ב) שיוך הוצאה לרכב — ל-`expenses` אין עמודת רכב, ולכן עלות התחזוקה בדוח הצי נגזרת מ-`fault_events` ולא מהנהלת החשבונות |
| **BLD-09** | **דוחות נוכחות ושכר לפעולת שרת ייעודית** | ממשק+שרת | אין | `attendance`/`payroll` לא נחתכו ב-B51 ויגדלו בקצב קבוע. **הפתרון אינו חיתוך לפי ימים** — דוח השעות של B50 ומדד Y1 נשענים עליהן ישירות. **קשור ל-PERF-01** |
| **BLD-10** | **ניצולת ברמת יחידה ממוספרת** | ממשק | אין | דוח הניצולת מודד ניצולת **פריטים**, לא `units`. מספיק להחלטות רכש; דרוש רק אם יידרש דיוק ברמת יחידה |

## גדול — אצווה ייעודית, סיכון גבוה

| מזהה | מה | היקף | סכימה | הערות |
|---|---|---|---|---|
| **BLD-11** | **יתרת פתיחה תקופתית ל-`stock_moves`** | ממשק+שרת | **יש** | הטבלה היחידה שאי אפשר לחתוך — יתרת המלאי היא סכום כל התנועות. הפתרון: snapshot חודשי. **נוגע בליבת המלאי; טעות כאן שקטה ומזיקה.** בעיה עתידית, לא כאב נוכחי |

---
---

# חלק 6 — חסום בתלות חיצונית

| מזהה | מה | מה חוסם |
|---|---|---|
| **BLK-01** | **חיבור ספק סליקה אמיתי** | ממתין לחשבון סליקה. **התפר מוכן:** ה-callback של הספק קורא ל-`b55ConfirmDeclaration` עם אותו `txn_id`. תפר אחד, בלי שכתוב |
| **BLK-02** | **פקדון אמיתי (D5/D10)** | אותו חשבון סליקה. היום stub: אימות כרטיס ₪1 + ערבות חלופית |

---

# חלק 7 — Backlog מוצהר: לא מתוכנן

מובא כדי שמיזוג עתידי לא "יגלה" אותם מחדש כרעיונות חדשים. **לא נדרשת פעולה.**

**חוויית משתמש:** E1 Document Flow · E2 כרטיס לקוח 360 · E6 יצירה מהירה · E7 תצוגות שמורות ·
E8 חיפוש גלובלי (Cmd+K) · E9 שפת צבעים אחידה · E10 Undo Toast · E11 זיהוי כפילויות לקוח ·
E12 מצבי ריק · Onboarding

**יכולות:** QR ללקוח · Loyalty · רב-לשוני · PWA · AI Insights · GPS חי (D14 — נדחה במפורש
ל-Phase 3) · SMS/WhatsApp (עמודת `channel` כבר קיימת ב-`notifications`)

**ארכיטקטורה:** פיצול לאפליקציות ייעודיות לפי סיווג (הנהלה/נהגים/ייצור) — כיוון עתידי
מוצהר; משפיע רק על כך שקוד חדש נכתב מודולרי

**מחוץ למערכת:** מערכת ניהול נכסים להשכרה ("נכסים") — פרויקט שני, starter kit הוכן, טרם החל

---

# חלק 8 — סגור. לתיעוד בלבד. אין לפתוח מחדש

| מה | ההכרעה |
|---|---|
| מסך `bi` לא עבר לדפוס הקטלוג של B49 | **מכוון.** BUILD_PLAN B49 סעיף 3 |
| `technician_visits` אינו כולל רכב | **מכוון (B56).** לרכב יש מוסך, לא "ביקור טכנאי". ראה BLD-07 |
| `customerBalance` לא הוסרה | **מכוון (B54).** נשארה כעטיפה דקה מעל ספר החיובים |
| מפרט B49g | **בוטל.** נבלע ב-B54, שני ממצאיו נשמרו שם במלואם. **אין לבנות אותו** |
| תוספת B36 (קיוסק) | **אבדה ואינה רלוונטית.** המפרט נכתב מחדש מאפס (K1–K4). **אין לחפש אותה** |
| דוח "עלות תחזוקה לפי חודש" | **נבנה ב-B50** כרשומת קטלוג נפרדת (`maint`), לא כדוח-משנה |
| חשבוניות מכבסה מרוכזות בלי PDF | **נסגר ב-B54** — כל חשבונית מכל מקור עוברת נתיב מסמך אחד |
| Bug 1 — חתימה בטאבלט | **תוקן ב-B49e** (הוקדם מ-B51). ממתין לאימות בלבד — ACT-12 |
| חיוב כפול/שווא ללקוח | **תוקן ב-B54** (ארבעת הממצאים). ממתין לאימות — ACT-09 |
| `portalPayDebt` | **הוסר לחלוטין ב-B55** |

---
---

# חלק 9 — התור המוכרע · ✅ אושר ע"י אבי 29.07.2026

## פעולות אבי — ❗ **אינן חוסמות את B57. אין להמתין להן.**

**תיקון 29.07.2026:** קודם נכתב כאן "שלב 0 — לפני הצ'אט הראשון". **זה היה שגוי.**
`B57` היא אצוות אבחון `READ_ONLY` — היא אינה כותבת דבר, אינה נוגעת בסכימה, ואינה
תלויה במסמכי PDF, בסיסמאות ביומן או במדידה מוקדמת. **הצ'אט הראשון נפתח מיד.**

הפעולות האלה נעשות **במקביל**, מתי שנוח, בסדר הזה:

| סדר | מזהה | הפעולה | דחיפות אמיתית |
|---|---|---|---|
| — | `ACT-07` | ✅ **בוצע** — Drive API כבר מופעל (אומת 29.07.2026) | — |
| 1 | `ACT-06` | הרצת `setupDatabase` — 30 שניות | 🔴 עמודות B49f חסרות עד שתרוץ |
| 2 | `ACT-08` | "תיקון מסמכים שנשמרו כ-HTML" — **אחרי `ACT-06`** | 🔴 חשבוניות ישנות נראות כזבל ללקוח |
| 3 | `ACT-01` | ניקוי סיסמאות מ-`audit_log` (תצוגה מקדימה → ביצוע) | 🔴 אבטחה |
| 4 | `ACT-02` | 📊 משקל הנתונים | 🟢 נחמד כקו בסיס, **לא נדרש** — B57 בונה מדידה משלו |

⚠ **אם `ACT-06` טרם רצה כשמגיעים ל-`BLD-07` או ל-`BLD-11`** (שתיהן בשינוי סכימה) —
שם זה כן חוסם. עד אז: מקביל.

---

## התור המאוחד — **8 אצוות נותרו** (B60–B67), אצווה אחת לצ'אט

> **B57 ✅ · B58 + B58a ✅ · B59 (`T5`) ✅ · B60 (`T3`+`T4`) ✅ בוצעו.**
> נותרו **14 פריטים** ב-**7 אצוות (B61–B67)**. בהכרעת אבי (30.07.2026) הם קובצו ל-**8 אצוות** — חיסכון של
> 8 צ'אטים. **הקיבוץ לפי אזור קוד, לא לפי גודל:** פריטים שנוגעים באותו אזור נבנים
> יחד, כי הגדלת הראש (`WORKING_PROTOCOL` חלק 3) ממילא עוברת על כולם בבת אחת.
> **המספור B60–B67 הוא חדש ומחליף את B60–B75.**

| # | Batch | פריטים באצווה | אזור הקוד המשותף | היקף | מודל | חסימה |
|---|---|---|---|---|---|---|
| ~~1~~ | **B60** ✅ + **B60a** ✅ | `T3` ✅ + `T4` ✅ | **מצב מסך וניווט** — `go()`, `B60_VIEW_RESET`, `pushState`/`popstate`. B60a: תיקון יציאה מהאתר + יומן אבחון היסטוריה | בוצע 30.07.2026 | — | — |
| ~~2~~ | **B61** ✅ | `BLD-08` ✅ | **תשתית** — `tests.js` בריפו + בדיקה עצמית | בוצע 30.07.2026 | — | — |
| ~~3~~ | **B62** ✅ | `T2` ✅ + `BLD-04` ✅ | **סרגל צד וקיצורי דרך** — נעיצה, `moreMenu`, גרירה ב-Pointer Events. `BLD-05` ירד ל-B63 | בוצע 31.07.2026 | — | — |
| ~~4~~ | **B63 + B63a** ✅ | **פריסת ניווט חדשה** (בקשת אבי, מחוץ לתור) | **תפריט עליון ראשי + רצועת צד נבחרת, בלי נעיצות.** מבטל את N1/N2/N3 מ-B53 ואת הקיפול מ-B62 | בוצע 31.07.2026 | — | — |
| ~~5~~ ✅ | **B64** | ~~`BLD-05`~~ + `T1` ✅ + `BLD-12` ✅ + `BLD-02` ✅ + `T2ב` ◐ | **נקודות כניסה ותצוגה** — בוצע 31.07.2026. 🔴 **`BLD-05` ירד ל-B65**: הבדיקה העצמית לא הורצה, אין מספר, ואסור לנחש (R11) | בינוני | Sonnet | ✅ הושלם · 175 בדיקות |
| ~~5~~ ✅ | **B65** | **טופס כביסה** + `BLD-01` ✅ + **מד רוחב** | **הזמנות כביסה ורגל החזור שלהן** — בוצע 01.08.2026. ⚠ ההיקף שונה בהכרעת אבי באמצע הסשן (אפשרות ב'): `BLD-03` נדחה ל-B66, טופס הכביסה נכנס במקומו. 🔴 **`BLD-05` לא בוצע — נבנה הכלי שימדוד אותו** | בינוני | Sonnet | ✅ הושלם · 228 בדיקות |
| ~~6~~ ✅ | **B66** | ~~`BLD-05`~~ + `BLD-03` ✅ + `BLD-15` ✅ + **`WASH-02`** ✅ | **קיוסק, שורות אומדן ובאג ייצור** — בוצע 02.08.2026. ⛔ נפתח בעצירה: קובץ השרת בריפו היה **ללא B65**. 🔴 **`BLD-05` ירד בפעם החמישית** — אבי ביקש לדלג על הבדיקה העצמית. ⭐ באמצע הסשן צורף מסמך ממצאי ביקורת; `WASH-02` אומת עצמאית ונכנס בהכרעת אבי | בינוני | Sonnet | ✅ הושלם · 266 בדיקות |
| **7** | **B67** | `BLD-09` + `BLD-10` + `BLD-06` | **דוחות ומסמכים בשרת** — נוכחות/שכר לפעולה ייעודית, ניצולת ליחידה, חשבוניות סלקטיביות | בינוני-גדול | Sonnet | ⚠ `DEC-04` (ל-`BLD-06` בלבד) |
| **8** | **B68** | **תכנון עומס מכבסה** — פריט חדש, `BLD-13` | **תת-מערכת חדשה** — טון ליום, מחשבון זמינות, גלילת עודף, אישור בפורטל, תצוגה בלוח החי וביומן. ⛔ **שינוי סכימה** · ⛔ **מפרט חובה לפני קוד (R2)** | גדול · **סיכון גבוה** | Sonnet | ⚠ דורש `setupDatabase` |
| **7** | **B67** | `BLD-07` | **שינוי סכימה** — תחזוקת רכב שלב ב'. ⚠ **מצהיר גם על עמודות `BLD-11`** כדי שאבי יריץ `setupDatabase` פעם אחת | בינוני | Sonnet | ⚠ `ACT-06` |
| **8** | **B68** | `BLD-11` | **ליבת המלאי** — יתרת פתיחה תקופתית ל-`stock_moves` | גדול · **סיכון גבוה** | Sonnet | העמודות מוצהרות ב-B66 |

### מה קבוע ומה ניתן לשינוי
- ~~B60~~ ✅ · ~~B61~~ ✅ · ~~B62~~ ✅ · ~~B63~~ ✅ · ~~B64~~ ✅ · ~~B65~~ ✅ בוצע · **B66 הבא בתור · B68 אחרון.**
- ⚠ **B63 נכנס מחוץ לתור, בבקשת אבי מול צילום מסך של ERP מסחרי.** המספור
  B63–B67 בטבלה נדחף באחד. זה תקין — אבי הוא הבעלים ומסמך זה מתעדכן אחריו.
- ✅ **`T2ב` סופח ל-B64 ונסגר חלקית.** `doScan` ו-`loadApprovals` בוצעו;
  שלושת הנותרים **נסגרו בלי ביצוע** — R9: אין בהם צפיפות לתקן. הפירוט
  בשורת `T2ב` בטבלת הקטנים.
- ✅ **`BLD-05` — הסיבה לארבע הדחיות נמצאה ב-B65 והיא הייתה בכלי, לא אצל אבי.**
  הרישום הישן נשמר למטה לתיעוד; הפתרון בשורת `BLD-05` בחלק 5.
- 🔴 **`BLD-05` ירד ארבע פעמים ברציפות** (B62 → B63 → B64 → B65 → B66) מאותה סיבה:
  אבי טרם הריץ את הבדיקה העצמית. **זה הפריט היחיד בתור שחסום על ידי פעולה
  של אבי ולא על ידי קוד.** הרצה אחת פותחת אותו.
- ~~**B61 (הבדיקות) לפני B62**~~ ✅ **התנאי מולא.** ⚠ אוחדו ל-`tests.js` אחד ב-B63.
  **B62 חייב להוסיף אליה** — הוא אזור הרגרסיה המסוכן במסמך.
- ✅ **`ACT-20` בוצע.** המדידה חזרה (Chrome 150 · Windows · 1422×650 · מגע: כן ·
  מצב side · 29 מסכים) ועליה נבנה B62. **זה עבד: הגובה 650px הוא שהפך את `T2`
  מ"שיפור" ל"תנאי", והמגע הוא שקבע Pointer Events במקום HTML5 drag.**
  ⛔ **מעכשיו זה נוהל קבוע:** אצווה שנוגעת בפריסה, במגע או ביכולת דפדפן
  מבקשת הרצה של הבדיקה העצמית **לפני** הבנייה, לא אחריה.
- **`BLD-06` תלוי ב-`DEC-04`.** אם ההכרעה לא התקבלה עד B65 — הפריט **יורד מהאצווה**
  ועובר ל-B68. אין להמתין ואין לחסום את B67 בגללו.
- **`BLD-05` תלוי ב-`ACT-05`.** ⚠ **קרה בפועל: לא אומת, והפריט ירד מ-B62 ל-B64.**
  ⚠ **B63 שינה את החשבון:** הרצועה צרה מהסרגל הישן (206px מול 228px) ומוסתרת
  לגמרי כשלא נבחרה קטגוריה. **המספר מטענת המדידה בשכבה 2 הוא מה שיכריע.**
  ✅ **אבל לא נזרק לדחייה סתם:** לפי R11 נבנה הכלי שיאמת — טענה בשכבה 2
  שמודדת את הרוחב שהמערכת באמת צריכה במסך הפתוח. **`ACT-05` אינו חוסם
  את B63 יותר**; הרצה אחת של הבדיקה העצמית מחזירה את המספר.

### נימוקי הקיבוץ — במשפט לכל אצווה
- **B60 (`T3`+`T4`):** שניהם מטפלים במה שקורה כשמחליפים מסך. אותה פונקציה, אותו `grep`.
  לבנות אותם בנפרד = לעשות את אותה הגדלת ראש פעמיים ולסכן התנגשות בין האצוות.
- **B61 (הבדיקות) לבד:** תשתית טהורה, לא נוגעת בהתנהגות. מחזירה את ההשקעה מיד באצוות המאוחדות שאחריה.
- **B62 (`T2`+`BLD-04`+`BLD-05`):** שלושתם **סרגל הצד**. לגעת בו שלוש פעמים בשלושה
  צ'אטים זה שלוש הזדמנויות לרגרסיה על אותו קוד.
- **B63:** שלושה פריטי ממשק באזורים נפרדים ובסיכון אפס — בדיוק המקרה שבו מיזוג חינמי.
- **B64:** שניהם נוגעים בזרימת הכביסה/קיוסק ובשרת, ואף אחד אינו נוגע בסכימה.
- **B65:** שלושתם פעולות שרת בתחום הדוחות והמסמכים. `BLD-09` ו-`BLD-10` קוראים מאותן טבלאות.
- **B66 לבד:** **שינוי סכימה אינו מתערבב עם כלום.** הכלל אינו נתון למיזוג.
- **B67 לבד:** ליבת המלאי, טעות שקטה ומזיקה, סיכון גבוה. לא מתמזג לעולם.

---

# חלק 9א — כללי המיזוג · ✅ אושר ע"י אבי 30.07.2026

**כל צ'אט מחויב לכללים האלה. הם גוברים על "אצווה אחת לצ'אט" שבמסמכים הישנים.**

## M1 — מה מותר למזג
פריטים מתמזגים כשמתקיימים **כל** אלה:
1. **אותו אזור קוד, או אזורים שאינם נוגעים זה בזה בכלל.** אמצע הדרך — לא ממזגים.
2. **אף אחד מהם אינו נוגע בסכימה.**
3. **אף אחד מהם אינו חסום** ב-`DEC` או ב-`ACT` פתוח.
4. **סך האצווה אינו עולה על שלושה פריטים**, או שניים אם אחד מהם "בינוני-גדול".

## M2 — מה אסור למזג. בלי חריגים.
- **שינוי סכימה עם כל דבר אחר.** אצוות סכימה נבנות לבד תמיד.
- **פריט בסיכון גבוה** (`BLD-11`) עם כל דבר אחר.
- **פריט חסום** עם פריט לא חסום — החסום יורד מהאצווה ועובר קדימה, לא עוצר את השאר.
- **שרת + ממשק כששני צ'אטים פתוחים במקביל.** ראה M4.

## M3 — הגדלת ראש באצווה מאוחדת
**חמש השאלות של `WORKING_PROTOCOL` חלק 3 נענות לכל פריט בנפרד, בכתב.**
מיזוג חוסך צ'אטים — הוא **אינו** חוסך את שלב ההבנה. אצווה שענתה על חמש השאלות
"לכל האצווה יחד" נפסלת. זו בדיוק הסכנה שהמיזוג יוצר, וזה התשלום עליו.

## M4 — צ'אט אחד בכל רגע נתון
**אסור לפתוח שני צ'אטים מקבילים.** שניהם עורכים את `index.html` ודורסים זה את זה.
זה כבר קרה. מיזוג מקטין את הפיתוי לפתוח צ'אט שני — זו אחת ממטרותיו.

## M5 — מסירה אחת, canary אחד, PROGRESS אחד
אצווה מאוחדת מוסרת **פעם אחת בסוף**, לא פריט-פריט. `PROGRESS.md` מפרט את
הפריטים בנפרד תחת אותו `B##`, כדי שיהיה אפשר לזהות מי שבר מה אם נשבר משהו.

## M6 — מתי לעצור ולפצל
אם באמצע האצווה מתברר שפריט נוגע בסכימה, או שהוא גדול פי כמה ממה שנרשם —
**עצור, מסור את מה שהושלם, ורשום את השאר לאצווה הבאה.** אצווה חלקית שנמסרה
נקייה עדיפה על אצווה שלמה שנקטעה.

---

## מפת השלבים — תמונת מצב מתקופת B57 (נשמר לתיעוד)

> ⚠ **הטבלה הבאה התיישנה פעמיים.** המספרים המעודכנים הם בטבלת התור המאוחד
> ובתמונת המצב שבסוף המסמך. נכון ל-30.07.2026 אחרי המיזוג:
> **16 פריטים נותרו, מקובצים ל-8 אצוות, B60–B67.**


| שלב | כמות | מי מבצע | הערה |
|---|---|---|---|
| **פעולות אבי (ACT)** | **14** | אבי, לחיצות | 7 מהן 🔴. חמש הראשונות קודמות לצ'אט הראשון |
| **אצוות לבנייה** | **17** | צ'אט לכל אצווה | B58–B74. B57 ✅ בוצע. `T4` נשאר בתור, בהיקף מצומצם |
| **הכרעות שנותרו (DEC)** | **3** | אבי | DEC-02 · DEC-03 · DEC-04 — אף אחת אינה חוסמת את B57–B71 |
| **חסומים חיצונית (BLK)** | **2** | ממתין לחשבון סליקה | לא נספרים בתור |
| **Backlog לא מתוכנן** | ~20 | — | לא נספר |

> **התשובה הקצרה אחרי B57 (התיישנה): 17 צ'אטים לבנייה (B58–B74). נכון ל-B59: 16, B60–B75.**
> **נכון ל-30.07.2026 אחרי המיזוג: 8 צ'אטים, B60–B67. זו התשובה התקפה.**
> `T4` **לא** התייתר — B57 הוכיח שהוא שני באגים, ורק חציו היה תסמין.
> ⚠ **B57 כן אישר שהמטמון נדרס בכל כתיבה** (ממצא 2), ולכן התרחיש שהוזכר כאן
> מראש חי: אם האבחון יראה בנוסף שהמטען אינו נכנס למגבלת `CacheService`,
> `PERF-02` יתפצל לשתיים ואז **18**. הטווח הריאלי: **17–18 צ'אטים.**

---

# חלק 10 — תבנית ה-OPENER לכל צ'אט מבצע

```
בונים B## = <רשימת המזהים>. קרא את WORKING_PROTOCOL.md בידע הפרויקט ופעל לפיו במלואו.
ההיקף המחייב שלך: TASK_QUEUE.md, הפריטים <רשימת המזהים> — ורק הם.
⚠ זו אצווה מאוחדת. קרא את חלק 9א (M1–M6) לפני שאתה מתחיל.
בפרט M3: הגדלת ראש נענית לכל פריט **בנפרד**, בכתב. אין קיצור דרך.

⛔ לפני כל דבר אחר: קרא את חלק 1 של TASK_QUEUE.md — שנים-עשר חוקי-העל R1–R12.
בפרט R2 (מפת חיבורים בשש שאלות — מפרט בלי סעיף כזה פסול),
R3 (אין "מחוץ להיקף") ו-R4 (רשימת המנגנונים החסינים).

משוך את ארבעת הקבצים החיים מהריפו דרך curl:
index.html · קוד שרת.txt · PROGRESS.md · TASK_QUEUE.md
⚠ ודא: canary v4.53-B56 בממשק · קוד שרת.txt מתחיל ב-'// ===='.
אם לא תואם — עצור והתרע.

⚠ R9: המפרט הוא כוונה, לא מציאות. אמת כל עוגן מול הקוד החי לפני שאתה בונה.
אם מצאת סתירה בין הפריט לקוד — עצור ואמור, אל תאלתר.

עבוד לפי שני השלבים (WORKING_PROTOCOL חלק 3), לכל פריט בנפרד.
מסירה אחת בסוף לכל האצווה — לא פריט-פריט (M5).
בסיום: קובץ/י הקוד + PROGRESS.md מלא (פירוט לכל פריט בנפרד) +
TASK_QUEUE.md מלא (כל פריטי האצווה מסומנים ✅) + HANDOFF עם OPENER לאצווה הבאה.
מודל: Sonnet.
```

---

---

# חלק 12 — ממצאי ביקורת מסלול הכביסה (02.08.2026)

**מקור:** צ'אט ביקורת מקביל, `WASH_FINDINGS_FOR_BUILD.md`, נמסר לאבי ב-02.08.2026.
⛔ **המסמך אינו הוראת בנייה.** כל ממצא נבנה רק בהכרעת אבי, ורק אחרי אימות
עצמאי מול הקוד החי (R9 — מפרט הוא כוונה, לא מציאות).

⛔ **לא יותר מממצא אחד לאצווה** בכל מה שנוגע בכסף. שלושת ה-BLOCKER הפתוחים
יושבים כולם באזור בלי אף בדיקה קיימת.

| מזהה | חומרה | מה | מצב |
|---|---|---|---|
| **WASH-01** | BLOCKER | קוד השרת של B65 לא היה בריפו | ✅ **נסגר ב-B66** — אבי העלה ופרס. הבדיקה בפתיחת הסשן היא שתפסה |
| **WASH-02** | BLOCKER | הזמנת כביסה נסגרת בזמן שהכביסה במכונה | ✅ **בוצע ב-B66** — `B56_CLOSED_INTAKE='נמסר'` + `b56IntakeOpen()`, בדיקה שלילית |
| **WASH-03** | BLOCKER | עגלה שחוזרת לאותה קליטה מוחקת את החיוב הקודם | ✅ **בוצע ב-B70 (08.08.2026).** כל שקילה היא שורת חיוב · תיקון מנהל עם סיבה ו-`ref_id` · `nobleMarkReady` חוסמת מחזור שלא נשקל. ⚠ שינוי סכימה: `laundry_events.ref_id` |
| **WASH-04** | BLOCKER | מודול NOBLE כולו לא עבר את B64a | ✅ **בוצע ב-B69 (08.08.2026).** ⚠ הספירה במסמך הביקורת (~20) הייתה שגויה — **91 אתרים** בפועל, ו**שבעה** נגעו בכסף ולא שלושה |
| **WASH-05** | HIGH | אין מניעה לשיוך שתי **משימות** לאותה מכונה | ✅ **נסגר ב-B75 (17.08.2026).** `w05Gate` — נקודה אחת שמשרתת גם את `advanceLaundry` וגם את `b40SplitLaundry` (`R8`). ⛔ **הכרעת אבי:** חסימה **עם דילוג בקוד מנהל ופירוט חובה**, שנרשם ביומן המשימה יחד עם מזהה המשימה התופסת. ⛔ משימה ב`'מוכן'` אינה תופסת — אחרת כל המכונות נחסמות לנצח. הממשק עובר דרך **אותו דפוס** `need_count` הקיים ב-`act` |
| **WASH-06** | HIGH | ברצפת NOBLE החסימה מוגבלת לאותה עגלה בלבד | ✅ **נסגר כמכוון** — הכרעת אבי 02.08.2026. ריבוי מנות במכונה הוא העיצוב |
| **WASH-07** | HIGH | הקיבולת אינה נאכפת בשום מקום | ✅ **נסגר — אין לאכוף.** הכרעת אבי: `capacity` הוא מכנה של מדידה ולא שער. ⭐ הוליד את `WASH-15` |
| **WASH-08** ✅ | HIGH | מכונה שנשלחה לתיקון ממשיכה לגרור את המשימה עד הסוף | ✅ **נסגר ב-B75 (17.08.2026).** ⛔ **הכרעת אבי `3ג` — ניתוק אוטומטי:** `w08DetachMachine` נקראת מ-`b42ReportFault` ומנקה את `machine_id` מכל משימת כביסה פתוחה, עם שורה ביומן כל משימה. מאותו רגע `advanceLaundry` חוסם ממילא — **אין שער שני** (`R8`). ⛔ **NOBLE אינו מנותק** — שם הכביסה פיזית בתוף ו-`nobleStageEnd` חייבת לעבוד גם על מכונה מקולקלת. ⚠ נוספה רשת בטיחות לנתוני עבר: קידום נחסם גם כשהמכונה **המשויכת** אינה פעילה |
| **WASH-09** ✅ | HIGH | המיפוי בין סוג מכונה לשלב הוא המלצה, לא כלל | ✅ **הוכרע 09.08.2026 — נשאר מייעץ.** נימוק אבי: אין לכך משמעות תפעולית אמיתית. ⛔ **אין אצווה. אין קוד. הקוד הקיים כבר נכון — סגור בלי שינוי** |
| **WASH-10** ✅ | HIGH | הכסף במסלול הכביסה בשברים עשרוניים, מעוגל ארבע פעמים | ✅ **בוצע ב-B71 (09.08.2026).** נספרו **שמונה** קריאות `nRound2` בארבעה שלבים. נמדד: **1.9%** מהשקילות ו-**0.4%** מסכומי המע"מ איבדו אגורה, תמיד לרעת העסק. ⛔ היסטוריה נשארה כפי שהיא (הכרעת אבי) |
| **WASH-13** ✅ | MEDIUM | `nobleDriverScan` יוצרת משלוח עם `order_id` ריק קבוע | ✅ **נסגר ב-B75 (17.08.2026).** המשלוח נושא את `order_id` של הקליטה. ⭐ העבודה כאן הייתה **מפת החיבורים** ולא התיקון: אומת ש-`kind='משלוח מכבסה'` אינו מתנגש עם רגלי ההלוך/חזור לא ב-`b38EnsureOrderDeliveries` ולא ב-`b49fCloseLegs` (שתיהן מסננות לפי `kind`), ושה-`nobleDeliver` מסמן `'בוצע'` **לפני** `b56CloseLaundryOrder`. ⛔ קליטה פנימית נשארת עם `order_id` ריק |
| **WASH-14** ✅ | MEDIUM | `b40SplitLaundry` מעתיקה `stage` גולמי לילדה — הבאג מתרבה | ✅ **נסגר ב-B72 (16.08.2026).** הילדה נולדת עם הערך הקנוני מ-`STAGES`; שלב לא מוכר עובר `sVal` ולא נמחק. ⭐ סופח ממצא B69: `rLaundry` + ארבע השוואות `stage` נוספות בממשק |
| **WASH-15** | 🔴 **חוסם** | **המונה של הניצולת חסר** — `load_kg` של כל כביסה פנימית של MAPA הוא אפס תמיד | ✅ **נסגר ב-B75 (17.08.2026).** `w15TaskKg` = `items.weight_kg × qty`, נכתב לתוך `portion_kg` באירוע `התחלה`. ⛔ **אין שינוי סכימה** — שני השדות היו קיימים; הפער היה במילוי. ⛔ **אין שער חסימה** (`WASH-07`): פריט בלי משקל מחזיר `''` ולא `0` — יש בדיקה, כי אפס נראה כמו מדידה אמיתית. ⭐ **הכרעת אבי `1ב`:** נבנה `w15WeightAudit` ב-`READ_ONLY_ACTIONS` שמראה על נתוני אמת את המונה בכל מכונה, אחוז הניצולת ואילו פריטים חסרי משקל (`R1`). ⭐⭐ **סופח ממצא B69 והוא היה חמור מהצפוי:** `nobleOpenStarts` השוותה מזהה מכונה **גולמי** לצד `sVal(e.stage)`, ולכן `סיום` עם מזהה מלוכלך לא סגר את `התחלה` — **המנה נשארה פתוחה לנצח**; תוקן ב-6 אתרים דרך `w15MachKey`. ⚠ **צבירה על פני יום היא `BLD-13`** ולא `WASH-15` — `nobleMachineLoad` סופרת תפוסה רגעית בכוונה. ✅ **`BLD-13` שוחרר** |
| **WASH-16** | HIGH | ⭐ **חדש — נגזר מ-`DEC-03`.** אין חסימה על קליטת כביסה להזמנה שכבר נסגרה | ✅ **נסגר ב-B75 (17.08.2026).** `w16OrderGate` — **שער אחד, שלושה אתרי כניסה** (`R8`): `b49dResolveOrder` (פתיחת קליטה) · `b49dAddCarts` (הוספת עגלות) · `nobleWeigh` (**שקילה = יצירת חיוב**, הכרעת אבי `4ב`). ההודעה מכילה את מספר החשבונית. ⛔ `nobleWeighFix` בכוונה **מחוץ** לשער — היא מסלול התיקון היחיד של שקילה קיימת. ⛔ חשבונית מבוטלת אינה חוסמת. ⛔ קליטה פנימית אינה מושפעת. ⭐ **תוקן אגב `B64a`:** השער הישן השווה `String(o.status)` גולמי ולכן **חסם בטעות** הזמנה תקינה עם רווח קשיח. ⭐ איחוד הזמנות נשאר תפעולי בלבד — השער אינו נוגע בו |
| **WASH-17** ✅ | 🔴 **חוסם עבודה** | ⭐ **דווח על ידי אבי 09.08.2026, עם צילומי מסך.** בהזמנת כביסה, "**+ הוסף פריט**" פותח בורר מקטלוג המלאי, בודק זמינות ומשריין. ⛔ **המלאי הוא של המכבסה** — לקוח שמביא וילונות משלו אינו אמור להופיע בספירת המלאי | ✅ **נסגר ב-B72 (16.08.2026).** הבורר הוסר לחלוטין; תיאור חופשי רשות עד 20 תווים ל-`orders.notes`. **שני שומרים** — הכפתור אינו מוצג · `addLineForm` דוחה קריאה ישירה · `create` על `order_lines` נדחה בשרת דרך `sPick`. ⭐ אומת שההסרה אינה מזיזה שקל ואינה מזיזה יחידת מלאי. ⛔ ההשכרה לא נגעה. אין שינוי סכימה |
| **WASH-18** ✅ | 🔴 **חוסם עבודה** | ⭐ **דווח על ידי אבי 09.08.2026.** קליטת כביסה של לקוח **אינה מופיעה בלוח הכביסה** ואי אפשר לקדם לה שלבים משם. **השורש אומת:** `laundry_tasks` בנויה סביב `item_id`+`qty` ונוצרת רק ב-`receiveReturn` — כלומר היא **הטקסטיל של MAPA שחוזר מהשכרה**. לקליטת NOBLE אין פריט, ולכן אין לה שורת משימה. **שני מודלים מקבילים לאותו מסך** | ✅ **נסגר ב-B73 (16.08.2026).** הלוח מציג את `laundry_intakes` כ**שכבת תצוגה** בלבד — ⛔ לא נוצרו `laundry_tasks` מזויפות, ויש בדיקה ש-`inLaundryQty` ו-`availableQty` מחזירות אותו מספר. ⭐ נוסף `w18Advance` — קידום שלב ידני למנהל/משרד בלי סריקה. ⛔ "מוכן" עובר ב-`nobleMarkReady` בלבד — חובת השקילה אינה נעקפת. 456 בדיקות |
| **WASH-20** ✅ | 🔴 **כסף** | ⭐ **חדש — נמצא ב-B72 (16.08.2026) ולא תוקן.** `b54RawOrderTotalAg` משווה `String(o.type) !== 'כביסה'` בהשוואה **גולמית**. הזמנת כביסה שסוגה בגיליון מלוכלך (רווח קשיח · סימן כיוון · רווח נגרר) **תחייב את שורות ההזמנה שלה ככסף**, בניגוד ל-C5 שקובע ששורות כביסה אינן כסף | ✅ **נסגר ב-B74 (16.08.2026).** ⚠ **R9 — הפריט תיאר נקודה אחת; הקוד החי הראה ארבע:** `b54Ledger` (הספר עצמו!) · `b54RawOrderTotalAg` · `b54LedgerFE` · `b54RawOrderTotalAgFE`. כולן עוברות עכשיו דרך `w17IsWash` — **נקודת אמת אחת, זהה תו-בתו בשני הקבצים** (R8, בלי שם שני). ⭐ **ממצא:** הזמנה שכבר הופקה לה חשבונית — **הכסף אינו זז** כי `inv.total` גובר (C4א); החשיפה היא הזמנה מלוכלכת בלי חשבונית. ⛔ הנתון בגיליון **לא תוקן** (הכרעת אבי). ⭐ נבנה אבחון `READ_ONLY` בכרטיס "בדיקת נתוני חיובים". 482 בדיקות. ⚠ הוליד את `WASH-21` |
| **WASH-21** | HIGH | ⭐ **חדש — נספר ב-B74 (16.08.2026) ולא תוקן.** **19 אתרי השוואת סוג הזמנה גולמית שאינם כסף.** הכבדים: `reservedQty` · `b48CommittedQty` (שרת) · `b40ItemBreakdown` · דוח הניצולת (ממשק) משווים `o.type !== 'השכרה'` גולמית — כלומר **הזמנת השכרה עם סוג מלוכלך אינה משריינת מלאי, והפריט נראה זמין**. זו התמונה הראי של `WASH-20`, בסיכון תפעולי ולא כספי. 15 הנותרים: `b49eLegKind` · `b49eShipMode` · `b49dResolveOrder` · יומן · התראות · דוחות | ✅ **נסגר ב-B75 (17.08.2026).** `w21IsRental(o)` — תאום מדויק ל-`w17IsWash`, **זהה תו-בתו בשני הקבצים** (יש בדיקה שמשווה את הגופים). ⛔ **הכרעת אבי `5א`:** לא נבנה עוזר "סוג קנוני" — הוא היה דורש הכרעה חדשה מה מחזירים על סוג לא מוכר, בסתירה לכלל B74. ⛔ סוג לא מוכר וסוג ריק נשארים **השכרה**. ⚠ **`R9`: הפריט אמר 19, הקוד החי נתן 20** — `b49eLegLabel` בממשק לא נספר ב-B74. ⭐ **`linePrice` לא נגעה בכוונה:** היא משווה גולמי, אבל האתר היחיד שמעביר לה סוג מהגיליון נחסם קודם ב-`sPick` מ-B72 — **אין חשיפה כספית** (`R4`). בדיקת מלאי מלאה בוצעה בשני הקבצים, לפני ואחרי, על נקי ועל ארבע צורות לכלוך. ⚠ **הוליד את `WASH-22`** (ציר הסטטוס) |
| **WASH-22** ✅ | HIGH | ⭐ **נפתח ב-B75 (17.08.2026).** אותה מחלקת כשל של `WASH-20`/`WASH-21`, בציר ה`status` ולא ב-`type`. `reservedQty` · `b48CommittedQty` · `b40ItemBreakdown` ורשימות סטטוס נוספות השוו סטטוס גולמי — **הזמנה מאושרת שהסטטוס שלה בגיליון נושא רווח קשיח אינה משריינת מלאי, והפריט נראה זמין** | ✅ **נסגר ב-B76 (17.08.2026).** `w22Stat`/`w22Known`/`w22Reserving` — נקודת אמת אחת, זהה תו-בתו בשני הקבצים (בדיקה משווה גופים וגם את `ORDER_STATUSES`/`RESERVING_STATUSES` שנוספו לממשק). ⚠ **`R9`: הפריט נרשם כ-20 אתרים; הקוד החי נתן 69** — 35 בשרת, 34 בממשק. ⭐ **הכרעת אבי `1ב`:** סטטוס לא מוכר וסטטוס ריק **משריינים מלאי**, ⛔ במלאי בלבד — אין ריכוך באישור, בליקוט, במחיקה ובקליטת כביסה. ⭐ **תיקון נלווה בכסף:** `B54_SKIP_ORDER` הושוותה גולמית ולכן **טיוטה מלוכלכת נספרה כחוב**; עכשיו מדולגת, `R6` אומת בשלושת המקורות. ⭐ **שני אתרים מחוץ לרשימה:** טאבי `ORD_CLOSED_STATUSES` ודונאט ההזמנות. בדיקת מלאי מלאה בוצעה בשני הקבצים, לפני ואחרי. ⚠ **הוליד את `WASH-23`** |
| **WASH-23א** ✅ | 🔴 **כסף** | ⭐ **נספר ב-B76 (17.08.2026).** ציר הסטטוס של `invoices`: `v.status !== 'בוטלה'` בהשוואה גולמית — **כסף ישיר**: חשבונית שבוטלה והסטטוס שלה בגיליון מלוכלך נחשבה **פעילה**, ולכן הלקוח הוצג כחייב על מסמך שבוטל, מסגרת האשראי שלו נטענה, והמסמך המבוטל חסם הפקת חשבונית חדשה | ✅ **נסגר ב-B77 (18.08.2026).** `w23InvStat`/`w23InvActive` מעל `INVOICE_STATUSES` — נקודת אמת אחת, זהה תו-בתו בשני הקבצים (בדיקה משווה גופים ורשימה). ⚠ **`R9`: הפריט נרשם כ-~25 אתרים; הקוד החי נתן 41** — 25 בשרת, 16 בממשק. ⭐ **הכרעת אבי `1א`:** סטטוס לא מוכר וסטטוס ריק נחשבים **פעילים** (הצד הזהיר — חוב לא נעלם בשקט ואין כפל חיוב). ⭐ **הכרעת אבי `2א`:** עוזר אחד. ⭐ **אומת מול הקוד הישן (`R11`):** 118000 אגורות לפני, אפס אחרי. `R6` אומת בשלושת המקורות על עשרה סטטוסים. ⛔ `WASH-16` לא נשבר — חשבונית מבוטלת עדיין אינה חוסמת הפקה. ⚠ **הוליד את `WASH-23ב` ותפס דליפת `WASH-22`** (`B44_PICK_STATUSES`) |
| **WASH-23ב** | 🟡 | ⭐ **חדש — נספר ב-B77 (18.08.2026) ולא תוקן.** אותה מחלקת כשל בארבע הטבלאות שנותרו: `payroll.status` (⛔ כסף שיוצא בפועל) · `tasks.status` · `purchases.status` · `deliveryTrips.status`. **~35 אתרים** — 12 בשרת, 23 בממשק | 🔴 **פתוח · B78.** ⛔ אף אחד מהם אינו בספר החיובים, ולכן קל מציר החשבוניות. ⚠ **לספור מול הקוד החי** (`R9`) — חלק מאתרי `payroll` כבר עוברים `sVal` מ-B67/B68. ⛔ **אין לבנות שם רביעי** (`R8`) |
| **WASH-19** ✅ | MEDIUM | ⭐ **דווח על ידי אבי 09.08.2026.** בחירת עגלה בקליטה היא הקלדה/סריקה בלבד — אין רשימה לבחור ממנה. **אומת:** `floorAddCart` קוראת `el('fl_icart').value` | ✅ **נסגר ב-B72 (16.08.2026).** רשימת עגלות פנויות לצד הסריקה; הסריקה נשארה במלואה (שדה · Enter · מצלמה · כפתור ידני). ⛔ וירטואליות מוסתרות. **R8** — הבחירה מזינה את שדה הסריקה וקוראת ל-`floorAddCart`, כלומר עוברת את אותה בדיקת `nobleCartInfo` |

⚠ **`WASH-11` ו-`WASH-12` נכללים ב-`WASH-04`.** `WASH-12` (שורש `BLD-01` —
`b56CloseLaundryReturnLeg` בהשוואה מדויקת) ✅ **סופח ותוקן ב-B66** באותה נשימה
עם `WASH-02`, כי זו אותה שורה בדיוק.

### ⛔ הכרעות אבי — 02.08.2026, אחרי מסירת B66

**1. סדר הבנייה: מסלול הכביסה קודם לכל השאר.** לשון אבי: *"תתעדף את הבעיות
של הכביסה כך שייבנו ראשונה בצ'אטים הקרובים."* → חמש האצוות הבאות הן כביסה
בלבד. `BLD-05` **יוצא מהרצף** ונכנס לאצווה הראשונה שבה אבי מדביק את בלוק
הבדיקה העצמית — לא לפני.

**2. `WASH-07` נסגר — אין לאכוף ק"ג על מכונה.** לשון אבי: *"אין צורך לאכוף
ק"ג על מכונה. לכל מכונה יש את הקיבולת שלה. ניצולת מכונות וזמינות עתידית של
כביסה נמדדת לפי הקיבולת שמוגדרת בהוספת מכונה חדשה."*
→ ⛔ **`capacity` הוא מכנה של מדידה, לא שער חסימה.** אין להוסיף חסימה, ואין
להפוך את `portion_kg` לחובה כאמצעי אכיפה. האזהרה הקיימת נשארת כפי שהיא.
✅ **אומת שהשדה כבר קיים ואינו דורש בנייה:** `machForm` (`index.html`) כולל
`<label>קיבולת (ק"ג)</label>` עם `id="f_mcap"`, נשמר לעמודה `machines.capacity`,
מוצג בכרטיס המכונה, ורצפת הייצור כבר מציגה `load_kg/capacity`.

**3. `WASH-06` נסגר כמכוון.** נגזר מהכרעה 2 — אם מעמיסים מכונה עד הקיבולת,
ריבוי מנות במכונה אחת הוא העיצוב ולא באג. ⚠ **`WASH-05` נשאר פתוח** והוא
שאלה אחרת: שתי **משימות** נפרדות שמחזיקות אותה מכונה בו-זמנית.

**4. ⭐ נגזרת חדשה — `WASH-15`.** ההכרעה על הניצולת יוצרת דרישה שלא הייתה
קודם: אם הניצולת נמדדת מול `capacity`, חייב להיות **מונה אמין** מולו.
היום אין. ראה `WASH-15` בטבלה.

---

### סדר הבנייה שנקבע

⚠ **עודכן 02.08.2026 בערב.** `BLD-16` נכנס והוקדם לראש: אבי ביקש אותו
במפורש אחרי מסירת B66, והוא נוגע בכסף שמשולם **היום**. מסלול הכביסה
נדחה באצווה אחת ונשאר רציף אחריו.

| אצווה | פריט | למה דווקא כאן |
|---|---|---|
| **B67** ✅ | ⭐ `BLD-16` — חוב שכר ברמת העובד | **בוצע 03.08.2026.** 300 בדיקות, 0 כשלים |
| **B68** ✅ | ניקוי שכר (מחוץ לרצף, בקשת אבי) | **בוצע 08.08.2026.** 323 בדיקות, 0 כשלים |
| **B69** ✅ | `WASH-04` — נרמול מודול NOBLE | **בוצע 08.08.2026.** 347 בדיקות, 0 כשלים. 91 אתרים, שבעה מהם כסף |
| **B70** ✅ | `WASH-03` — עגלה חוזרת מוחקת חיוב | **בוצע 08.08.2026.** 378 בדיקות, 0 כשלים. מדוד: ₪500 אבדו בקליטה אחת |
| **B71** ✅ | `WASH-10` — הכסף בשברים עשרוניים | **בוצע 09.08.2026.** 395 בדיקות, 0 כשלים. אין שינוי סכימה |
| **B72** ✅ | ⭐ `WASH-17` + `WASH-19` + `WASH-14` | ✅ **נמסר 16.08.2026.** canary `v4.72-B72` · **420 בדיקות · 0 כשלים** (25 חדשות) · אין שינוי סכימה. ⚠ הוליד את `WASH-20` |
| **B73** ✅ | `WASH-18` | ✅ **נמסר 16.08.2026.** canary `v4.73-B73` · **456 בדיקות · 0 כשלים** · אין שינוי סכימה. ⛔ המלאי לא זז |
| **B74** ✅ | `WASH-20` | ✅ **נמסר 16.08.2026.** canary `v4.74-B74` · **482 בדיקות · 0 כשלים** (26 חדשות) · אין שינוי סכימה. ⚠ ארבע נקודות כסף ולא אחת. ⚠ הוליד את `WASH-21` |
| **B75** ✅ | `WASH-15` + `WASH-05` + `WASH-08` + `WASH-13` + `WASH-16` + `WASH-21` | ✅ **נמסר 17.08.2026.** canary `v4.75-B75` · **529 בדיקות · 0 כשלים** (47 חדשות) · אין שינוי סכימה. ⚠ **שישה** פריטים — מעל תקרת `M1`, בהוראת אבי; `M3` קוים לכל פריט בנפרד. ⭐ סופח: מזהי מכונה לא מנורמלים ב-`nobleOpenStarts` (ממצא B69) — והוא היה חמור מהצפוי. ⚠ הוליד את `WASH-22` |
| **B76** ✅ | ⭐ `WASH-22` | ✅ **נמסר 17.08.2026.** canary `v4.76-B76` · **550 בדיקות · 0 כשלים** (21 חדשות) · אין שינוי סכימה. ⚠ `R9`: 20 אתרים ברשימה, **69 בקוד**. ⚠ הוליד את `WASH-23` |
| **B77** ✅ | ⭐ `WASH-23א` | ✅ **נמסר 18.08.2026.** canary `v4.77-B77` · **570 בדיקות · 0 כשלים** (20 חדשות) · אין שינוי סכימה. ⚠ `R9`: ~25 אתרים ברשימה, **41 בקוד**. ⭐ תפס דליפת `WASH-22` בשער הליקוט. ⚠ הוליד את `WASH-23ב` |
| **B78** | 🟡 `WASH-23ב` | ציר הסטטוס ב-`payroll` · `tasks` · `purchases` · `deliveryTrips`. ⚠ `payroll` נוגע בכסף שיוצא בפועל |
| **B77** | `BLD-13` | תכנון עומס מכבסה. ✅ **שוחרר** — `WASH-15` נסגר ב-B75 והמונה קיים. ⛔ **`M2`: אצווה לבד** — `orders` צריכה עמודת אומדן משקל, כלומר **שינוי סכימה + `setupDatabase`**. ⛔ **מפרט חובה לפני קוד** (`R2`) |
| **B78+** | `BLD-09`/`BLD-10`/`BLD-06` · `BLD-07` · `BLD-11` | שאר המערכת. ✅ `BLD-06` שוחרר — `DEC-04` הוכרע |

⚠ **`BLD-05`** אינו ברצף. נכנס לאצווה הראשונה שאליה אבי מדביק בלוק בדיקה
עצמית מלא, ולא קודם. הכלי מוכן; חסרה רק ההרצה. ⚠ ירד **שלוש-עשרה** פעמים ברצף.
✅ **`WASH-09`** הוכרע 09.08.2026 — **נשאר מייעץ. אין קוד.**

---

### ⭐ `BLD-16` ✅ — חוב שכר ברמת העובד. **בוצע ב-B67, 03.08.2026**

⚠ **המפרט נשמר כלשונו לתיעוד.** מה שנבנה בפועל, ומה שנוסף מעבר למפרט:

| מה | איפה |
|---|---|
| `b67PayOneRow` — גוף `case 'payPayroll'` שהוצא לפונקציה | שרת |
| `b67PayEmployee` · `b67EmployeeOpenRows` · `b67EmployeeDebt` | שרת |
| ⭐ `B67_WRITE_DEFER` · `b67FlushWrites` — צבירת כתיבות ב-`writeTable` | שרת |
| `b67EmpView` · `b67EmpDebt` · `b67EmpMonthRows` · `b67PayEmp` · `b67EmpNameCell` | ממשק |
| `b67DraftRows` · `b67DraftTotal` · `b67DraftCardHtml` · `b67DraftsList` | ממשק |

⭐ **שתי הכרעות ארכיטקטוניות שלא היו במפרט המקורי:**
1. **`B67_WRITE_DEFER`.** `syncFutureExpenseForPayroll` כותבת לגיליון בכל
   קריאה ואסור לגעת בה, ולכן תשלום על שלושה חודשים היה מייצר תשע נסיעות.
   הדגל גורם ל-`writeTable` לצבור בזיכרון, ו-`b67FlushWrites` כותבת כל
   טבלה פעם אחת. **הרווח הגדול הוא אטומיות:** כשל באמצע זורק את הבאפר
   ואף שורה לא הגיעה לגיליון.
2. **ההקצאה באגורות שלמות בזיכרון**, מומרת בחזרה לשברים בכתיבה — כדי
   שסכום ההקצאות ישווה בדיוק לסכום ששולם. ⛔ האחסון לא השתנה.

---

### `BLD-16` — הניסוח המקורי (נשמר לתיעוד)

**לשון אבי:** *"תשלום יחול על כל החוב של החברה כלפי העובד. תשלום יכסה את
החוב המוקדם ביותר — אם חייבים 10 אלף על יולי ו-20 אלף על אוגוסט ושילמתי
5 אלף, ה-5 אלף יצמצמו את החוב של יולי."* וכן: *"תצוגה נוספת של הוצאות
צפויות כולל טיוטות משכורות שעוד לא אושרו, וגם בתצוגת שכר סיכום של טיוטות
שכר שעוד לא אושרו. לחיצה תפתח רשימת פירוט, ולחיצה על שורה תיקח לתצוגה של
אותו השכר."*

**הרקע:** *"כיום להגיע לתצוגה של תשלום חלקי צריך לעבור כמה מסכים."*

#### ⛔ שלוש הכרעות של אבי — מחייבות

**1. ההקצאה: הישן קודם, אוטומטית.** התשלום אינו נבחר לחודש. הוא נכנס לחוב
הפתוח הישן ביותר וגולש הלאה.

**2. השיוך שקוף למשתמש.** אבי רואה **מספר אחד** — סך החוב — מקליד סכום,
לוחץ, והחוב יורד. ⛔ **אין מסך אישור שמפרט חודשים לפני התשלום.** ⛔ אין
כפתור תשלום נפרד בכל שורה בטבלה. הפירוט נראה מעצמו **אחרי**, ברשימת
החודשים שממילא פתוחה. אבי דחה במפורש את הגרסה המפורטת יותר.

**3. טיוטות מוצגות, לא נספרות.** שכר שלא אושר אינו חוב — אך אבי רוצה
לראות אותו מגיע.

#### ✅ מה שאומת על הקוד החי לפני הניסוח

- טופס תשלום לרשומה בודדת **כבר קיים** בתוך `payrollDetail`, עם היתרה
  ממולאת מראש. ⛔ אין לבנות טופס שני (R8) ואין להסיר אותו.
- מסלול הכתיבה היחיד: `savePayrollPayment` → `act('payPayroll')` →
  `case 'payPayroll'` (שרת, ~3172). הוא מבצע **חמש** פעולות לכל תשלום:
  `payrollPayments` · סטטוס השכר · `expenses` ·
  `syncFutureExpenseForPayroll` · בדיקת יתרת מזומן.
- ⭐ **הוצאה צפויה לשכר נוצרת רק באישור.** ב-`case 'addPayrollAdj'` יש
  `if (prA.status !== 'טיוטה')`, ולכן **טיוטה אינה מופיעה היום בשום
  תחזית.** זה בדיוק הפער שאבי מבקש לסגור.
- `payrollTable`/`payrollRowsFor` בנויות **לפי חודש נבחר בלבד** — אין
  היום שום מקום שמראה עובד לאורך זמן.

#### ⛔ ארבע מלכודות שסומנו במכתב לצ'אט הבא

1. **רשומת טיוטה ישנה יותר** — התשלום ידלג עליה ויפגע בחודש חדש יותר,
   הפוך מכוונת אבי. ⛔ אסור לעקוף אישור ואסור לדלג בשקט.
2. **בדיקת המזומן פעם אחת מול הסכום המלא**, לפני ההקצאה. בדיקה פר-שורה
   תרוקן את הקופה באמצע ותיכשל אחרי ששורות כבר נכתבו.
3. **R10** — צבירה בזיכרון וכתיבת כל טבלה **פעם אחת** בסוף.
   ⛔ אסור `writeTable` בתוך הלולאה.
4. **סכום מעל סך החוב** — נדחה. ⛔ בלי זיכוי ובלי עודף.

⛔ **הטיוטות נגזרות לתצוגה ואינן נכתבות ל-`futureExpenses`.** הרגע שטיוטה
נכנסת לטבלה היא נספרת בתחזית התזרים כהתחייבות אמיתית — וזו טעות.
⛔ `finPayrollTotal` ו-`fexpOpen` **אינם משנים את ערכם.** שני מספרים נפרדים.

⚠ **הכסף בשכר בשברים עשרוניים** (`Math.round(x*100)/100`). ⛔ אין לעבור
לאגורות באצווה הזו — זהו תיקון נפרד, ותיקון חלקי יפצל את החשבון.

---

### ⭐ `WASH-15` — המונה של הניצולת. פריט חדש, נגזר מהכרעת אבי

**מה אבי ביקש:** ניצולת מכונות וזמינות עתידית של כביסה, נמדדות מול
`machines.capacity`.

**מה חסר (✅ אומת ב-`grep` על הקוד החי):** המכנה קיים. **המונה לא.**

1. ⛔ **`b49cAfterAdvance` — הגשר ממסלול MAPA — כותב אירוע `התחלה` בלי
   `portion_kg` בכלל.** לכן `load_kg` של **כל כביסה פנימית של MAPA הוא אפס,
   תמיד.** רק `load_count` נספר. ניצולת שמחושבת מזה תראה מכבסה ריקה.
2. `portion_kg` **אופציונלי** ב-NOBLE. ⛔ אין להפוך אותו לחובה כשער חסימה
   (הכרעה 2) — אבל חייבת להיות דרך אמינה למלא אותו, למשל גזירה אוטומטית
   ממשקל העגלה שכבר נשקל.
3. `nobleMachineLoad` סופר **תפוסה רגעית** — אירועי `התחלה` בלי `סיום` תואם.
   זו אינה צבירה על פני יום, ולכן אינה מספיקה ל"טון ליום".
4. ⛔ **ההיסטוריה נעלמת:** קליטה ב`נמסר` מסוננת החוצה, ועגלה שאינה
   `active='כן'` גם. `nobleMarkReady` משחררת עגלות כברירת מחדל — כלומר
   ברגע שהקליטה מסתיימת, מה שעברה במכונה נמחק מהספירה.

**הנתונים הגולמיים כן קיימים** ב-`laundry_events` (`ts` · `machine_id` ·
`stage` · `portion_kg`) — הפער הוא במילוי ובשאילתה, לא בסכימה.

✅ **`WASH-15` נסגר ב-B75 (17.08.2026) — `BLD-13` שוחרר.**
`b49cAfterAdvance` כותבת `portion_kg` (`w15TaskKg` = `items.weight_kg × qty`),
ו-`w15WeightAudit` מראה על נתוני אמת מה המונה מחזיר בכל מכונה.
⚠ **מה `BLD-13` עוד צריך לבנות בעצמו:** `nobleMachineLoad` סופרת **תפוסה
רגעית** (`התחלה` בלי `סיום` תואם) ולא צבירה על פני יום, וקליטה ב`נמסר`
מסוננת החוצה. "טון ליום" הוא **שאילתה על `laundry_events`** — הנתונים
הגולמיים קיימים (`ts` · `machine_id` · `stage` · `portion_kg`), והפער
שנשאר הוא בשאילתה ולא בסכימה. ⛔ אומדן משקל **בהזמנה** כן דורש עמודה
חדשה ב-`orders` ולכן `setupDatabase`.

---

# חלק 11 — OPENER מוכן לצ'אט הבא · B83 = `BLD-09` + `BLD-10`

⚠ **עודכן 18.08.2026** — B82 נמסר (`W23-שאריות` + `BLD-06`). אין שינוי סכימה.
✅ **`WASH-23` ו-`W23-שאריות` נסגרו סופית.** ציר הסטטוס סגור בכל המערכת.
✅ **`BLD-06` נסגר.** `DEC-04` מומש במלואו.
⛔⛔ **`BLD-13` נשאר חסום** — אבי לא אישר שינוי סכימה (הכרעת `2ב`, 18.08.2026).
⛔ **`BLD-05` חסום בהרצת הבדיקה העצמית של אבי** — פעולה שלו, לא של הצ'אט.

```
בונים B83. קרא את WORKING_PROTOCOL.md בידע הפרויקט ופעל לפיו במלואו.

════════ מה כבר בוצע — סדר כרונולוגי ════════
B64a — נרמול sVal/sPick במודול MAPA
B69  — WASH-04: מודול NOBLE עבר את B64a. 91 אתרים, שבעה מהם כסף.
B70  — WASH-03: כל שקילה היא שורת חיוב. תיקון מנהל עם ref_id.
B71  — WASH-10: כל כסף הכביסה באגורות שלמות.
B72  — WASH-17 + WASH-19 + WASH-14. הבורר הוסר מהזמנת כביסה.
B73  — WASH-18. קליטת NOBLE כשכבת תצוגה בלוח · w18Advance.
B74  — WASH-20. ⭐ ארבע נקודות כסף, כולן דרך w17IsWash.
B75  — WASH-15+05+08+13+16+21. ⭐⭐ w21IsRental — ציר הסוג נסגר.
B76  — WASH-22. ⭐⭐ ציר הסטטוס של orders: w22Stat · w22Reserving.
B77  — WASH-23א. ⭐⭐ invoices: w23InvStat · w23InvActive.
B78  — WASH-23ב. ⭐ הכרעת 1ב: עוזר גנרי אחד — w24Stat(rec, LIST, dflt).
B79  — WASH-23ג חלק א'. ⭐⭐ deliveries. הכרעת 2ב: לא מוכר = 'מתוכנן'.
B80  — WASH-23ג חלק ב'. ⭐⭐ ציר הכסף והשערים. 55 אתרים. w25Usable.
B81  — WASH-23ג חלק ג'. ⭐⭐ תשע הטבלאות האחרונות. 96 אתרים.
       ⛔⛔ הממצא: שער מחיקת היחידות — מלאי אצל לקוח נמחק בשקט.
B82  — W23-שאריות + BLD-06. ⭐⭐ **שני הצירים סגורים סופית.**
       ⚠⚠ R9 בפעם השביעית: חלק 11 רשם 4 אתרים, הקוד נתן **6**.
       b41PickupReceived מכיל **שני צירים באותן ארבע שורות**, ו-
       bld01Candidates הציגה סטטוס נסיעה גולמי בעוד הסינון שלה
       עצמה עבר w24Stat ב-B78 — **האבחון הציג לאבי משהו אחר ממה
       שפעולת הריפוי פועלת לפיו.**
       ⛔ שינוי התנהגות יחיד: b49eLiveDeliv — נסיעה בסטטוס ריק או
       לא מוכר נחשבת 'מתוכנן' ולכן "קיימת"; עד B82 היא נחשבה
       מבוטלת ומעבר חזרה למשלוח **שכפל אותה**.
       ⚠⚠ R9 שוב על BLD-06: **מחצית ממנו כבר הייתה בנויה** —
       ההקצאה FIFO קיימת מ-D15 ומסך הגבייה כבר גובה מול מסמך בודד.
       ⛔⛔ מה שהיה חסר היה **באג**: b55EnsureDocsForDebt הפיקה
       חשבונית לכל החוב הפתוח בלי קשר לסכום שהתקבל.
       ⭐⭐ **כל בדיקה חדשה נושאת תווית [מודד]/[שומר] בשם שלה.**
       724 בדיקות. 20 מ-36 החדשות נכשלו בבקרה הנגדית.

════════ מה ממתין ════════
🔴 **BLD-09 + BLD-10** — הפריטים של האצווה הזו. מפורטים למטה.
🔴 **BLD-13 (תכנון עומס מכבסה).** ⛔⛔ **חסום.** orders צריכה עמודת
   אומדן משקל = שינוי סכימה (M2), וזו אצווה **לבד תמיד**.
   ⛔ אבי נשאל ב-B82 והשיב **לא לאשר בשלב זה** (הכרעת 2ב).
   ⛔ אין לפתוח אותה בלי שאבי אמר במפורש "מאשר שינוי סכימה".
   ⚠ R2 — גם אחרי אישור, **מפרט כתוב לפני קוד**.
🔴 **BLD-05** — ⛔ חסום בהרצת הבדיקה העצמית של אבי (ACT). לא בהיקף.
🔴 BLD-07 · BLD-11 — שינוי סכימה. חסומים באותו אישור.
✅ אין הכרעה עסקית פתוחה שחוסמת את B83.

🔴 ממתין לאימות אבי בייצור:
   🔴🔴 **אישור הצהרת תשלום אחרי B82** — יופקו מעכשיו **פחות**
   מסמכים. ⛔ אם חשבונית שהייתה נוצרת עד היום לא נוצרה — **זו
   ההתנהגות שהוכרעה ב-DEC-04, לא תקלה.** ניתן להפיק ידנית מכרטיס
   הלקוח. ⛔ אם הופקה חשבונית על חוב שלא שולם — זה כן באג.
   🔴🔴 **בורר "שיוך התשלום"** — ברירת המחדל חייבת להישאר
   "אוטומטי — החוב הוותיק ביותר קודם". אם נבחר מסמך מראש, זה באג.
   🔴 **רגל חזור כפולה (B82)** — הזמנה שהחליפה מצב מסירה תיצור
   מעכשיו נסיעה אחת ולא שתיים. זו התוצאה המכוונת.
   🔴🔴 מחיקת יחידות · מסך התקלות · תיבת האישורים · התראות (B81)
   🔴 עגלות פנויות · מארזים · לוח הכביסה (B81)
   🔴 בוררי הרכב · יתרות הלקוחות · הודעות התשלום (B80)
   🔴 מסלול הנהג · סימון הזמנה כסופקה (B79)
   🔴 תשלום שכר · רכש טיוטה · מספור נסיעות · לוח משימות (B78)
   🔴 ליקוט הזמנה שלא נפתחה (B77) · מלאי הפריטים (B76)
   🔴 משקלי פריטים (B75) · נתוני חיובים (B74) · לוח הכביסה (B73) ·
   שקילה (B71/B70) · רצפת הייצור (B69) · תשלום אמיתי (B67) ·
   טאבלט שני (BLD-03).

⛔ קרא את חלק 1 של TASK_QUEUE.md — R1–R12. בפרט R1, R3, R8, R9, R10, R11.
⛔⛔ R12 — שאלות בהודעה **אחת** מרוכזת עם המלצה ⭐; המסירה = חמישה
חלקים קצרים. הפירוט חי ב-PROGRESS.md.
⛔⛔ R10 חל ישירות על BLD-09 — הוא פריט ביצועים. לצמצם את **מספר**
הבקשות ולא רק את משקלן. כל נסיעה לגיליון ~215 מ״ש; רצפת התקורה
של Apps Script ~2.2 שנ' לבקשת HTTP.

════════ מנגנונים שאסור לשבור ════════
· B64a/B69 — sVal/sPick זהים תו-בתו. ⛔ אסור על כסף, משקל ומזהים.
· B67  — ⛔⛔ b67PayOneRow מסלול הכתיבה היחיד · B67_WRITE_DEFER→null.
· B70  — ⛔⛔ nobleWeighRollup מצטברת · laundry_events append-only.
· B71  — ⛔⛔ כסף כביסה באגורות. ⛔ toAg לא נגע ולא ייגע (R4/R6).
· B74–B79 — w17IsWash · w21IsRental · w22Stat · w23InvActive ·
  ⛔⛔ **w24Stat הוא המנרמל היחיד לציר הסטטוס** · DELIVERY_STATUSES.
· B80  — ⛔⛔ CHARGE_STATUSES ו-PAYMENT_STATUSES נוגעים ישירות
  ב-b54Ledger ו-b48BalancesAg. כל נגיעה = R6 מלא.
  ⛔⛔ w25Usable — ריק מותר, לא מוכר חסום.
· B81  — ⛔⛔ שער מחיקת היחידות: UNIT_BUSY חוסם, לא מוכר חוסם,
  ⛔ **ריק נשאר ניתן למחיקה**. ⛔ FAULT_DFLT='פתוחה'.
  ⛔⛔ ארבע רשימות בלי dflt במכוון: NOTIF_STATUSES ·
  TCOMMENT_STATUSES (⛔ '' ערך **חוקי**) · VISIT_STATUSES · MSG_STATUSES.
  ⛔⛔ carts.condition הוא שדה התקלות ולא status — R5/B56.
· B82  — ⛔⛔ **b82DebtSort הוא מפתח המיון היחיד לחוב** (created_at
  || start_date), בשלושה אתרים: b2ProcessPayment · b55EnsureDocsForDebt
  · collectDocRows. ⛔ אין ליצור מפתח שני (R8).
  ⛔⛔ **b2ProcessPayment הוא מסלול הכתיבה היחיד לתשלום.**
  debt_target רק **מצמצם** את רשימת debts — אין ענף כתיבה נפרד.
  ⛔ **debt_target ריק או פגום = ברירת מחדל FIFO, לא שגיאה.**
  תשלום שנחסם בגלל ערך בורר פגום הוא כסף שלא נכנס.
  ⛔ **b55EnsureDocsForDebt בלי opts = ההתנהגות הישנה במדויק.**
  יש בדיקה. אל תהפוך את opts לחובה.
  ⛔ b82DebtSort ו-b82ParseTarget **זהים תו-בתו** בשני הקבצים.
  ⛔ בורר "שיוך התשלום" נבנה מ-collectDocRows הקיימת — אין רשימה שנייה.
· הרשימה המלאה — חלק 1 (R4) ו-חלק 11.

════════ הבדיקות ════════
⚠⚠ tests.js בשורש הריפו. npm i jsdom ואז node tests.js
← **724 בדיקות**, אמור לעבור 0 כשלים.
⛔ נכשל משהו לפני שנגעת בקוד — עצור והתרע. זו רגרסיה קיימת.
⚠ הרתמה קוראת לקובץ בשם 'קוד שרת.txt' בדיוק.
⚠ route() טוענת גיליון אמיתי ואינה נגישה. handle() כן.
⚠ סדר הארגומנטים: handle(action, p, db, user).
⚠⚠ H.login(w, ROLE, srv, {user:'שם'}) — הארגומנט השני הוא **תפקיד**.
⚠ srv.b54Bump() לפני כל מדידת יתרה; delete DB._b54Ledger ו-_b48Bal.
⛔ **grep על `case '` ב-handle() לפני שאתה כותב שם action.**
⛔ **בדיקת שער טוענת על נוסח הדחייה המדויק**, לא על ok:false.
⛔⛔ **בקרה נגדית חובה.** בדיקה שעוברת מול הריפו המקורי היא שומר
קבועים ולא מדידה. ⭐⭐ **B82 קבע נוהל: כל בדיקה נושאת תווית
`[מודד]` או `[שומר]` בתחילת שמה.** [מודד] חייבת להיכשל מול הריפו
המקורי; [שומר] עוברת שם במכוון. **הרץ את הבקרה, והתאם תוויות
שהתבררו כשגויות** — ב-B82 שלוש תויגו מחדש.
⭐ שיעורי הבקרה: B80 18/30 · B81 16/30 · B82 20/36.
⚠ שמות אמיתיים שנבדקו ב-B82: b2ProcessPayment · b55EnsureDocsForDebt ·
b55ConfirmDeclaration · b56CloseLaundryOrder · w16OrderGate ·
bld01Candidates · b41PickupReceived · b49eLiveDeliv · collectDocRows ·
apprConfirmDecl · apprConfirmDeclGo.
⚠ b49eLegKind(o,'חזור') על הזמנת **כביסה** מחזיר 'אספקה' ולא 'החזרה'
— fixture עם kind שגוי מפיל בדיקה שנראית כמו באג בקוד.
⚠⚠ לקח B75–B81: H.stripComments אינו בטוח על קובץ השרת כולו.
חתוך הערות **בתוך הגוף שחולץ**.
⚠⚠ משוך את הריפו ב-git clone ולא ב-curl.

════════ הפריטים ════════

🟡 **BLD-09 — דוחות נוכחות ושכר לפעולת שרת ייעודית.** שרת+ממשק. אין סכימה.
   הרקע: `attendance`/`payroll` לא נחתכו ב-B51 ויגדלו בקצב קבוע.
   ⛔ **הפתרון אינו חיתוך לפי ימים** — דוח השעות של B50 ומדד Y1
   נשענים עליהן ישירות ויאבדו נתונים.
   ⛔⛔ **R10 הוא לב הפריט:** לצמצם את **מספר** הנסיעות לגיליון,
   לא את משקלן. ⛔ אין לשכתב מסכים קיימים בגללו.
   ⛔ **R8** — יש להשתמש ב-`b58Prefetch`/`batchGet` הקיימים ולא
   לבנות שכבת שליפה שנייה.
   ⛔ **b67PayOneRow נשאר מסלול הכתיבה היחיד לשכר** ו-`B67_WRITE_DEFER`
   לא נגע. ⛔ **R6** — אם הפעולה נוגעת בסכומי שכר, שלושת מקורות
   הכסף חייבים להמשיך להסכים.
   ⛔ **לספור מול הקוד החי לפני שמתחילים (R9).**

🟡 **BLD-10 — ניצולת ברמת יחידה ממוספרת.** ממשק בלבד. אין סכימה.
   דוח הניצולת מודד ניצולת **פריטים**, לא `units`.
   ⛔ **R8** — דוח חדש נרשם ב-`REPORTS_CATALOG`/`REPORT_RENDERERS`
   עם פונקציית render, **בלי לגעת ב-`rReports`**. ייצוא דרך
   `b50Reg(key, filename, headers, rows)` + `b50ExportBtn(key)`;
   ⛔ **אין לשרשר נתונים ל-`onclick`**.
   ⛔ **29 מסכים.** דוח נוסף בקטלוג אינו מסך חדש — אל תוסיף מסך.
   ⛔ **לספור מול הקוד החי לפני שמתחילים (R9).** בדוק מה
   `REPORTS_CATALOG` כבר מכיל לפני שאתה מתכנן דוח "חדש" —
   ב-B48/B50 מדד "חדש" התברר כזהה לקיים.

⭐ **שניהם באזור אחד: דוחות ופעולות שרת.** M1 מתקיים — שני פריטים,
אף אחד אינו סכימה, אף אחד אינו חסום. ⛔ **M3: חמש השאלות נענות
לכל פריט בנפרד, בכתב.** אצווה שענתה "לכל האצווה יחד" נפסלת.

--- מלכודות ---
⛔ 1. **שינוי סכימה** — אין. אם התברר שצריך: **עצור** (M2/M6),
   מסור את מה שהושלם, ורשום את השאר.
⛔ 2. **קדימות אופרטורים** — `!X.indexOf(...)>-1` שגוי. הנכון
   `===-1`. בדיקה על **שני** צדדי כל תנאי שאתה הופך.
⛔ 3. **שמות ה-action** — grep על `case '` לפני כתיבת בדיקה.
⛔ 4. **p.status אינו ערך מהגיליון** — אל תנרמל קלט משתמש.
⛔ 5. אין לגעת ב-toAg (R4) ולא בארבע נקודות האגורות של B71.
⛔ 6. **מסך ריק = לעצור.** ⚠ WORKING_PROTOCOL חלק 5: דוח "ביצועי
   מכבסה" אובחן פעמיים מקריאת קוד בלבד והתברר שהטבלאות ריקות.
   ⛔ **R1: אבי אינו מייצא בסיס נתונים.** במקום זה — בנה פעולת
   אבחון READ_ONLY עם כפתור בממשק, ומסור אותה.
⛔ 7. M6 — אם באמצע מתברר שפריט גדול פי כמה: עצור, מסור, רשום.

--- בדיקות חובה ---
· ⛔⛔ B71–B82 לא נשברו (רגרסיה מלאה), ובפרט R6 — b48BalancesAg ·
  custBalance/b48BalancesAgFE · b2CreditUsedAg מחזירים אותו מספר
· ⛔⛔ B82: debt_target ריק/פגום עדיין נופל ל-FIFO ואינו חוסם תשלום
· ⛔⛔ B82: b55EnsureDocsForDebt בלי opts עדיין מפיקה לכל החוב
· ⛔ שער מחיקת היחידות: לא מוכר חסום · ריק ופנוי עדיין נמחקים
· ⛔ R5 לא נשבר — carts.condition לא הפך ל-status
· ⛔ לכל היפוך תנאי — בדיקה על **שני** הצדדים (מלכודת 2)
· ⛔ כל בדיקת שער טוענת על **נוסח הדחייה המדויק** (מלכודת 3)
· ⛔⛔ בקרה נגדית + תיוג [מודד]/[שומר] לכל בדיקה חדשה
⚠ שכבה 2: להוסיף טענה ל-b61Tests() רק אם נגעת ביכולת דפדפן.
  BLD-09/BLD-10 אינם יכולת דפדפן — ⛔ אל תוסיף.

⛔ אין בקוד history.back/go ולא תהיה. t01 ו-t02 סורקים ונכשלים.
⛔ IDLE_LIMIT_MS ונעילת B7 — לא לגעת.

════════ פתיחה ════════
משוך חמישה קבצים חיים מהריפו (git clone):
index.html · קוד שרת.txt · PROGRESS.md · TASK_QUEUE.md · tests.js
⚠ ודא: canary v4.82-B82 בממשק · קוד שרת.txt מתחיל ב-'// ===='.
⚠⚠ ודא שקיימים בשרת: b82DebtSort · b82ParseTarget · w24Stat ·
w25Usable · w22Stat · w23InvActive · DELIVERY_STATUSES · DELIVERY_DFLT ·
B49D_ORDER_OPEN · B54_SKIP_ORDER · b55EnsureDocsForDebt · b2ProcessPayment.
⚠⚠ ודא שקיימים בממשק: b82DebtSort · b82ParseTarget · collectDocRows ·
apprConfirmDecl · b49eLiveDeliv · b41PickupReceived · custBalance ·
fexpOpen · b59Machines · b42OpenFaults.
אם חסרים — אבי לא העלה את קבצי B82. עצור והתרע.
⚠ npm i jsdom ואז node tests.js ← 724 בדיקות, 0 כשלים.
⚠⚠ ודא שחלק 11 בקובץ הזה מדבר על B83. אם הוא מדבר על אצווה ישנה —
אבי לא העלה את TASK_QUEUE.md. עצור והתרע. (קרה ב-B78.)

תוספות/שינויים להיקף שסוכמו מעבר לחלק 11: אין.
עבוד לפי שני השלבים (WORKING_PROTOCOL חלק 3).
בסיום: הקבצים שנגעת בהם + tests.js + PROGRESS.md מלא +
TASK_QUEUE.md מלא + HANDOFF עם OPENER לאצווה הבאה.
כולם לשורש הריפו.
מודל: Sonnet.
```
