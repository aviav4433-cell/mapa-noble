# MAPA-NOBLE — מצב המערכת

## מצב נוכחי
canary ממשק: **v4.41-B49** | canary שרת: **v4.40-B48** (ללא שינוי — B49 ממשק בלבד, לא נגע בשרת) | עדכון אחרון: 26.07.2026

**מה עובד:** כל הליבה — הזמנות, מלאי רב-מחסני, משלוחים ומסלולי נהג (Driver-Lite B5),
החזרות ושקילה, מודול מכבסה מלא (NC1–NC4), רצפת ייצור, משימות, פגישות, פורטל לקוח,
חנות ציבורית, מנוע תשלום ואשראי (B2), פקדון/ערבויות (B3), התראות אימייל (B4),
חשבוניות עם מספור רציף ומספרי הקצאה (B1/B31), דוח חובות לפי גיל ודשבורד חריגים (B9),
מרכז אישורים (B10), BI (B14), מדפים (B35), שעון נוכחות ושכר (B36–B37), אבטחת שדות
רגישים (B38), תחזוקת ציוד ותקלות (B42), תזרים וחובות עם מועדי פירעון (B43),
סריקת העמסה מרובה (B44), כלל מע"מ אחיד (B45), מסמכים כ-PDF אמיתי ואריחי עובד/רכב (B46),
תפריט עליון בדסקטופ (B47) — פתיחת החלוניות תוקנה ב-B48a,
מקור אמת אחד ליתרות לקוחות ומסנן מחסן במסך המלאי (B48),
**מרכז דוחות — קטלוג + פרמטרים + דריל-דאון (B49).**

**מה שבור:**
1. 🟡 Bug 1: חתימה דיגיטלית בטאבלט לא נרשמת כראוי (מ-B13). → B51.

**ממתין לאימות אבי:**
1. ⚠ **הפעלת שירות Drive API בעורך Apps Script** (שירותים ← + ← Drive API) — בלי
   הצעד הזה תיקון ה-PDF ממשיך ליפול ל-HTML, עכשיו עם אזהרה גלויה.
2. ⚠ **הרצת `setupDatabase`** — B46 הוסיף `photo_url` ל-`employees` ול-`vehicles`.
   בלי זה העלאת תמונה תיכשל.
3. הרצת "תיקון מסמכים שנשמרו כ-HTML" במסך ההגדרות (אחרי 1 ו-2).
4. הפקת חשבונית חדשה ובדיקה ב-Drive שהקובץ הוא PDF אמיתי.
5. **אריחי מסך "עובדים"** ו**"צי רכב"** — תמונה/אייקון, צ'יפים; לחיצה פותחת כרטיס (B46).
6. **העלאת תמונת עובד/רכב** מתוך הכרטיס (B46).
7. **B48 — יתרת לקוח זהה בכל המסכים** (כרטיס לקוח, גבייה, דוחות, פורטל).
8. **B48 — מסנן המחסן במסך המלאי** ושינוי המספרים לפי המחסן הנבחר.
9. **B48 — התראת "מתחת למינימום"** — ייתכן שיופיעו פריטים חדשים ברשימה (בכוונה).
10. **חדש (B49, אחרי תיקון) — מסך "דוחות" נראה אחרת מקצה לקצה:** נכנסים למסך ורואים קטלוג
    אריחים בארבע קבוצות (כספים · תפעול · מכבסה · ניהול) במקום כל הדוחות טעונים
    בבת אחת. לחיצה על אריח פותחת דוח בודד עם טופס פרמטרים למעלה, **רצועת KPI
    מעוצבת** (כמו בלוח הבקרה) וכפתור "→ חזרה לקטלוג". לוודא שכל חמשת הדוחות
    (הזמנות, הכנסות/הוצאות, חובות לפי גיל, דשבורד חריגים, ביצועי מכבסה) מציגים
    בדיוק את מה שהציגו קודם, ושדוח "ביצועי מכבסה" נפתח כראוי (אם אין עדיין נתוני
    מכבסה — יופיע כרטיס "אין עדיין נתוני מכבסה להצגה" במקום מסך ריק).
11. **חדש (B49) — דריל-דאון:** בלוח הבקרה, במודל "סכומים לגבייה" — כפתור
    "→ דוח חובות לפי גיל". בכרטיס לקוח, בטאב "חובות ותשלומים" — כפתור "דוח גיל
    חובות" שפותח את דוח הגיל מסונן לאותו לקוח בלבד. **חדש:** במסך "כביסה" (לוח
    כביסה) — כפתור "📊 דוח ביצועי מכבסה" (מוצג רק למי שרואה את מסך הדוחות).
12. **חדש (B49) — משרד ומנהל** רואים את מסך הדוחות; נהג/עובד רצפה/מכבסה — כקודם, לא.

## שלושת ה-batches האחרונים

### B49 — מרכז דוחות (26.07.2026)

**מה נבנה.** ממשק בלבד, קובץ השרת לא נגע. אובייקט קטלוג גלובלי `REPORTS_CATALOG`
(חמש רשומות: `orders`/`summary`/`aging`/`exceptions`/`noble`, כל אחת עם
`{id,title,desc,group,roles,params,container,autoRun}`), פונקציית טופס פרמטרים
אחידה `repParamsForm` (סוגי שדה: date/customer/select/text), `runReport(id)` שאוסף
ערכים ומפעיל את `REPORT_RENDERERS[id]`, ונקודת הכניסה לדריל-דאון `goReport(id,params)`.
`rReports()` נכתבה מחדש: בלי `REPORT_ID` מוצג קטלוג אריחים (`.eqgrid`/`.eqtile`,
בדפוס של B46) מקובצים לפי `REPORT_GROUPS_ORDER` (כספים·תפעול·מכבסה·ניהול); עם
`REPORT_ID` מוצג הדוח הבודד + טופס פרמטרים + כפתור `repBack()`.

**החלטות שהתקבלו ולמה.**
- **אפס דוחות חדשים, אפס שינוי לוגיקת חישוב.** `REPORT_RENDERERS` הן עטיפות דקות
  שמפעילות בדיוק את הפונקציות הקיימות (`loadSummary`, `loadAgingReport`,
  `loadExceptionsDashboard`, `runOrdersReport`, `renderNobleReports`) על אותם
  `container` id-ים כמו קודם (`repSummary`/`repAging`/`repExceptions`/`repRes`/`repNoble`) —
  אפס סיכון רגרסיה בחישוב עצמו.
- **דוח 'orders' שמר על שדות ה-DOM המקוריים** (`rf_from`/`rf_to`/`rf_cust`/`rf_status`/
  `rf_type`) דרך `id` מפורש בהגדרת הפרמטר — כי `runOrdersReport()` הקיימת קוראת אליהם
  ישירות ולא עברה שינוי. פרמטרים בלי `id` מפורש מקבלים `rp_<reportId>_<key>` (מונע
  התנגשות בין דוחות עתידיים ב-B50 עם אותו שם שדה, למשל שני דוחות עם `customer_id`).
- **`autoRun` פר-דוח, לא גלובלי.** דוחות בלי פרמטרים (summary/exceptions/noble) ודוח
  החובות (aging, שהפרמטר בו אופציונלי) רצים אוטומטית בכניסה — התנהגות זהה למסך
  הישן. דוח ההזמנות (`orders`) **לא** רץ אוטומטית — היה כך גם לפני B49 (שינוי היקף
  היה עלול לשלוח שאילתה כבדה על כל הכניסה למסך בלי בקשה).
- **`loadAgingReport` קיבלה פרמטר `customerId` אופציונלי** — סינון של `rows` שמגיעות
  מ-`b9AgingReport` **בממשק בלבד**, בלי קריאת שרת נוספת ובלי לגעת בחישוב. כשמסוננת
  ללקוח בודד, השורה התחתונה ("סה״כ") מחושבת מהשורה המסוננת ולא מ-`r.totals` הכללי.
- **דריל-דאון מינימלי ומדויק** (לא לכל מספר): כפתור בכרטיס לקוח (`custTabFinance`,
  ליד "פירוט החוב"/"גבה תשלום") וכפתור במודל `collectListModal` בלוח הבקרה — שניהם
  אל `goReport('aging', ...)`. לא נוסף דריל-דאון למלאי נמוך — אין דוח `lowstock` נפרד
  (הוא חלק מ-`summary`), והנחיית ה-BUILD_PLAN המפורשת: לא מוסיפים קישור שאין לו דוח.
- **`collectListModal` לא הוחלפה** — היא כבר דרילדאון פעיל וטוב יותר לצורך גבייה
  (כולל כפתור "גבה" לכל לקוח). נוסף כפתור משלים ולא הוסר דבר.
- **אייקוני קבוצות הקטלוג** — שימוש חוזר באייקוני `NAV_GROUPS` הקיימים דרך מיפוי שם
  קבוצה→תווית `NAV_GROUPS` (`reportGroupIcon`), בלי לצייר אייקון חדש.
- **חיסכון בקריאות שרת:** כניסה למסך דוחות כיום שולחת 0–1 קריאות (לפי הדוח שנבחר,
  ורק summary/aging/exceptions רצות אוטומטית) במקום 3 קריאות שרת בבת אחת (summary,
  aging, exceptions) כפי שהיה קודם.

**הגדלת ראש — איפה עוד זה נוגע.**
`rReports` הוא הקורא היחיד ל-`loadSummary`/`loadAgingReport`/`loadExceptionsDashboard`/
`runOrdersReport` (אומת ב-grep) — אין קוד חיצוני שקורא להן ישירות, כך שהמעבר לקטלוג
לא שובר שום מסך אחר. `renderNobleReports` ממשיכה לשמש גם את `rBi` (לוח הבקרה העסקי,
`biNobleToggle`) — לא נגעתי בחתימתה, רק קראתי לה עם הפרמטר הרגיל (`repNoble`).
`allowedViews()` (B47/B38) ממשיכה לקבוע גישה למסך `reports` כמסך שלם — מנהל ומשרד
רואים אותו, נהג/עובד רצפה/מכבסה לא (ללא שינוי); שדה `roles` בקטלוג הוא שכבת סינון
נוספת פר-דוח, לא בשימוש עדיין (כל חמשת הדוחות `roles:null`) — מוכן ל-B50.

**מה נשבר / סיכונים שנבדקו.** מסך הדוחות הישן טען את כל הארבעה בבת אחת; כעכשיו
נכנסים לקטלוג ריק מנתונים ובוחרים דוח. זו כוונה (חיסכון), לא רגרסיה — צוין ב"ממתין
לאימות אבי". `NAV_GROUPS`/`allowedViews`/`DRIVER_TAB_ORDER`/מנוע האשראי/כלל המע"מ —
לא נגעתי בהם.

**מה נבדק. 19 בדיקות jsdom, כולן עברו** (על הקובץ האמיתי, `api()` מדומה בלבד):
קטלוג מציג 5 אריחים בארבע הקבוצות · `goReport('aging')` רץ אוטומטית ומציג את כל
הלקוחות · `goReport('aging',{customer_id})` מסנן לשורה אחת ומאכלס את שדה הפרמטר ·
`repBack()` מחזיר לקטלוג · דוח `exceptions` רץ אוטומטית · דוח `orders` **לא** רץ
אוטומטית ושדות ה-DOM המקוריים (`rf_from` וכו') קיימים · קליק **`MouseEvent` אמיתי
עם `bubbles:true`** על אריח בקטלוג פותח דוח (לא קריאה ישירה לפונקציה — כלל B48a) ·
עובד רצפה לא רואה את מסך `reports` (`allowedViews`) · **דוח מכבסה עם 0 אירועים
מציג הודעה ידידותית ולא מסך ריק** · **דוח מכבסה לא קורס כש-`DB.machines` אינו
מוגדר** · summary/aging/exceptions מציגים רצועת KPI · כפתור "דוח ביצועי מכבסה"
במסך כביסה מוצג למנהל ומוסתר לתפקיד מכבסה. `node --check` על בלוק ה-`<script>` — עבר.

**⚠ סבב תיקונים בתוך אותו batch, אחרי בדיקת אבי בפועל (26.07.2026).**
אבי דיווח: דוח "ביצועי מכבסה" לא נפתח בכלל; חלק מהדוחות "לא עולים מיד"; בקשה
לרצועת KPI מעוצבת בכל דוח ולקישור אל דוחות המכבסה מתוך מסך "כביסה".

**אבחון (מהקוד, לא השערה).**
1. `renderNobleReports` — כש-`DB.laundryEvents` ריק (או שלא נרשמו עדיין אירועי
   מכבסה) הפונקציה החזירה `box.innerHTML=''` **בלי שום הודעה**. במסך הישן (כל
   ארבעת הדוחות יחד) זה לא הורגש כי שלוש כרטיסיות אחרות עדיין הציגו תוכן; במסך
   הבודד של B49 זה נראה כמו "המסך לא נפתח". תוקן: כרטיס "אין עדיין נתוני מכבסה
   להצגה" במקום מחרוזת ריקה.
2. באותה פונקציה: `DB.machines.map(...)` בלי הגנת `||[]` (בניגוד לכל שאר 20+
   אתרי הקריאה ל-`DB.machines` בקובץ, שכולם מוגנים). אם `DB.machines` אינו מערך
   (undefined) — הפונקציה זורקת חריגה ועוצרת באמצע, משאירה את המסך חצי-מרונדר.
   תוקן להגנה עקבית.
3. `loadSummary` (דוח "הכנסות מול הוצאות") — היחיד מבין חמשת הדוחות **בלי** מצב
   "טוען..." לפני קבלת התשובה מהשרת. במסך הישן זה לא בלט (אתרים אחרים כן הציגו
   "טוען"); במסך הבודד — מסך ריק לגמרי עד שהתשובה חוזרת, שנראה כתקוע. תוקן:
   `<div class="card empty">טוען...</div>` מיידי, וגם הודעת שגיאה אם `!r.ok`
   (היה חוזר בשקט בלי שום הודעה).

**מה נוסף (לא באג — שיפור עיצוב שביקש אבי).**
- **רצועת KPI אחידה** (`repKpi`/`repKpiRow`, אותו דפוס ויזואלי כמו כרטיסי לוח
  הבקרה — `.kpi`/`.gridkpi`, בלי ניווט) בראש כל אחד מארבעת הדוחות שהיו טבלה
  יבשה: summary (הכנסות/הוצאות/רווח/חוב/מלאי נמוך), aging (סה"כ חוב, 60+ יום,
  מספר לקוחות/שורות), exceptions (סה"כ חריגים + פילוח), orders (מספר הזמנות,
  סה"כ, שולם, יתרה). דוח "ביצועי מכבסה" כבר היה עם כרטיסי מיני-KPI משלו
  (`mini()`) מ-B15/NC3 — לא נגעתי בו כדי לא לסכן את הטאב "מכבסה" בלוח הבקרה
  העסקי (`rBi`) שמשתמש באותה פונקציה.
- **קישור ממסך "כביסה" (`rLaundry`)** לדוח ביצועי המכבסה: כפתור "📊 דוח ביצועי
  מכבסה" בכותרת המסך, קורא ל-`goReport('noble')`. מוצג רק כש-`allowedViews()`
  כוללת `reports` (כלומר למנהל/משרד) — לעובד מכבסה עצמו אין גישה למסך הדוחות
  (כפי שהיה לפני B49) ולכן הכפתור מוסתר לו במקום להוביל למסך ריק.

**חישוב לא השתנה בשום דוח** — כל התיקונים הם הצגה/מצב טעינה/מצב ריק/עיצוב.
`b9AgingReport`, `b9ExceptionsDashboard`, `case 'reports'` (שרת) — לא נגעתי.

**מה לא נבדק.** דפדפן אמיתי (מראה רצועת ה-KPI וכרטיסי הדוחות, פריסת טופס
הפרמטרים ברוחבים שונים) · נתוני ייצור אמיתיים · מסך `bi` (לא נגעתי, אך משתמש
ב-`renderNobleReports` המשותפת — מומלץ לבדוק ידנית שהטאב "מכבסה" שם עדיין עובד
אחרי הוספת ה-`||[]` על `DB.machines`) · פורטל/חנות (לא נוגעים במסך `reports`
מבנית, אך לא נטענו בפועל בבדיקה).

### B48 — מקור אמת אחד ליתרות לקוחות + ניקיון מלאי (26.07.2026)

**מה נבנה.** *שרת:* `b48BalancesAg(db)` — מפת יתרות של כל הלקוחות בפאס אחד, נגזרת
משורות החוב הקנוניות של `b43Receivables` בתוספת איבר זיכוי. `customerBalance` הפכה
לעטיפה דקה מעליה. `case 'reports'`: ה-aging נגזר מהמפה; `lowStock` עבר ל-`b48FreeOnShelf`.
*ממשק:* `custBalance` נכתבה מחדש כתאום מדויק של השרת; `rItems` קיבל בורר מחסן.

**⚠ שני פערים ב-BUILD_PLAN שהתגלו ותוקנו:** (1) `b43Receivables(db)` כבר היה קיים
כמקור אמת — לא נבנתה `b48ReceivableRows` כפולה; (2) לא היה בורר מחסן קיים — נבנה בסשן.

**מה נבדק. 104 בדיקות אוטומטיות, כולן עברו** — הצלבת שרת↔ממשק שדה-שדה, `b48CustomerBalanceAg
== b2CreditUsedAg` לכל לקוח, 18 בדיקות jsdom עם אירוע `change` אמיתי (`bubbles:true`).

**מה לא נבדק.** דפדפן אמיתי · נתוני ייצור אמיתיים · השפעת מטמון ה-DB בשימוש ממושך.

### B48a — תיקון חוסם: חלוניות התפריט לא נפתחו בדסקטופ (26.07.2026)

**מה נבנה.** `evtInside(e, selector)` — מחליפה את `e.target.closest(...)` בכל מאזין
"סגור בלחיצה מבחוץ" ברמת `document`, ונשענת על `composedPath()` כדי לשרוד רינדור מחדש
של ה-DOM **בתוך אותו קליק** (שורש הבאג: `navToggle` מחליף את `#nav.innerHTML` לפני
שהקליק סיים להתפשט, והכפתור המנותק נכשל בבדיקת `closest`).

**מה נבדק.** 70 בדיקות jsdom — כולן דרך `MouseEvent` עם `bubbles:true`, בלי קריאה
ישירה ל-`navToggle`. מול הקובץ שלפני התיקון: 46/70; אחרי: 70/70.
**כלל בדיקה מחייב לכל הבא:** כל בדיקת ניווט/תפריט מדספאטצ'ת אירוע DOM אמיתי.

## היסטוריה (שורה לאצווה; פירוט מלא בכותרת קובץ השרת)
C1–C3, D, F1, G1 — תשתיות מוקדמות: עמודות, אימיילים, מדפים/ברקודים, תיוג, טעינה עצלה, קיצוץ טקסטים ארוכים.
NC1–NC4 — מודול מכבסה: רצפה, לוח חי, חשבונית מרוכזת, יתרות.
B1 — חשבוניות/קבלות + מספור + הקצאה (S7) + PDF ל-Drive.
B2 — מנוע תשלום ואשראי (S1/S2/S5, D7/D9/D15), אגורות, idempotency.
B3 — פקדון/אימות אשראי + ערבויות (stub סליקה; credentials ב-ScriptProperties — S4).
B4 — התראות אימייל + טריגר יומי (b4RunNow).
B5 — Driver-Lite: מסלול, הסרת כתובת, תיעוד שיחות, סטטוסים.
B7/B9/B10 — superadmin · דוח גיל חובות + חריגים · מרכז אישורים.
B13 — חתימה דיגיטלית במסירה (Bug 1 פתוח) · B14 — BI · B16b — מסירה בצילום בלי לקוח.
B20 — כרטיס עובד מרוכז (openEmp) · B23 — תפריט מובייל.
B31 — הבטחת חשבונית · B34 — סריקת העמסה · B35 — מדפים ואצוות.
B36–B37 — שעון נוכחות, מספר אישי, בורר עובד למנהל.
B38 — אבטחה: סינון pin/password_hash, אימות PIN בשרת, hard-fail, ymdLocal/dYmd, בלוק "טרם שובץ נהג".
B39–B42 — אייקוני ציוד · פיצול כביסה · סגירת איסופים תקועים · תחזוקה, תקלות, ספקי שירות.
B43 — תזרים, מועדי פירעון, חובות לקוחות (b43ReceivablesFE).
B44–B45a — כלל מע"מ אחיד.
B46 — PDF אמיתי · אריחי עובד ורכב · photo_url.
B47 — תפריט עליון בדסקטופ · B48a — תיקון פתיחת החלוניות (evtInside).
B48 — מקור אמת אחד ליתרות לקוחות (b48BalancesAg) · פנוי-על-המדף להתראת מינימום · מסנן מחסן במלאי.
B49 — מרכז דוחות: קטלוג REPORTS_CATALOG, טופס פרמטרים אחיד, goReport לדריל-דאון.

## פתוח לטיפול (לפי עדיפות)
1. דוחות חסרים: שעות עבודה, ניצולת השכרות, צי רכב + ייצוא CSV (כולל תזרים B43) — B50.
2. מדד ניצולת מכונות עם חלון קשיח 05:00–17:00 — להחליף לגזירה מהאירוע הראשון ביום
   (הכרעת אבי Y1) — B50.
3. ביצועים: `getAll` שולח את כל 55 הטבלאות במלואן — B51.
4. Bug 1: חתימה דיגיטלית בטאבלט — B51.
5. ניקוי גיליון `audit_log` מסיסמאות שנכתבו לפני B36 — B51.
6. מסך קיוסק — B52 (המפרט נכתב מחדש ב-BUILD_PLAN; החסימה הוסרה).
7. פריסת ניווט משולבת: שורה עליונה + סרגל צד ימני קבוע — B53.
8. כפתור השבתת ספק שירות (השדה `active` קיים, אין כפתור).
9. החלטה סופית: סדר תצוגה בחנות ובפורטל (נטו ראשי מול ברוטו ראשי).
10. דוח "עלות תחזוקה לפי חודש" מ-`fault_events` (מ-B42) — מיועד ל-B50 (סעיף 4 שם).
11. חשבוניות מכבסה מרוכזות (`nobleCreateInvoice`) אינן מפיקות מסמך PDF כלל. דורש
    החלטה: להפיק להן מסמך, ואם כן — מאיזה מקור נתונים (`laundry_intakes`).
12. `customerBalance` בשרת נותרה בלי קוראים אחרי שה-aging עבר ל-`b48BalancesAg`.
    נשמרה בכוונה כנקודת גישה נקייה ללקוח בודד. B49 לא השתמש בה (הסינון של דוח הגיל
    נעשה בממשק, על שורות `b9AgingReport`, לא בשרת) — לשקול הסרה אם B50 גם לא ישתמש.
13. **חדש (B49):** מסך `bi` (לוח הבקרה העסקי) עדיין טוען את חמשת הכרטיסים שלו בנפרד
    ולא עבר לדפוס קטלוג — נשאר כך בכוונה (BUILD_PLAN B49 סעיף 3: "מסך BI נשאר מסך
    נפרד — לא נבלע"). לא פריט לטיפול, לתיעוד בלבד.

## מפרטים ממתינים
- `BUILD_PLAN.md` בידע הפרויקט: B50 · B51 · B52 · B53 — כתובים במלואם, כולל הכרעות
  ארכיטקטוניות. (B46, B48, B48a, B49 בוצעו.)
- ⚠ **לקח מ-B48, אומת מחדש ב-B49:** סעיפי "מצב קיים" ב-`BUILD_PLAN` עלולים לתאר קוד
  שהתיישן. תמיד לאמת את העוגנים מול הקוד החי לפני שבונים.
- ⚠ **תלות מחייבת שנוצרה ב-B48, לא נשברה ב-B49:** `b48BalancesAg` (שרת) ·
  `custBalance` (ממשק) · `b2CreditUsedAg` (מנוע האשראי) — שלושתם חייבים להחזיר אותו
  מספר. B49 לא נגע בשרת ולא בפונקציות האלה.
- כלל בדיקה מחייב שנוסף ב-B48a, יושם גם ב-B49: **כל בדיקת ניווט/קליק מדספאטצ'ת אירוע
  DOM אמיתי (`MouseEvent`/`change` עם `bubbles:true`).** בדיקה שקוראת לפונקציית ה-handler
  ישירות אינה קבילה.
- **חדש (B49) — לב הרחבה עתידית ל-B50:** `REPORTS_CATALOG`/`REPORT_RENDERERS`/
  `repParamsForm` בנויים כך שדוח נוסף = רשומה בקטלוג + פונקציית render + (אופציונלי)
  הגדרת פרמטרים — בלי לגעת ב-`rReports` עצמה. B50 יוסיף שלוש רשומות (`rep_hours`,
  `rep_utilization`, `rep_fleet`) בדיוק בדפוס הזה.
- כיוון עתידי (לא מתוכנן): פיצול לאפליקציות ייעודיות לפי סיווג (הנהלה/נהגים/ייצור).
