# MAPA-NOBLE — מצב המערכת

## מצב נוכחי
canary ממשק: **v4.75-B75** | canary שרת: **v4.75-B75** | עדכון אחרון: 17.08.2026

**מה עובד:** כל המערכת כפי שנמסרה עד B74, ובנוסף שישה תיקונים ב-B75:

⭐⭐ **המונה של הניצולת הפסיק להיות אפס** (`WASH-15`). `b49cAfterAdvance` —
הגשר שמשקף כל מעבר שלב בלוח הכביסה ליומן NOBLE — כתבה אירוע `התחלה`
**בלי `portion_kg` בכלל**, ולכן `load_kg` של כל כביסה פנימית של MAPA היה
אפס תמיד. עכשיו המשקל נגזר מ-`items.weight_kg × qty`.
⭐ **בנוסף נבנה אבחון `READ_ONLY`** — הגדרות ← "בדיקת משקלי פריטים
וניצולת" — שמראה על נתוני אמת כמה ק"ג נמדדים בכל מכונה ואילו פריטים
חסרי משקל ולכן תורמים אפס.

⭐⭐ **מכונה אחת, משימה אחת** (`WASH-05`). שתי משימות כביסה נפרדות שויכו
לאותה מכונה בו-זמנית בלי שום התראה. עכשיו נחסם, **עם דילוג בקוד מנהל
וחובת פירוט** — והדילוג נרשם ביומן המשימה.

⭐⭐ **מכונה שנשלחה לתיקון מתנתקת מהמשימות שלה** (`WASH-08`). סטטוס
המכונה נבדק רק ברגע השיוך, ולכן משימה שכבר יושבת במכונה שדווחה בתקלה
המשיכה להתקדם עד "מוכן". עכשיו דיווח התקלה מנתק אותה מיד.

⭐ **משלוח המכבסה נושא את מזהה ההזמנה** (`WASH-13`) — עד כה `order_id` היה
ריק קבוע, והמשלוח היה תלוש מההזמנה שהוליד אותו.

⭐⭐ **אין לקלוט כביסה על הזמנה שנסגרה או שהופקה לה חשבונית** (`WASH-16`,
`DEC-03`). שער אחד בשלושה אתרי כניסה: פתיחת קליטה · הוספת עגלות · שקילה.

⭐⭐ **הזמנת השכרה עם סוג מלוכלך בגיליון משריינת מלאי** (`WASH-21`) —
התמונה הראי של `WASH-20`. עשרים אתרי השוואת סוג גולמית עוברים עכשיו
דרך `w21IsRental`, תאום מדויק ל-`w17IsWash` וזהה תו-בתו בשני הקבצים.

**וממשיך לעבוד:** ⭐⭐ **הזמנת כביסה
שסוג ההזמנה שלה מלוכלך בגיליון כבר אינה מחייבת את שורות ההזמנה ככסף**
(`WASH-20`, B74). ארבע נקודות כסף — שתיים בשרת ושתיים בממשק — השוו
`String(o.type) !== 'כביסה'` בהשוואה **גולמית**, ולכן ערך עם רווח קשיח,
סימן כיוון או רווח נגרר נפל לצד ההשכרה. כל הארבע עוברות עכשיו דרך
`w17IsWash`, נקודת אמת אחת שזהה תו-בתו בשני הקבצים.
⭐ **בנוסף נבנה אבחון `READ_ONLY`** בכרטיס "בדיקת נתוני חיובים" שאומר
לאבי, על נתוני אמת, **אם זה בכלל קרה אצלו וכמה כסף זז**.

**וממשיך לעבוד:** ⭐⭐ **קליטת כביסה
של לקוח נראית על לוח הכביסה** (`WASH-18`, B73). עד היום הלוח קרא
`laundry_tasks` בלבד — והן נוצרות רק ב-`receiveReturn`, כלומר הטקסטיל
של MAPA שחוזר מהשכרה. לקליטת NOBLE אין פריט, ולכן לא הייתה לה
שורת משימה — **שני מודלים מקבילים לאותו מסך**. מעכשיו הלוח מציג
את שני המקורות זה לצד זה, ומנהל או משרד יכולים **לקדם שלב
ידנית, בלי סריקת עגלה ומכונה** — לבדיקות ולעובד חדש.
⛔⛔ **המלאי לא זז**: לא נוצרו `laundry_tasks` מזויפות, ולכן `inLaundryQty`
ו-`availableQty` מחזירות אותו מספר בדיוק. ⛔⛔ **"מוכן" אינו עוקף את
חובת השקילה** — הוא זורם ל-`nobleMarkReady` עם כל השומרים של B70/B71.

**מה עבד קודם וממשיך לעבוד:** **שלושה תיקונים במסך
הקליטה ובכרטיס הזמנת הכביסה** (B72): ⛔ **המלאי הוא של המכבסה** — הזמנת
כביסה כבר אינה מאפשרת לבחור פריט מקטלוג המלאי, ובמקום זה יש שורת תיאור
חופשית קצרה (`WASH-17`); **בחירת עגלה מרשימת הפנויות** לצד הסריקה, שנשארה
במקומה (`WASH-19`); ⛔ **פיצול משימת כביסה כבר אינו מתרבה עם שלב מלוכלך** —
הילדה נולדת עם הערך הקנוני מ-`sPick` (`WASH-14`).

**וגם:** **כל הכסף במסלול הכביסה
מחושב באגורות שלמות** (`WASH-10`). עד היום כל שלב במסלול עבר דרך `nRound2`,
שהיא `Math.round(n*100)/100`, והיא **מפילה אגורה** כשהתוצאה נופלת בדיוק על
חצי אגורה — בגלל הייצוג הבינארי של המספר. הסטייה תמיד לרעת העסק.

**עדות מדודה על הקוד שהיה בייצור** (סריקת טווח מלא, לא הערכה):
נטו 0.59 ק"ג × 8.5 ₪ = 5.015 → המערכת כתבה **5.01** במקום **5.02**.
**1.9% מכלל השקילות** ו-**0.4% מסכומי המע"מ** איבדו אגורה, בכיוון אחד בלבד.

**וממשיך לעבוד גם:** **עגלה שחוזרת לקלוט מאותה הזמנה שומרת את
החיוב הקודם** (`WASH-03`, B70) — כל שקילה היא שורת חיוב בפני עצמה והן
מצטברות. **מודול NOBLE כולו עובר את כלל B64a** (`WASH-04`, B69) — כל השוואת
מחרוזת במסלול הכביסה עוברת `sVal`/`sPick`.

**מה שבור:** ⚠ **מצב ההזמנה (`status`) נקרא מהגיליון בהשוואה גולמית
בכמה עשרות אתרים** — ממצא חדש שנפתח ב-B75 ולא תוקן: `WASH-22`.
`RESERVING_STATUSES.indexOf(o.status)` ב-`reservedQty` וב-`b48CommittedQty`,
`B40_OUT_STATUSES`/`B40_RESERVED_STATUSES` בממשק, ורשימות דומות ביומן
ובהתראות. **הזמנה מאושרת שהסטטוס שלה בגיליון נושא רווח קשיח אינה
משריינת מלאי** — בדיוק אותה מחלקת כשל של `WASH-20`/`WASH-21`, בציר אחר.
⛔ B75 טיפל ב**סוג** בכוונה ולא בסטטוס: איחוד שני הצירים באצווה אחת
היה מכפיל את שטח הבדיקה על המלאי. ⛔ **מוקצה ל-B76.**

✅ `WASH-20` נסגר ב-B74 · ✅ `WASH-15` · `WASH-05` · `WASH-08` · `WASH-13` ·
`WASH-16` · `WASH-21` נסגרו ב-B75 — ראה למטה.

✅ שלושת ממצאי ביקורת הכביסה שנפתחו ב-02.08.2026 נסגרו: `#4` ב-B69 ·
`#3` ב-B70 · `#10` ב-B71.

✅ **B75 אינו כולל שינוי סכימה — אין להריץ `setupDatabase`.**
`laundry_events` (18 עמודות, כולל `portion_kg`) ו-`items` (20 עמודות, כולל
`weight_kg`) לא זזו, ויש בדיקה שסופרת את שתיהן ונכשלת אם מישהו יזיז אותן.

✅ **B74 אינו כולל שינוי סכימה — אין להריץ `setupDatabase`.**
`orders` לא זזה, ואף עמודה לא נוספה. ⛔ **הנתון בגיליון לא תוקן** — בהכרעת
אבי הקוד עומד בלכלוך, והנתון נשאר כפי שהוא.

✅ **B73 אינו כולל שינוי סכימה — אין להריץ `setupDatabase`.**
`orders` · `order_lines` · `laundry_tasks` · `laundry_events` · `laundry_intakes`
לא זזו, ויש בדיקה אוטומטית שנכשלת אם מישהו יזיז אותן.

**מה ממתין לאימות אבי:** 🔴🔴 **בדיקת משקלי פריטים וניצולת (B75)** —
הגדרות ← תחזוקת מסמכים ← "בדיקת משקלי פריטים וניצולת". **זו הפעם
הראשונה שיהיה מספר אמיתי לשאלה כמה ק"ג באמת עוברים במכבסה**, וכמה
פריטים חסרי משקל · 🔴 **קידום שלב לשתי משימות לאותה מכונה (B75)** —
שהחסימה מופיעה, שטופס הדילוג נפתח, ושדילוג עם קוד מנהל ופירוט נרשם
ביומן המשימה · 🔴 **דיווח תקלה על מכונה שיש עליה משימת כביסה פתוחה
(B75)** — שהמשימה מאבדת את שיוך המכונה ושהיומן שלה מסביר למה ·
🔴 **קליטה על הזמנת כביסה שכבר הופקה לה חשבונית (B75)** — שהמערכת חוסמת
ומציגה את מספר החשבונית · 🔴🔴 **בדיקת נתוני חיובים (B74)** — הגדרות ←
"בדיקת נתוני חיובים". **זו הפעם הראשונה שיהיה מספר אמיתי לשאלה אם
`WASH-20` פגע בכסף בייצור.** שני סעיפים חדשים בסוף הכרטיס · 🔴 **קליטת כביסה על לוח הכביסה (B73)** —
לפתוח קליטה ברצפת הייצור ולראות שהיא מופיעה בלוח, ולוודא
שקידום ידני עובד וש-"מוכן" נחסם ללא שקילה · 🔴 **הזמנת כביסה אחרי B72** — לוודא שאין בכרטיס
שום כפתור שמוסיף פריט, ושהתיאור החופשי נשמר · 🔴 **בחירת עגלה מהרשימה
במסך הקליטה** — ושהסריקה ממשיכה לעבוד בדיוק כמו קודם · 🔴 **שקילה על
נתוני אמת אחרי B71** — לוודא
שהסכום על המסך שווה בדיוק לנטו × מחיר, בלי אגורה חסרה · 🔴 **חשבונית
מכבסה חדשה** — נטו + מע"מ = סה"כ, בדיוק · 🔴 **שקילה כפולה על נתוני אמת
אחרי B70** — שני החיובים מופיעים · 🔴 **תיקון שקילה על ידי מנהל** —
סיבה, מספר אישי, והשורה מסומנת כמתוקנת · 🔴 **רצפת הייצור על נתוני אמת
אחרי B69** · 🔴 **הרצת הבדיקה העצמית אחרי פתיחת ששת המסכים הרחבים** —
`BLD-05` חסום עליה בפעם העשירית · **B68** — מיזוג הכפילויות של "אברהם" ·
**B67** — תשלום אמיתי לעובד · **BLD-03** — רישום טאבלט שני.
✅ B64a אומת בייצור. ✅ B65 ו-B66 נפרסו. ✅ `setupDatabase` של B70 רץ.

⛔ **הבדיקות הן קובץ אחד: `tests.js` בשורש הריפו.** קובץ אחד יורד, קובץ אחד עולה.

---

### ⭐⭐ B75 — WASH-15 + WASH-05 + WASH-08 + WASH-13 + WASH-16 + WASH-21 (17.08.2026)

**canary: `v4.75-B75` · 529 בדיקות · 0 כשלים (47 חדשות) · ✅ אין שינוי סכימה**

⚠ אצווה מאוחדת של **שישה** פריטים — מעל התקרה של `M1` (שלושה), בהוראת אבי
מפורשת. `M3` קוים במלואו: חמש השאלות נענו בכתב **לכל פריט בנפרד** לפני
שנכתבה שורת קוד. `M5` — מסירה אחת בסוף.

**קו בסיס לפני נגיעה:** 482 בדיקות · 0 כשלים. ✅ אין רגרסיה קיימת.

**הכרעות אבי (17.08.2026):** `1ב` `2א+דילוג מנהל` `3ג` `4ב` `5א`.

---

#### פריט 1 — `WASH-15`: המונה של הניצולת

**מה היה שבור.** `b49cAfterAdvance` — הגשר היחיד ממסלול MAPA ליומן NOBLE —
פתחה שלב חדש כך:

```
nobleEvent(db, { ..., stage: toStage, event_type: 'התחלה', ... })
```

**בלי `portion_kg` בכלל.** `nobleMachineLoad` סופרת `Number(ev.portion_kg)||0`,
ולכן `load_kg` של **כל** כביסה פנימית של MAPA היה **אפס תמיד** — רק
`load_count` נספר. ניצולת שמחושבת מזה מציגה מכבסה ריקה, ולכן `BLD-13`
(תכנון עומס מכבסה) היה חסום: אין טעם לבנות מחשבון זמינות על מונה שמחזיר אפס.

**מקור המשקל (הכרעת אבי `1ב`).** `w15TaskKg(db, t)` = `items.weight_kg × qty`.
⛔ **אין שינוי סכימה:** `items.weight_kg` קיים ומשמש כבר לשקילת החזרות,
ו-`laundry_events.portion_kg` קיים מאז B39b. הפער היה במילוי ובשאילתה.

⛔ **אין שער חסימה.** פריט בלי משקל מחזיר `''` — בדיוק כמו מנת NOBLE
שנפתחה בלי משקל. זו עמידה בהכרעת `WASH-07`: `capacity` הוא **מכנה של
מדידה ולא שער**. יש בדיקה שמוכיחה שהקידום עובר, ובדיקה שמוכיחה שנכתב
`''` ולא `0` — אפס נראה כמו מדידה אמיתית ומזהם את הדוח.

**⭐ האבחון שנבנה לצידו.** בלי מספר אמיתי אין דרך לדעת אם הניצולת חלקית,
ואבי אינו מייצא בסיס נתונים (`R1`). לכן `w15WeightAudit` — פעולה
ב-`READ_ONLY_ACTIONS`, אפס כתיבה (יש בדיקה שמשווה את ה-`db` לפני ואחרי
תו-בתו) — מחזירה: משימות פתוחות · כמה מהן עם משקל · המשקל הכולל הנמדד ·
כמה פריטים בקטלוג חסרי משקל · פירוט 40 הפריטים הכבדים · והמונה בפועל
בכל מכונה עם אחוז הניצולת. הכפתור בהגדרות ← תחזוקת מסמכים, מנהל בלבד.

**⭐ ממצא B69 שסופח כאן — והוא היה חמור יותר מהצפוי.**
`nobleOpenStarts` בנתה את מפתח המנה כך:

```
var k = String(e.machine_id || '') + '|' + sVal(e.stage);
```

**מזהה המכונה גולמי, לצד שלב מנורמל.** אירוע `סיום` שמזהה המכונה שלו נושא
רווח קשיח יצר **מפתח שני**, ולכן לא סגר את `התחלה` התואמת: המנה נשארה
פתוחה **לנצח**, המכונה נראתה תפוסה, והניצולת נמדדה על עומס שאינו קיים.
תוקן דרך `w15MachKey` (=`sVal`) בשישה אתרים: `nobleOpenStarts` ·
`nobleMachineLoad` (המפתח **וגם** שלוש נקודות השליפה) · ההשוואה
ב-`nobleStageStart` וב-`nobleStageEnd`. יש בדיקה שמוכיחה שהעומס אינו
מתפצל לשני מפתחות ובדיקה שמוכיחה שסגירה עובדת בארבע צורות לכלוך.

⚠ **מה **לא** נעשה כאן ובכוונה:** `nobleMachineLoad` סופרת **תפוסה רגעית**
ולא צבירה על פני יום, וקליטה ב`נמסר` מסוננת החוצה. "טון ליום" הוא שאילתה
על `laundry_events` והוא **תוכן `BLD-13`**, לא של `WASH-15`. `WASH-15`
היה חסר המונה; המונה קיים עכשיו.

---

#### פריט 2 — `WASH-05`: מכונה אחת, משימה אחת

`advanceLaundry` בדקה שהמכונה קיימת ושהיא `'פעילה'` — **ולא** אם משימה
אחרת יושבת בה כרגע. שתי משימות נפרדות נשלחו לאותה מכונה בו-זמנית.

⚠ **ההבחנה מ-`WASH-06` שנסגר כמכוון:** שם מדובר במנות של **אותה** קליטת
NOBLE שהתחלקו בין מכונות — וזה בדיוק מה שקורה במכבסה אמיתית. כאן מדובר
בשתי **משימות** נפרדות, ו-B40 קובע במפורש שכל משימה מפוצלת מקבלת עגלה
ומכונה **משלה**.

**הכרעת אבי `2א+`:** חסימה, **עם דילוג בקוד מנהל וחובת פירוט**.
`w05Gate` — נקודה אחת שנקראת גם מ-`advanceLaundry` וגם
מ-`b40SplitLaundry` (⛔ `R8`, בלי דפוס שני). סדר הבדיקות מכוון: קוד חסר →
נחסם · פירוט קצר מ-3 תווים → נחסם · קוד שגוי → `b38VerifyManagerPin` דוחה.
הדילוג נרשם ביומן המשימה עם מזהה המשימה התופסת ועם הפירוט של המנהל —
**אחרת אין למי לחזור אחר כך.**

⛔ משימה בשלב `'מוכן'` **אינה** תופסת מכונה (`machine_id` נשאר עליה לצורך
היסטוריה). בלי הסייג הזה כל המכונות היו נחסמות לנצח — יש בדיקה.

**ממשק:** השרת מחזיר `machine_busy`, והממשק פותח את טופס הדילוג דרך
**אותו דפוס בדיוק** שקיים ב-`act` ל-`need_count` של `returnByWeight` (`R8`).
הטופס מציע קודם את הדרך הנכונה — "בחר מכונה אחרת".

---

#### פריט 3 — `WASH-08`: מכונה שנשלחה לתיקון

סטטוס המכונה נבדק **רק** ברגע השיוך (`if (opts.machine_id)`). משימה שכבר
יושבת על מכונה שדווחה בתקלה המשיכה להתקדם עד `'מוכן'`.

**הכרעת אבי `3ג` — ניתוק אוטומטי.** `w08DetachMachine` נקראת מ-`b42ReportFault`
כשסוג הנכס הוא `'מכונה'`, מנקה `machine_id` מכל משימת כביסה פתוחה שלה,
ורושמת שורה ביומן כל משימה. מאותו רגע `advanceLaundry` חוסם ממילא
("חובה לשייך מכונה") — ⛔ **אין שער שני** (`R8`).

⛔ **הניתוק אינו מוחק היסטוריה** — הוא מוסיף לה שורה, ומשימה ב`'מוכן'`
אינה נוגעת. ⛔ **עגלות ורכבים אינם מנותקים**: העגלות מנוהלות ב-`releaseCart`,
והרכב אינו נוגע בכביסה (`R5`).

⛔ **NOBLE אינו מנותק ובכוונה.** שם `התחלה` פתוחה היא כביסה **שנמצאת
פיזית בתוף**, ו-`nobleStageEnd` חייבת להמשיך לעבוד גם על מכונה מקולקלת —
אחרת הכביסה נתקעת. `nobleStageStart` כבר חוסם התחלה חדשה במכונה שאינה
פעילה, כך שהצד הזה היה תקין מלכתחילה.

⚠ **רשת בטיחות לנתוני עבר:** תקלה שדווחה **לפני** B75 השאירה שיוך תקוע,
ולכן `advanceLaundry` בודק עכשיו גם את סטטוס המכונה **המשויכת** ולא רק
חדשה. בלי זה הבאג היה ממשיך לחיות על כל מכונה שכבר בתיקון היום.

---

#### פריט 4 — `WASH-13`: `order_id` במשלוח המכבסה

`nobleDriverScan` יצרה `{ id: genId('DELI'), order_id: '', kind: 'משלוח מכבסה', ... }` —
**ריק קבוע**, גם כשלקליטה היה `order_id` מ-B49d. המשלוח היה תלוש מההזמנה:
כרטיס ההזמנה לא הראה אותו, ובדיקת "רגל פתוחה" ב-`b56CloseLaundryOrder`
לא ידעה על קיומו.

**מפת החיבורים שנבדקה לפני השינוי** (זה החלק שדרש עבודה, לא התיקון):
`kind` נשאר `'משלוח מכבסה'` ולכן **אינו מתנגש** עם רגלי ההלוך/חזור
(`'אספקה'`/`'איסוף'`) לא ב-`b38EnsureOrderDeliveries` ולא ב-`b49fCloseLegs`,
ששתיהן מסננות לפי `kind`. `nobleDeliver` מסמן את המשלוח `'בוצע'` **לפני**
שהוא קורא ל-`b56CloseLaundryOrder`, ולכן בדיקת הרגל הפתוחה לא נשברת.
⛔ קליטה פנימית של MAPA נשארת עם `order_id` ריק — יש בדיקה.

---

#### פריט 5 — `WASH-16`: חסימה במקור (`DEC-03`)

**הכרעת אבי `4ב`.** `w16OrderGate(db, orderId, what)` — שער אחד, שלושה
אתרי כניסה:

| אתר | מה נחסם |
|---|---|
| `b49dResolveOrder` | פתיחת קליטה חדשה להזמנה |
| `b49dAddCarts` | הוספת עגלות לקליטה שקשורה להזמנה |
| `nobleWeigh` | שקילה — כלומר **יצירת חיוב** (B70/`WASH-03`) |

השער חוסם כשההזמנה אינה `'מאושרת'`/`'סופקה'`, או כשיש לה חשבונית פעילה.
ההודעה מכילה את **מספר החשבונית** — אחרת אבי אינו יודע במה מדובר.

⛔ **`nobleWeighFix` בכוונה מחוץ לשער.** היא מסלול התיקון **היחיד** של
שקילה קיימת (B70), וחסימה שלה הייתה משאירה שקילה שגויה בלי תיקון.

⭐ **`B64a` תוקן אגב:** `b49dResolveOrder` השווה `String(o.status)` גולמית,
כלומר הזמנה **תקינה** שהסטטוס שלה בגיליון נושא רווח קשיח **נחסמה
בטעות**. עכשיו `sVal`. יש בדיקה על שני הכיוונים.

⛔ **חשבונית מבוטלת אינה חוסמת** — היא אינה חיוב. יש בדיקה.
⚠ קליטה פנימית (בלי `order_id`) אינה מושפעת בכלל — שלוש בדיקות.
⭐ **איחוד הזמנות נשאר אפשרי לצרכים תפעוליים** ואין לו אפקט חשבונאי;
השער הזה אינו נוגע בו.

---

#### פריט 6 — `WASH-21`: התמונה הראי של `WASH-20`

B74 תיקן את ארבע נקודות הכסף. הסריקה שלו ספרה **19 אתרים נוספים** שאינם
כסף — וארבעה מהם הם **מלאי**:

| קובץ | פונקציה | התוצאה בשטח |
|---|---|---|
| שרת | `reservedQty` | הזמנת השכרה עם סוג מלוכלך **אינה משריינת** |
| שרת | `b48CommittedQty` | ואינה נספרת כמחויבת להתראת הרכש |
| ממשק | `b40ItemBreakdown` | והפריט **נראה זמין** בכרטיס הפריט |
| ממשק | דוח הניצולת | ואינו נספר בדוח |

**מה נבנה (הכרעת אבי `5א`).** `w21IsRental(o)` — תאום מדויק ל-`w17IsWash`,
זהה תו-בתו בשני הקבצים, עם בדיקה שמשווה את הגופים:

```
function w21IsRental(o){ return !!o && sVal(o.type)!=='כביסה'; }
```

⛔ **לא נבנה עוזר "סוג קנוני"** (אפשרות ב'): הוא היה דורש הכרעה חדשה מה
מחזירים על סוג לא מוכר, וזה סותר את הכלל שנקבע ב-B74. ⛔ **סוג לא מוכר
וסוג ריק נשארים השכרה** — בדיוק כמו לפני התיקון. שלוש בדיקות.

**⚠ `R9` — הפריט אמר 19; הקוד החי נתן 20.** `b49eLegLabel` בממשק לא נספר
ב-B74. תוקן. **פירוט 20 האתרים:** שרת — `b49eLegKind` · `b49eShipMode` ·
`reservedQty` · `b48CommittedQty` · `b49dResolveOrder` ·
`b4ScanReturnReminders` · `b9ExceptionsDashboard` · `b14` פילוח חשבוניות.
ממשק — `w21IsRental` עצמו · לוח הבקרה · `b49dPendingOrders` ·
`b49dOrderIntakeHtml` · `b49eLegKind` · `b49eLegLabel` · `b49eShipMode` ·
טופס ההחזרה (`isWash49d`) · `b40ItemBreakdown` · דוח הניצולת · `calEvents`.

**⭐ ממצא: `linePrice` — לא נגעתי, ובכוונה.** היא משווה
`orderType === 'כביסה'` גולמית, וזו נראית נקודת כסף. `grep` על אתר
הקריאה היחיד שמעביר סוג מהגיליון (`create` על `order_lines`, שורה 2216)
הראה שהוא נחסם **קודם** ב-`sPick(po.type, ORDER_TYPES)` מ-B72 — כלומר
הזמנת כביסה מלוכלכת אינה מגיעה לשם לעולם. **אין חשיפה כספית, ולכן אין
לגעת** (`R4`: לא נוגעים במנוע המחירים בלי צורך מוכח).

---

#### הבדיקות — 529, מהן 47 חדשות

| מה | איך נבדק |
|---|---|
| ⛔ `WASH-21`: השכרה מלוכלכת **כן** משריינת | 4 צורות לכלוך × `reservedQty` · `b48CommittedQty` · `availableQty` · `b48FreeOnShelf`, מול קו בסיס נקי — בשרת **וגם** בממשק |
| ⛔ `WASH-21`: המלאי על סוג נקי לא זז | רגרסיה מלאה על ארבעת המדדים + `inLaundryQty` |
| ⛔ `WASH-15`: ניצולת פנימית אינה אפס | קידום שלב אמיתי → האירוע ביומן → `nobleMachineLoad` מחזירה 10 ק"ג |
| ⛔ `R6` בשלושת המקורות | `b48BalancesAg` = `b2CreditUsedAg` = `b54CustomerOpenAg`, על נקי ועל ארבע צורות לכלוך |
| ⛔ B71/B72/B73/B74 לא נשברו | כל 482 הבדיקות הקיימות עברו בלי שינוי (למעט ציפיית ה-canary) |
| שומרי מקור | ההשוואה הגולמית אינה חוזרת ל-6 פונקציות · `w21IsRental` זהה בשני הקבצים · `w17IsWash` ו-`toAg` לא נגעו · `laundry_events` 18 עמודות ו-`items` 20 |

⚠ **לקח על הרתמה עצמה:** `H.stripComments` **אינו מסיר את ההערות באזור
של `b49eLegKind`** בקוד השרת — הסורק שלו נתקע במצב מחרוזת בשלב מוקדם
יותר בקובץ. שומר מקור שנשען עליו נכשל על טקסט שמופיע **בהערה בלבד**
(כשל שווא, וזה מה שקרה לי בהרצה הראשונה). שני השומרים החדשים חותכים
הערות **בתוך הגוף שחולץ** ולא על הקובץ כולו. ⛔ מי שכותב שומר מקור
באזור הזה — אל תסמוך על `stripComments`.

⚠ **שכבה 2 לא נגעה** — אף אחד מששת הפריטים אינו יכולת דפדפן. יש בדיקה
שנכשלת אם מישהו יוסיף טענה ל-`b61Tests()` על אחד מהם.

**⚠ שני הקבצים משתנים.** ✅ אין שינוי סכימה.

---

### ⭐⭐ B74 — WASH-20: סוג הזמנה מלוכלך חייב שורות כביסה ככסף (16.08.2026)

**canary: `v4.74-B74` · 482 בדיקות · 0 כשלים · ✅ אין שינוי סכימה**

**מה היה שבור.** `C5` קובע ששורות בהזמנת כביסה הן **אומדן כמותי ואינן
כסף** — החיוב מגיע מהשקילה בקליטה. הבדיקה שמממשת את הכלל הייתה
`String(o.type) !== 'כביסה'`, **השוואת מחרוזת גולמית**. ערך שנקרא מגיליון
עם רווח קשיח (`\u00A0`), סימן כיוון בלתי נראה (`\u200E`), רווח מוביל או
רווח נגרר — **אינו שווה** ל-`'כביסה'`, ולכן ההזמנה נפלה לצד ההשכרה
ושורות האומדן שלה נספרו ככסף. **הכשל שקט לחלוטין**: אין שגיאה, אין
חריגה, רק מספר גדול יותר בחוב הלקוח. זו בדיוק מחלקת הכשל של B64a.

**⚠ R9 — הפריט תיאר נקודה אחת. הקוד החי הראה ארבע.**
`WASH-20` נרשם על `b54RawOrderTotalAg` בלבד. `grep` ממצה על שני הקבצים
מצא ש**ספר החיובים עצמו** נושא את אותה השוואה, ושלשתיהן יש מראה
מדויקת בממשק:

| קובץ | פונקציה | מה היא |
|---|---|---|
| שרת | **`b54Ledger`** | הספר עצמו — מכאן נגזר החוב של כל לקוח |
| שרת | `b54RawOrderTotalAg` | סכום ההזמנה · `orderTotal` · `b2OrderBalanceAg` |
| ממשק | `b54LedgerFE` | מראה מדויקת של הספר |
| ממשק | `b54RawOrderTotalAgFE` | מראה מדויקת של סכום ההזמנה |

⛔ **תיקון של אחת בלבד היה יוצר סתירה** בין סכום ההזמנה לספר, ובין
הממשק לשרת. ארבעתן שונו יחד.

**מה נבנה — נקודת אמת אחת (R8).**
`w17IsWash(o)` כבר היה קיים ב-`index.html` מ-B72 והוא בדיוק המושג
הנדרש. **לא נבנה שם שני** — הפונקציה הועתקה לקוד השרת **תו-בתו**,
וארבע נקודות הכסף קוראות לה. יש בדיקה שמשווה את שני הגופים ונכשלת
אם הם התפצלו, בדיוק כמו השומר של `sVal`/`sPick` מ-B64a.

```
function w17IsWash(o){ return !!o && sVal(o.type)==='כביסה'; }
```

⛔ **אין ריכוך לצד השני.** סוג שאינו ברשימה, וסוג ריק, ממשיכים להיות
מטופלים כהשכרה — בדיוק כמו לפני התיקון. יש שתי בדיקות לכך.

**⭐ הממצא שקבע את היקף הבדיקות: הזמנה עם חשבונית — הכסף אינו זז.**
ב-`b54Ledger` הבסיס הוא `inv ? toAg(inv.total) : netTot` (כלל C4א).
כשקיימת חשבונית פעילה, **סכום המסמך גובר**, והסרת שורת "שורות הזמנה"
רק מחלקת מחדש את אותו סכום בין שורות הספר. ⛔ **החוב זהה לחלוטין.**
החשיפה האמיתית היא הזמנת כביסה מלוכלכת **בלי** חשבונית — שם החוב
יורד בפועל, וזה התיקון.

**R6 — ✅ מובטח מבנית, ונבדק בכל זאת.**
`b48BalancesAg(db)` ו-`b2CreditUsedAg(db,cid)` הם **עטיפות של שורה אחת**
מעל `b54CustomerOpenAg`, שנגזר מ-`b54Ledger`. שינוי בספר מזיז את
שלושתם באותו כיוון בהכרח. הבדיקה רצה על ארבעה סוגים — נקי, מלוכלך,
לא מוכר וריק — ומאשרת שלושה מספרים זהים בכל אחד.

**⭐ האבחון — התשובה ל-R11.**
אין דרך לדעת מהצ'אט אם הזמנה מלוכלכת קיימת בייצור. לכן **הורחב
`b54LedgerAudit` הקיים** (R8 — בלי דפוס שני, בלי כפתור שני) בשני סעיפים:

- **הזמנות שסוג ההזמנה שלהן מלוכלך** — לכל אחת: הערך הגולמי כפי שהוא
  בגיליון, **שם הלכלוך בעברית** (רווח קשיח · סימן כיוון · רווח נגרר ·
  רווח כפול · רווח מוביל), אם קיימת חשבונית, **וכמה כסף ירד מהחוב**.
  ⛔ בלי שם הלכלוך אבי היה רואה "כביסה" מול "כביסה" ולא מבין למה זו שורה.
- **הזמנות עם סוג שאינו מוכר** — מטופלות כהשכרה, מדווחות לידיעה.

⛔ **סוג ריק אינו ממצא** — זו התנהגות ותיקה ותקינה, ודיווח עליה היה רעש.
⛔ שתי הפונקציות שהאבחון עצמו השתמש בהן (`orders_wash` וסעיף החוב השווא)
**סבלו מאותו באג ופספסו את הממצא** — תוקנו גם הן.

**⛔ הנתון בגיליון לא תוקן — הכרעת אבי.** הקוד עומד בלכלוך; לא נבנתה
פעולת כתיבה שמנרמלת את `orders.type`. כתיבה לטבלת ההזמנות היא סיכון
מיותר כשהקוד כבר חסין.

**מה נבדק — 26 בדיקות חדשות (`t20`), 482 בסך הכול:**

- ⭐⭐ כביסה עם סוג מלוכלך ב**ארבע** צורות — הכסף אפס בשלושת המקורות
- ⭐⭐ כביסה מלוכלכת עם חשבונית — **סך החוב לא זז** (C4א)
- ⛔⛔ R6 בשלושת המקורות, על ארבעה סוגים
- ⛔ רגרסיה: הזמנת השכרה — 250 ₪ בדיוק, לפני ואחרי
- ⛔ השכרה מלוכלכת עדיין מחויבת · סוג לא מוכר וסוג ריק — כמו קודם
- ⛔ **המלאי לא זז**: `availableQty` · `reservedQty` · `inLaundryQty`
- ⛔⛔ `w17IsWash` זהה תו-בתו בשני הקבצים
- ⛔⛔ שומר מקור: אין השוואת סוג גולמית **בתוך** שלוש פונקציות הכסף
- ⛔ B71 לא נשבר: `nRound2` לא חזר לנקודות הכסף · `toAg` לא נגע
- ⭐ האבחון: מדווח, מדווח `moved=0` כשיש חשבונית, שקט כשנקי, ואינו
  מייצר `undefined` על תשובת שרת ישנה

**✅ אומת בבדיקת מוטציה:** החזרת ההשוואה הגולמית ל-`b54Ledger` בלבד
**מפילה 4 בדיקות**. הבדיקות תופסות את הבאג, לא רק מתעדות אותו.

**⛔ 19 אתרי השוואת סוג גולמית שנשארו — בכוונה, ומוקצים ל-B75.**
נספרו ותועדו, ואינם כסף. הכבדים שבהם הם **ארבעה אתרי מלאי** —
`reservedQty` · `b48CommittedQty` · `b40ItemBreakdown` · דוח הניצולת —
שמשווים `o.type !== 'השכרה'` גולמית, כלומר **הזמנת השכרה מלוכלכת אינה
משריינת מלאי והפריט נראה זמין**. זו התמונה הראי של `WASH-20`, בסיכון
תפעולי ולא כספי. חמישה-עשר הנותרים: רגלי מסירה (`b49eLegKind`,
`b49eShipMode`), פתיחת קליטה (`b49dResolveOrder`), יומן, התראות ודוחות.

**⚠ מה שלא אומת ולא יכול היה להיאמת מכאן (R11):**
**האם קיימת בייצור הזמנה עם סוג מלוכלך, וכמה כסף זז אצל אבי בפועל.**
זה מה שנבנה כדי לאמת — הכרטיס בהגדרות. **עד שהוא ירוץ, המספר אינו ידוע.**

---

### ⭐⭐ B73 — WASH-18: קליטת כביסה של לקוח על לוח הכביסה (16.08.2026)

**canary: `v4.73-B73` · 456 בדיקות · 0 כשלים · ✅ אין שינוי סכימה**

**השורש — שני מודלים מקבילים לאותו מסך:**

| | טקסטיל MAPA | כביסת לקוח NOBLE |
|---|---|---|
| איפה הנתון | `laundry_tasks` (item_id + qty) | `laundry_intakes` + `intake_carts` + `laundry_events` |
| מי כותב | `receiveReturn` · `b40SplitLaundry` | `nobleIntake` |
| מי קורא | `rLaundry` — לוח הכביסה | `nobleBoard` — הלוח החי ברצפת הייצור |
| שלבים | `STAGES` — 5 | `NOBLE_STAGES` — 8 |
| כסף | `order_lines` → `b54RawOrderTotalAg` | שקילה → `nobleWeighRollup` → `total_charge` |
| מלאי | `inLaundryQty` סופרת | **אינו נוגע במלאי כלל** |

⛔⛔ **המלכודת שלא נפלנו בה:** הפתרון האינטואיטיבי הוא ליצור `laundry_tasks`
לקליטת NOBLE. זה היה מזליג את הקליטה ל-`inLaundryQty`, משם ל-`availableQty`,
ומוליד את `WASH-17` מחדש בצורה גרועה יותר. **מה שנבנה הוא שכבת
תצוגה** — הלוח קורא את `laundry_intakes` ישירות, בלי שורת משימה אחת.

**הכרעות אבי (16.08.2026):**
1. **1א** — קידום מהלוח גם בלי סריקה פיזית (בדיקות · עובד חדש),
   ⛔ אבל **"מוכן" אינו עוקף** את `nobleMarkReady` ואת חובת השקילה.
2. **2** — בורר שלב יעד עם דילוג וחזרה אחורה — אותו דפוס של B59.
3. **3ב** — הקידום חל על **עגלה נבחרת** בתוך הקליטה.
4. **4א** — קליטה שיש לה `laundry_tasks` (החזרת טקסטיל B49c) **אינה מוצגת
   שוב** — אחרת כל החזרה הייתה מופיעה פעמיים.
5. **2א** — קידום ידני למנהל ולמשרד בלבד (`b34OfficeOk`).

**השרת — `w18Advance`:**
⛔ אין דפוס שני (R8): הכתיבה זהה ל-`b49cAfterAdvance` — סגירת המנות
הפתוחות ב-'סיום' ופתיחת השלב החדש ב-'התחלה'. ההבדל היחיד:
`machine_id` ריק. `nobleMachineLoad` מדלגת על אירוע בלי מכונה, ולכן
**תצוגת המכונות בלוח החי אינה מזדהמת** — יש בדיקה על זה.
⛔ השלבים המותרים הם `NOBLE_WORK_STAGES` הקיימים — אין רשימה שנייה.
⛔ `laundry_events` נשאר append-only — `nobleEvent` בלבד.

⛔⛔ **למה "מוכן" אינו עובר ב-`w18Advance`:** `nobleMarkReady` חייב לרוץ
**לפני** כל כתיבה, כי `laundry_events` הוא append-only ואי-אפשר לבטל
'סיום' שכבר נכתב. לכן הממשק שולח "מוכן" ישירות ל-`nobleMarkReady`
עם כל השומרים של B70/B71, ו-`w18Advance` חוסם את הערך עם הפניה.
יש בדיקה שסורקת את `w18DoAdvance` בממשק ונכשלת אם הקשר הזה נשבר.

**הממשק — `rLaundry`:**
מיפוי העמודות (`w18BoardCol`): "באריזה" → עמודת "בגיהוץ וקיפול"
(אותה הכרעה של B59 — "באריזה" אינו שלב בלוח אלא שלב מכונה) ·
"במשלוח"/"נמסר" יורדים מהלוח · "מוכן" באותו חלון 3 ימים.
⛔ **שלב מלוכלך או לא מוכר אינו מעלים את הקליטה** — זה בדיוק הלקח
של `WASH-14`, ויש בדיקה על ארבע צורות לכלוך.

**מה נבדק ועבר — 36 בדיקות חדשות:**
קליטה נראית בלוח ואפשר לקדם לה שלב · ⛔⛔ `inLaundryQty` ו-`availableQty`
זהים לפני ואחרי · ⛔ לא נוצרה אף `laundry_tasks` · ⛔ `receiveReturn`
עדיין יוצר משימות טקסטיל · ⛔ "מוכן" נחסם ולא נכתב כלום · ⛔ עובד
מכבסה נדחה · ⛔ סריקה רגילה ממשיכה לעבוד אחרי קידום ידני · ⛔ R6
בשלושת המקורות · ⛔ B70/B71/B72 לא נשברו · ⛔ אין שינוי סכימה.

**⚠ מה לא נעשה ולמה:**
· `WASH-20` (כסף — `b54RawOrderTotalAg`) נשאר פתוח — אצווה משלו.
· לא נוספה עמודת "באריזה" ללוח — זה היה משנה את הלוח גם לטקסטיל.
· קידום ידני נרשם ביומן ובישרות משפיע על `nobleStageAvgs` (ממוצעי זמן
  לשלב). זה נכון — הזמן שעבר הוא זמן אמיתי — אבל ראוי לדעת:
  שרשרת קידומים לצורך בדיקה תופיע בממוצעים של דוח המכבסה.

---

### ⭐⭐ B72 — WASH-17 + WASH-19 + WASH-14: המלאי, העגלה והשלב (16.08.2026)

**canary: `v4.72-B72` · 420 בדיקות · 0 כשלים · ✅ אין שינוי סכימה**

שלושה פריטים במסך הקליטה ובכרטיס ההזמנה. אף אחד מהם אינו כסף, ואף אחד
אינו נוגע בסכימה. **הבסיס הורץ לפני כל נגיעה והחזיר 395/0.**

#### `WASH-17` — המלאי של המכבסה נכנס לכביסה של הלקוח

**הבאג, כפי שדווח על ידי אבי עם צילומי מסך (09.08.2026) ואומת על הקוד החי:**
בכרטיס הזמנת כביסה הכפתור **"+ הוסף פריט"** פתח את `addLineForm` — בורר
מקטלוג המלאי של MAPA, עם `checkAvail` שקורא ל-`availability`. כלומר: לקוח
שמביא וילונות משלו נאלץ לבחור פריט מהמלאי של המכבסה.

⛔ **הכרעת אבי: המלאי הוא של המכבסה.** כביסה של הלקוח אינה רכוש שלנו
ואינה אמורה להופיע בספירת המלאי.

**מה נבנה:** הבורר **הוסר לחלוטין** מהזמנת כביסה. במקומו — שורת **תיאור
חופשי, רשות, עד 20 תווים** ("חלוקים" / "וילונות" / "מעורב"), שנכנסת
ל-`orders.notes` הקיים. ⛔ **בלי שינוי סכימה.**

**מפת החיבורים (R2), כפי שאומתה ב-`grep` על הקוד החי:**
· **איפה נשמר** — `orders.notes`, שדה קיים. ⛔ אינו ב-`LONG_TEXT_FIELDS`,
  ולכן אין בעיית `ensureFullTexts` (שמירה אינה דורסת מקור קצוץ).
· **מי כותב** — `w17SaveDesc` בממשק דרך `update` הגנרי; ו-`newOrderForm`
  הקיים, ששדה ההערות שלו לא נגע.
· **מי קורא** — כותרת כרטיס ההזמנה (`openOrder`) והמקטע החדש.
· **לאן זורם — כסף:** ⛔ **לשום מקום.** אומת: `b54RawOrderTotalAg` **מדלגת**
  על שורות הזמנה כשהסוג `'כביסה'`, ו-`reservedQty`/`b48CommittedQty` סופרות
  **רק** הזמנות `'השכרה'`. כלומר שורות כביסה מעולם לא ייצרו חיוב ומעולם לא
  שריינו מלאי — ולכן ההסרה **אינה מזיזה שקל ואינה מזיזה יחידה אחת במלאי**.
  אין חיוב · אין ספר חיובים · אין חשבונית · אין הוצאה · אין תזרים · אין
  מסגרת אשראי · אין מע"מ. יש בדיקה לכל אחד.
· **מצבי קצה** — שורות היסטוריות שנוצרו לפני האצווה **נשארות ומוצגות
  לצפייה בלבד**, תחת כותרת שאומרת במפורש שהן אינן נספרות בחיוב ואינן
  משריינות מלאי. אין דרך להוסיף חדשות.
· **מי רואה** — כל התפקידים; החסימה בשרת חלה על **כל** הקשרי הריצה.
  ⚠ אומת שהפורטל והחנות יוצרים תמיד `type:'השכרה'`, ולכן נקודת הכתיבה
  היחידה שנדרשה לשומר היא `case 'create'`.

**שני שומרים, לא אחד:** הכפתור אינו מוצג בכרטיס כביסה; `addLineForm` דוחה
קריאה ישירה; ובשרת — `create` על `order_lines` נדחה כשהסוג כביסה,
דרך `sPick(po.type, ORDER_TYPES)` כדי שסוג מלוכלך לא יעקוף (B64a).

⛔ **הזמנת השכרה לא נגעה כלל** — אותו בורר, אותה `checkAvail`, אותה טבלה,
אותו `linePrice`. יש בדיקת רגרסיה מפורשת.

⭐ **סופח:** הודעת השמירה של הזמנת כביסה עברה מ"אפשר להוסיף אומדן כמותי"
ל"אפשר להוסיף תיאור קצר" — הניסוח של B66 הפנה למנגנון שהוסר.

#### `WASH-19` — בחירת עגלה הייתה הקלדה בלבד

**אומת:** `floorAddCart` קראה `el('fl_icart').value` — שדה טקסט אחד, בלי
שום רשימה. עובד שאינו זוכר את מספר העגלה ואין לו ברקוד ביד היה תקוע.

**מה נבנה:** רשימת **עגלות פנויות** לצד שדה הסריקה.
⛔ **הסריקה לא הוסרה** — היא המסלול המהיר ברצפה: שדה הברקוד, `Enter`,
כפתור המצלמה וכפתור "הוסף עגלה" כולם במקומם, ויש בדיקה על כל אחד.
⛔ **עגלה וירטואלית מוסתרת** (`b49bIsVirtual`), כמו בכל מסכי הציוד.

**הכרעה — אין דפוס שני (R8):** הבחירה מהרשימה **מזינה את שדה הסריקה
וקוראת ל-`floorAddCart`**. כלומר עגלה שנבחרה עוברת בדיוק את אותה בדיקת
`nobleCartInfo` בשרת שעוברת עגלה שנסרקה — אותה דחייה, אותה הודעה.
המסנן ברשימה (`status === 'פנויה'` דרך `sVal`) הוא **בדיוק** התנאי
ש-`nobleIntake` אוכפת בשרת — לא הועתק, אלא נקרא ממנה.

#### `WASH-14` — `b40SplitLaundry` העתיקה שלב גולמי לילדה

**הבאג של B64a התרבה בכל פיצול:** השלב הועתק כמחרוזת ישירות מהגיליון,
בלי `sPick`. משימה שנולדה מרווח קשיח או סימן כיוון **נעלמה מלוח הכביסה
לגמרי** — היא לא התאימה לאף עמודה.

**מה נבנה:** הילדה נולדת עם הערך הקנוני מ-`STAGES`. ⛔ שלב שאינו ברשימה
בכלל עדיין עובר ניקוי `sVal` ולא נמחק — מוטב ערך נקי מאשר מחרוזת ריקה
שמאבדת את המשימה.

**⭐ סופח (ממצא B69):** `rLaundry` השוותה `t.stage===st` גולמי — כלומר
**גם משימה קיימת** עם שלב מלוכלך לא הופיעה בלוח. תוקן דרך `sPick`, וכן
ארבע השוואות `stage` נוספות בממשק (`b40CartMates`, שתיים ב-`openLaundryTask`,
ואחת ב-`b40ItemBreakdown`).

#### מה נבדק

**420 בדיקות, 0 כשלים — 25 חדשות** (`t18-b72-wash-srv` · `t18-b72-wash-ui`).
בהן: אין שום דרך להוסיף פריט להזמנת כביסה, כולל בסוג מלוכלך בארבע צורות ·
ההשכרה מקבלת שורות כמו תמיד · הזמנת כביסה מאושרת בלי אף שורה (רגרסיה B66) ·
`b54RawOrderTotalAg` לא זזה על שורות היסטוריות · `availableQty`/`reservedQty`
לא זזו · R6 בשלושת המקורות · התיאור נחתך ב-20 תווים ומנוקה מסימני כיוון ·
רשימת העגלות מציגה פנויות בלבד ומסתירה וירטואליות · הסריקה במקומה ·
הבחירה מגיעה דרך שדה הסריקה (R7, אירוע `change` אמיתי) · הילדה בפיצול
נולדת קנונית בארבע צורות לכלוך · הפיצול עצמו לא נשבר · אין שינוי סכימה ·
B71 לא נשבר.

⛔ **ארבע בדיקות B66 עודכנו ולא נמחקו.** הן טענו לניסוח "אומדן כמותי"
ולקיום הכפתור — בדיוק מה ש-WASH-17 מחליף. הכוונה של B66 (אפשר לאשר הזמנת
כביסה בלי שורות) **נשמרה במלואה** ונבדקת גם בשרת.

**מה לא נבדק:** נתוני אמת. הבדיקות רצות על מסד מדומה.

**מה לא נגע בכוונה:**
· ⛔ `WASH-20` — `b54RawOrderTotalAg` עם השוואת סוג גולמית. **נקודת כסף,
  ו-B72 הוגדר במפורש כאצווה ללא כסף.** נעצר לפי R3 מסיבת *סיכון אמיתי
  לשבירה*, ונרשם ב-`TASK_QUEUE.md` לאצווה נפרדת.
· ⛔ `WASH-18` — לוח הכביסה וקליטת NOBLE. אצווה נפרדת (B73), דורשת R2.
· ⛔ מסלול הטקסטיל של MAPA שחוזר מהשכרה — `receiveReturn` יוצרת
  `laundry_tasks` עם `item_id`, **וזה נכון**: שם הפריט והמלאי הם העיקר.
· ⛔ `toAg` · `nRound2` · `laundry_events` (append-only) · `IDLE_LIMIT_MS`.

---

### ⭐⭐ B71 — WASH-10: כסף הכביסה באגורות שלמות (09.08.2026)

**canary: `v4.71-B71` · 395 בדיקות · 0 כשלים · ✅ אין שינוי סכימה**

**הבאג, מאומת ונמדד על הקוד החי (R11).** כל מסלול הכסף של הכביסה עבר דרך:

```
function nRound2(n) { return Math.round(Number(n) * 100) / 100; }
```

זו אינה עיגול עשרוני. כשהתוצאה נופלת בדיוק על חצי אגורה, הייצוג הבינארי
של המספר נמצא **מעט מתחת** לערך העשרוני, ולכן `Math.round` יורד במקום
לעלות. הדוגמה שנבדקה במסלול האמיתי:

```
0.59 × 8.5 = 5.015
5.015 * 100 = 501.49999999999994
Math.round(501.49999999999994) = 501  →  5.01
```

**המדידה** (סריקת טווח מלא של משקלים 1.000–20.000 ק"ג מול שישה מחירונים,
ושל 200,000 סכומי מע"מ):

| מה | שיעור הסטייה | כיוון |
|---|---|---|
| חיוב שקילה | **1.9%** מהשקילות | תמיד −1 אגורה, לרעת העסק |
| מע"מ בחשבונית | **0.4%** מהסכומים | תמיד −1 אגורה |
| סכימת `nobleWeighRollup` | **0 אגורות** ב-2,000 ניסויים × 100 שקילות | אין הפסד בפועל — חשבון נכון-במקרה |

⚠ **הסכימה ב-Rollup לא הפסידה כסף בפועל** — נמדד ולא שוער. היא תוקנה
משום שסכימת שברים עשרוניים אינה עומדת בכלל האגורות השלמות של המערכת,
ולא משום שהיא דלפה.

**ספירת נקודות העיגול בפועל** (הניסוח במסמך אמר "ארבע פעמים" — נספר ואומת):
ארבעה שלבים, **שמונה** קריאות ל-`nRound2` —
`nobleWeigh` (נטו · חיוב) · `nobleWeighFix` (נטו · חיוב) ·
`nobleWeighRollup` (נטו · חיוב) · `nobleCreateInvoice` (סכום · מע"מ · סה"כ).

**מה נבנה.** שלושה עוזרים חדשים בקוד השרת, וכל חשבון הכסף במסלול עבר אליהם:

```
w10Cent(v)         — המרה בטוחה למאיות, בלי שגיאת השבר הבינארי
w10MulAg(a, b)     — מכפלת שני ערכים עשרוניים → אגורות שלמות
w10PctAg(ag, rate) — שיעור (מע"מ) מסכום שכבר באגורות → אגורות שלמות
fromAg(ag)         — אגורות → שקלים. זהה תו-בתו לזה שבממשק
```

ההמרה חזרה לשקלים נעשית **פעם אחת בלבד**, בזמן הכתיבה.

**ארבעת השלבים שתוקנו:**

| פונקציה | לפני | אחרי |
|---|---|---|
| `nobleWeigh` | `nRound2(net * price)` | `fromAg(w10MulAg(net, price))` |
| `nobleWeighFix` | `nRound2(net * price)` | `fromAg(w10MulAg(net, price))` |
| `nobleWeighRollup` | סכימת שברים ואז `nRound2` | צבירה במאיות ובאגורות שלמות |
| `nobleCreateInvoice` | `nRound2` על סכום · מע"מ · סה"כ | סכום ומע"מ באגורות; **הסה"כ הוא חיבור מדויק** ולא עיגול שלישי |

**החלטות שהתקבלו (אבי, 09.08.2026 — שאלה מרוכזת אחת לפני הבנייה):**

1. **חיובים היסטוריים בגיליון נשארים בדיוק כפי שהם.** התיקון חל על
   שקילות ועל חשבוניות מכאן והלאה בלבד. הנימוק: `laundry_events` הוא
   append-only, יש חשבוניות שכבר יצאו ללקוחות, והפער הוא אגורה בודדת.
2. **⚠ אושר מפורשות שהסכומים שמוצגים ללקוח משתנים** (מלכודת 4 · כלל B45):
   כ-2% מהשקילות החדשות יהיו **גבוהות באגורה אחת**, וכ-0.4% מהחשבוניות
   גבוהות באגורה אחת במע"מ. זהו התיקון עצמו, לא תופעת לוואי.
3. **משקל אינו כסף.** `nRound2(gross - tare)` **לא נגע**. שינוי שלו היה
   מזיז משקלים מוצגים ב-10 גרם, ואין לו נגיעה לנטישת האגורות.

**מה נבדק — חלק `t17-b71-wash10-srv`, 17 בדיקות חדשות:**

- ⭐⭐ **הסטייה עצמה**, לפני ואחרי: `nRound2(0.59*8.5)` הוא 5.01, ו-`nobleWeigh`
  על אותם נתונים מחזיר עכשיו 5.02. הבדיקה נכשלת אם הסטייה נעלמת מקו הבסיס.
- ⭐ **100 שקילות קטנות** דרך `nobleWeigh` — הסכום זהה לחישוב עצמאי באגורות
  שלמות, וזהה גם לסכימה ישירה מהיומן.
- `intake.total_charge` שווה לסכום שספר החיובים (`b54Ledger`) מכיר בו.
- **R6** — `b54Ledger`, `b48BalancesAg` ו-`b2CreditUsedAg` מחזירים אותו מספר,
  על חשבונית שנוצרה דרך המסלול האמיתי.
- החשבונית: מע"מ באגורות שלמות (1.25 → 0.23 ולא 0.22), והסה"כ הוא
  **חיבור מדויק** של הנטו והמע"מ.
- ⛔ כביסה פנימית נשארת בחיוב 0 בכל מסלול — שקילה, מחזור שני, סיכום,
  `total_charge` וספר החיובים.
- ⛔ שתי שקילות ויותר לאותה עגלה — **צבירת B70 לא נשברה** (₪500 על שתיים).
- ⛔ תיקון שקילה על אותו משקל — אותו סכום בדיוק; ביטול שקילה מסיר בדיוק
  את האגורות שלה ולא יותר.
- שומרי מקור: אין `writeTable('laundry_events')` · `lastByCart` לא חזר ·
  ארבע נקודות ה-`nRound2` הכספיות לא חזרו · **`toAg` לא נגע** · שתי
  הטבלאות לא זזו · לא נוספה טענה ל-`b61Tests`.

**מה לא נבדק:** נתוני אמת. הבדיקות רצות על מסד מדומה. ⚠ נדרשת שקילה
אחת בייצור ואישור שהסכום על המסך שווה בדיוק לנטו × מחיר.

**מה לא נגע בכוונה:** `nRound2` נשארה בקוד ומשרתת קבלות, תשלומים ומשקלים
(שורות 2796 · 2808 · 3635 · 7100 · 7532 · 7548 · 7756 · 8328). אלה אינם
מסלול הכביסה, והם מחוץ להיקף `WASH-10` — ⛔ **וגם מוגנים ב-R4** (מנוע
התשלום). מי שירצה להעביר גם אותם לאגורות שלמות — זו אצווה בפני עצמה.

---

### ⭐⭐ B70 — WASH-03: עגלה שחוזרת לאותה קליטה שומרת את החיוב (08.08.2026)

**canary: `v4.70-B70` · 378 בדיקות · 0 כשלים · ⛔⛔ שינוי סכימה — יש להריץ `setupDatabase`**

**הבאג, מאומת על הקוד החי (R11).** `nobleWeighRollup` נבנתה כך:

```
lastByCart[e.cart_id] = e
```

כלומר מכל עגלה נלקחה **השקילה האחרונה בלבד**, וכל מה שקדם לה נמחק
בשקט מהחיוב. ההנחה שמאחורי השורה — "שקילה חוזרת היא תיקון" — אינה
נכונה במכבסה אמיתית.

**המציאות, בלשון אבי:** עגלה שנשקלה ולא "נוסעת" עם הכביסה הנקייה
ללקוח יכולה לקלוט שוב כביסה **מאותה הזמנה**. הזמנה 001, עגלה 001:
כיבסה ב-10:00, שוב ב-12:00, שוב ב-15:00. שלושה מחזורים אמיתיים,
שלושה חיובים אמיתיים.

**המסלול היה פתוח לרווחה:** `nobleStageStart` ו-`nobleWeigh` חוסמות רק
כשהקליטה כבר "מוכן / במשלוח / נמסר". כלומר שקילה שנייה באותה קליטה
תמיד התקבלה — ותמיד מחקה את הקודמת.

**מה נבנה**

| מה | איך |
|---|---|
| **הסיכום מצטבר** | `nobleWeighRollup` סוכמת את **כל** השקילות התקפות. אין מפתח "עגלה" שדורס |
| **מספור לתצוגה** | `b70Weighs` מחזירה לכל שקילה `seq` — 01 · 02 · 03. **נגזר מהיומן, לא נשמר** |
| **תיקון = שורה חדשה** | `nobleWeighFix`: מנהל בלבד (`b38VerifyManagerPin`), **סיבה חובה**, נרשמת שורה עם `ref_id` לשקילה שתוקנה. ⛔ `laundry_events` נשאר append-only — השורה המקורית לא נגעה |
| **ביטול שקילה** | אותו מסלול בדיוק, עם `cancel` — יוצאת מהחיוב, נשארת גלויה ביומן ומסומנת "בוטלה" |
| **שרשרת תיקונים** | תיקון של תיקון — האחרון קובע. עד 50 חוליות, בלי לולאה אינסופית |
| ⛔ **הדלת האחורית** | `nobleMarkReady` שאלה "האם העגלה נשקלה **אי פעם**". עגלה שנשקלה בבוקר, סיימה מחזור נוסף אחר הצהריים וטרם נשקלה עליו — עברה. `b70CartNeedsWeigh` סוגרת: **סיום מחזור אחרי השקילה האחרונה = טרם נשקלה** |
| **מסך הרצפה** | טבלת שקילות עם השעה, המספר, המשקל והחיוב. כפתור "תיקון" לכל שורה. אישור לפני שקילה נוספת ("זו שקילה נוספת שתתווסף לחיוב") |

**⛔ מה לא נבנה, במכוון**

- **אין מנגנון "סיבובים" נגזר.** נשקל ונדחה: היומן כבר מחזיק את כל
  המידע, ומנגנון שמנחש "תיקון או מחזור" הוא בדיוק סוג הטלאי שיישבר.
- **אין אבחון לנתונים היסטוריים.** הכרעת אבי: הנתונים הקיימים בגיליון
  הם בדיקה בלבד.
- **אין חסימה של שקילה חוזרת.** היא לגיטימית — היא מחזור נוסף.

**המחיר מוקפא באירוע השקילה.** תיקון משתמש ב-`price_per_kg` של השורה
המקורית ואינו מחיל מחירון חדש. כביסה פנימית נשארת בחיוב 0 בכל מסלול,
כולל תיקון — יש בדיקה.

**הוכחת הבאג לפני התיקון (R11).** אותו תרחיש הורץ מול קובץ השרת שהיה
בייצור ומול החדש: לפני — ₪100. אחרי — ₪600.

**הבדיקות:** `t16-b70-wash03-srv` (20) · `t16b-b70-wash03-ui` (7).
כולל R6 (שלושת מקורות היתרה), append-only, ושומר שנכשל אם `lastByCart`
יחזור לקוד.

---

### ⭐ B69 — WASH-04: מודול NOBLE עובר את כלל B64a (08.08.2026)

**canary: `v4.69-B69` · 347 בדיקות · 0 כשלים · ⛔ אין שינוי סכימה — אין להריץ `setupDatabase`**

**⚠⚠ הספירה במסמך הביקורת הייתה שגויה — וזה הדבר הראשון שנבדק (R9/R11).**
הביקורת מנתה **~20 אתרים**. `grep` על הקוד החי מצא **91**:

| איפה | אתרים |
|---|---|
| `קוד שרת.txt` — NC1/NC2/NC3 (מודול NOBLE עצמו) | 47 |
| `קוד שרת.txt` — הגשרים: `b49c` · `b49d` · `b54Ledger` · יתרת ההזמנה | 11 |
| `index.html` — רצפה · דוחות · ציוד · ספר חיובים · חשבונית מרוכזת | 33 |
| **סה"כ** | **91** |

**⛔ אתרי הכסף — לא שלושה. שבעה.** כל אחד מהם אומת בנפרד: הורצה בדיקה
מול הקוד **הישן** שהוכיחה שהוא נכשל, ומול החדש שהוכיחה שהוא עובר.

| # | איפה | מה קרה בייצור עם ערך מלוכלך |
|---|---|---|
| 1 | `nobleWeighRollup` | `event_type` מלוכלך → השקילה לא נספרת → **`total_charge` יוצא אפס** |
| 2 | `nobleWeigh` | `internal` מלוכלך → כביסה פנימית מחויבת, או השקילה נחסמת לגמרי |
| 3 | `nobleCartInfo` | `price_per_kg` שגוי במסך הרצפה |
| 4 | `nobleCreateInvoice` | `status`/`internal` מלוכלך → הקליטה **לא נכנסת לחשבונית לעולם** |
| 5 | `b54Ledger` (שרת) | `ik.internal` → כביסה פנימית נכנסת לספר החיובים |
| 6 | `b54Ledger` (שרת) | `ik.status` → החיוב **לא נכנס לספר החיובים** |
| 7 | `b54LedgerFE` + `nobleInvoice` (ממשק) | אותו הדבר בצד הלקוח |

**החלטות שהתקבלו ולמה:**

1. **`sVal`/`sPick` ישירות, בלי שכבת עוזרים חדשה.** נשקל להוסיף `nIsYes()`
   ו-`nBindActive()`, ונדחה: זה היה **דפוס שני** לאותה בעיה (R8). B64a כבר
   קבע את הדפוס, וכל אתר חדש נראה עכשיו בדיוק כמו אתרי B64a וניתן ל-`grep`.
2. **`sPick` רק במקום אחד — `nobleStageStart`.** שם צריך את הערך **הקנוני**,
   כי הוא נכתב ליומן האירועים ולסטטוס הקליטה. בכל שאר המקומות ההשוואה היא
   שאלת השתייכות בלבד, ולכן `sVal` מספיק ואינו יוצר מצב `''` שצריך לטפל בו.
3. **⛔ `stage` שנקרא מ-`auto_stage` הפסיק להיכתב כמות שהוא.** עד היום
   `String(mach.auto_stage||'').trim()` נכתב לתוך `laundry_events.stage`
   ולתוך `laundry_intakes.status`. כלומר **הלכלוך התרבה** מהמכונה אל
   הקליטה ואל היומן. מעכשיו נכתב הקבוע מ-`NOBLE_MACHINE_STAGES`.
   ⚠ זו אינה "כתיבת ערך מנורמל לגיליון" — זו כתיבת הקבוע שלנו במקום הד
   של ערך מלוכלך. השורה בגיליון של המכונה עצמה **לא נגעה**, ויש בדיקה.
4. **גבול ההיקף: חמש טבלאות.** `laundry_intakes` · `laundry_events` ·
   `intake_carts` · `carts` · `machines`. `laundry_tasks` (לוח הכביסה של
   MAPA) נכלל **רק** במקומות שבהם פונקציית NOBLE קוראת אותו כדי להחליט אם
   לשחרר עגלה. שאר `laundry_tasks` הוא ממצא נפרד — ראה למטה.

**מה נבדק — 24 בדיקות חדשות (323 ← 347):**
· כל אתר עם שלוש צורות לכלוך: רווח נגרר · `\u00A0` · `\u200F`, מול קו בסיס נקי
· שבעת אתרי הכסף — **החיוב נוצר, בסכום הנכון**, ולא רק שההשוואה מנורמלת
· הצינור המלא מקצה לקצה: קליטה ← שלב ← סיום ← שקילה ← מוכן ← העמסה ← מסירה,
  כשכל שדה בגיליון מלוכלך מחדש **בין כל שני שלבים**
· ⛔ **שום ערך מנורמל לא נכתב חזרה לגיליון** — נבדק מפורשות על שישה שדות
· `sPick` שמחזיר `''` נדחה במפורש, ולא נרשם אירוע ביומן
· R6 — ספר החיובים, `b48BalancesAg` ו-`b2CreditUsedAg` מחזירים אותו מספר
· R4 — `B56_CLOSED_INTAKE` · `b56IntakeOpen` · `STAGES` · `B67_WRITE_DEFER`
  (חוזר ל-`null`) · `b68PayrollFor` — כולם ללא שינוי

**מה לא נבדק:**
· נתוני אמת. הבדיקות מוכיחות שהלוגיקה עמידה — לא כמה שורות מלוכלכות יש
  בגיליון בפועל. ⚠ **הדבר הזה נמדד רק ברצפה אצל אבי.**
· שכבה 2 לא נגעה בכוונה — WASH-04 אינו נוגע ביכולת דפדפן.

**⚠ שני ממצאים נפרדים — דווחו ולא תוקנו (מלכודת 4):**
1. **מזהי מכונה אינם מנורמלים** ב-`nobleOpenStarts`: `machine_id` עם רווח
   בצד אחד שובר את זיווג התחלה↔סיום, והמנה נשארת "פתוחה" לנצח. זו מחלקת
   באגים אחרת — **מזהה**, לא רשימה קבועה. → `B72`.
2. **`rLaundry` — `t.stage===st` גולמי**: משימה עם רווח נעלמת מכל עמודות
   לוח הכביסה. זה מודול MAPA ונוגע ל-`WASH-14`. → `B70`.

---

### ⭐ B68 — ניקוי שכר: כפילויות עובד+חודש · מחיקה · יתרת חובה (08.08.2026)

**canary: `v4.68-B68` · 323 בדיקות · 0 כשלים · ⛔ אין שינוי סכימה — אין להריץ `setupDatabase`**

**הבעיה שאבי הביא:** שאריות של רישומי שכר חלקיים ושבורים, ואי אפשר היה
למחוק אותם. אומת בקוד החי: **לא הייתה שום פעולת מחיקה לשכר**, וטבלת
`payroll` מעולם לא נאכפה לייחודיות עובד+חודש. שורה כפולה נספרה **פעמיים**
ב-`b67EmployeeOpenRows` ולכן ניפחה את חוב השכר של העובד. באג כסף.

**הכרעות אבי:**
1. שורה עם תשלום בפועל **אינה נמחקת לעולם** — רק ממוזגת. הכסף כבר יצא
   מהקופה ונרשם ב-`expenses`; מחיקה הייתה משאירה הוצאה בלי מקור.
2. מיזוג שיוצר תשלום עודף **מתבצע**, והעודף הופך ל**יתרת חובה של העובד**.
3. כפילות **אינה חוסמת** הפקת שכר — אזהרה בולטת בלבד.

**מה נבנה:**
- `b68PayrollFor` — נקודת אמת יחידה לצירוף עובד+חודש, עם `sVal` (B64a).
  ⛔ החליפה את ה-`find` הגולמי גם ב-`buildPayrollForMonth` וגם ב-`voidAttendance`.
- `b68DupGroups` · `b68DupScan` — אבחון קריאה בלבד (R1), מנהל בלבד.
- `b68MergePayroll` — מעביר תוספות ותשלומים לשורה הנשמרת, מחשב `total`
  מחדש, מעדכן סטטוס, מסנכרן הוצאה עתידית, ומוחק את המיותרת.
- `b68EmployeeOverpay` — **חישוב, לא שדה.** אין עמודה חדשה.
- `b68OffsetOverpay` — קיזוז ידני כניכוי `קיזוז יתרת חובה` דרך
  `payroll_adjustments` הקיימת. ⛔ אין קיזוז אוטומטי.
- מחיקה: **הורחבה פעולת `delete` הקיימת** עם חסם ל-`payroll` (R8 — לא
  נבנה דפוס מחיקה שני). דורשת מספר אישי של מנהל בשרת + סיבה, מנקה
  תוספות, ומסמנת את ההוצאה העתידית כמחוקה (מחיקה רכה, דפוס B28).
- ממשק: אזהרה אדומה במסך השכר · תווית "כפולה" על השורה עצמה · חלון מיזוג ·
  כפתור מחיקה בכרטיס השכר (רק כשלא שולם) · יתרת חובה בכרטיס העובד.

**⚠ ממתין לאימות אבי:** מיזוג הכפילויות של "אברהם" בייצור, ואימות
שהחוב שלו ירד לסכום האמיתי.

---

### ⭐ B67 — `BLD-16`: חוב שכר ברמת העובד + טיוטות שטרם אושרו (03.08.2026)

**canary: `v4.67-B67` · 300 בדיקות · 0 כשלים · ⛔ אין שינוי סכימה — אין להריץ `setupDatabase`**

⚠ **שני הקבצים משתנים.** `index.html` **ו**-`קוד שרת.txt`.

**הבקשה, בלשון אבי (02.08.2026):** *"תשלום יחול על כל החוב של החברה כלפי
העובד. תשלום יכסה את החוב המוקדם ביותר."* ובנוסף: *"תצוגה נוספת של הוצאות
צפויות כולל טיוטות משכורות שעוד לא אושרו, וגם בתצוגת שכר תצוגת סיכום של
טיוטות שכר שעוד לא אושרו."*

#### מה נבנה — השרת

- ⛔ **גוף `case 'payPayroll'` הוצא ל-`b67PayOneRow`.** ה-`case` הישן קורא
  לה ומתנהג בדיוק כמו קודם — יש שתי בדיקות רגרסיה שמוכיחות זאת, כולל
  השוואת ארבע הודעות הדחייה מילה במילה. **בלי ההוצאה הזו הייתה נוצרת
  לוגיקת תשלום שנייה,** וזה בדיוק מה שאסור.
- **פעולה חדשה: `b67PayEmployee`.** ⛔ **ההקצאה כולה בשרת.** הממשק שולח
  סכום אחד ובקשה אחת. פיצול בממשק אינו אטומי — בקשה שנופלת באמצע משאירה
  כסף חצי-רשום.
- סדר ההקצאה: `month` עולה. הישן קודם. רק רשומות שאינן טיוטה ועם יתרה
  חיובית. לכל רשומה `min(יתרה, מה שנשאר)`.
- ⛔ **מנהל בלבד** (`MANAGER_ONLY`) · ⛔ **אינה ב-`READ_ONLY_ACTIONS`** —
  היא כותבת. יש בדיקה שמוודאת את שניהם.
- `auditNote` אחד שמפרט את כל החודשים והסכומים ואת הטיוטות שדולגו.

#### ארבע המלכודות — כל אחת היא באג כספי

1. **רשומת טיוטה ישנה יותר.** אם יולי בטיוטה ואוגוסט מאושר, התשלום נכנס
   לאוגוסט — הפוך מכוונת אבי. ⛔ אסור לעקוף אישור ואסור לדלג בשקט.
   הפעולה מחזירה `skipped_drafts` ו-`warning`, והממשק מציג את האזהרה
   **בסיכום שאחרי התשלום**.
2. **בדיקת המזומן פעם אחת מול הסכום המלא**, לפני ההקצאה. יש בדיקה
   שמוכיחה שתשלום 8,000 מקופה של 8,000 עובר על שתי שורות — כלומר הבדיקה
   אינה פר-שורה ואינה מרוקנת את הקופה באמצע.
3. **R10 — כל טבלה נכתבת פעם אחת.** ראה "ההכרעה הארכיטקטונית" למטה.
4. **סכום מעל סך החוב נדחה,** וההודעה אומרת מה הסך. ⛔ בלי זיכוי ובלי עודף.

#### ⭐ שתי הכרעות ארכיטקטוניות שהתקבלו בסשן

**1. דגל צבירת כתיבות ב-`writeTable`.** הבעיה: `syncFutureExpenseForPayroll`
כותבת לגיליון **בכל קריאה**, ואסור לגעת בה. תשלום שנפרס על שלושה חודשים
היה מייצר תשע נסיעות לגיליון (~215 מ״ש כל אחת) — הפרה ישירה של R10.
הפתרון: `B67_WRITE_DEFER`. כשהדגל דלוק — `writeTable` צוברת בזיכרון,
ו-`b67FlushWrites` כותבת כל טבלה **פעם אחת** בסוף.
⛔ הדגל נדלק אך ורק בתוך `b67PayEmployee`, בתוך `try/finally`.
⭐ **תופעת לוואי שהיא הרווח האמיתי: אטומיות.** כשל באמצע זורק את הבאפר
ואף שורה לא הגיעה לגיליון. יש בדיקה שמפילה את השורה השנייה ומוודאת
ש-`ensureSheet` לא נקראה אפילו פעם אחת.

**2. ההקצאה מחושבת באגורות שלמות בזיכרון** ומומרת בחזרה לשברים בכתיבה.
זו הדרך היחידה להבטיח שסכום ההקצאות שווה **בדיוק** לסכום ששולם, בלי סחיפת
עיגול. ⛔ **האחסון לא השתנה** — הכסף בשכר נשאר בשברים; המעבר לאגורות הוא
אצווה נפרדת (`WASH-10`/`B70`), ותיקון חלקי היה מפצל את החשבון.

#### מה נבנה — הממשק

**חלק 1 — חוב ותשלום.** לחיצה על **שם העובד** בטבלת השכר פותחת תצוגת עובד:
בראש **מספר אחד** (סך החוב הפתוח), שדה סכום עם ברירת מחדל של מלוא החוב,
וכפתור. מתחת — כל חודשי העובד: סה"כ · שולם · יתרה.

⛔⛔ **הכרעת אבי המפורשת: השיוך לחודשים שקוף.** אין מסך אישור שמפרט חודשים
**לפני** התשלום, ואין כפתור תשלום נפרד בכל שורה בטבלה. הפירוט נראה מעצמו
**אחרי**, ברשימה שממילא פתוחה. יש שתי בדיקות שנכשלות אם מישהו יוסיף אותם.

⚠ `payrollTable`/`payrollRowsFor` בנויות לפי חודש נבחר בלבד, ולכן זה
**המקום היחיד במערכת שמראה עובד לאורך זמן**. ⛔ נבנה מ-`DB` שכבר בזיכרון —
אפס בקשות שרת נוספות. ⛔ לחיצה על שורת חודש → `payrollDetail` הקיים.
⛔ טופס התשלום לרשומה בודדת שבתוך `payrollDetail` **נשאר במקומו** (R8).

**חלק 2 — טיוטות.** ⛔⛔ **טיוטות אינן נכתבות ל-`futureExpenses`.** הן
נגזרות לתצוגה בלבד מ-`DB.payroll` עם `status==='טיוטה'`. הרגע שטיוטה נכנסת
לטבלה היא נספרת בתחזית התזרים כהתחייבות אמיתית — וזו טעות.
`syncFutureExpenseForPayroll` לא נגעה, והתנאי שמסנן טיוטות ב-`addPayrollAdjustment`
לא הוסר. יש שלוש בדיקות שמוודאות שאין רשומת `future_expenses` לטיוטה.

- **במסך השכר** — כרטיס "טיוטות שכר שטרם אושרו: X ₪". לחיצה → רשימה →
  לחיצה על שורה → `payrollDetail` של אותה רשומה.
- **בהוצאות הצפויות** — אותו סכום, **בנפרד ולידו**. התווית:
  **"טיוטות שכר — טרם אושר, אינו נכלל בסך"**. ⛔ `finPayrollTotal`
  ו-`fexpOpen` **לא שינו את ערכם** — יש בדיקה שמוודאת ששניהם מחזירים אפס
  בזמן שסיכום הטיוטות מציג 8,000.

#### הרשאות

⛔ `ROLE==='נהג'` — אין כפתור תשלום, אין שדה סכום, אין סיכום טיוטות, ושם
העובד אינו לחיץ. יש שתי בדיקות. ⛔ `ROLE==='משרד'` חסום ממסך שכר לגמרי —
לא נגענו. ⛔ `DRIVER_TAB_ORDER` לא השתנה.

#### מה נבדק · מה לא

**נבדק:** 34 בדיקות חדשות (20 שרת · 14 ממשק). כולל הדוגמה של אבי מילה
במילה, גלישה בין חודשים, תשלום מדויק, דחיית סכום מעל החוב, דילוג על טיוטה
עם אזהרה, אפס סחיפת עיגול על סכומים כמו 7,777.77, מזומן מעל הקופה,
אטומיות בכשל באמצע, R10, והתנהגות זהה של `payPayroll` הישן.
**הבסיס הורץ לפני כל נגיעה בקוד והחזיר 266/0.**

**לא נבדק (R11):** ⚠ **התנהגות מול גיליון אמיתי.** הרתמה מדמה את
`SpreadsheetApp`; מה שנבדק הוא ש-`ensureSheet` נקראה בדיוק פעם אחת לכל
טבלה — לא שהכתיבה עצמה הצליחה. ⚠ **זמן הריצה בפועל** של תשלום שנפרס על
שלושה חודשים לא נמדד אצל אבי.

⚠ **שכבה 2 לא נגעה בכוונה.** `BLD-16` אינו נוגע בשום יכולת דפדפן, ולכן לא
נוספה טענה ל-`b61Tests()`. יש בדיקה שנכשלת אם מישהו יוסיף.

---
### B66 — `BLD-15` + `BLD-03` + תיקון באג ייצור בסגירת הזמנת כביסה (02.08.2026)

**canary: `v4.66-B66` · 266 בדיקות · 0 כשלים · ⛔ אין שינוי סכימה — אין להריץ `setupDatabase`**

⚠ **ההיקף שונה מהמתוכנן.** התוכנית הייתה `BLD-05` + `BLD-03` + `BLD-15`.
🔴 **`BLD-05` ירד בפעם החמישית** — אבי ביקש לדלג על הבדיקה העצמית. הכלי
(`b65WidthSummary`) מוכן ועובד; חסר רק שאבי יפתח את ששת המסכים הרחבים
ויריץ. **הפריט עובר ל-B67 בלי שינוי.**

⚠ **הסשן נפתח בעצירה שנייה, מוצדקת:** קובץ השרת בריפו היה **ללא B65** —
`bld01Heal` · `bld01Diagnose` · `bld01Candidates` הופיעו 0 פעמים, בזמן
שהממשק קרא להן. אבי העלה מחדש ופרס, ורק אז התחילה הבנייה. ✅ אומת
ב-`grep` לפני ואחרי. **לקח: `PROGRESS.md` עצמו סימן את זה בשורה 4
("נגע — יש להדביק מחדש") ואיש לא קרא אותה. הבדיקה בפתיחת הסשן היא
שתפסה, לא המסמך.**

⭐ **באמצע הסשן אבי צירף מסמך ממצאים מצ'אט ביקורת מקביל** (14 ממצאים על
מסלול הכביסה). ממצא #2 אומת עצמאית מול הקוד החי ונכנס להיקף בהכרעת אבי.
השאר נרשם ל-`TASK_QUEUE.md` — ⛔ **לא נבנה מהמסמך בלי הוראה מפורשת.**

---

#### פריט 1 — `BLD-15`: שורות בהזמנת כביסה הן אומדן ולא חובה

**מה היה שבור (✅ אומת ב-`grep` על הקוד החי, שורה 4771):** `approveOrder`
חסמה **כל** הזמנה בלי `orderLines`, בלי חריג לכביסה. זה סתר את C5 בספר
החיובים, שמצהיר מפורשות ששורות הזמנת כביסה הן "אומדן כמותי, אינן כסף
ואינן נספרות" — כלומר המערכת דרשה נתון שהיא עצמה מתעלמת ממנו, והכריחה
לבחור פריט מקטלוג ההשכרה רק כדי לאשר הזמנת כביסה.

**מה נבנה:**
- **שרת:** התנאי מותנה בסוג. `var ORDER_TYPES = ['השכרה','כביסה']` ליד
  `STAGES`, והסוג עובר `sPick` (B64a).
- ⛔ **הזמנת השכרה בלי שורות נשארת חסומה.** שם השורות הן הכסף.
- ⛔ **סוג לא מוכר או ריק מקבל את ההתנהגות המחמירה** — שורות נדרשות,
  בדיוק כמו היום. הפטור חל רק על סוג שזוהה ודאית ככביסה.
- ⭐ **באג שני שנמצא באותן עשר שורות:** הבדיקה שמתחתיה השוותה
  `o.type === 'השכרה'` בהשוואה מדויקת. סוג עם רווח נגרר **דילג על בדיקת
  המלאי כולה בשקט** — הזמנת השכרה הייתה מאושרת בלי בדיקת זמינות. תוקן
  באותו `sPick`. יש שלוש בדיקות שמוכיחות שהמלאי נבדק גם עם רווח קשיח.
- **ממשק:** ההודעה אחרי שמירה הייתה `'נשמר — הוסף פריטים'` לכל סוג — שפת
  השכרה ששלחה את המשתמש לחפש פריט. עכשיו נגזרת מהסוג. כותרת מקטע הפריטים
  בכרטיס כביסה: **"אומדן כמותי (לא חובה)"**, וההסבר אומר מפורשות שאפשר
  לאשר בלי אף שורה.
- ⛔ **מקטע הפריטים לא הוסתר.** הכרעת אבי: שורות נשארות אפשריות בכביסה.
  יש בדיקה שנכשלת אם מישהו יסתיר את "+ הוסף פריט".

✅ **אומת שהתיקון נקודתי ואינו דורש עוד כלום:** `deliverOrder` אינו זקוק
לשורות · `b54RawOrderTotalAg` כבר סופר את חיוב הקליטה (C8) ולכן החשבונית
עוברת `sub > 0` בלי שורות — יש בדיקה · `availableQty` כבר פטור נכון.
⛔ **חסימת התשלום של B2/D2 לא נפגעה** — יש בדיקה ייעודית שמוודאת
ש-`BLD-15` לא פתח פרצה בהזמנת פורטל/חנות שטרם שולמה.

---

#### פריט 2 — תיקון באג ייצור: הזמנת כביסה נסגרה בזמן שהכביסה במכונה

**הכרעת אבי באמצע הסשן (אפשרות א').** ✅ אומת עצמאית ב-`grep`, לא נלקח
מהמסמך כמות שהוא.

**מה היה שבור:** `B56_OPEN_INTAKE = ['התקבל','בעבודה','מוכן','במשלוח']` —
רשימה ידנית של סטטוסים "פתוחים", שגויה בשתי דרכים:
1. הערך `'בעבודה'` **אינו נכתב בשום מקום במערכת.** שלוש התוצאות היחידות
   שלו בקובץ השרת הן ההגדרה, הערה, וטקסט שגיאה — אף אחת אינה השמה.
2. חסרו בה **ארבעת שלבי העבודה בפועל**: `בכביסה` · `בייבוש` ·
   `בגיהוץ וקיפול` · `באריזה`.

התוצאה בשטח: `openIntake` יצא `false` וההזמנה עברה ל`הושלמה` **בזמן
שהכביסה הייתה במכונה**.

**מה נבנה:** ⛔ **לא הוספנו ארבעה ערכים לרשימה** — זו בדיוק הדרך שבה היא
תתיישן שוב בשלב הבא שיתווסף. במקום זה ההגדרה התהפכה:
`var B56_CLOSED_INTAKE = 'נמסר'` ובדיקה שלילית ב-`b56IntakeOpen(ik)`.
`'נמסר'` הוא סוף המסלול היחיד (`nobleDeliver`), ולכן ההגדרה ההפוכה אינה
יכולה להתיישן. שני הצרכנים — `b56CloseLaundryOrder` ו-`bld01Candidates` —
משתמשים עכשיו **באותה פונקציה אחת**, ולא בשתי העתקות.

⭐ **סופח באותה נשימה — שורש `BLD-01`:** `b56CloseLaundryReturnLeg` השוותה
`String(order.type) !== 'כביסה'` בהשוואה מדויקת. **זו הסיבה שרגל החזור לא
נסגרה מלכתחילה** — הריפוי של B65 טיפל בתוצאה. עכשיו עובר `sVal`.

**מה נבדק:** לולאה על **כל** `NOBLE_STAGES` פרט ל`נמסר` — כל אחד מהם מונע
סגירה. לולאה נפרדת על `NOBLE_MACHINE_STAGES` (אלה שהרשימה החסירה).
הבדיקה שאבי דרש: שתי קליטות, אחת ב`בייבוש` ואחת ב`נמסר` → **ההזמנה אינה
נסגרת.** וגם: כשהכל נמסר — כן נסגרת.

⚠ **שינוי התנהגות מודע:** קליטה בלי סטטוס נחשבת עכשיו **פתוחה**. הכיוון
הבטוח — עדיף הזמנה שנשארת פתוחה מהזמנה שנסגרת מוקדם. ✅ אומת ששני אתרי
יצירת הקליטה (`nobleIntake`, `b49cOpenReturnIntake`) כותבים `status:'התקבל'`
תמיד, ולכן המצב הזה אינו קיים בפועל.

---

#### פריט 3 — `BLD-03`: קיוסק לשני טאבלטים ומעלה

**מה היה שבור:** `kiosk_token_hash` מפתח **יחיד**. רישום טאבלט שני **דרס
את הראשון בשקט** — המתחם קיבל טאבלט חדש והישן הפסיק להחתים בלי שאיש ידע.

**מה נבנה:** `kiosk_token_hash_<n>` עם שם ידידותי, עד 10 טאבלטים.
- ⛔ **מרחיבים, לא מחליפים (R4).** `B52_TOKEN_KEY` נשאר בדיוק כפי שהיה,
  ו**הטאבלט שכבר רשום אצל אבי ממשיך לעבוד בלי רישום מחדש** — הוא מופיע
  ברשימה כמכשיר 0 עם תגית "רישום ישן". יש בדיקה ייעודית.
- ⛔ **hash בלבד, ולעולם לא יוצא ללקוח.** `b52KioskStatus` ממפה ומחזיר
  מספר · שם · תאריך. יש בדיקה שמחפשת את ה-hash ואת הטוקן בתשובה.
- ⛔ **ביטול הוא תמיד פר-מכשיר.** אין כפתור שמבטל את כולם — במתחם עם כמה
  טאבלטים לחיצה כזו הייתה מורידה את כל ההחתמה בבת אחת. ביטול בלי מזהה
  מוחזר כשגיאה ואינו מוחק כלום. יש בדיקה.
- ⛔ **`B52_KIOSK_ACTIONS` לא נפתחה.** נשארה `['b52KioskClock','checkVersion']`,
  ויש בדיקה שנכשלת אם מישהו יוסיף לה את `b52RegisterKiosk` או `b52RevokeKiosk`.
- ✅ **מזהה הגבלת הקצב נשאר נכון:** `b52DeviceKey` נגזר מה-hash, ולכן שני
  טאבלטים מקבלים מזהים שונים — נעילה של אחד אינה נועלת את השני. יש בדיקה.
- ⚠ **הרישום והביטול הוצאו מה-`switch` לפונקציות `b52RegisterDevice`/
  `b52RevokeDevice`.** `route()` טוענת גיליון אמיתי ולכן אינה נגישה לרתמת
  הבדיקות; בלי ההוצאה הזו הלוגיקה הייתה נשארת בלי כיסוי.
- ⚠ **תיקון קטן שנתפס בבנייה:** שם הטאבלט עבר בהתחלה בתוך `onclick`.
  `esc()` מגן על `<` בלבד, ולכן שם עם גרש היה מנתק את הכפתור בשקט. עכשיו
  עובר **המספר בלבד**, והשם נשלף מ-`B52_DEVS`. יש בדיקה.

⛔ **ScriptProperties, לא סכימה. אין שינוי בגיליון.**

**מה לא נבדק:** רישום בפועל מול טאבלט אמיתי. הבדיקות מוכיחות את הלוגיקה
ואת האימות; הן אינן מוכיחות שהקישור נפתח בטאבלט. ⚠ אבי — ראה רשימת
הבדיקות הידניות.

---

#### שכבה 2 — לא הורחבה, בכוונה

`B66` **אינו נוגע בשום יכולת דפדפן** — לא היסטוריה, לא אחסון, לא מצלמה,
לא הדפסה, לא מגע ולא פריסה. `BLD-15` הוא לוגיקת שרת וטקסט, `BLD-03` הוא
`ScriptProperties` ושדה טקסט. לכן לא נוספה טענה ל-`b61Tests()`, ו-`t05`
ו-`t09-b65-width` לא נגעו. ⚠ **הרגע שבו זה משתנה הוא `BLD-05` ב-B67.**

**מה נבדק:** 266 בדיקות, 0 כשלים (228 → 266; **38 חדשות**).
`node --check` עבר על שני הקבצים. הבסיס הורץ **לפני** כל נגיעה בקוד
והחזיר 228/0 — אין רגרסיה קיימת שנסחבה פנימה.

---

### B65 — טופס כביסה · ריפוי היסטורי · מד רוחב (01.08.2026)

**canary: `v4.65-B65` · 228 בדיקות · 0 כשלים · ⛔ אין שינוי סכימה — אין להריץ `setupDatabase`**

⚠ **ההיקף שונה מהמתוכנן בהכרעת אבי (אפשרות ב').** התוכנית המקורית הייתה
`BLD-05` + `BLD-01` + `BLD-03`. במהלך הסשן אבי דיווח על תקלה בטופס הזמנת
כביסה, וההכרעה הייתה: **טופס הכביסה + BLD-01 + כלי המדידה**, ו-`BLD-03`
(קיוסק רב-טאבלטי) נדחה ל-B66. הנימוק: טופס הכביסה ו-BLD-01 נוגעים באותו
אזור קוד בדיוק — הזמנות כביסה ורגל החזור שלהן.

---

#### פריט 1 — טופס הזמנה שמשנה צורה לפי הסוג

**מה היה שבור (✅ אומת ב-`grep` על הקוד החי):** בורר "סוג" ב-`newOrderForm`
היה `<select>` בלי `onchange`, ובלי שום רינדור מותנה בקוד. הזמנת כביסה קיבלה
את מילון ההשכרה — "תאריך אירוע / איסוף", "אופן המסירה — הלוך". אבי דיווח
שאי אפשר לפתוח כביסה ללקוח רגיל; בפועל אפשר היה, אבל הטופס דיבר בשפה הלא
נכונה ולכן נראה כאילו אינו מיועד לזה.

**מה נבנה:** `b65ApplyType` מחליפה תוויות וטקסט עזר לפי הסוג. נוספו שתי
נקודות כניסה — כפתור "🧺 + כביסה" במסך ההזמנות, וכפתור "＋ הזמנת כביסה
חדשה" בלוח הכביסה. שניהם קוראים ל-`newOrderForm` הקיים עם `presetType`.

**הכרעות:**
1. **החלפת תוויות בטקסט בלבד, לא רינדור מחדש.** רינדור מחדש של הטופס היה
   מוחק ערכים שהמשתמש כבר הקליד — אותה מחלקת נזק שהגנת `activeElement`
   ב-`b58AfterRefresh` מונעת. יש בדיקה ייעודית.
2. **⚠ R9 — מה שאבי ביקש בסעיף "מסירה והחזרה על ידי הלקוח" כבר היה בנוי.**
   `B49e` נתן שתי רגליים עצמאיות עם "איסוף עצמי" עוד ביולי. הבעיה הייתה
   שהתוויות היו בשפת השכרה ולכן אבי לא זיהה את המנגנון. **לא נבנה מנגנון
   שני** — רק ניתן לו השם הנכון, דרך `b49eLegLabel` שהוא מקור האמת היחיד.
3. **`presetType` אופציונלי ובסוף** (כלל B64) — `newOrderForm(false)` ו-
   `newOrderForm(false, iso)` ממשיכות לעבוד. יש בדיקה לשתיהן.
4. **אין שדה חדש ואין עמודה חדשה.** אותם `id`, אותם ערכים לשרת.

---

#### פריט 2 — BLD-01: ריפוי הזמנות כביסה היסטוריות

**הרקע:** B56 גרם למסירת הכביסה הנקייה לסגור את רגל החזור — אבל מכאן והלאה
בלבד. הזמנות שנמסרו לפני 29.07.2026 נשארו עם נסיעת `אספקה` ב'מתוכנן' לנצח,
וממשיכות להופיע כפתוחות ביומן, במשלוחים ובמסך ההזמנות.

**מה נבנה (⛔ R1 — בלי ייצוא בסיס נתונים):**
`bld01Diagnose` — פעולת `READ_ONLY`, אפס כתיבה, ב-`READ_ONLY_ACTIONS`.
`bld01Heal` — כתיבה, **בכוונה לא** ב-`READ_ONLY_ACTIONS`, מנהל/משרד בלבד.
כרטיס ביומן פעולות: אבחן ← רשימה מלאה על המסך ← רפא.

**הכרעות:**
1. **הריפוי אינו מכיל לוגיקה משלו.** הוא קורא לאותן שתי פונקציות של B56 —
   `b56CloseLaundryReturnLeg` ו-`b56CloseLaundryOrder`. אין מסלול תיקון שני
   שיכול להתפצל מ-B56 בעתיד.
2. **אידמפוטנטיות** נובעת מכך ששתיהן בודקות סטטוס לפני שהן נוגעות. הרצה
   שנייה מוצאת רשימה ריקה ואינה כותבת. יש בדיקה שמשווה `JSON.stringify`
   של המסד לפני ואחרי ההרצה השנייה.
3. **מנהל/משרד בלבד** — הריפוי מצהיר `declared='כן'`, כלומר שנסיעה בוצעה
   בפועל. אותה הרשאה כמו `b49fDeclareGate`. ההנמקה של B56 תקפה גם
   רטרואקטיבית: הקליטה הגיעה ל'נמסר' דרך `nobleDeliver`, שחוסם מסירה בלי
   חתימה או צילום, ולכן המסירה הוכחה.
4. **אין נגיעה בכסף.** `B54_SKIP_ORDER` מחריג רק בוטלה/טיוטה/הצעת מחיר,
   ולכן 'סופקה' ו'הושלמה' נספרות זהה בספר החיובים. יש בדיקה.
5. **⚠ B64a הוחל כאן במלואו.** כל השוואה לערך מגיליון עוברת `sVal`. יש
   בדיקה שמזינה הזמנה עם רווח קשיח בסוג, בסטטוס וב-kind — בלי `sVal` היא
   הייתה "לא נמצאת" והרשימה הייתה חוזרת ריקה. כשל שקט על נתונים היסטוריים.

---

#### פריט 3 — מד הרוחב: הכלי שפותח את BLD-05

🔴 **BLD-05 ירד מארבע אצוות ברציפות. קריאת הקוד ב-B65 הראתה שהסיבה אינה
אצל אבי — הכלי עצמו לא היה מסוגל למסור את המספר. שתי תקלות, שתיהן אומתו:**

1. **`b61Text` העתיק רק בדיקות שנכשלו.** הפריסה נכנסת, הבדיקה עוברת, והשורה
   עם המספר לא נכנסת לבלוק שמודבק לצ'אט. אבי הריץ, העתיק, הדביק — והמספר
   פשוט לא היה שם. **זו הסיבה שהמספר לא הגיע, לא דילוג.**
2. **המדידה כיסתה את המסך שפתוח בלבד**, והכרטיס יושב בתוך יומן פעולות. לכן
   נמדד תמיד יומן פעולות. המספר הדרוש הוא המקסימום מששת המסכים הרחבים.

**מה נבנה:** `b65RecordWidth` — מדידה **פסיבית** אחרי כל רינדור מסך, בתוך
`try`. נשמר המקסימום לכל מסך ב-`localStorage` (`mn_b65width`). אבי עובד
רגיל, וכשהוא פותח את הבדיקה העצמית הטבלה כבר מלאה. **אפס בקשות שרת.**

**הכרעות:**
1. **הטענה החדשה נכשלת בכוונה כל עוד חסר אחד מששת המסכים.** מספר חלקי גרוע
   ממספר חסר — הוא נראה כמו תשובה. הבדיקה מציגה בדיוק אילו מסכים חסרים.
2. **הטענה הישנה (ACT-05) לא הוסרה** — יש בדיקה ב-t05 שמוודאת זאת.
3. **⛔ B65 אינו נוגע בנקודת השבירה.** הוא מוסר מספר בלבד. מי שמוריד את
   הנקודה הוא B66, ורק אם המספר יאשר. **אם המספר יאמר שהמערכת צריכה יותר
   מ-1024px — לא מורידים. זו התשובה הנכונה, לא כישלון.**

---

#### נלווה — B64a הורחב לאופן המסירה (שני הקבצים)

`b49eShipMode` השתמשה ב-`trim()` בלבד. `trim` אינו מסיר רווח קשיח ותווי
כיווניות, ולכן `ship_back` עם רווח קשיח בגיליון נפל לגזירה מ-`delivery_fee`
בלי שום שגיאה — איסוף עצמי הופך למשלוח או להפך, ורגל נוצרת או לא נוצרת.
**בדיוק מחלקת הכשל השקט של B64a.** הוחלף ב-`sPick` **בשני הקבצים, זהה**.

**מה נבדק:** 228 בדיקות · 0 כשלים · `node --check` על שני הקבצים · שלושת
הקשרי הריצה נבדקו לטופס (צוות/פורטל/חנות — הטופס הוא צוות בלבד, `DB` מלא).
**מה לא נבדק:** המדידה עצמה בדפדפן אמיתי — ב-jsdom `clientWidth` הוא 0
ולכן המדידה יוצאת בשקט. ⚠ **זו בדיוק שכבה 2, ולכן היא בידיים של אבי.**


---

## ⚠ החלטת ניהול — 30.07.2026 · מיזוג התור ל-8 אצוות

**סשן תכנון בלבד. לא נגע בקוד. אין שינוי canary ואין שינוי סכימה.**

הכלל "פריט אחד לצ'אט" **בוטל**. 16 הפריטים שנותרו קובצו ל-**8 אצוות, B60–B67**,
לפי אזור קוד משותף. המספור B60–B75 מבוטל ומוחלף.

**מה שמאפשר את המיזוג — ואינו ניתן לוויתור:**
- **M3** — חמש שאלות הגדלת הראש נענות לכל פריט **בנפרד ובכתב**. אצווה שענתה
  "לכל האצווה יחד" נפסלת.
- **M5** — `PROGRESS.md` מפרט כל פריט בנפרד תחת אותו `B##`, כדי שאפשר יהיה
  לזהות מי שבר מה.
- **B61 = `BLD-08` (`tests/`) נבנה שני בתור**, לפני כל אצווה מאוחדת גדולה.
- **שינוי סכימה וסיכון גבוה אינם מתמזגים לעולם** — B66 ו-B67 נשארים לבד.

**המפה המלאה וכללי המיזוג M1–M6: `TASK_QUEUE.md` חלקים 9 ו-9א.**
⚠ **אין לפצל אצווה מאוחדת בחזרה לפריטים בלי הכרעת אבי.**

---

**✅ PERF-02 סגור סופית. `ACT-19` ו-`ACT-18` הורצו על נתוני אמת (30.07.2026, 19:07).**

> **זהות מלאה: 55 מתוך 55 טבלאות זהות ביט-אחר-ביט. המסלול המהיר פעיל.**

**המספרים הסופיים, על הגיליון האמיתי:**

| מדד | לפני | אחרי |
|---|---|---|
| `getAll` מלא, מהדפדפן | **~14 שנ׳** | **1.6 שנ׳** |
| קריאת כל הגיליונות בשרת | 12.8–16.6 שנ׳ (55 נסיעות) | **553 מ״ש** (`batchGet`) |
| יחס | — | **פי 23.2** |

**⚠ הגענו לתקרת הפלטפורמה, וזה הממצא החשוב ביותר לכל אצווה עתידית.**
קריאה שנייה **מיד** אחרי הראשונה לקחה 1.7 שנ׳ — כמעט זהה. הסיבה אינה מטמון
שלא עובד: **רצפת התקורה של Apps Script נמדדה אצל אבי ב-2.7 שנ׳ לבקשה**, גם
לזעירה ביותר. כל מה שהיה בשליטתנו ירד לאפס-פסיק-משהו:
בניית המטען **44 מ״ש** · סריאליזציה **0 מ״ש** · פענוח בדפדפן **1 מ״ש** ·
רינדור **9 מ״ש**. מטען: **49.2 KB** (מתוך 92.4 KB בגיליונות; `audit_log`
במשקל 44.4 KB אינו נשלח).
**המסקנה המעשית: אין יותר מה לסחוט מהמשקל. כל שיפור עתידי חייב להיות
צמצום מספר הבקשות — כלומר `R10`, ולא אופטימיזציה של נתונים.**

**ממצא שתועד ולא נפתחה עליו אצווה:** שיעור פגיעות המטמון **26.7%**
(4 פגיעות · 11 החטאות), ו-**47.4% מהפערים בין כתיבות קצרים מה-TTL של 120 שנ׳**
— כלומר כתיבה דורסת את המטמון הגלובלי לפני שהספיק לשרת. **זה כבר לא מזיק:**
החטאה עולה עכשיו 1.6 שנ׳ במקום 14, ובדיוק בשביל זה נבנה המטמון פר-טבלה של
B58 ②. חוזרים לזה רק אם מדידה עתידית תראה שזה נכנס לצוואר.

**B58a — תיקון שנספח לסשן אחרי שאבי הריץ את `ACT-17` (30.07.2026).**
האבחון עשה בדיוק את מה שנבנה בשבילו: תפס אי-התאמה **לפני** שהיא הזיקה,
כיבה את המסלול המהיר לבד, והמערכת המשיכה לעבוד במסלול הרגיל.

**B58 נגע בשני הקבצים ולכן שני ה-canary התיישרו ל-v4.55-B58.
אין שינוי סכימה ב-B58 — אין להריץ setupDatabase בגללו.**

**⚠ B58 = PERF-02: זהו התיקון עצמו. `getAll` בהחטאת מטמון ירד מ-55 נסיעות
לגיליון לקריאת `batchGet` אחת (נמדד בבדיקות: 55 → 1, ואפס `getValues`).
צפי: 11.9 שנ׳ → ~1 שנ׳, ועוד 2.2 שנ׳ תקורת Apps Script שאינה ניתנת להסרה.**

**⚠ פעולה נדרשת מאבי: להריץ פעם אחת *יומן פעולות ← קריאה מהירה (B58) ←
"בדוק זהות מטען ומדוד"*. זו ההוכחה על הנתונים האמיתיים ששני המסלולים
מחזירים בדיוק אותו דבר. אם תימצא אי-התאמה — המסלול המהיר נכבה לבד
והמערכת ממשיכה לעבוד כרגיל, בלי שבירה.**

**B57 נגע בשני הקבצים ולכן שני ה-canary התיישרו ל-v4.54-B57.
אין שינוי סכימה ב-B57 — אין להריץ setupDatabase בגללו.**

**⚠ B57 הוא אצוות אבחון בלבד. שום דבר במערכת לא הואץ בה.
התיקון עצמו הוא PERF-02 = B58, וההיקף שלו נגזר מהמספרים ש-B57 מציג.**

**B56 נגע בשני הקבצים ולכן שני ה-canary התיישרו ל-v4.53-B56.
אין שינוי סכימה ב-B56 — אין להריץ setupDatabase בגללו.**

**⚠ הכרעת אבי B56-א (29.07.2026) — הפרדה מלאה בין תקלות רכב לתקלות מכבסה.**
לשון אבי: *"אין קשר בין תקלות של רכב לתקלות של מכונות כביסה. תקלה של רכב
אמורה להיות מתועדת ב'רכב', לא במכונות כביסה."* ההפרדה אינה המלצה — היא כלל
מחייב. כל batch עתידי שנוגע ב-B42 חייב לשמור עליה: מסך `assets` מסונן
ל-`['מכונה','עגלה']`, מסך `fleet` ל-`['רכב']`, דרך `b42ScopeTypes`/`b42InScope`.
**אין להציג רשימת תקלות מאוחדת בשום מסך.**

**⚠ תקלת סנכרון שהתגלתה בפתיחת B51: `PROGRESS.md` של B50 מעולם לא הגיע לריפו —
מה שישב שם היה עדיין הגרסה של B49f. התוכן שוחזר ושולב כאן. הקובץ הזה מכיל את
B50 ואת B51 גם יחד. לקח: לוודא בכל סשן שגם `PROGRESS.md` נדרס, לא רק הקוד.**

**B50 היה ממשק בלבד. B51 נגע בשני הקבצים ולכן שני ה-canary התיישרו ל-v4.50-B51.
אין שינוי סכימה ב-B50 ואין שינוי סכימה ב-B51.**

⚠ **שינוי סכימה פתוח מ-B49f (לא מ-B50): שתי עמודות ב-`deliveries` —
`trip_confirmed_by` · `trip_auto_cancel`. אם `setupDatabase` טרם רצה מאז B49f —
יש להריץ אותה פעם אחת. B50 עצמו אינו דורש הרצה.**
⚠ אם `setupDatabase` של B49b/B49c/B49e טרם רצה — ההרצה הזו מכסה גם אותה:
`carts.virtual` · `laundry_tasks.intake_id` · `laundry_intakes.order_id` ·
`laundry_intakes.ready_ts` · `orders.ship_out` · `orders.ship_back` · טבלה `signatures`
· טבלה `charges` (B54) · טבלאות `payment_declarations` ו-`customer_messages` (B55) ·
**`deliveries.trip_confirmed_by` ו-`deliveries.trip_auto_cancel` (B49f)**.
כל העמודות נוספו בסוף המערכים — מיגרציה בטוחה.

**מה עובד:** כל הליבה — הזמנות, מלאי רב-מחסני, משלוחים ומסלולי נהג (Driver-Lite B5),
החזרות ושקילה, מודול מכבסה מלא (NC1–NC4), רצפת ייצור, משימות, פגישות, פורטל לקוח,
חנות ציבורית, מנוע תשלום ואשראי (B2), פקדון/ערבויות (B3), התראות אימייל (B4),
חשבוניות עם מספור רציף ומספרי הקצאה (B1/B31), דוח חובות לפי גיל ודשבורד חריגים (B9),
מרכז אישורים (B10), BI (B14), מדפים (B35), שעון נוכחות ושכר (B36–B37), אבטחת שדות
רגישים (B38), תחזוקת ציוד ותקלות (B42), תזרים וחובות עם מועדי פירעון (B43),
סריקת העמסה מרובה (B44), כלל מע"מ אחיד (B45), מסמכים כ-PDF אמיתי ואריחי עובד/רכב (B46),
תפריט עליון בדסקטופ (B47) — פתיחת החלוניות תוקנה ב-B48a,
מקור אמת אחד ליתרות לקוחות ומסנן מחסן במסך המלאי (B48),
מרכז דוחות (B49), עגלה וירטואלית (B49b), גשר לוח הכביסה↔יומן NOBLE (B49c),
איסוף עצמי ומשלוח כשני מצבים עצמאיים + חתימה מופרדת מהמשלוח (B49e),
כביסה מלקוח: הזמנת `כביסה` → קליטת NOBLE (B49d),
ספר החיובים — מקור אחד לכל חיוב לקוח, עם מקור לכל חיוב (B54),
פורטל לקוח מלא: פירוט חיובים · הודעת תשלום עם אסמכתא · פניות מקושרות (B55),
אישור הנהלה שהנסיעה בוצעה בפועל + ריפוי בדיעבד (B49f),
שלושה דוחות חדשים — שעות עבודה · ניצולת השכרות · צי רכב — בתוספת עלות תחזוקה
לפי חודש, ייצוא CSV מכל טבלת דוח וממסך התזרים, וביטול חלון השעות הקבוע
במדד ניצולת המכונות (B50),
**קיצוץ מטען `getAll` לפי `SLIM_POLICY` · יומן פעולות בעימוד שרת · ניקוי סיסמאות
היסטוריות מ-`audit_log` · אבחון משקל הנתונים על המסך (B51).**
**מסך קיוסק — הקשר ריצה רביעי `?kiosk=1`: טאבלט משותף שמחתים נוכחות במספר אישי,
בלי סשן ובלי DB, מאומת בטוקן מכשיר (B52).**
**פריסת ניווט משולבת: שורה עליונה עם קיצורי דרך מוגדרים + סרגל צד ימני קבוע
שמציג את כל המסכים מ-1100px ומעלה (B53).**

**מה שבור:**
1. ✅ **חיוב כפול/שווא ללקוח — תוקן ב-B54.** ארבעת הממצאים נסגרו: (א) שורות הזמנת
   כביסה אינן נספרות כסף (C5) → אין יותר חוב שווא; (ב) `nobleCreateInvoice` מסנן
   `order_id` (C8) → אין חיוב כפול; (ג) דמי שינוע וחיוב חוסרים הם שורות חיוב
   עצמאיות עם זהות משלהן (C6); (ד) קיים מסלול להקלדת חיוב ידני (`charges`, C3).
   ⚠ **ממתין לאימות אבי על נתוני אמת** — כפתור "בדיקת נתוני חיובים" בהגדרות.
2. ✅ **הפורטל — תוקן ב-B55.** הלקוח רואה פירוט חיובים מלא, מודיע על תשלום
   (סכום + אסמכתא), ושולח פניות מקושרות. `portalPayDebt` הוסר לחלוטין.
   ⚠ **מגבלה מוצהרת:** אין חשבון סליקה מחובר, ולכן כרטיס אשראי אינו מחייב
   בפועל — הכפתור פותח את `payment_link` ויוצר הודעת תשלום לאימות המשרד.
   כשיחובר ספק, ה-callback שלו יקרא לאותה `b55ConfirmDeclaration`. תפר אחד.
3. ✅ Bug 1 (חתימה בטאבלט) — **תוקן ב-B49e**, הוקדם מ-B51. ממתין לאימות על טאבלט אמיתי.
4. ✅ **באג שקט מ-B37 — תוקן ב-B52.** הממשק שלח ל-`clock` את `employee_id`
   (הבורר שנוסף ב-B37), אך `case 'clock'` בשרת קרא רק `p.pin` — ולכן **מנהל
   שבחר עובד מהרשימה החתים בפועל את עצמו**, בלי שום הודעת שגיאה.
   ⚠ **נוכחות היסטורית שנרשמה כך שגויה** — אם אבי השתמש בבורר, יש לתקן ידנית
   (נוכחות ← רישום ידני / פסילת שעות).

**ממתין לאימות אבי:**
0. ⭐ **חדש (B52) — טאבלט קיוסק.** מיתוג והגדרות ← "טאבלט קיוסק" ← רישום →
   לפתוח את הקישור בטאבלט → הקשת מספר אישי → כניסה/יציאה. פירוט במקטע B52 להלן.
0**a**. 🔴 **B52 — לוודא שהבורר בשעון הנוכחות מחתים את מי שנבחר.** זה הבאג
   שתוקן; לבדוק בפועל: נוכחות ← בחירת עובד אחר ← החתמה ← השם שמופיע.
0*. ⭐ **חדש (B51) — מסך יומן הפעולות נטען עכשיו מהשרת בעמודים.** הפילטרים זהים;
   מה שהשתנה הוא שהיומן כבר לא מגיע בכל רענון. לבדוק שהמסך נטען, שהסינון עובד
   ושכפתור "טען עוד" מוסיף שורות.
0**. ⭐ **חדש (B51) — "כלי תחזוקה" במסך היומן (מנהל בלבד):**
   (א) **📊 משקל הנתונים** — מודד כמה שוקל `getAll` לפני ואחרי הקיצוץ, טבלה-טבלה.
   זה המספר שאומר אם ה-batch עשה את שלו. הפעולה קוראת את כל הגיליונות ולכן
   לוקחת כמה שניות — זה תקין.
   (ב) **🧹 ניקוי סיסמאות היסטוריות** — קודם תצוגה מקדימה ("נמצאו N"), ורק אז
   כפתור ביצוע. ריצה שנייה חייבת להחזיר 0.
0***. 🟡 **B51 — מסכי היסטוריה מציגים עכשיו "מוצגים N הימים האחרונים".** יומן
   אירועי המכבסה ויומן שלבי המשימה מוגבלים ל-180 יום ברענון, שינויי מועד ל-365,
   התראות ל-90 (ממתינות ונכשלות מוצגות בכל גיל). הנתונים בגיליון לא נגעו.
0×. ⭐ **חדש (B50) — ארבעה דוחות במרכז הדוחות:** שעות עבודה (מנהל בלבד) ·
   ניצולת השכרות · צי רכב · עלות תחזוקה לפי חודש. פירוט הבדיקות במקטע B50 להלן.
0××. ⭐ **חדש (B50) — כפתור ⬇ ייצוא** בכל טבלת דוח ובשתי הטבלאות של מסך התזרים.
   ⚠ לוודא שהקובץ נפתח באקסל עם עברית תקינה (BOM) ושהסכומים ניתנים לסכימה.
0×××. 🟡 **B50 — מדד ניצולת המכונות השתנה.** האחוזים שאבי ראה עד היום ישתנו:
   החלון הקבוע בוטל והמדד נמדד מול יום העבודה של הצוות בפועל (הכרעת Y1).
   שינוי מכוון, לא באג.
0. 🔴 **B49f — שאלת "האם נהג ביצע את הנסיעה בפועל?" בשלושת מסלולי הקליטה,
   ורשימת "נסיעות שבוטלו בקליטה" במסך המשלוחים.** פירוט במקטע B49f להלן.
0א. 🔴 **B55 — כל פריטי האימות בסוף מקטע B55 להלן.**
1. 🔴 **הרצת `setupDatabase`** — חובה אחרי B49f (שתי עמודות ב-`deliveries`;
   מכסה גם את B54 ו-B55 אם טרם רצה).
2. ⚠ **הפעלת שירות Drive API** בעורך Apps Script (שירותים ← + ← Drive API).
3. הרצת "תיקון מסמכים שנשמרו כ-HTML" במסך ההגדרות (אחרי 1 ו-2).
4. 🔴 **חדש (B54) — "בדיקת נתוני חיובים"** במסך ההגדרות. זו הפעולה שמראה מה
   השתנה במספרים על נתוני האמת. כלל R1 — אין ייצוא, הכל על המסך.
5. ⭐ **חדש (B54) — לשונית "חיובים"** במסך הכספים: כל חיוב, עם המקור שלו.
6. ⭐ **חדש (B54) — "+ חיוב חדש"** בלשונית החיובים ובכרטיס הלקוח (מנהל/משרד).
7. ⭐ **חדש (B54) — "הפק חשבונית מהחיובים המסומנים"** בלשונית החיובים.
8. ⭐ **חדש (B54)** — בכרטיס הזמנת כביסה: שורות הפריטים מסומנות
   "אומדן — החיוב לפי משקל", ומקטע הכספים מפולח לפי מקור.
9. B49d — רשימת "ממתין לקליטה" · "פתח קליטה במכבסה" · "הוסף לקליטה הקיימת".
10. B49e — "מילוי אופן מסירה" בהגדרות · מקטע אופן המסירה · חתימה מכרטיס ההזמנה
   (⚠ לבדוק על הטאבלט של הדלפק) · ביטול נסיעת סרק.
11. B46/B48/B49/B49b/B49c — פריטי האימות מהאצוות הקודמות עדיין פתוחים.

## ⛔ כללי עבודה קבועים — הכרעות אבי. גוברים על כל מסמך

**R1 (28.07.2026) — אבי אינו מייצא ואינו שומר בסיס נתונים. לעולם.**
אין לבקש ממנו ייצוא גיליון, קובץ CSV, צילום מסך של טבלה או "תבדוק מה יש לך ב-X".
זה מבטל את הכלל ב-`WORKING_PROTOCOL.md` חלק 5 ("מסך ריק = לבקש ייצוא") — **הכלל
הזה מבוטל ואין לפעול לפיו.**
**מה עושים במקומו:** כשצ'אט צריך לדעת מה יש בפועל בנתונים, הוא **בונה פעולת
אבחון READ_ONLY לתוך המערכת** (ספירות, דגימות, אי-התאמות) ומציג אותה כמסך/כפתור.
אבי לוחץ, רואה על המסך, וזהו. אין קבצים, אין העברות, אין דבר שהוא צריך לשמור.
**וכלל עליון על זה:** קוד שדורש ידיעה מוקדמת על הנתונים הוא קוד שתוכנן לא נכון.
תיקון נתונים חייב להיות **מגלה-בעצמו ואידמפוטנטי**, עם תצוגה מקדימה לפני ביצוע.

## אצוות קודמות — פירוט

### B64a — תיקון: ערך טקסט מהגיליון עם רווח הפיל תפריטים בשקט (01.08.2026)

**⚠ נמצא על ידי אבי בשטח, לא על ידי הבדיקות.** אבי דיווח ש"תפריטים מתוך
תצוגות לא נפתחים" ונתן שתי דוגמאות: בחירת סיבה למה נהג לא אסף, וקידום שלב
בכביסה.

**⛔ תהליך האבחון — מה שנשלל לפני התיקון.** ההשערה הראשונה (הרענון
האוטומטי מוחק את המסך) **הופרכה על ידי אבי**: הוא אמר "לא קורה כלום
בכלל", ורענון היה מייצר "נפתחת ונסגרת". נשללו גם `pointer-events`, שכבה
מכסה (נבדקו כל 15 ערכי ה-`z-index`), לכידת מצביע תקועה מ-B62, וארבעת
מאזיני הלחיצה הגלובליים. התשובה "כפתור רגיל ליד כן עובד" חתכה את
האפשרויות סופית: הכרטיס נטען, רק הבורר לא נולד.

**השחזור.** הורצו שתי התצוגות בפועל ונבדק ה-DOM שנוצר:

| ערך בעמודת `stage` | תוצאה |
|---|---|
| `"בכביסה"` | ✅ המודל נפתח |
| `"בכביסה "` — רווח נגרר | ⛔ **לא נפתח** |
| `" בכביסה"` — רווח מוביל | ⛔ **לא נפתח** |
| `"בכביסה ראשונה"` — שם ישן | ⛔ **לא נפתח** |
| תא ריק | ⛔ **לא נפתח** |

**שורש הבעיה.** `STAGES.indexOf(t.stage)` השווה ערך שנקרא מגיליון מול
רשימה קבועה בהשוואת מחרוזת מדויקת. רווח אחד → `-1` → `advanceForm` יצאה
**לפני** `openModal`. אותו דפוס בדיוק ב-`b41IntakeState`: `kind` עם רווח
→ `picks` ריק → `needsReason=false` → כרטיס "בחר סיבה" לא צויר כלל. **זה
מסביר למה הכפתור לידו עבד.**
⚠ **ההודעה שכן הוצגה הייתה שגויה** — "המשימה כבר בשלב מוכן" — ולכן שלחה
את אבי לחפש במקום הלא נכון. זו הסיבה שהבאג חי כל כך הרבה זמן.

**זו אותה משפחה של B38b.** שם תאריכים מהגיליון חזרו בפורמט שונה ונפתרו עם
`dYmd()`. הפעם זה טקסט. `sVal()` הוא ה-`dYmd()` של מחרוזות.

**התיקון — `sVal()` ו-`sPick()`, בשני הקבצים, זהים תו-בתו.** מנקים רווח
מוביל/נגרר, רווח כפול פנימי, רווח קשיח (`\u00A0` — נוצר בהדבקה מדפדפן)
ותווי כיווניות RTL בלתי נראים (`\u200E`/`\u200F` — נוצרים בהקלדת עברית).
⛔ **נרמול להשוואה בלבד — הערך בגיליון אינו משתנה.**
⛔ **אסור על כסף ומספרים** — שם ההשוואה מספרית. יש בדיקה שסורקת זאת.
הוחל על: `advanceForm` · `b41IntakeState` · `b41Pickups` · `b41DriverHandled`
· `b41PickupReceived` · `b49eLiveDeliv` · מחווני NOBLE בפורטל וברצפת
הייצור · ובשרת על `advanceLaundryStage` · `b5DriverOpenDeliveries` ·
`splitLaundryTask`.

**ההודעה תוקנה.** שלב לא מוכר מקבל עכשיו: `שלב לא מוכר בגיליון: "X"
במשימה LT9. הערכים המותרים: ... תקן את עמודת stage.` — עם הערך הבעייתי
ועם מזהה הרשומה. משימה שבאמת הסתיימה ממשיכה לקבל את ההודעה הישנה.

**⭐ באג שני שנתפס אגב האבחון והוכח בנפרד:** `b58AfterRefresh` מצייר מחדש
כל 30 שניות ונמנע מזה רק כשמודל פתוח. כרטיס "בחר סיבה" מוצג **בתוך מסך**
ולא במודל — ולכן רינדור באמצע בחירה מחק את הרשימה הפתוחה **ואת הפירוט
שכבר הוקלד**. נוספה הגנה שנייה: אם המיקוד בתוך `select`/`input`/`textarea`
— הרינדור נדחה. הנתונים בזיכרון כבר עודכנו והמסך יתרענן ברענון הבא.

**192 בדיקות · 0 כשלים** (175 → 192: `t08` חדש עם 17). שתיים מהן נכשלו
בבנייה וחשפו דליפה אמיתית — תווית השלב והצ'יפ בפורטל עדיין הדפיסו את
הערך הגולמי, כך שהלקוח היה רואה `"בייבוש "`. תוקן.

⚠ **אין שינוי סכימה. שני הקבצים משתנים — צריך להדביק גם את קובץ השרת.**

---

### B64 — יצירה מהיומן · מע"מ בחנות ובפורטל · השבתת ספק שירות (31.07.2026)

**ממשק בלבד. אין שינוי סכימה ואין פעולת שרת חדשה. קובץ השרת לא נגע.**

**⛔ נפתח בעצירה:** הבדיקה העצמית לא הורצה, אבי ביקש לדלג, ולכן **BLD-05
ירד ל-B65**. אין מספר, ואסור לנחש נקודת שבירה — R11. האצווה נבנתה בשלושה
פריטים, ו-T2ב סופח בהסכמת אבי.

**T1 — לחיצה על יום ביומן פותחת יצירה.** `calEvents()` נמנתה במלואה: היא
הציגה 7 סוגי אירועים (שתי רגלי הזמנה · משימות · פגישות · טיפול מכונה ·
טסט/ביטוח/טיפול רכב) ו**לא הציגה שום מועד תשלום**. נוספו שני מקורות, שניהם
נגזרים ואף אחד מהם אינו ישות חדשה:
· **כסף יוצא** — הוצאות עתידיות פתוחות (`fexpOpen`/`fexpRemain`), אותו מקור
  של תזרים B43. יצירה מהיומן פותחת את `futureExpForm` הקיים.
· **כסף נכנס** — מועדי פירעון מ-`b54LedgerFE`, אותו מקור של דוח גיל החובות,
  מקובצים לפי מסמך כדי שלא תיווצר שורת יומן לכל שורת חיוב.
⛔ **מועד פירעון של לקוח אינו ניתן ליצירה ידנית** — הוא נולד מהחשבונית
ומתנאי התשלום. יצירה ידנית הייתה מייצרת שני מספרים סותרים לאותו חוב.
הוא מוצג ולא נוצר, ויש בדיקה שסורקת את הקוד ומוודאת ש-`b64RecvDue` נשען
על `b54LedgerFE` ולא על מקור כסף שני.
⛔ **טסט · ביטוח · טיפול רכב/מכונה — מוצגים ואינם ניתנים ליצירה**, בהכרעת
אבי: אלה שדות בכרטיס הרכב ולא רשומה חדשה.
⛔ **לא נוצר טופס שני לאף ישות.** ארבעת הטפסים הקיימים קיבלו פרמטר `iso`
אופציונלי בסוף; כל הקריאות הישנות ממשיכות לעבוד ויש בדיקה לכך.
⛔ **כסף ביומן — מנהל ומשרד בלבד.** נהג ומחסן אינם רואים סכומים.
R10: אפס בקשות שרת נוספו — הכל נגזר מ-`DB` שכבר בזיכרון.

**BLD-12 — כולל מע"מ כמספר ראשי בחנות ובפורטל.** grep על שני ההקשרים העלה
8 אתרי מחיר; 6 הוחלפו ו-2 כבר היו נכונים. ⛔ **תצוגה בלבד** — הסכום שנשלח
לשרת נשאר נטו באגורות, ויש בדיקה שסורקת את `shopSubmit`/`portalSubmit`
ונכשלת אם מישהו ישלח ברוטו. ⛔ **כלל B45 (נטו ראשי) נשאר בכל שאר המערכת**
(R4) — יש בדיקה ש-`b45Money` לא התהפך ושעוזרי `b12*` לא חלחלו למסך הכספים.
⚠ הכרעת אבי במסך התשלום בפורטל: **מסגרת האשראי והחוב הקודם נשארים נטו**
כי אלה מספרי המנוע, ומתויגים במפורש "לפני מע\"מ". להפוך גם אותם היה יוצר
מספר שאינו תואם את המנוע.

**BLD-02 — השבתת ספק שירות.** ⚠ R9 — הממצא: השדה `active` היה קיים מאז B42
והשרת כבר ידע לעדכן אותו (`b42SaveProvider` עם `id`), אבל **לא היה מסך
בכלל** — אנשי המקצוע הופיעו רק בתוך הבורר. נבנתה רשימה במסך **רכש וספקים**
(מנהל ומשרד), לא מסך חדש: 29 המסכים נשארו 29. ⛔ השבתה אינה מחיקה —
`b42Providers()` כבר מסנן `active!=='לא'`, ולכן המושבת נעלם מהבוררים ונשאר
בהיסטוריית התקלות והעלויות. הקריאה שולחת את כל השדות הקיימים כדי שעדכון
חלקי לא ירוקן עמודה.

**T2ב — סופח, ובוצע חלקית בכוונה.** הוחל דפוס `b62OrderActions` על
`doScan` (יחידה · עגלה · מדף) ועל `loadApprovals` (אישור גלוי, דחייה ב-⋯).
⚠ **R9 — שלושת המסכים הנותרים לא טופלו, וזו התשובה הנכונה:** הספירות
11/11/10 במפרט נספרו ולא נקראו. ב-`itemForm` הכפתורים הם "＋ הוסף" צמודים
כל אחד לשדה שלו (דפוס B39) · ב-`rShelves` זו רצועת אייקונים `.rowact`
קיימת ודחוסה · `floorRenderInfo` הוא מסך רצפת ייצור עם כפתורים גדולים
ברוחב מלא לעבודה בכפפות, ותפריט ⋯ שם היה מזיק. **אין שם צפיפות לתקן.**

**175 בדיקות · 0 כשלים** (138 → 175: t07 חדש עם 30 · t06 +5 · t03 +2).
⚠ שכבה 2 **לא** קיבלה טענות חדשות ובכוונה: האצווה לא נגעה בשום יכולת
דפדפן — לא רוחב, לא מגע, לא הדפסה, לא מצלמה.

---

> ⚠ פירוט B63a · B63 · B62 · B61 הוסר כדי שהקובץ לא יתנפח.
> השורה המלאה של כל אחת מהן נשמרה בסעיף **היסטוריה** למטה.

## היסטוריה (שורה לאצווה; פירוט מלא בכותרת קובץ השרת)
B75 — **`WASH-15` + `WASH-05` + `WASH-08` + `WASH-13` + `WASH-16` + `WASH-21`: המונה של הניצולת ושכבת המכונות.** אצווה של שישה פריטים, מעל תקרת `M1`, בהוראת אבי; `M3` קוים במלואו — חמש השאלות נענו בכתב לכל פריט בנפרד. **WASH-15** — `b49cAfterAdvance` כתבה `התחלה` **בלי `portion_kg` בכלל**, ולכן `load_kg` של כל כביסה פנימית של MAPA היה **אפס תמיד** ו-`BLD-13` היה חסום; המשקל נגזר עכשיו מ-`items.weight_kg × qty` דרך `w15TaskKg`, ⛔ בלי שער חסימה (פריט בלי משקל מחזיר `''` ולא `0` — יש בדיקה, כי אפס נראה כמו מדידה). ⭐ נבנה `w15WeightAudit` ב-`READ_ONLY_ACTIONS` (בדיקה משווה את ה-`db` לפני ואחרי) שמראה על נתוני אמת את המונה בכל מכונה, אחוז הניצולת, ואילו פריטים חסרי משקל — `R1`. ⭐⭐ **סופח ממצא B69 והוא היה חמור מהצפוי:** `nobleOpenStarts` בנתה מפתח עם `String(e.machine_id)` **גולמי** לצד `sVal(e.stage)`, ולכן `סיום` עם מזהה מלוכלך פתח מפתח שני ולא סגר את `התחלה` — **המנה נשארה פתוחה לנצח והמכונה נראתה תפוסה**; תוקן ב-6 אתרים דרך `w15MachKey`. **WASH-05** — `w05Gate`: מכונה תפוסה חוסמת משימה שנייה, עם דילוג בקוד מנהל ופירוט חובה שנרשם ביומן; ⛔ נקודה אחת שמשרתת גם את `advanceLaundry` וגם את `b40SplitLaundry` (`R8`); ⛔ משימה ב`'מוכן'` אינה תופסת, אחרת כל המכונות נחסמות לנצח; הממשק עובר דרך **אותו דפוס** `need_count` הקיים ב-`act`. **WASH-08** — הכרעת `3ג`: `w08DetachMachine` מנתק את משימות הכביסה מהמכונה ברגע דיווח התקלה ורושם ביומן כל משימה, ומאותו רגע `advanceLaundry` חוסם ממילא (`R8`, בלי שער שני); ⛔ NOBLE **אינו** מנותק — שם הכביסה פיזית בתוף ו-`nobleStageEnd` חייבת לעבוד גם על מכונה מקולקלת; ⚠ נוספה רשת בטיחות לנתוני עבר (תקלות שדווחו לפני B75 השאירו שיוך תקוע). **WASH-13** — `nobleDriverScan` נושאת את `order_id` של הקליטה; ⭐ העבודה כאן הייתה **מפת החיבורים**: `kind='משלוח מכבסה'` אינו מתנגש עם רגלי ההלוך/חזור לא ב-`b38EnsureOrderDeliveries` ולא ב-`b49fCloseLegs`, ו-`nobleDeliver` מסמן `'בוצע'` לפני `b56CloseLaundryOrder`. **WASH-16** (`DEC-03`, הכרעת `4ב`) — `w16OrderGate`: שער אחד, שלושה אתרי כניסה (פתיחת קליטה · הוספת עגלות · **שקילה**, שהיא יצירת חיוב); ⛔ `nobleWeighFix` בכוונה מחוץ לשער — היא מסלול התיקון היחיד; ⛔ חשבונית מבוטלת אינה חוסמת; ⭐ תוקן אגב `B64a`: `b49dResolveOrder` השווה סטטוס גולמי ולכן **חסמה בטעות** הזמנה תקינה עם רווח קשיח. **WASH-21** — `w21IsRental`, תאום מדויק ל-`w17IsWash` וזהה תו-בתו (יש בדיקה); ⛔ לא נבנה עוזר "סוג קנוני" — הוא היה דורש הכרעה חדשה על סוג לא מוכר, בסתירה לכלל B74; ⚠ **`R9`: הפריט אמר 19, הקוד נתן 20** (`b49eLegLabel` בממשק לא נספר ב-B74); ⭐ `linePrice` **לא נגעה** — היא משווה גולמי אבל האתר היחיד שמעביר לה סוג מהגיליון נחסם קודם ב-`sPick` מ-B72, כלומר אין חשיפה כספית (`R4`). **529 בדיקות, 0 כשלים** (47 חדשות; הבסיס הורץ לפני כל נגיעה והחזיר 482/0). ⚠ **לקח על הרתמה:** `H.stripComments` אינו מסיר הערות באזור `b49eLegKind` בשרת (נתקע במצב מחרוזת קודם בקובץ) — שומר מקור שנשען עליו נכשל על טקסט שבהערה בלבד; שני השומרים החדשים חותכים הערות בתוך הגוף שחולץ. ⚠ **ממצא חדש שלא תוקן — `WASH-22`:** אותה מחלקת כשל בציר ה**סטטוס** (`RESERVING_STATUSES.indexOf(o.status)` · `B40_OUT_STATUSES` וכו') — הזמנה מאושרת עם סטטוס מלוכלך אינה משריינת; **נעצר לפי `R3` ונרשם ל-B76.** ⚠ **שני הקבצים משתנים.** אין שינוי סכימה. (17.08.2026)
B72 — **`WASH-17` + `WASH-19` + `WASH-14`: המלאי, העגלה והשלב.** שלושה פריטים במסך הקליטה ובכרטיס ההזמנה, אף אחד אינו כסף ואף אחד אינו נוגע בסכימה. **WASH-17** — בורר המלאי **הוסר לחלוטין** מהזמנת כביסה (הכרעת אבי: המלאי הוא של המכבסה); במקומו תיאור חופשי רשות עד 20 תווים ל-`orders.notes` הקיים. ⭐ אומת ב-`grep` שההסרה **אינה מזיזה שקל ואינה מזיזה יחידה**: `b54RawOrderTotalAg` מדלגת על שורות כשהסוג `'כביסה'`, ו-`reservedQty`/`b48CommittedQty` סופרות רק `'השכרה'`. **שני שומרים:** הכפתור אינו מוצג · `addLineForm` דוחה קריאה ישירה · `create` על `order_lines` נדחה בשרת דרך `sPick(po.type, ORDER_TYPES)` כדי שסוג מלוכלך לא יעקוף (B64a). ⚠ אומת שהפורטל והחנות יוצרים תמיד `type:'השכרה'` ולכן די בנקודת כתיבה אחת. שורות היסטוריות נשארות **לצפייה בלבד** עם כותרת מפורשת. ⛔ ההשכרה לא נגעה — יש בדיקת רגרסיה. **WASH-19** — רשימת עגלות פנויות לצד הסריקה; ⛔ הסריקה לא הוסרה (שדה · Enter · מצלמה · כפתור ידני, בדיקה על כל אחד); ⛔ וירטואליות מוסתרות; **R8** — הבחירה מזינה את שדה הסריקה וקוראת ל-`floorAddCart`, כלומר עוברת את **אותה** בדיקת `nobleCartInfo`, והמסנן הוא בדיוק התנאי ש-`nobleIntake` אוכפת. **WASH-14** — `b40SplitLaundry` כתבה `stage: t.stage` גולמי, והילדה נולדה עם ערך שאינו מתאים לאף עמודה בלוח; עכשיו הערך הקנוני מ-`STAGES`, ושלב לא מוכר עובר `sVal` ולא נמחק. ⭐ סופח ממצא B69: `rLaundry` השוותה גולמי ולכן גם **משימות קיימות** נעלמו — תוקן, ועוד ארבע השוואות `stage` בממשק. **420 בדיקות, 0 כשלים** (25 חדשות; הבסיס הורץ לפני כל נגיעה והחזיר 395/0). ⛔ ארבע בדיקות B66 **עודכנו ולא נמחקו**. ⚠ **ממצא חדש שלא תוקן — `WASH-20`:** `b54RawOrderTotalAg` משווה `String(o.type)` גולמי, כלומר הזמנת כביסה עם סוג מלוכלך תחייב את שורותיה; **נקודת כסף, נעצר לפי R3 ונרשם לאצווה נפרדת.** ⚠ **שני הקבצים משתנים.** אין שינוי סכימה. (16.08.2026)
B69 — **`WASH-04`: מודול NOBLE עובר את כלל B64a.** ⚠⚠ **הספירה במסמך הביקורת הייתה שגויה** — היא מנתה ~20 אתרים; `grep` על הקוד החי מצא **91** (47 ב-NC1/NC2/NC3, 11 בגשרים `b49c`/`b49d`/`b54Ledger`/יתרת הזמנה, 33 בממשק). **⛔ אתרי הכסף — שבעה ולא שלושה**, וכל אחד אומת פעמיים: הורצה בדיקה מול הקוד **הישן** שהוכיחה שהוא נכשל, ומול החדש שהוכיחה שהוא עובר. הכשלים האמיתיים שנמצאו: `event_type` מלוכלך ב-`nobleWeighRollup` → **`total_charge` יוצא אפס**; `internal` מלוכלך ב-`nobleWeigh` → כביסה פנימית מחויבת או שהשקילה נחסמת; `status` מלוכלך ב-`nobleCreateInvoice` → הקליטה **לא נכנסת לחשבונית לעולם**; `ik.status` ב-`b54Ledger` → החיוב **לא נכנס לספר החיובים** (בשני הצדדים). **שלוש הכרעות:** (1) `sVal`/`sPick` ישירות ובלי שכבת עוזרים — שכבה כזו הייתה **דפוס שני** לאותה בעיה (R8), ו-B64a כבר קבע את הדפוס. (2) `sPick` רק ב-`nobleStageStart`, כי שם צריך את הערך **הקנוני**; בכל השאר ההשוואה היא שאלת השתייכות ולכן `sVal` מספיק ואין מצב `''` לטפל בו. (3) ⛔ **`stage` הפסיק להיכתב כהד של `auto_stage`** — עד היום הערך המלוכלך של המכונה נכתב לתוך `laundry_events.stage` ולתוך `laundry_intakes.status`, כלומר **הלכלוך התרבה**; מעכשיו נכתב הקבוע מ-`NOBLE_MACHINE_STAGES`, ושורת המכונה בגיליון לא נגעה. **גבול ההיקף:** חמש טבלאות — `laundry_intakes`/`laundry_events`/`intake_carts`/`carts`/`machines`; `laundry_tasks` נכלל רק היכן שפונקציית NOBLE מחליטה לפיו אם לשחרר עגלה. **347 בדיקות, 0 כשלים** (24 חדשות; הבסיס הורץ לפני כל נגיעה והחזיר 323/0). כל אתר נבדק בשלוש צורות לכלוך — רווח נגרר · `\u00A0` · `\u200F` — מול קו בסיס נקי; הצינור המלא נבדק מקצה לקצה כשכל שדה מלוכלך מחדש **בין כל שני שלבים**; ⛔ נבדק מפורשות ש**שום ערך מנורמל לא נכתב חזרה לגיליון**; ⛔ `sPick` שמחזיר `''` נדחה ולא נרשם אירוע ביומן; R6 אומת בשלושת המקורות. **⚠ שני ממצאים נפרדים דווחו ולא תוקנו** (מלכודת 4): מזהי מכונה אינם מנורמלים ב-`nobleOpenStarts` (מחלקת באגים של **מזהה**, לא רשימה קבועה) → `B72`; ו-`rLaundry` עם `t.stage===st` גולמי → `B70`. ⚠ **שני הקבצים משתנים.** אין שינוי סכימה. (08.08.2026)
B67 — **`BLD-16`: חוב שכר ברמת העובד.** ⭐ בקשת אבי, נוגעת בכסף שיוצא בפועל ולכן נבנתה לבדה. **שרת:** גוף `case 'payPayroll'` הוצא ל-`b67PayOneRow` וה-`case` קורא לה — בלי ההוצאה הזו הייתה נוצרת לוגיקת תשלום שנייה; שתי בדיקות רגרסיה מוכיחות התנהגות זהה, כולל ארבע הודעות הדחייה מילה במילה. פעולה חדשה `b67PayEmployee`: ⛔ ההקצאה כולה בשרת (פיצול בממשק אינו אטומי), סדר `month` עולה, רק רשומות שאינן טיוטה ועם יתרה>0, `min(יתרה, מה שנשאר)`. מנהל בלבד, ⛔ לא ב-`READ_ONLY_ACTIONS`. **ארבע מלכודות:** ⛔ טיוטה ישנה יותר מדולגת **עם אזהרה** ולא בשקט; ⛔ בדיקת מזומן **פעם אחת** מול הסכום המלא (בדיקה מוכיחה ש-8,000 מקופה של 8,000 עובר על שתי שורות); ⛔ R10; ⛔ סכום מעל החוב נדחה עם ציון הסך, בלי זיכוי. **⭐ שתי הכרעות ארכיטקטוניות:** (1) `B67_WRITE_DEFER` — `syncFutureExpenseForPayroll` כותבת בכל קריאה ואסור לגעת בה, ולכן `writeTable` צוברת בזיכרון ו-`b67FlushWrites` כותבת כל טבלה פעם אחת; **הרווח הגדול הוא אטומיות** — כשל באמצע זורק את הבאפר ו-`ensureSheet` לא נקראה אפילו פעם אחת (יש בדיקה). (2) ההקצאה באגורות שלמות בזיכרון ומומרת בחזרה — סכום ההקצאות שווה **בדיוק** לסכום ששולם; ⛔ האחסון נשאר בשברים, המעבר לאגורות הוא `WASH-10`. **ממשק:** לחיצה על שם העובד → תצוגת עובד עם **מספר אחד** בראש, שדה סכום (ברירת מחדל: מלוא החוב) וכפתור; מתחת כל חודשיו. ⛔⛔ **הכרעת אבי: השיוך שקוף** — אין מסך אישור לפני התשלום ואין כפתור תשלום בשורה; הפירוט נראה **אחרי**. ⚠ זה המקום היחיד במערכת שמראה עובד לאורך זמן. ⛔ אפס בקשות שרת נוספות; טופס התשלום הקיים ב-`payrollDetail` נשאר (R8). **טיוטות:** ⛔ **נגזרות ואינן נכתבות ל-`futureExpenses`** — הרגע שטיוטה נכנסת לטבלה היא נספרת בתזרים כהתחייבות אמיתית; כרטיס במסך השכר ובהוצאות הצפויות **בנפרד ולידו** ("טרם אושר, אינו נכלל בסך"), ⛔ `finPayrollTotal`/`fexpOpen` לא שינו ערך — יש בדיקה. ⛔ לנהג אין תשלום, אין סיכום טיוטות ואין תצוגת עובד. **300 בדיקות, 0 כשלים** (34 חדשות; הבסיס הורץ לפני כל נגיעה והחזיר 266/0). ⚠ **שני הקבצים משתנים.** אין שינוי סכימה. (03.08.2026)
B66 — **`BLD-15` + `BLD-03` + תיקון באג ייצור בסגירת הזמנת כביסה.** ⛔ נפתח ב**עצירה**: קובץ השרת בריפו היה ללא B65 (`bld01Heal` 0 תוצאות) בזמן שהממשק קרא לו — אבי העלה ופרס, ורק אז החלה בנייה. 🔴 **`BLD-05` ירד בפעם החמישית** (אבי ביקש לדלג על הבדיקה העצמית) — הכלי מוכן, חסרה רק ההרצה. **BLD-15:** `approveOrder` חסמה כל הזמנה בלי `orderLines`, בסתירה ל-C5 שמצהיר ששורות כביסה "אינן כסף ואינן נספרות" — המערכת דרשה נתון שהיא מתעלמת ממנו. עכשיו הפטור מותנה ב-`sPick(o.type, ORDER_TYPES)`. ⛔ השכרה בלי שורות נשארת חסומה; סוג לא מוכר מקבל את ההתנהגות המחמירה. **⭐ באג שני באותן עשר שורות:** `o.type === 'השכרה'` בהשוואה מדויקת — סוג עם רווח **דילג על בדיקת המלאי כולה בשקט**; תוקן. **תיקון #2 (הכרעת אבי, מממצאי ביקורת מקבילה, אומת עצמאית):** `B56_OPEN_INTAKE` הכיל `'בעבודה'` שלא נכתב לעולם והחסיר את ארבעת שלבי העבודה — הזמנת כביסה נסגרה **בזמן שהכביסה במכונה**. ⛔ לא הוספנו ערכים לרשימה; ההגדרה התהפכה ל-`B56_CLOSED_INTAKE='נמסר'` + `b56IntakeOpen()`, בדיקה שלילית שאינה יכולה להתיישן. ⭐ סופח שורש `BLD-01` — `b56CloseLaundryReturnLeg` עבר `sVal`. **BLD-03:** מפתח יחיד → `kiosk_token_hash_<n>` עד 10 טאבלטים עם שם ידידותי; ⛔ מרחיבים ולא מחליפים — הטאבלט הרשום ממשיך לעבוד כמכשיר 0; ⛔ ביטול תמיד פר-מכשיר; ⛔ hash לעולם לא יוצא ללקוח; `B52_KIOSK_ACTIONS` לא נפתחה. **266 בדיקות, 0 כשלים** (38 חדשות; הבסיס הורץ לפני כל נגיעה והחזיר 228/0). ⚠ **שני הקבצים משתנים — יש להדביק גם את קובץ השרת.** אין שינוי סכימה. (02.08.2026)
B64a — **תיקון: ערך טקסט מהגיליון עם רווח הפיל תפריטים בשקט.** ⚠ נמצא על ידי אבי בשטח. `STAGES.indexOf(t.stage)` ו-`kind==='איסוף'` השוו ערכים שנקראו מגיליון בהשוואת מחרוזת מדויקת — **רווח נגרר אחד** החזיר `-1`/רשימה ריקה, `advanceForm` יצאה **לפני** `openModal`, וכרטיס "בחר סיבה" לא צויר כלל. ⚠ **ההודעה שהוצגה הייתה שגויה** ("המשימה כבר בשלב מוכן") ולכן הבאג חי זמן רב. ⛔ שוחזר במעבדה לפני התיקון; ההשערה הראשונה (רענון אוטומטי) **הופרכה על ידי תשובת אבי** "לא קורה כלום בכלל". **התיקון:** `sVal()`/`sPick()` — ה-`dYmd()` של מחרוזות, אותה משפחה כמו B38b — **זהים תו-בתו בשני הקבצים** (יש בדיקה שמשווה אותם). מנקים רווחים, רווח קשיח `\u00A0` ותווי כיווניות `\u200E`/`\u200F`. ⛔ נרמול להשוואה בלבד, הגיליון אינו משתנה; ⛔ אסור על כסף ומספרים — יש בדיקה. ההודעה תוקנה ומציגה את הערך הבעייתי ואת מזהה הרשומה. **⭐ באג שני אגב האבחון:** `b58AfterRefresh` צייר מחדש כל 30 שנ' ונמנע רק כשמודל פתוח — כרטיס הסיבה יושב **בתוך מסך**, ולכן הרינדור מחק בחירה פתוחה ופירוט שהוקלד; נוספה הגנה על מיקוד בשדה. **192 בדיקות, 0 כשלים** (t08 חדש עם 17; שתיים נכשלו בבנייה וחשפו שהפורטל עדיין הדפיס ללקוח `"בייבוש "` — תוקן). ⚠ **שני הקבצים משתנים — יש להדביק גם את קובץ השרת.** אין שינוי סכימה. (01.08.2026)
B64 — **`T1` + `BLD-12` + `BLD-02` + `T2ב`: יצירה מהיומן, מע"מ בחנות ובפורטל, השבתת ספק שירות, סרגלי פעולות.** ממשק בלבד, אין שינוי סכימה, השרת לא נגע. ⛔ נפתח בעצירה: הבדיקה העצמית לא הורצה ואבי ביקש לדלג — **`BLD-05` ירד ל-B65**, כי המספר שמכריע את נקודת השבירה נמדד רק שם (R11). **T1:** `calEvents()` נמנתה — 7 סוגי אירועים ואפס מועדי תשלום; נוספו שני מקורות **נגזרים** — הוצאות עתידיות (מקור התזרים B43) ומועדי פירעון מ-`b54LedgerFE` (מקור דוח גיל החובות). ⛔ פירעון לקוח **מוצג ואינו נוצר** — הוא נולד מהחשבונית ומתנאי התשלום, ויצירה ידנית הייתה מייצרת שני מספרים סותרים לאותו חוב. ⛔ טסט/ביטוח/טיפול — מוצגים ואינם ניתנים ליצירה (הכרעת אבי; שדה בכרטיס הרכב). ⛔ אפס טפסים חדשים: ארבעת הטפסים הקיימים קיבלו פרמטר `iso` אופציונלי בסוף. כסף ביומן — מנהל/משרד בלבד. R10: אפס בקשות נוספו. **BLD-12:** 8 אתרי מחיר בשני ההקשרים, 6 הוחלפו; ⛔ תצוגה בלבד — הסכום לשרת נשאר נטו באגורות ויש בדיקה שסורקת את `shopSubmit`/`portalSubmit`; ⛔ כלל B45 נשאר בכל שאר המערכת (R4); מסגרת האשראי בפורטל נשארה נטו ומתויגת — היא מספר המנוע. **BLD-02:** ⚠ R9 — `active` והפעולה בשרת היו קיימים מ-B42, מה שחסר היה **מסך**; נבנתה רשימה בתוך רכש וספקים ולא מסך חדש (29 מסכים נשארו 29); השבתה אינה מחיקה. **T2ב:** הוחל על `doScan` ו-`loadApprovals`; ⚠ R9 — `itemForm`/`rShelves`/`floorRenderInfo` **לא טופלו בכוונה**: הספירות 11/11/10 נספרו ולא נקראו, ובפועל אלה כפתורי "＋" צמודי-שדה, רצועת `.rowact` קיימת, ומסך רצפת ייצור עם כפתורים גדולים לעבודה בכפפות. **175 בדיקות, 0 כשלים.** שכבה 2 לא הורחבה בכוונה — לא נגענו בשום יכולת דפדפן. (31.07.2026)
B63a — **הסרת הנעיצה מרצועת הצד**, בהוראת אבי. הוסרו מהקוד: `b62NavPin` ומשפחתו · `B62_PINS_KEY`/`mn_nav_pins` · `B62_PIN_SVG` · `grid-template-rows` · `.gbody`/`.gpin`/`.gcnt` · טענת שכבה 2 על שמירת הקיפול. הרצועה חזרה **בדיוק לדפוס B53/N2 שלפני B62**: כותרת קטגוריה = תווית לא-לחיצה, כל המסכים גלויים תמיד. הנימוק: הנעיצה נבנתה כדי להכניס 29 מסכים ל-596px, ואחרי B63 הרצועה מחזיקה 2–3 קטגוריות (13 מסכים) שנכנסות בנוחות — מנגנון בלי בעיה לפתור. נוספה בדיקה שסורקת את קוד המקור ונכשלת אם מישהו יחזיר אותו. **138 בדיקות, 0 כשלים.** (31.07.2026)
B63 — **התפריט הראשי עלה לשורה העליונה + רצועת צד נבחרת.** הכרעת אבי מול צילום מסך של NetSuite. ⚠ R9 — הממצא שהוזיל את האצווה: **התפריט העליון האופקי כבר היה בנוי ובדוק** (דפוס B47, `NAV_DD`/`.gmenu`, בייצור ב-900–1099px) ורק היה חסום מעל 1100px, כך שאבי ב-1422px מעולם לא ראה אותו — זה שחרור מנגנון, לא בנייה מאפס. **מ-900px:** שתי שורות sticky — מותג ומתחתיה תפריט; `#nav` הוצא מ-`#shell` לרוחב מלא. **מ-1100px:** רצועה (`#rail`) עם עד 3 קטגוריות שאבי בוחר בהגדרות (`nav_rail`, ברירת מחדל מכירות ומשלוחים · ייצור ומכבסה · מלאי ומחסן), בדפוס הקיפול של B62 — **בשינוי אחד: ברירת המחדל פתוח**, כי מסלול מהיר לא יכול לדרוש לחיצה נוספת. ⛔ **הרצועה היא קיצור דרך ולא הרשאה** — כל 29 המסכים נגישים מהתפריט העליון, שני הכיוונים נבדקים. R4: `navMode`/`navIsSide`/`navIsTop`/`NAV_MODE` לא שינו משמעות; מתחת ל-900px ומצב נהג לא נגעו (`#nav` שם `position:fixed`). מבטל את N1/N2/N3 מ-B53; שומר את T2/N3א על הרצועה. **142 בדיקות, 0 כשלים** — ⚠ השינוי הפיל 14 בדיקות קיימות בבת אחת, כולן נמצאו בהרצה ולא בקריאת קוד. שכבה 2: +טענת גלישת התפריט העליון. (31.07.2026)
B62 — `T2`+`BLD-04`: **סרגל צד מתקפל, קיצורים בגרירה, סרגלי פעולות.** ⛔ נפתח בממצא: `tests/` לא הייתה בריפו — שבעה קבצים בשורש בשמות מוחלפים ו-`t02` נעדר לגמרי; אומת בהרצה (66 במקום 87 = 21 חסרות) ונכתב מחדש. **T2/סרגל:** 1278px תוכן ב-596px זמינים (פי 2.14) — כלל אחד: **פתוחה אם נעוצה או אם המסך הנוכחי בתוכה**; הנפשה ב-`grid-template-rows` עם החלפת מחלקה בלי `renderNav` (בלי קפיצת פריסה); נשמר ב-`localStorage`, אפס בקשות שרת; ⛔ שום מסך לא נמחק מה-DOM. **T2/פעולות:** ⚠ R9 — 31 מ-38 סרגלי הכותרת ריקים; העומס ב-`openOrder` (14 בשורה שטוחה) בעוד `openDelivery`/`driverStopCard` כבר מיישמים את הדפוס. `openOrder` והשורה העליונה עברו ל-⋯ הקיים; אף כפתור והרשאה לא השתנו. ⚠ נתפסה ותוקנה רגרסיה: הסתרת ⋯ במצב נהג הייתה מוחקת לו את כפתור היציאה. **BLD-04:** גרירה ב-Pointer Events (לא HTML5 — לא נורה במגע) + `touch-action:none` + `insertBefore` בלי רינדור מחדש + חצים כמסלול חלופי; תקרה 6→10 עם `qmini` אוטומטי. **BLD-05 ירד ל-B63** (`ACT-05` לא אומת) ובמקומו נבנתה טענת מדידה שתכריע אותו. שכבה 2: +5 טענות. **129 בדיקות, 0 כשלים, 6 קבצים.** (31.07.2026)
B61 — `BLD-08`: **ספריית `tests/` קבועה בריפו** (harness + מריץ + 5 קבצים, **87 בדיקות, 0 כשלים**) במקום לכתוב רגרסיה מאפס בכל צ'אט ולאבד אותה — 330 בדיקות אבדו כך עד כה. מניעת ריקבון דרך `requires`: סמל שנמחק מהקוד מפיל את כל הקובץ ברעש. `node --check` קודם לכל בדיקה. `node_modules` ו-`package.json` **אינם** בריפו. **+ שכבה שנייה בהכרעת אבי:** כרטיס **"בדיקה עצמית"** ביומן פעולות (מנהל בלבד, **אפס בקשות שרת**) שמריץ 19 בדיקות בדפדפן האמיתי — היסטוריה, אחסון, מצלמה, הדפסה, מגע, אזור זמן, פריסה — ומייצר בלוק העתקה קצר. ✅ שלוש הוכחות נגדיות שהשומרים נכשלים כשמפרים אותם. ⚠ **לא אומת:** הכרטיס טרם רץ בדפדפן אמיתי — זה פריט האימות. R9: תוקנו 5 שמות מנגנונים ב-`R4` שלא קיימים בקוד. (30.07.2026)
B60a — תיקון בייצור + **כלל R11 (אין ניחוש)**. אבי: "אחרי 2 לחיצות האתר יצא לגוגל". ✅ אומת ב-grep: `history.back()` בענף עוגן הבסיס היה המסלול היחיד שהקוד עצמו הוציא בו את אבי — **נמחק לחלוטין**, ונוספה בדיקה שסורקת את קוד המקור ונכשלת אם מישהו יחזיר אותו. ✅ אומת: `b60HistInit` דחף רשומה בזמן טעינה בלי מחוות משתמש — **הוחלף ב-`replaceState` בלבד**. ⚠ **לא אומת** ולכן התיקון אינו נשען עליה: ההשערה ש-Chrome מסמן רשומות כאלה כ"לדלג עליהן". נבנה **יומן אבחון היסטוריה READ_ONLY** ב-sessionStorage (מוצג ביומן פעולות, שורד יציאה מהאתר) — הכלי שמאמת בדפדפן האמיתי במקום לנחש. 74 בדיקות, 0 כשלים. R10: אפס בקשות. (30.07.2026)
B60 — T3+T4: מצב מסך והיסטוריית דפדפן. **האצווה המאוחדת הראשונה.** ממשק בלבד, אין שינוי סכימה · T3: `REPORT_ID` לא אופס ב-`go()` — נכתב כלל אחיד בשלוש מחלקות (תת-מסך מתאפס · העדפת תצוגה נשמרת · הקשר מודל לא נוגעים), מרוכז ב-`B60_VIEW_RESET`; `UNDO_PENDING` חריג מפורש שלעולם לא מתאפס; נמצאו ותוקנו שתי מלכודות נוספות (`FLOOR_INTAKE_ORDER` · `RET`); `goFin` הועבר לעבור דרך `go()` · T4: `pushState`/`popstate` נבנו מאפס — "הקודם" מחזיר מסך, סוגר מודל, ולא יוצא מהאתר; הכתובת לא משתנה לעולם ולכן `replaceState` של הקיוסק לא נגע; מכובה לחלוטין בפורטל/חנות/קיוסק/מסך כניסה; אין נגיעה ב-`IDLE_LIMIT_MS` · החיכוך בין השניים נפתר בדגלים `keep`/`fromHistory` ב-`go(v,o)`. 60 בדיקות (28 T3 · 18 T4 · 8 הקשרי ריצה · 6 תפקידים), R7 באירועי DOM אמיתיים. R10: אפס בקשות שרת נוספו. (30.07.2026)
B59 — T5: קידום שלב לפי בחירה ולא לפי הסדר. השורש: לא היה בורר שלבים — בורר המכונה נראה כמוהו, ו-`advanceLaundry` חישבה `STAGES[idx+1]` קשיח · בורר "שלב יעד" עם תוויות השלב-הבא/דילוג/חזרה · `opts.to_stage` בשרת, ובהעדרו התנהגות B40 זההה ביט-אחר-ביט · מכונת אריזה ממופתת ל"בגיהוץ וקיפול" בלי עמודה חדשה · דרישת עגלה+מכונה של B40 לא נגעה. 85 בדיקות (39 שרת + 46 ממשק), כולל רגרסיה שמשחזרת את התקלה ובדיקת R10 (בקשה אחת לקידום; דילוג חוסך בקשה שלמה = 2.7 שנ׳). (30.07.2026)
B58 · אימות סופי (ACT-19 + ACT-18, 30.07.2026 19:07) — זהות מלאה 55/55, המסלול המהיר פעיל. `getAll` מלא ירד מ-~14 שנ׳ ל-**1.6 שנ׳**; `batchGet` 553 מ״ש מול 12.8 שנ׳ = **פי 23.2**. הגענו לרצפת התקורה של Apps Script (2.7 שנ׳ לבקשה) — כל שיפור עתידי חייב להיות צמצום **מספר** הבקשות (R10), לא אופטימיזציה של משקל. PERF-02 סגור.
B58a — תיקון אי-ההתאמה שאבחון ACT-17 גילה בנתוני אמת (4 טבלאות עם עמודות תאריך): קריאה בשני ייצוגים (`SERIAL_NUMBER` + `FORMATTED_STRING`) מזהה תא-תאריך בוודאות במקום לנחש לפי תבנית · `b58SerialYmd` באריתמטיקה טהורה · האבחון מצביע על התא המדויק (שורה·עמודה·ערכים·טיפוסים) · זהות מלאה מפעילה מחדש את המסלול המהיר לבד. נמדד אצל אבי: 486 מ״ש מול 13.3 שנ׳ — פי 27.3. 97 בדיקות, כולל רגרסיה שמשחזרת את התקלה ובדיקת R10 (בקשה אחת לקידום; דילוג חוסך בקשה שלמה = 2.7 שנ׳). (30.07.2026)
B58 — PERF-02 תיקון האיטיות: `batchGet` אחד במקום 55 נסיעות (`b58Prefetch`, עם אימות עצמי מול `readTable` פעם ב-6 שעות וכיבוי אוטומטי באי-התאמה) · מטמון פר-טבלה (`b58_table_versions`) בלי לשנות את סמנטיקת `checkVersion` · חיווי טעינה בשני מסלולי `autoLogin` · `pollDataVersion` מושך רק טבלאות שהשתנו (`b58Tables`) ואינו שולח דבר כשהלשונית ברקע. 80 בדיקות אוטומטיות, מטען זהה ביט-אחר-ביט לחמשת התפקידים. (29.07.2026)
B57 — PERF-01 אבחון האיטיות (READ_ONLY, בלי תיקון): `b57PerfProbe` — זמן ומשקל לכל טבלה · זמן שליפה/בנייה/סריאליזציה · מבחן ישיר האם המטען נכנס למגבלת CacheService · מוני hit/miss ב-`getAllCached` · מקצב דריסת המטמון מחושב מחותמות `audit_log` בלי לגעת ב-`bumpDataVersion`. הכרעת T4: הרענון תסמין, “הקודם” באג נפרד. (29.07.2026)
B65 — טופס הזמנה שמשנה צורה לפי הסוג · ריפוי הזמנות כביסה היסטוריות (BLD-01) · מד רוחב פסיבי שפותח את BLD-05 · sPick על אופן המסירה (01.08.2026)
B56 — רגל החזור של הזמנת כביסה נסגרת במסירה · רכב כסוג נכס שלישי בתקלות, בהפרדה מלאה מהמכבסה · דוח הצי קיבל תקלות ועלות תחזוקה (29.07.2026)
B53 — פריסת ניווט משולבת: `navIsSide()` 1100px+ · סרגל צד ימני קבוע עם כל המסכים (`.gcap` תווית, לא כפתור) · `#shell` כעוטף פריסה במקום grid על `#app` · קיצורי `nav_quick` מוגדרים בהגדרות בסימון תיבות · תפריט B47 נשאר לטווח 900–1099.
B52 — מסך קיוסק: הקשר ריצה רביעי `?kiosk=1` · טוקן מכשיר (hash ב-ScriptProperties) · `B52_KIOSK_ACTIONS` רשימה סגורה · `clockCore` כליבת נוכחות יחידה · הגבלת 5 ניסיונות/10 דק' · אבחון מספרים אישיים · **תיקון באג ההחתמה של B37**.
B51 — SLIM_POLICY לקיצוץ getAll (audit_log/call_logs/B55 לא נשלחות כלל; laundry 180י'; date_changes 365י'; notifications 90י' + ממתין/נכשל) · b51AuditPage עימוד שרת · b51ScrubAudit עם תצוגה מקדימה · b51PayloadStats. stock_moves/package_moves/fault_events/attendance/payroll **לא נחתכו** — יתרות והיסטוריה מצטברת.
B50 — דוחות שעות עבודה / ניצולת השכרות / צי רכב + עלות תחזוקה לפי חודש · exportCsv גנרי עם BOM (כולל תזרים B43) · הכרעת Y1: חלון השעות הקבוע במדד ניצולת המכונות בוטל.
B49f — אישור הנהלה שהנסיעה בוצעה: b49fCloseLegs מאחדת שלושה אתרי הכרעה · שאלה בשער הקליטה · trip_confirmed_by/trip_auto_cancel · b49fRestoreTrip לריפוי בדיעבד.
B55 — פורטל הלקוח המלא: פירוט חיובים · הודעת תשלום עם אסמכתא + אישור במשרד · שיק ממתין לפירעון · פניות מקושרות · חיוב ידני הפך לגָביל.
B54 — ספר החיובים: b54Ledger כמקור יחיד · טבלה charges · חמישה מקורות · C8 מוציא קליטה מקושרת מהמרוכזת · C9 מובטח בהגדרה · C10 תשלום נספר רק אם נגבה.
B49d — כביסה מלקוח: nobleIntake מקבל order_id · b49dAddCarts (L2, בלי תקרה) · פתיחת קליטה סוגרת את רגל ההלוך.
B49e — איסוף עצמי מול משלוח כשני מצבים עצמאיים · חתימה מופרדת (signatures) · P7 ביטול נסיעה שלא בוצעה · תיקון פד החתימה (הוקדם מ-B51)
C1–C3, D, F1, G1 — תשתיות מוקדמות: עמודות, אימיילים, מדפים/ברקודים, תיוג, טעינה עצלה, קיצוץ טקסטים ארוכים.
NC1–NC4 — מודול מכבסה: רצפה, לוח חי, חשבונית מרוכזת, יתרות.
B1 — חשבוניות/קבלות + מספור + הקצאה (S7) + PDF ל-Drive.
B2 — מנוע תשלום ואשראי (S1/S2/S5, D7/D9/D15), אגורות, idempotency.
B3 — פקדון/אימות אשראי + ערבויות (stub סליקה; credentials ב-ScriptProperties — S4).
B4 — התראות אימייל + טריגר יומי (b4RunNow).
B5 — Driver-Lite: מסלול, הסרת כתובת, תיעוד שיחות, סטטוסים.
B7/B9/B10 — superadmin · דוח גיל חובות + חריגים · מרכז אישורים.
B13 — חתימה דיגיטלית במסירה (Bug 1 פתוח) · B14 — BI · B16b — מסירה בצילום בלי לקוח.
B20 — כרטיס עובד מרוכז (openEmp) · B23 — תפריט מובייל.
B31 — הבטחת חשבונית · B34 — סריקת העמסה · B35 — מדפים ואצוות.
B36–B37 — שעון נוכחות, מספר אישי, בורר עובד למנהל.
B38 — אבטחה: סינון pin/password_hash, אימות PIN בשרת, hard-fail, ymdLocal/dYmd, בלוק "טרם שובץ נהג".
B39–B42 — אייקוני ציוד · פיצול כביסה · סגירת איסופים תקועים · תחזוקה, תקלות, ספקי שירות.
B43 — תזרים, מועדי פירעון, חובות לקוחות (b43ReceivablesFE).
B44–B45a — כלל מע"מ אחיד.
B46 — PDF אמיתי · אריחי עובד ורכב · photo_url.
B47 — תפריט עליון בדסקטופ · B48a — תיקון פתיחת החלוניות (evtInside).
B48 — מקור אמת אחד ליתרות לקוחות (b48BalancesAg) · פנוי-על-המדף להתראת מינימום · מסנן מחסן במלאי.
B49 — מרכז דוחות: קטלוג REPORTS_CATALOG, טופס פרמטרים אחיד, goReport לדריל-דאון.
B49b — קליטת מכבסה ללא עגלה פיזית: עגלה וירטואלית (carts.virtual, tare_kg=0), בלי שינוי בשקילה/שלבים/מסירה.
B49c — גשר לוח הכביסה ← יומן NOBLE: החזרה פותחת קליטה פנימית, קידום שלב כותב לשני היומנים, L3 (ready_ts).

## פתוח לטיפול (לפי עדיפות)

0000. 🔴 **`BLD-08` = B61 — בוצע 30.07.2026. פריט אימות אחד ויחיד:**
    **אבי נכנס ליומן פעולות ← "בדיקה עצמית בדפדפן" ← "▶ הרץ בדיקה עצמית".**
    ירוק ⇦ לא עושה כלום. אדום ⇦ "📋 העתק תוצאה" ומדביק בצ׳אט הבא.
    ⚠ **זו הפעם הראשונה שיהיו לי נתונים על הדפדפן והמכשיר של אבי** — סוג
    הדפדפן, גודל המסך בפועל, מצב הניווט (סרגל צד / עליון / מובייל) ואזור הזמן.
    **B62 (סרגל צד + נקודת שבירה 1024px) תלוי בנתונים האלה** — הוא נבנה היום
    על הנחות. ⛔ אין לבנות את B62 לפני שההרצה הזו חזרה.

000. 🔴 **`T4` — אימות פתוח בייצור. B60 נכשל, B60a נמסר לתיקון.**
    אבי דיווח שאחרי שתי לחיצות "הקודם" האתר יצא לגוגל. B60a מחק את
    `history.back()` וביטל את דחיפת הרשומה בטעינה. **חייב אימות בדפדפן אמיתי
    ובטלפון** — jsdom לא תפס את זה ולא יתפוס.
    **הכלי לאימות כבר בפנים:** יומן פעולות ← *אבחון היסטוריית דפדפן (B60a)*.
    אם זה ייכשל שוב — לא מנחשים, קוראים את היומן.
    ⚠ **מגבלה ידועה:** כרטיס שנפתח לפני שנווטת ולו פעם אחת — "הקודם" יצא
    מהאתר. אי אפשר ליירט "הקודם" שעוזב את המסמך.

000א. ✅ **`T3` = B60. בוצע 30.07.2026.** ממתין לאימות אבי (בדיקות 1 · 5 · 6).

00. ✅ **PERF-02 = B58 — תיקון האיטיות. בוצע 29.07.2026.** נותרו שני פריטי
    אימות שרק אבי יכול לסגור, ושניהם על **נתוני אמת**:
    (א) ✅ **סגור.** `ACT-17` (30.07) מצא תקלה בארבע טבלאות תאריך → תוקן ב-B58a.
    `ACT-19` (30.07, 19:07) אישר: **זהות מלאה 55/55, המסלול המהיר פעיל.**
    `batchGet` **553 מ״ש** מול **12.8 שנ׳** — **פי 23.2**.
    (ב) ✅ **סגור.** `ACT-18` — קו הבסיס החדש נמדד: `getAll` מלא **1.6 שנ׳**
    מהדפדפן. **הגענו לרצפת התקורה של Apps Script (2.7 שנ׳ לבקשה).**
    ⚠ **אין פריטים פתוחים ב-PERF-02. האצווה סגורה במלואה.**

00א. 🟡 **`BLD-11` — snapshot ליתרות מלאי. נדחה במכוון ב-B58.**
    `stock_moves` שוקלת אפס ואינה בעשרים הטבלאות הכבדות. **חוזרים אליה רק
    אם מדידה עתידית תראה שהיא נכנסה לצוואר** — לא לפני כן.

0. ✅ **B54 — ספר החיובים. בוצע 28.07.2026.** נותר פריט המשך אחד:
   (א) ✅ **נסגר ב-B55** — חיוב ידני עצמאי הפך לגָביל: `b55EnsureDocsForDebt`
   מפיק לו חשבונית לפני הגבייה, ובמסך הגביה יש כפתור "הפק חשבונית וגבה".
   `payments` לא שונתה ו-C7 נשמר.
   (ב) קליטה שנקשרה להזמנה **אחרי** שהופקה לה חשבונית — הסכום אינו כלול במסמך.
   `b54LedgerAudit` מדווח על מקרים כאלה; אם יימצאו — להכריע איך מטפלים.
0א. ✅ **B55 — פורטל הלקוח המלא. בוצע 28.07.2026.** נותרו שני פריטי המשך:
   (א) **חיבור ספק סליקה** — כשיהיה חשבון, ה-callback שלו קורא ל-
   `b55ConfirmDeclaration` עם אותו `txn_id`. תפר אחד, בלי שכתוב.
   (ב) `b55EnsureDocsForDebt` מפיק חשבוניות ל**כל** החוב הפתוח של הלקוח בעת
   האישור, לא רק לחלק שמשולם — זהה להתנהגות `b31EnsureInvoice` הקיימת.
   אם אבי ירצה הפקה סלקטיבית — batch קטן.
0ב. ✅ **B49f — אישור הנהלה שהנסיעה בוצעה. בוצע 28.07.2026.** נותר פריט המשך אחד:
   **דוח צי הרכב ב-B50 חייב לפלח לפי `trip_confirmed_by`** — נסיעה שהנהג הוכיח
   ונסיעה שההנהלה אישרה בדיעבד אינן אותו דבר, ואיחוד שלהן יאבד את כל הערך
   של ההפרדה שנבנתה כאן.
1. ✅ **B50 — דוחות חסרים + ייצוא CSV. בוצע 28.07.2026.** דוח הצי מפלח לפי
   `trip_confirmed_by` כנדרש; הכנסות דוח הניצולת נגזרות מספר החיובים (B54).
   נותרו שני פריטי המשך:
   (א) ✅ **נסגר ב-B56 (29.07.2026).** רכב הוא סוג נכס שלישי ב-B42, ודוח הצי קיבל
   עמודת תקלות ועמודת עלות תחזוקה. ⚠ **בהפרדה מלאה מתקלות המכבסה — הכרעת אבי.**
   (ב) דוח הניצולת מודד ניצולת **פריטים**, לא ניצולת יחידות ממוספרות (`units`).
   מספיק להחלטות רכש; אם יידרש דיוק ברמת יחידה — batch נפרד.
2. ✅ **B50 — הכרעת Y1 יושמה.** חלון השעות הקבוע במדד ניצולת המכונות בוטל;
   המדד נמדד מול יום העבודה של הצוות בפועל (`b50TeamDayWindow`).
3. ✅ **B51 — ביצועים ויומן הפעולות. בוצע 29.07.2026.** נותרו שלושה פריטי המשך:
   (א) **החיסכון בפועל לא נמדד על נתוני ייצור** — יש כפתור "📊 משקל הנתונים"
   במסך היומן. אחרי שאבי ילחץ, לרשום כאן את המספר האמיתי.
   (ב) **`attendance` ו-`payroll` לא נחתכו** ויגדלו בקצב קבוע. כשיתחילו להכביד,
   הפתרון אינו חיתוך לפי ימים — דוח השעות של B50 ומדד Y1 נשענים עליהן ישירות —
   אלא העברת שני הדוחות לפעולת שרת ייעודית. batch נפרד.
   (ג) **`stock_moves` תגדל ללא גבול ואי אפשר לחתוך אותה** כי יתרת המלאי היא
   סכום כל התנועות. הפתרון הנכון הוא **יתרת פתיחה תקופתית** (snapshot חודשי
   ב-`items` או טבלה חדשה) — batch בפני עצמו, נוגע בליבת המלאי.
4. ✅ **B51 — ניקוי `audit_log` מסיסמאות. נבנה 29.07.2026.**
   ⚠ **ממתין לביצוע בפועל ע"י אבי** — הכפתור קיים, ההרצה לא בוצעה.
   (Bug 1, חתימה בטאבלט, הוקדם ובוצע ב-B49e.)
5. ✅ **B52 — מסך קיוסק. בוצע 29.07.2026.** נותרו שלושה פריטי המשך:
   (א) **מכשיר יחיד בלבד** — `kiosk_token_hash` הוא מפתח בודד, ורישום שני דורס
   את הראשון. אם יידרשו שני טאבלטים (למשל שער נוסף) — מעבר למפתח לכל מכשיר
   (`kiosk_token_hash_<n>`) עם שם ידידותי. batch קטן.
   (ב) **תיקון נוכחות היסטורית מבאג B37** — נוכחות שנרשמה דרך בורר העובד לפני
   B52 שויכה למנהל במקום לעובד. אין דרך אוטומטית לזהות אילו רשומות — אבי צריך
   להחליט אם לתקן ידנית ואילו חודשים.
   (ג) **עובדים בלי מספר אישי אינם יכולים להשתמש בקיוסק.** האבחון בהגדרות מציג
   את הרשימה; מילוי המספרים הוא פעולה של אבי בכרטיסי העובדים.
6. ✅ **B53 — פריסת ניווט משולבת. בוצע 29.07.2026.** נותרו שלושה פריטי המשך:
   (א) **ששת המסכים הרחבים לא נבדקו בדפדפן** — `items`, `orders`, `finance`,
   `payroll`, `audit`, `reports` מאבדים 228px (198px מתחת ל-1280). אם אבי מדווח
   על גלישה אופקית — הפתרון הוא גלילה בתוך הכרטיס למסך הספציפי, לא הרחבת הסרגל.
   (ב) **מובייל וטווח 900–1099 לא שונו במכוון.** אם אבי ירצה את הסרגל גם בטאבלט
   בשכיבה (1024px) — זו הורדת נקודת השבירה, batch קטן, אבל צריך קודם לראות
   שהטבלאות נכנסות.
   (ג) **סדר הקיצורים** נקבע לפי סדר הסימון בפאנל. אם יידרש סידור מחדש בגרירה —
   batch קטן ונפרד.
7. ✅ **נסגר ב-B56 (29.07.2026).** מסירת הכביסה הנקייה סוגרת עכשיו את רגל החזור
   של ההזמנה (`b56CloseLaundryReturnLeg`) ואת ההזמנה עצמה (`b56CloseLaundryOrder`).
   ⚠ **ממתין לאימות אבי על נתוני ייצור:** הזמנות כביסה **היסטוריות** שנמסרו לפני
   B56 נשארו עם רגל חזור פתוחה — התיקון חל מכאן והלאה בלבד ואינו רטרואקטיבי.
   אם יתברר שיש הרבה כאלה, פעולת ריפוי אידמפוטנטית עם תצוגה מקדימה (כלל R1)
   היא batch קטן בפני עצמו.
8. (נבלע ב-B54 — בוצע.)
8א. **מ-B54:** קובצי הבדיקות אינם נשמרים בריפו, ולכן כל batch כותב את הרגרסיה
    מאפס. לשקול `tests.js` בריפו — יחסוך זמן וימנע אובדן כיסוי.
8ב. **מ-B56:** `technician_visits` (ביקורי טכנאי) נשאר **בתחום המכבסה בלבד** —
    לרכב אין "ביקור טכנאי" אלא מוסך, ותקלת רכב נסגרת עם עלות ישירות בלי ביקור.
    אם אבי ירצה יומן ביקורי מוסך — batch קטן ונפרד. **החלטה מכוונת, לא השמטה.**
8ג. **מ-B56:** הוצאות תחזוקת רכב נכנסות למנגנון ההוצאות הקיים דרך `b42CreateExpense`
    כמו כל תקלה, אבל ל-`expenses` עדיין אין שיוך ישיר לרכב. עלות התחזוקה בדוח הצי
    נגזרת מ-`fault_events`, לא מ-`expenses`. מספיק להיום; אם יידרש דוח הוצאות
    לפי רכב מתוך הנהלת החשבונות — צריך עמודה ב-`expenses` (שינוי סכימה).
9. כפתור השבתת ספק שירות (השדה `active` קיים, אין כפתור).
10. החלטה סופית: סדר תצוגה בחנות ובפורטל (נטו ראשי מול ברוטו ראשי).
11. חשבוניות מכבסה מרוכזות (`nobleCreateInvoice`) אינן מפיקות מסמך PDF כלל.
    **נסגר ב-B54:** כל חשבונית — מכל מקור — עוברת דרך נתיב מסמך אחד.
12. ✅ **נסגר ב-B50:** דוח "עלות תחזוקה לפי חודש" נבנה כרשומת קטלוג נפרדת
    (`maint`) ולא כדוח-משנה בדוח הצי — כי יומן התקלות מכסה עגלות ומכונות בלבד.
13. ✅ **נסגר ב-B54:** `customerBalance` נשארה כעטיפה דקה מעל הספר
    (`agIls(b48CustomerBalanceAg)`). לא הוסרה — היא נקראת מ-`case 'reports'`.
14. מסך `bi` לא עבר לדפוס הקטלוג של B49 — נשאר כך בכוונה (BUILD_PLAN B49 סעיף 3).
    לתיעוד בלבד, לא פריט לטיפול.

## מפרטים ממתינים
- ✅ **מפרט B54** — **בוצע 28.07.2026.** נשמר בסוף מסמך זה לתיעוד ההכרעות C1–C11.
- ✅ **מפרט B55** — **בוצע 28.07.2026.** נשמר בסוף מסמך זה לתיעוד ההכרעות P1–P7
  ולתיעוד שתי הסטיות המודעות ממנו (טבלה נפרדת להצהרות; סכום במקום allocations).
- ✅ **מפרט B49f** — **בוצע 28.07.2026.** נשמר בסוף מסמך זה. שני הסעיפים שהמפרט
  השאיר פתוחים לצ'אט המבצע הוכרעו ע"י אבי: עמודה חדשה (לא מיחזור `manual_intake_by`)
  וריפוי בדיעבד — כן, 30 יום. **העוגן השלישי (`b49dClosePickupAfterIntake`) טופל
  ע"י איחוד שלושת אתרי ההכרעה לפונקציה אחת, כפי שהמפרט הציע כחלופה.**
- ❌ **מפרט B49g — בוטל 28.07.2026.** נבלע ב-B54. שני הממצאים שלו נשמרו שם
  במלואם (חשבונית מרוכזת בלי סינון `order_id` · `orderTotal` סופר שורות כביסה).
  אין לבנות אותו בנפרד.
- `BUILD_PLAN.md` בידע הפרויקט: B53 — כתוב במלואו. **B50, B51 ו-B52 בוצעו.**
- ⚠ **לקח מ-B52, רביעי במספר:** סעיף "רענון עצמי" של B52 ב-`BUILD_PLAN` הציע
  להשתמש ב-`checkVersion` כדי לזהות גרסה חדשה — אבל `data_version` מתעדכן בכל
  כתיבה במערכת, לא בפריסה. המפרט תיאר כוונה נכונה במנגנון לא נכון. **תמיד לאמת
  מה הפונקציה שהמפרט מזכיר עושה בפועל.**
- ⚠ **לקח מ-B50, שלישי במספר:** סעיף 1 של B50 ב-`BUILD_PLAN` סתר את עצמו — הגדיר
  מדד "חדש" שהיה זהה למדד קיים, ורק הניסוח האחרון באותו סעיף היה הנכון. סעיף 4
  שלו הניח אולי שתקלות רכב נרשמות — הן לא. **מפרט תכנון הוא כוונה, לא מציאות.**
- ⚠ **לקח מ-B48, אומת מחדש ב-B49, ב-B49d ושוב בסשן התכנון של B54:** סעיפי
  "מצב קיים" במסמכי מפרט עלולים לתאר קוד שהתיישן, ולעיתים גם טריגר שאינו קיים
  בקוד כלל. תמיד לאמת את העוגנים מול הקוד החי לפני שבונים, ולעצור מול אבי
  כשנמצאת סתירה.
- ⚠ **תלות מחייבת מ-B48:** `b48BalancesAg` (שרת) · `custBalance` (ממשק) ·
  `b2CreditUsedAg` (מנוע האשראי) — שלושתם חייבים להחזיר אותו מספר.
  **ב-B54 שלושתם נגזרים מספר החיובים, והבדיקה האוטומטית של השוויון נשמרת.**
- כלל בדיקה מחייב מ-B48a: **כל בדיקת ניווט/קליק מדספאטצ'ת אירוע DOM אמיתי**
  (`MouseEvent`/`change` עם `bubbles:true`). ב-jsdom במצב `outside-only` מטפלי
  `on*` בתגית אינם מקומפלים — יש לחבר אותם כמאזינים אמיתיים לפני הדיספאטץ'
  (עוזר `wire()`). קריאה ישירה ל-handler אינה קבילה.
- **לב ההרחבה (הוכח ב-B50):** `REPORTS_CATALOG`/`REPORT_RENDERERS`/`repParamsForm` —
  דוח נוסף = רשומה בקטלוג + פונקציית render, בלי לגעת ב-`rReports`. ב-B50 נוספו
  ארבעה דוחות כך. הטופס תומך עכשיו גם בסוג שדה `employee` ובברירת מחדל (`def`).
- **ייצוא CSV (B50):** `exportCsv(filename, headers, rows)` היא הפונקציה היחידה.
  טבלה חדשה שרוצה ייצוא = `b50Reg(key, filename, headers, rows)` + `b50ExportBtn(key)`.
  **אין לשרשר נתונים לתוך `onclick`.**
- כיוון עתידי (לא מתוכנן): פיצול לאפליקציות ייעודיות לפי סיווג (הנהלה/נהגים/ייצור).

---

---

## מפרט B54 — ספר החיובים: מקור אחד לכל חיוב לקוח

נכתב 28.07.2026 בסשן תכנון על מודל חזק, אחרי חקירת כל שרשרת הכסף בקוד החי
(v4.45-B49d). **מחליף ומבטל את מפרט B49g** — שני הממצאים שלו נשמרו כאן.
**דחיפות: גבוהה — חשיפה כספית פעילה.**

### הכרעת אבי (28.07.2026) — מילה במילה

> "שיהיה רק מקום אחד שיירשמו בו חיובים ללקוח. לא רוצה כפילויות ובלבולים.
> לכל חיוב יהיה המקור של החיוב (כביסה/השכרה/חיוב חוסרים/משלוח/חיוב כללי).
> חיוב כביסה — רק אחרי שקילה או באמצעות הקלדה שלנו במשרד/הנהלה."

### הממצא: חמישה מקורות כסף, אף אחד לא יודע על האחרים

אומת בקוד החי, כולל מספרי שורות:

| מקור הכסף | מי מחזיק אותו | איך נכנס לחוב היום |
|---|---|---|
| שורות הזמנה (השכרה) | `order_lines.unit_price`, תמחור ב-`linePrice` (שרת 3594) | `orderTotal` (4275) → `b2OrderBalanceAg` (5445) → `b43Receivables` (6405) |
| שורות הזמנה (כביסה) | אותו מקום, `items.wash_price` | **אותו מסלול — ולכן חוב שווא** |
| דמי שינוע | `orders.delivery_fee` | נבלע בתוך `orderTotal`, בלי זהות משלו |
| חיוב חוסרים | `orders.shortage_charge` | נבלע בתוך `orderTotal`, בלי זהות משלו |
| כביסה לפי משקל | `laundry_intakes.total_charge`, נקבע ב-`nobleWeigh` (4699) | חשבונית מרוכזת `nobleCreateInvoice` (5010) → בלוק החשבוניות ב-`b43Receivables` |
| חיוב כללי / ידני | **אינו קיים כלל** | — |

**שני הממצאים של B49g, שנשמרים כאן:**
1. `nobleCreateInvoice` (5013) והמסך המקביל בממשק (שורה 4639) מסננים חמישה תנאים —
   לקוח · `internal !== 'כן'` · `status === 'נמסר'` · אין `invoice_id` · `total_charge > 0` —
   **ואין בהם `order_id`.** קליטת B49d עם `order_id` תיכנס לחשבונית המרוכזת גם אם
   ההזמנה חויבה. חיוב כפול.
2. `orderTotal` סופר `qty × unit_price` **בלי לבדוק סוג הזמנה**. כל הזמנת כביסה
   מאושרת = חוב שווא בכרטיס הלקוח, בגיל החובות, בתזרים, במסגרת האשראי ובפורטל.

**ממצא שלישי, חדש:** דמי השינוע וחיוב החוסרים אינם ניתנים לזיהוי בשום דוח —
הם מספר אנונימי בתוך `orderTotal`. אבי ביקש מקור לכל חיוב; זה חסר גם בהשכרה.

**ממצא רביעי:** אין שום מסלול להקלדת חיוב ידני. הדרך היחידה היום היא חשבונית
ללא הזמנה — כלומר לייצר מסמך רק כדי לרשום חיוב. זה בדיוק ה"טלאי" שאבי ביקש למנוע.

### עקרון-העל של המפרט הזה

**כסף לא נשמר פעמיים.** מקום אחד שממנו **קוראים** את כל החיובים, ומקום אחד
לכל חיוב שממנו הוא **נגזר**. טבלה חדשה שמעתיקה כסף שקיים כבר בטבלה אחרת
היא בדיוק המחלה שהכרעה S5 ו-B48 נועדו לרפא (drift בין שני מקורות). לכן:

> **persist רק את מה שאין לו טבלת אב. את השאר לגזור.**

### ההכרעות הארכיטקטוניות C1–C11 (מחייבות; אין לצ'אט המבצע שיקול דעת בהן)

**C1 — ספר אחד, פונקציה אחת.** `b54Ledger(db)` בשרת ו-`b54LedgerFE()` בממשק
הן **המקום היחיד** שבו חיוב לקוח מחושב. כל צרכן — חוב, גיל חובות, אשראי, תזרים,
כרטיס לקוח, פורטל, דוחות, חשבונית — קורא משם ורק משם. אין חישוב כסף מקומי בשום
מסך. `b43Receivables` נשארת בשם שלה כ**עטיפה** שמחזירה את שורות הספר עם יתרה
חיובית (חתימה זהה, כדי שלא לשבור את שמונת הקוראים שלה), אבל הלוגיקה עוברת ל-`b54Ledger`.

**C2 — חמישה מקורות, אוצר מילים סגור.** קבוע יחיד בשני הקבצים:
`var CHARGE_SOURCES = ['השכרה','כביסה','חוסרים','משלוח','כללי'];`
כל שורת חיוב נושאת בדיוק אחד מהם. אין מקור שישי בלי הכרעת אבי.

**C3 — persistence מינימלי: טבלה `charges` לחיוב ידני בלבד.**
טבלה חדשה, ורק לחיוב שאין לו טבלת אב — כלומר חיוב שהמשרד/ההנהלה מקלידים.
כל שאר החיובים **נגזרים** מהאב שלהם (`order_lines` · `orders.delivery_fee` ·
`orders.shortage_charge` · `laundry_intakes.total_charge`) ואינם משוכפלים לטבלה.
סכימה (להוסיף ל-`TABLES`, בסוף המערך של הטבלאות):
```
charges: ['id','customer_id','source','date','amount','description',
          'order_id','intake_id','invoice_id','status','created_by','created_at','cancel_reason']
```
- `source` — מתוך `CHARGE_SOURCES`. חיוב כביסה שהוקלד ידנית = `'כביסה'`;
  חיוב שאינו נופל לאף מקור = `'כללי'`.
- `amount` — **נטו בשקלים**, כמו כל עמודת כסף בכל 56 הטבלאות (עקביות לייצוא
  ולקריאה של אבי). החישוב עצמו באגורות דרך `toAg`/`agIls`, ככלל הברזל.
- `order_id` / `intake_id` — אופציונליים. חיוב ידני שקושר להזמנה ייכנס לחשבונית
  שלה; חיוב בלי קישור עומד בפני עצמו.
- `status` — `'פתוח'` / `'בוטל'`. **אין מחיקה** (אותו עקרון כמו חשבוניות).
  ביטול דורש `cancel_reason`.
- חיוב ידני **אינו** מפיק חשבונית מעצמו. הוא חיוב שממתין לחיוב/גבייה.

**C4 — חשבונית = מעטפת, לא חיוב.** חשבונית לעולם אינה מוסיפה כסף לספר. חוב =
Σ יתרות שורות החיוב, ובלבד. שורת חיוב שחויבה נושאת `invoice_id`; החשבונית קובעת
את זהות המסמך ואת מועד הפירעון של השורה.
**C4א — הקצאת סכום החשבונית לשורותיה.** `b2OrderBalanceAg` היום מחליף את בסיס
היתרה לסכום החשבונית (ברוטו, כולל מע"מ) כשקיימת חשבונית פעילה. ההתנהגות הזו
**נשמרת** — הלקוח חייב את סכום המסמך. בספר: סכום החשבונית באגורות מתחלק בין שורות
החיוב שלה **לפי חלקן היחסי בנטו**, והשורה האחרונה סופגת את שארית העיגול. כך
Σ השורות = `inv.total` בדיוק, והפילוח לפי מקור נשמר.
**C4ב — חשבונית מדור קודם ללא שורות ניתנות לגזירה** (חשבונית ידנית ללא הזמנה
וללא קליטות מסומנות) → הספר מייצר לה שורה סינתטית אחת `source:'כללי'`,
`origin_type:'invoice'`. **כך אין מיגרציה של נתונים היסטוריים ואין כסף שנעלם.**

**C5 — שורות הזמנת כביסה הן אומדן כמותי, לא כסף.** `orderTotal` לסוג `'כביסה'`
מחזירה `delivery_fee + shortage_charge` בלבד. `unit_price` בשורות **לא משתנה
ולא מאופס** — הוא נשאר כאומדן להצגה, מסומן בממשק "אומדן — החיוב לפי משקל".
**מכאן: אין תיקון רטרואקטיבי לנתונים.** ברגע שהמנוע מפסיק לספור את השורות,
החוב השווא ההיסטורי נעלם מעצמו. זו הסיבה שהמפרט הזה עדיף על אפשרות "לאפס
`unit_price`" שנשקלה ב-B49g: אפס כתיבה לנתוני עבר, אפס אובדן מידע.

**C6 — משלוח וחוסרים הם שורות חיוב עצמאיות.** לכל הזמנה עם `delivery_fee > 0`
נגזרת שורה `source:'משלוח'`; עם `shortage_charge > 0` — שורה `source:'חוסרים'`.
זה מה שסוגר את "דמי שינוע יתומים" (הבעיה שאפשרות 1א של B49g נכשלה בה) ומאפשר
פילוח הכנסות לפי מקור בדוחות B50.

**C7 — תשלומים נשארים כפי שהם. ההקצאה בתוך המקור דטרמיניסטית.**
`payments` **לא משתנה בסכימה ולא במיגרציה.** תשלום ממשיך לשאת `order_id`/`invoice_id`.
בתוך מקור אחד, סך התשלומים מוקצה לשורות החיוב שלו לפי הסדר הקבוע של
`CHARGE_SOURCES` (השכרה → כביסה → חוסרים → משלוח → כללי). הסדר שרירותי אך קבוע,
ומתועד בקוד. **Σ לכל מקור אינו משתנה** — ולכן זהות B48 נשמרת (C9).

**C8 — קליטה מקושרת להזמנה יוצאת מהחשבונית המרוכזת.** `nobleCreateInvoice`
(שרת 5010) והמסך המקביל (ממשק 4639) מסננים גם `!String(i.order_id||'').trim()`,
**או** `order_id` שמצביע על הזמנה שאינה קיימת/בוטלה. הקליטה המקושרת מחויבת דרך
חשבונית ההזמנה: `b1CreateInvoice` מרכיבה את שורות המסמך מ**כל** שורות הספר של
ההזמנה — השכרה/כביסה/משלוח/חוסרים/כללי מקושר. **אין שינוי סכימה בשביל זה**:
`laundry_intakes.order_id` כבר קיים (שרת 227). קליטה **פנימית** — ללא שינוי
(מסוננת ב-`internal !== 'כן'` כמו היום). קליטה **חופשית בלי `order_id`** — ללא
שינוי כלל, ממשיכה למרוכזת.

**C9 — זהות B48 נשמרת ונבדקת.** `b48BalancesAg` ו-`b2CreditUsedAg` **שתיהן**
נגזרות מ-`b54Ledger` (ולא אחת מהשנייה), וחייבות להחזיר אותו מספר לכל לקוח.
בדיקת השוויון הקיימת של B48 נשמרת כלשונה ומורחבת: fixture עם חמשת המקורות יחד.
⚠ אם אחרי המעבר מתגלה פער בין `b2CreditUsedAg` לפני ואחרי על נתוני אמת —
**עצור ודווח לאבי לפני מסירה. זה כסף.**

**C10 — תשלום נספר רק אם הוא נגבה.** כל קוראי `payments` היום סוכמים בלי לבדוק
`status` (אומת: שרת 4282, 5448, 5455; ממשק 934, 1793, 2246). B55 יוסיף שורות
תשלום במצב `'ממתין לאישור'`, ולכן B54 מקדים ומכניס עוזר יחיד:
`function b54PayCounts(p){ return String(p.status||'שולם') === 'שולם'; }`
(status ריק = נגבה, לתאימות היסטורית). **חובה grep על כל סכימת תשלומים בשני
הקבצים והעברת כולם לעוזר הזה.** בלי זה B55 יקטין חוב על תשלום שלא התקבל.

**C11 — מע"מ.** כלל B45 בעינו: שורות הספר נטו, המע"מ מחושב ברמת המסמך.
היחיד שמציג ברוטו הוא מסמך חשבונית/קבלה — ושורת ספר שכבר חויבה, לפי C4א.

### פעולות שרת חדשות
- `b54ChargeAdd {customer_id, source, amount, date?, description, order_id?, intake_id?}` —
  כתיבה, **מנהל + משרד**. ולידציה: לקוח קיים · `source` מתוך הקבוע · `amount > 0` ·
  `order_id` (אם ניתן) שייך לאותו לקוח. לא נכנס ל-`READ_ONLY_ACTIONS`.
- `b54ChargeCancel {charge_id, reason}` — כתיבה, **מנהל בלבד**. `status:'בוטל'`.
  חסום אם `invoice_id` מאוכלס (יש לבטל/לזכות את החשבונית קודם) — הודעה עברית ברורה.
- `b54LedgerAudit {}` — **קריאה** (נכנס ל-`READ_ONLY_ACTIONS`). דוח אבחון שאינו
  משנה כלום: הזמנות כביסה עם שורות מתומחרות וכמה חוב שווא הן ייצרו · קליטות עם
  `order_id` שנכנסו לחשבונית מרוכזת · חשבוניות ללא שורות ניתנות לגזירה (C4ב) ·
  תשלומים בלי `status`. זה מה שאבי מריץ אחרי הפריסה כדי לראות מה השתנה במספרים.

### צרכנים שחייבים לעבור לספר — רשימה מלאה (grep בוצע בסשן התכנון)
**שרת:** `b43Receivables` (6405) · `b48BalancesAg` (4309) · `b48CustomerBalanceAg` (4323) ·
`customerBalance` (4327) · `b2CreditUsedAg` (5461) · `b2OrderBalanceAg` (5445) ·
`b2InvoiceBalanceAg` (5453) · `b2ProcessPayment` (5580, מסלול קיזוז FIFO) ·
`b2PaymentContext` · `b9AgingReport` (6466+) · `case 'reports'` · תזרים (6750+) ·
תזכורות חוב (6108+) · `portalGetData` (2488) · `nobleCreateInvoice` (5010) ·
`b1CreateInvoice` (5324) · `orderTotal` (4275) · `orderBalance` (4285).
**ממשק:** `orderTotal` (934) · `orderPaid` (935) · `orderBalance` (936) ·
`custBalance` (1763) · `b43ReceivablesFE` (3814) · מסך הכספים ולשוניותיו (3673) ·
"חיובי מכבסה" (4637–4652) · כרטיס הזמנה — מקטע כספים (2323) · `b49dOrderIntakeHtml` (2064) ·
כרטיס לקוח — לשונית `finance` · `nobleSummaryData` (6063) · `renderNobleReports` (6238).
⚠ **הכלל: אחרי B54 אין בשום מסך `reduce` על כסף. יש קריאה לספר.**

### ממשק — מה אבי רואה
1. **מסך "כספים" → לשונית חדשה "חיובים"** לפני "תזרים". טבלה אחת: תאריך · לקוח ·
   **מקור** (צ'יפ צבעוני) · תיאור · סכום נטו (כולל מע"מ בכתב קטן, B45) · חשבונית ·
   שולם · יתרה · מועד פירעון. מסננים: לקוח · מקור · טווח תאריכים · רק פתוחים.
   סיכום עליון: סה"כ פתוח, ופילוח לפי מקור. **זה ה"מקום אחד" שאבי ביקש לראות.**
2. **כפתור "+ חיוב חדש"** באותה לשונית ובכרטיס הלקוח → מודאל: לקוח · מקור
   (בורר מ-`CHARGE_SOURCES`) · סכום נטו · תיאור · הזמנה לשיוך (אופציונלי).
   מנהל+משרד. אחרי שמירה — האריח מתרענן והחיוב מופיע מיד גם בכרטיס הלקוח ובפורטל.
3. **בחירה מרובה → "הפק חשבונית מהחיובים המסומנים"** — נתיב מסמך אחד (`b1CreateInvoice`
   מורחב) לכל המקורות, וכך נסגר גם פריט 11 (לחשבונית מכבסה לא היה PDF).
4. **כרטיס הזמנה, מקטע כספים** מוצג פר-מקור: השכרה/כביסה · שינוע · חוסרים · כללי,
   ותחתיהם "סה"כ לחיוב". בהזמנת כביסה: שורות הפריטים מסומנות **"אומדן — החיוב לפי משקל"**,
   ולצידן "החיוב בפועל" מהקליטה המקושרת (העיגון הקיים `b49dOrderIntakeHtml` 2064).
5. **כרטיס לקוח → לשונית כספים:** אותה טבלת חיובים מסוננת ללקוח. מספר אחד, בכל מקום.
6. **כפתור "בדיקת נתוני חיובים"** במסך ההגדרות (מנהל) → מריץ `b54LedgerAudit`
   ומציג את התוצאה **על המסך** כטבלה. זה מחליף את בקשת הייצוא מאבי (כלל R1):
   הוא לוחץ, רואה, ואם משהו חריג — פותח צ'אט ומספר. אין קובץ ואין מה לשמור.

### חמש השאלות (הגדלת ראש)
1. **איפה עוד נוגע:** כל הרשימה בסעיף "צרכנים" למעלה. בנוסף: דוח גיל החובות, דשבורד
   החריגים (ניצול אשראי), BI, תזכורות המייל של B4, ומסך הפורטל. `grep` על
   `orderTotal`, `total_charge`, `delivery_fee`, `shortage_charge`, `b43Receivables`
   בשני הקבצים — **חובה, ולדווח את רשימת אתרי הקריאה בתגובת ה-batch.**
2. **מה בכל שלב:** חיוב ידני על לקוח לא קיים → נדחה · סכום 0 או שלילי → נדחה ·
   ביטול חיוב שחויב → נדחה עם הסבר · לקוח בלי חיובים → מצב ריק "אין חיובים פתוחים ✓" ·
   לחיצה כפולה על "+ חיוב חדש" → `charges` בלי idempotency, לכן **מנעול UI**
   ו-`created_at`+`description` בבדיקת כפילות רכה (אזהרה, לא חסימה).
3. **עקביות:** צ'יפ מקור באותה שפת צבעים של המערכת · כלל מע"מ B45 · `ymdLocal`
   לכל השוואת תאריך · אגורות בחישוב, שקלים בגיליון.
4. **מי רואה:** לשונית חיובים — מנהל+משרד · "+ חיוב חדש" — מנהל+משרד ·
   ביטול חיוב — מנהל בלבד · לקוח בפורטל — החיובים שלו בלבד (B55) · נהג/מכבסה/רצפה —
   לא רואים כספים. **שלושת הקשרי הריצה חובה** (הפורטל והחנות עם `DB=null`).
5. **מה נשבר:** הסיכון המרכזי — מסך שמסכם כסף בעצמו וממשיך להציג מספר שונה.
   ההגנה: הכלל בסוף רשימת הצרכנים + בדיקת הצלבה אוטומטית שרת↔ממשק על אותו fixture,
   שדה-שדה, כמו ב-B48. סיכון שני: `b2CreditUsedAg` יזוז ויחסום הזמנות ללקוחות
   (C9 — לעצור ולדווח אם יש פער).

### בדיקות קבלה
- fixture עם חמשת המקורות על אותו לקוח → **מספר אחד זהה** בכרטיס הלקוח, בלשונית
  החיובים, בגיל החובות, בתזרים, במסגרת האשראי ובפורטל.
- הזמנת כביסה עם שורות מתומחרות + קליטה מקושרת שנמסרה → **חיוב אחד בלבד**
  (מהמשקל), דמי השינוע כשורה נפרדת, השורות מסומנות "אומדן".
- הקליטה המקושרת **אינה** מופיעה ב"חיובי מכבסה פתוחים" ואינה נכנסת למרוכזת.
- קליטה חופשית (בלי `order_id`) — **אפס שינוי**, נכנסת למרוכזת כמו היום.
- קליטה פנימית — **אפס שינוי**.
- הזמנת **השכרה** — אפס שינוי בכל החישובים, לפני ואחרי, על אותו fixture.
- חשבונית פעילה על הזמנה → Σ שורות הספר = `inv.total` בדיוק, כולל שארית עיגול (C4א).
- חשבונית היסטורית ללא הזמנה וללא קליטות → שורה סינתטית `כללי`, הכסף לא נעלם (C4ב).
- `b48BalancesAg` == `b2CreditUsedAg` לכל לקוח ב-fixture — הבדיקה של B48 עוברת כלשונה.
- הצלבת שרת↔ממשק: אותו fixture, שורות זהות שדה-שדה.
- חיוב ידני: נוסף · מופיע בשלושת המסכים · חויב בחשבונית · בוטל · ביטול אחרי חיוב נדחה.
- `b54LedgerAudit` מחזיר דוח על נתוני אמת בלי לשנות כלום; ריצה שנייה — תוצאה זהה.
- 288 הבדיקות של B49b/B49c/B49d/B49e נכתבות מחדש, מורצות, ועוברות כלשונן.

### מה אסור לגעת בו
`nobleWeigh` וחישוב `total_charge` (זה המקור, הוא נכון) · `customers.price_per_kg` ·
מנוע הסליקה/הפקדון (B3) · `b2ProcessPayment` **בזרימה** (רק מסלול הקיזוז עובר לספר,
ה-idempotency וה-`txn_id` וההפקת הקבלה — לא נוגעים) · `b41DriverHandled` ·
`b49dAddCarts`/`b49dResolveOrder` · `API_URL` · כלל מע"מ B45 · תפריט B47 ·
`READ_ONLY_ACTIONS` (רק `b54LedgerAudit` נכנסת) · **מחיקת נתונים היסטוריים —
אסורה לחלוטין; C5 מייתר אותה.**

**שינוי סכימה: יש — טבלה `charges`. להריץ `setupDatabase` פעם אחת.**
היקף: **גדול** (מנוע כסף) | מודל: **מודל חזק** | קבצים: שניהם.
### אבחון הנתונים — בלי לערב את אבי (כלל R1)

**אין לבקש ייצוא.** המפרט הזה תוכנן כך שאינו זקוק לו:
- **C5 מבטיח אפס כתיבה לנתוני עבר.** החוב השווא נעלם ברגע שהמנוע מפסיק לספור
  שורות כביסה — אין תיקון רטרואקטיבי, ולכן אין מה "לבדוק לפני" כדי לא להרוס.
- **C4ב מבטיח שאף שקל היסטורי לא נעלם** — חשבונית ישנה שאין לה שורות ניתנות
  לגזירה מקבלת שורה סינתטית `כללי`. הקוד עובד נכון על כל צורת נתונים שתימצא.
- מה שנשאר לדעת (כמה חוב שווא היה, אילו קליטות נגעו בחשבונית מרוכזת, אילו
  תשלומים בלי `status`) הוא **מידע, לא תנאי בנייה** — ולכן הוא נמסר כפעולת
  קריאה `b54LedgerAudit` + כפתור **"בדיקת נתוני חיובים"** במסך ההגדרות.
  אבי לוחץ אחרי הפריסה, רואה טבלה על המסך, ואם משהו נראה חריג — פותח צ'אט ומספר.
- ידוע מתיעוד קודם: ב-27.07.2026 `laundry_intakes` הכיל **0 רשומות** — מסלול
  הכביסה מעולם לא היה בשימוש בפועל. `b54LedgerAudit` יאמת זאת מול הנתונים החיים.
**הבדיקות נכתבות מול fixtures מלאכותיים שמכסים את כל הצורות** — כולל טבלאות
ריקות, הזמנת כביסה בלי קליטה, קליטה בלי הזמנה, וחשבונית יתומה.

---

---

## מפרט B55 — פורטל הלקוח המלא — בוצע 28.07.2026

⚠ שתי סטיות מודעות מומשו במקום המפרט המקורי — מתועדות במקטע B55 למעלה.

נכתב 28.07.2026, באותו סשן תכנון. **תלוי ב-B54 — אינו ניתן לבנייה לפניו**
(הוא מציג את ספר החיובים). הכרעת אבי:

> "לקוח תמיד יכול לראות מה החיובים שלו מתוך הקישור שלו, ויכול גם לראות פירוט,
> להוריד חשבוניות, לשלם חובות, לשלם חלק מחובות, ולשלוח פניה למשרד/הנהלה.
> אם אין משהו מתוך זה — צור אותו עכשיו. שכל דבר יהיה מקושר, בלי דברים יתומים."

### מה קיים ומה חסר (אומת בקוד החי)

| דרישה | מצב | הערה |
|---|---|---|
| רואה חוב | ✅ חלקי | `portalGetData` מחזיר `debt` — **מספר אחד מצטבר בלבד** |
| רואה **פירוט** חיובים | ❌ **חסר** | אין שורות. `b43Receivables` לא מגיעה לפורטל |
| מוריד חשבוניות | ✅ | `pgInvoices` עם `pdf_url` (20 אחרונות) |
| מוריד קבלות | ✅ | `pgReceipts` עם `pdf_url` (20 אחרונות) |
| **משלם חוב** | ❌ **חסר לחלוטין** | `portalPayDebt` (ממשק 7854) = מודאל עם קישור חיצוני וטלפון. לא נרשם, לא מקוזז, אין קבלה |
| **משלם חוב חלקי** | ❌ **חסר** | — |
| **פניה למשרד** | ❌ **חסר לחלוטין** | אין טבלה, אין מסך, אין התראה |
| מצב כביסה / הזמנות / מסגרת / פקדון | ✅ | NC2 · B2 · B3 — לא נוגעים |

### מציאות שחייבים לומר לאבי במפורש
**אין חשבון סליקה מחובר** (הכרעה D5 — מצב stub, `b3GatewayRedirectUrl` הוא נקודת
החיבור העתידית). לכן "הלקוח משלם בפורטל" **אינו יכול לחייב כרטיס היום**.
המפרט בונה את השרשרת כך שהיא נכונה עכשיו **ולא תיכתב מחדש** כשיתחבר ספק:

```
הלקוח בוחר מה לשלם  →  P1 הצהרת תשלום (payments, status='ממתין לאישור')
                        + התראה למשרד + הודעה ללקוח
                     →  P2 המשרד/ההנהלה מאשר בלחיצה  →  b2ProcessPayment
                        (אותו txn_id, idempotent) → חוב מקוזז + קבלה + מסגרת מתעדכנת
```
כשיתחבר ספק סליקה — ה-callback שלו קורא לאותה פונקציית אישור (P2) עם אותו
`txn_id`. **תפר אחד, לא שכתוב.** זו הסיבה ש-C10 (סינון `status`) נבנה כבר ב-B54.

### ההכרעות P1–P7

**P1 — הצהרת תשלום, לא תשלום.** `portalPayDeclare {code, allocations[], method_intent, note}`.
`allocations` = מערך `{charge_id, amount}` שהלקוח סימן, או `{mode:'all'}` /
`{mode:'partial', amount}` שהשרת מקצה FIFO לפי מועד פירעון. נוצרות שורות `payments`
עם `status:'ממתין לאישור'`, `type:'תשלום מהפורטל'`, `txn_id` שנוצר **בשרת**
(`genId('PTXN')`) ומוחזר ללקוח. **החוב אינו מוקטן** — C10 מבטיח שהשורות האלה לא
נספרות. ולידציות: סכום ≤ יתרת החיוב · סכום > 0 · לקוח פעיל · **הגבלת קצב**
(עד 5 הצהרות ל-10 דקות פר-לקוח ב-`CacheService`, דפוס `b38VerifyManagerPin`).
idempotency: הצהרה פתוחה זהה קיימת → מחזירים אותה, לא יוצרים שנייה.

**P2 — אישור בהנהלה, נקודה אחת.** `b55ConfirmPortalPayment {txn_id, method, note?}` —
כתיבה, **מנהל+משרד**. מוחקת את שורות ההצהרה וקוראת ל-`b2ProcessPayment` עם
אותו `txn_id`, אותו לקוח ואותה הקצאה → קבלה אחת, קיזוז FIFO, עדכון מסגרת, audit.
דחייה: `b55RejectPortalPayment {txn_id, reason}` → ההצהרה מסומנת `'נדחה'`
(לא נמחקת) + פניה אוטומטית ללקוח (P5) עם הסיבה. **אין הצהרה יתומה.**
⚠ העברת ההצהרה ל-`b2ProcessPayment` **חייבת** לעבור דרך המנוע הקיים ולא לשכפל
לוגיקת קבלה. אם מסתבר שהחתימה של `b2ProcessPayment` לא מכילה הקצאה לשורות ספר —
להרחיב אותה בפרמטר אופציונלי, **בלי לשנות את התנהגות ברירת המחדל.**

**P3 — מסך "אישורי תשלום מהפורטל"** — לשונית בתוך מסך הכספים (לא מסך חדש),
או שורה במרכז האישורים הקיים `b10ApprovalsInbox`. **הכרעה: מרכז האישורים** —
הוא בדיוק הכלי לזה, וכך אין דפוס שני. פריט = לקוח · סכום · מה סומן · אמצעי
מוצהר · תאריך · כפתורי אשר/דחה. התראת מייל לפי B4/S6.

**P4 — פירוט החיובים בפורטל.** `portalGetData` מחזיר `charges` = שורות ספר B54
של הלקוח (מקור · תיאור · תאריך · מועד פירעון · סכום · שולם · יתרה · קישור
לחשבונית אם קיימת). הפורטל מציג טבלה עם צ'יפ מקור, ובראשה "סה"כ לתשלום"
**שהוא בדיוק `debt` הקיים** — אותו מספר, שתי תצוגות.
⚠ **חובה:** להסיר את הכפילות שקיימת היום — `myOrders[].total` ו-`laundry[].charge`
מוצגים כסכומים לצד `debt`. אחרי B54 הזמנת כביסה אינה כסף, ולכן הפורטל
מציג **מקור אחד לכסף: טבלת החיובים.** ההזמנות והכביסה נשארות כמצב/מעקב בלבד.

**P5 — פניות.** טבלה חדשה:
```
customer_messages: ['id','customer_id','direction','subject','body','ref_type','ref_id',
                    'status','created_at','created_by','answer','answered_by','answered_at']
```
- `direction`: `'מלקוח'` / `'למשרד'`(תשובה). `ref_type`/`ref_id`: `'charge'`/`'invoice'`/
  `'order'`/`''` — כך פניה "למה חויבתי על זה?" **מקושרת לשורת החיוב**, לא יתומה.
- `status`: `'חדשה'` / `'בטיפול'` / `'נענתה'` / `'נסגרה'`.
- פעולות: `portalMessageSend {code, subject, body, ref_type?, ref_id?}` (הגבלת קצב
  כמו P1) · `b55MessageReply {message_id, answer}` (מנהל+משרד) · `b55MessageClose`.
- הלקוח רואה את השרשור והתשובה בפורטל. המשרד רואה במרכז האישורים/תיבה.
  התראת מייל ל-`notify_email` בפניה חדשה, ולפי `notify_pref` של הלקוח בתשובה.

**P6 — כפתור "פניה למשרד" בכל מקום רלוונטי:** ליד כל שורת חיוב, ליד כל חשבונית,
ובראש הפורטל כפניה כללית. `ref_id` מאוכלס אוטומטית — הלקוח לא מקליד מזהים.

**P7 — אין דבר יתום. הכלל המחייב של ה-batch:**
תשלום → מקושר ללקוח, לשורות חיוב, לקבלה, למסגרת האשראי, ל-audit.
הצהרה → מקושרת לתשלום או לדחייה מתועדת עם סיבה.
פניה → מקושרת ללקוח ולישות שעליה נשאלה, ונסגרת בתשובה.
כל אלה נבדקים בבדיקות הקבלה.

### חמש השאלות (הגדלת ראש)
1. **איפה עוד נוגע:** `portalGetData` (2488) · `portalPayDebt` (ממשק 7854) ·
   `renderPortal` (7816) · `b2ProcessPayment` (5580, פרמטר הקצאה אופציונלי) ·
   `b10ApprovalsInbox` · B4 (התראות) · מסך הכספים · `audit`.
2. **מה בכל שלב:** אין חוב → "אין חיובים פתוחים ✓" ואין כפתור תשלום · סכום מעל
   היתרה → נדחה · לחיצה כפולה → אותה הצהרה חוזרת (idempotent) · אין רשת → הודעה
   בלי אובדן הטופס · הצהרה שאושרה פעמיים → `b2ProcessPayment` מחזיר `duplicate`
   ולא מחייב שוב · פניה ריקה → נדחית.
3. **עקביות:** אותם צ'יפים ומקורות של B54 · B45 · אותו פורמט תאריך/מטבע ·
   הפורטל נשאר `DB=null`, כל נתון מגיע מ-`portalGetData`.
4. **מי רואה:** לקוח — רק את שלו, לפי `portal_code` · אישור תשלום — מנהל+משרד ·
   דחייה עם סיבה — מנהל+משרד · נהג/מכבסה/רצפה — לא. **שלושת הקשרי הריצה חובה.**
5. **מה נשבר:** הסיכון המרכזי — הצהרת תשלום שתיספר כתשלום ותקטין חוב על כסף
   שלא התקבל. ההגנה: C10 מ-B54 + בדיקה מפורשת שהחוב **אינו** משתנה בין הצהרה
   לאישור. סיכון שני: הרחבת `b2ProcessPayment` תשבור את מסלול התשלום במשרד —
   בדיקות התשלום הקיימות מורצות כלשונן.

### בדיקות קבלה
- לקוח עם חיובים מחמשת המקורות → הפורטל מציג את כולם, וסה"כ = `debt` = מסך ההנהלה.
- הצהרת תשלום מלא → החוב **לא** משתנה · פריט צץ במרכז האישורים · מייל נשלח.
- אישור → קבלה מופקת, החוב יורד בדיוק בסכום, המסגרת מתעדכנת, הקבלה מופיעה בפורטל.
- אישור פעמיים / רענון כפול → קבלה אחת (`duplicate`), חוב יורד פעם אחת.
- הצהרה חלקית של 30% מהחוב → קיזוז FIFO לפי מועד פירעון, יתרה נכונה.
- דחייה → אין תשלום, ההצהרה מתועדת, הלקוח רואה את הסיבה בפורטל.
- פניה על שורת חיוב → מקושרת ל-`charge_id`, המשרד עונה, הלקוח רואה תשובה.
- הגבלת קצב: 6 הצהרות/פניות רצופות → החמישית עוברת, השישית נחסמת בהודעה.
- קוד פורטל שגוי / לקוח לא פעיל → נדחה בכל הפעולות החדשות.
- שלושת הקשרי הריצה נטענים בלי חריגה; לפורטל לא מגיע שום נתון של לקוח אחר.

### מה אסור לגעת בו
`b2ProcessPayment` בהתנהגות ברירת המחדל · מנגנון הקבלות והמספור · B3 (פקדון) ·
`portal_code` ומנגנון הזיהוי · `API_URL` · ספר החיובים של B54 (קוראים בלבד) ·
`READ_ONLY_ACTIONS` (הפעולות החדשות כותבות, פרט לקריאת הפורטל).

**שינוי סכימה: יש — טבלה `customer_messages`. להריץ `setupDatabase`.**
היקף: **גדול** | מודל: **מודל חזק** | קבצים: שניהם.

---

---

## מפרט B49f — אישור הנהלה שהנסיעה בוצעה — בוצע 28.07.2026

⚠ **סדר: אחרי B54 ואחרי B55.** אינו תלוי בהם טכנית ואינו נוגע בכסף,
אבל B54/B55 קודמים בעדיפות (חשיפה כספית פעילה).

**הרקע.** B49e (P7) מבטל נסיעת איסוף שהנהג המשובץ לא תיעד בשטח
(b41DriverHandled = סריקה או צילום מהחשבון שלו). זה נכון ברוב המקרים,
אבל יוצר פער: נהג שנסע ואסף בפועל והטלפון שלו מת — הנסיעה מבוטלת,
הדלק והשעות נעלמים מדוח הצי, ואין דרך לתקן (נסיעה מבוטלת אינה
"פתוחה" ולכן גם b41CloseStalePickups לא מחזירה אותה).

**הכרעת אבי (28.07.2026).** בשער הקליטה, במקום שדה סיבה בלבד, תישאל
שאלה מפורשת: "האם נהג ביצע את הנסיעה בפועל?"
- כן → הנסיעה נסגרת 'בוצע' + תעודת משלוח, ומסומנת כאישור הנהלה
- לא → מבוטלת, כמו היום

**רשאים להצהיר: מנהל ומשרד.**

**עיקרון מחייב.** אין לשנות את b41DriverHandled ואת סמנטיקת
photo_by/scan_by (הכרעת B41b). ההצהרה היא שכבה נפרדת — הצהרה מתועדת,
לא ראיה. דוח הצי חייב להישאר מסוגל להפריד בין נסיעה שהנהג הוכיח
לבין נסיעה שההנהלה אישרה בדיעבד.

**עוגנים בקוד (אומתו מול v4.44-B49e).**
- שרת: b49eClosePickupsAfterReturn — הפונקציה שמכריעה בוצע/בוטל.
  שני אתרי קריאה: case 'receiveReturn' ו-מסלול הקליטה בשקילה.
  שניהם חייבים לקבל את אותו טיפול בדיוק.
- שרת: b41IntakeGate / b41IntakeState — שער הסיבה הקיים.
- ממשק: B41_REASONS · טופס קליטת ההחזרה.

**פתוח לתכנון בצ'אט המבצע.** איך נשמר סימון "אושר ע"י ההנהלה" —
עמודה חדשה על deliveries או שימוש ב-manual_intake_by הקיים;
והאם ניתן לתקן נסיעה שכבר בוטלה בעבר (ריפוי רטרואקטיבי).
להציג לאבי בהודעה אחת לפי חלק 4, ורק אחרי אישור לבנות.

⚠ נוגע ב-receiveReturn — תהליך ייצור קריטי. כל בדיקות הרגרסיה של
B49c ו-B49e נכתבות מחדש ומורצות לצד החדשות.

היקף: קטן-בינוני | מודל: Sonnet | קבצים: שניהם

⚠ **עוגן שלישי שנוסף ב-B49d (28.07.2026), חובה לכלול אותו בהיקף:**
`b49dClosePickupAfterIntake` בשרת מכריעה בוצע/בוטל **באותה סמנטיקה בדיוק**,
עבור רגל ההלוך של הזמנת כביסה שנפתחה לה קליטה. B49f חייב לטפל בשלושת
אתרי ההכרעה — `b49eClosePickupsAfterReturn` (שני קוראים) ו-
`b49dClosePickupAfterIntake` — או לאחד אותם לפונקציה משותפת אחת.
בממשק: הקריאה מגיעה מ-`floorSaveIntake` דרך `nobleIntake`, ולכן השאלה
"האם נהג ביצע את הנסיעה בפועל?" צריכה להישאל **בטופס הקליטה** ולא רק
בשער קליטת ההחזרה. `b49dLegState` כבר מציג את מצב הרגל לעובד — זו נקודת
העגינה הטבעית בממשק.

---

## מפרט B49d — בוצע 28.07.2026

המפרט מומש במלואו. שלושת סעיפיו:
(1) הזמנת כביסה שנאספת → קליטה עם `customer_id` ו-`price_per_kg` מכרטיס הלקוח —
בוצע, עם טריגר שנקבע מחדש בהכרעת אבי (ראה סעיף B49d למעלה);
(2) כפתור ייעודי לפתיחת קליטה בשימוש חוזר ב-`floorIntakeTab` — בוצע, בארבע נקודות כניסה;
(3) הוספה לקליטה פתוחה קיימת (L2) — בוצע ב-`b49dAddCarts`, בלי תקרה על מספר עגלות.

**הכרעות אבי L1–L3 נשמרות כתיעוד היסטורי** (L1 — שלוש נקודות כניסה לקליטת NOBLE;
L2 — קליטה אחת לאירוע עם אפשרות הוספה, בלי תקרה; L3 — כביסה פנימית נספרת בהגעה
ל"מוכן", כביסת לקוח רק במסירה). L3 מומש ב-B49c ולא שונה ב-B49d.

**הראיה מייצוא בסיס הנתונים (27.07.2026) נשמרת כתיעוד:**
`laundry_tasks`=3 · `laundry_task_events`=5 · `laundry_intakes`=0 · `intake_carts`=0 ·
`laundry_events`=0 — מסך רצפת הייצור מעולם לא היה בשימוש בפועל. שתי משימות נמצאו
בשלב "מוכן" בלי אף אירוע ובלי `cart_id`/`machine_id`; כל דוח חייב לסבול משימות
חסרות היסטוריה בלי ליפול. הבדיקות של B49c ו-B49d מכסות את המקרה הזה.
