# MAPA-NOBLE — מצב המערכת

## מצב נוכחי
canary ממשק: **v4.40-B48** | canary שרת: **v4.40-B48** | עדכון אחרון: 26.07.2026
שני הקבצים בריפו מסונכרנים עם הפרוס. אומת בסוף B48.

**מה עובד:** כל הליבה — הזמנות, מלאי רב-מחסני, משלוחים ומסלולי נהג (Driver-Lite B5),
החזרות ושקילה, מודול מכבסה מלא (NC1–NC4), רצפת ייצור, משימות, פגישות, פורטל לקוח,
חנות ציבורית, מנוע תשלום ואשראי (B2), פקדון/ערבויות (B3), התראות אימייל (B4),
חשבוניות עם מספור רציף ומספרי הקצאה (B1/B31), דוח חובות לפי גיל ודשבורד חריגים (B9),
מרכז אישורים (B10), BI (B14), מדפים (B35), שעון נוכחות ושכר (B36–B37), אבטחת שדות
רגישים (B38), תחזוקת ציוד ותקלות (B42), תזרים וחובות עם מועדי פירעון (B43),
סריקת העמסה מרובה (B44), כלל מע"מ אחיד (B45), מסמכים כ-PDF אמיתי ואריחי עובד/רכב (B46),
תפריט עליון בדסקטופ (B47) — **פתיחת החלוניות תוקנה ב-B48a**,
מקור אמת אחד ליתרות לקוחות ומסנן מחסן במסך המלאי (B48).

**מה שבור:**
1. 🟡 Bug 1: חתימה דיגיטלית בטאבלט לא נרשמת כראוי (מ-B13). → B51.

**אומת בפועל:** ✅ פתיחת חלוניות התפריט בדסקטופ (B48a) — אבי אישר 26.07.2026.

**ממתין לאימות אבי:**
1. ⚠ **הפעלת שירות Drive API בעורך Apps Script** (שירותים ← + ← Drive API) — בלי
   הצעד הזה תיקון ה-PDF ממשיך ליפול ל-HTML, עכשיו עם אזהרה גלויה.
2. ⚠ **הרצת `setupDatabase`** — B46 הוסיף `photo_url` ל-`employees` ול-`vehicles`.
   בלי זה העלאת תמונה תיכשל.
3. הרצת "תיקון מסמכים שנשמרו כ-HTML" במסך ההגדרות (אחרי 1 ו-2).
4. הפקת חשבונית חדשה ובדיקה ב-Drive שהקובץ הוא PDF אמיתי.
5. **אריחי מסך "עובדים"** — תמונה/אייקון, שם, תפקיד, צ'יפים; לחיצה פותחת כרטיס עובד
   (B46; נחסם עד B48a בגלל באג התפריט).
6. **העלאת תמונת עובד** מתוך כרטיס העובד (B46).
7. **אריחי מסך "צי רכב"** — תמונה/אייקון, לוחית, דגם, דגל טסט/ביטוח פג (B46).
8. **העלאת תמונת רכב** מתוך כרטיס הרכב (B46).
9. מראה החלונית הנפתחת בכמה רוחבי מסך, כולל הקטגוריות בקצה השורה
   (`navFixDD` רץ עכשיו בפעם הראשונה על חלונית שנשארת פתוחה).
10. **B48 — יתרת לקוח זהה בכל המסכים.** לפתוח לקוח עם חוב ולוודא אותו מספר
    בכרטיס הלקוח, במסך הגבייה, ב"יתרות לקוחות (גבייה)" שבדוחות, בדוח החובות
    לפי גיל ובפורטל הלקוח.
11. **B48 — מסנן המחסן במסך המלאי** ושינוי המספרים לפי המחסן הנבחר.
12. **B48 — התראת "מתחת למינימום"** מופיעה עכשיו גם על פריט שכולו מובטח
    להזמנה עתידית. ⚠ ייתכן שיופיעו פריטים חדשים ברשימה — זו ההתנהגות שנבחרה.

## שלושת ה-batches האחרונים

### B48 — מקור אמת אחד ליתרות לקוחות + ניקיון מלאי (26.07.2026)

**מה נבנה.**
*שרת:* `b48BalancesAg(db)` — מפת יתרות של כל הלקוחות בפאס אחד, נגזרת משורות החוב
הקנוניות של `b43Receivables` בתוספת איבר זיכוי. `customerBalance` הפכה לעטיפה דקה
מעליה (`agIls(b48CustomerBalanceAg(...))`) ואיבדה את האלגוריתם העצמאי שלה.
`case 'reports'`: ה-aging נגזר מהמפה; `lowStock` עבר ל-`b48FreeOnShelf`.
נוספו `b48CommittedQty` ו-`b48FreeOnShelf` ליד מנוע הזמינות.
*ממשק:* `custBalance` נכתבה מחדש כתאום מדויק של השרת (אגורות, מטמון על אובייקט
ה-DB); `b43ReceivablesFE` יושרה לסף של השרת; `b40ItemBreakdown` קיבלה פרמטר מחסן;
`rItems` קיבל בורר מחסן (`ITEMS_WH`) ודגל "מתחת למינימום" מתוקן; `openItemWhere`
מכבד את אותו מסנן.

**⚠ שני פערים ב-BUILD_PLAN שהתגלו בפתיחת הסשן ותוקנו בהתאם.**
1. סעיף "מצב קיים" תיאר שלושה אלגוריתמי חוב מקבילים. בפועל `b43Receivables(db)`
   **כבר היה קיים בשרת** כמקור אמת ברמת המסמך, ו-`b9AgingReport`, `b14Cashflow`
   ותזכורות B44 כבר נגזרו ממנו — כולל דליים לפי ימי איחור מעבר למועד הפירעון.
   לכן **לא** נבנתה `b48ReceivableRows` חדשה: היא הייתה יוצרת בדיוק את הכפילות
   ש-B48 בא לחסל. נותר צרכן אחד לא-מיושר, `customerBalance`, והוא זה שטופל.
2. הכרעה 5 הניחה מסנן מחסן קיים במסך המלאי — לא היה כזה. הבורר נבנה בסשן זה.

**החלטות שהתקבלו ולמה.**
- **איבר הזיכוי.** `b43Receivables` מחזירה יתרות חיוביות בלבד — שורה שלילית אינה
  חוב ואין לה מועד פירעון, ולכן אין לה מקום בדוח הגיל ובתזרים. אבל ביתרת הלקוח היא
  כן מקזזת. בלי האיבר הזה יתרת לקוח שקיבל זיכוי הייתה **עולה** — שינוי כסף שקט.
  עם האיבר, המספר נשאר זהה לחלוטין לקודם.
- **⚠ הכרעה 4 (הכסף) — נסגרה בבדיקה, לא בהנחה.** `b2CreditStatus` **אינו** נשען על
  `customerBalance`; `b2CreditUsedAg` הוא מימוש עצמאי באגורות. הוכח בבדיקה אוטומטית
  ש-`b48CustomerBalanceAg == b2CreditUsedAg` לכל לקוח, כולל לקוח עם זיכוי. **credit_used
  לא זז באגורה.** שתי הפונקציות מסומנות בקוד כתלויות זו בזו — אין לשנות אחת בלי השנייה.
- **הסף יושר ל-`> 0` אגורות** (במקום `> 0.01 ₪` בממשק). יתרה של אגורה בודדת נספרה
  בדוח הגיל ונעלמה מהתזרים — שורה שלמה שהופיעה במסך אחד ולא בשני.
- **הכרעת אבי: "מתחת למינימום" נמדד מול פנוי-על-המדף** — סה"כ פחות **כל** ההזמנות
  המאושרות/שסופקו, גם עתידיות. הכרעה זו **סוטה מ-BUILD_PLAN** (שהורה על מנוע
  הזמינות לתאריכי היום), באישור מפורש של אבי: מלאי שכבר התחייבנו עליו אינו זמין
  לרכש, ולכן ההתראה חייבת להתייחס אליו. נוסחת השרת (`b48FreeOnShelf`) זהה בדיוק
  למספר "פנוי" שהממשק כבר מציג בכרטיס הפריט — הצ'יפ האדום והמספר שלידו מסכימים.
- **מטמון על אובייקט ה-DB בממשק.** `custBalance` נקרא בלולאה על כל הלקוחות בחמישה
  מסכים. בלי מטמון היו נבנות כל שורות החוב מחדש לכל לקוח. כל רענון מחליף את `DB`
  באובייקט חדש ולכן המטמון נמחק מאליו — אומת שאין שום מוטציה במקום על הטבלאות האלה.
- **`b43ReceivablesFE` נשארה בממשק** (רצה על DB טעון, בלי קריאת שרת), ובמקומה נכתבה
  בדיקת הצלבה אוטומטית שדה-שדה מול השרת. זו מה שמונע סטייה עתידית.

**הגדלת ראש — איפה עוד זה נוגע (נבדק ב-grep, לא בהשערה).**
`customerBalance` בשרת התברר כבעל **קורא יחיד** (`case 'reports'`). בממשק ל-`custBalance`
תשעה אתרי קריאה: לוח בקרה (KPI חוב), מסך גבייה, כרטיס לקוח, מסך כספים, מסך לקוחות,
מחוון ניצול אשראי. כולם מקבלים את המספר המאוחד. הפורטל (`portalGetData`) ודשבורד
החריגים נשענים על `b2CreditStatusObj` — הוכח שהוא זהה, ולכן גם הם מיושרים בלי שינוי.
`b9AgingReport`, `b14Cashflow` ותזכורות B44 — כבר היו על השורות הקנוניות, לא נגענו.

**מה נבדק. 104 בדיקות אוטומטיות, כולן עברו.**
- 68 בדיקות שרת+הצלבה (`vm.createContext` עם Apps Script מדומה): fixture עם הזמנה
  משולמת חלקית, הזמנה עם חשבונית פעילה, חשבונית עצמאית (NC4), חשבונית מכבסה
  מרוכזת, טיוטה, לקוח שוטף+30, לקוח מזומן, לקוח עם זיכוי, ושני מחסנים.
  הושווה מול **עותק מדויק של האלגוריתם הישן** — יתרה זהה לכל לקוח.
  הצלבת שורות שרת↔ממשק: אותו מספר שורות ו-7 שדות זהים בכל שורה.
- 18 בדיקות jsdom על מסך המלאי — **כולן דרך אירוע `change` אמיתי עם `bubbles:true`**
  על הבורר, אף קריאה ישירה ל-handler (כלל B48a).
- 18 בדיקות הקשרי ריצה ותפקידים: צוות/פורטל/חנות נטענים בלי חריגה; חמשת התפקידים
  מריצים `rItems` ו-`custBalance`; מסד ריק לגמרי; לקוח שאינו קיים.
- `node --check` על שני הקבצים — עבר.

**מה לא נבדק.** דפדפן אמיתי · נתוני ייצור אמיתיים (המספרים אומתו מול fixture, לא מול
הגיליון) · השפעת המטמון תחת שימוש ממושך בלי רענון.

### B48a — תיקון חוסם: חלוניות התפריט לא נפתחו בדסקטופ (26.07.2026)
**מה נבנה.** פונקציית עזר אחת, `evtInside(e, selector)`, שמחליפה את הבדיקה
`e.target.closest(...)` בכל מאזין "סגור בלחיצה מבחוץ" ברמת ה-`document`.

**שורש הבאג (אומת מול הקוד החי).** `navToggle` מציב `NAV_DD` וקורא ל-`renderNav`,
שמחליף את `innerHTML` של `#nav` — **בתוך אותו קליק**. הכפתור שנלחץ מתנתק מה-DOM,
הקליק ממשיך להתפשט אל `document`, ושם המאזין של B47 בדק
`e.target.closest('#nav')` — שמחזיר `null` על צומת מנותק — ופירש זאת כלחיצה מחוץ
לתפריט. התוצאה: החלונית נפתחה ונסגרה באותו קליק.

**החלטות שהתקבלו ולמה.**
- **התיקון ברמת הדפוס, לא ברמת המסך.** `evtInside` נשענת על `composedPath()`,
  שנקבע בזמן הדיספאץ' ולכן משמר את שרשרת ההורים המקורית גם אחרי שה-DOM התחלף.
  נפילה חלופית לדפדפן ותיק: צומת שאינו `isConnected` אינו "לחיצה מבחוץ" —
  הניתוק עצמו הוא ההוכחה שהרינדור מחדש הוא שהזיז אותו.
- **נדחה: `stopPropagation` ב-`navToggle`** (הדפוס של `.more-wrap`). הוא היה מונע
  מהמאזין של `.more-wrap` לסגור תפריט ⋯ פתוח בעת ניווט בתפריט — רגרסיה שקטה.
  `evtInside` פותרת בלי לשנות את זרימת האירועים.
- **`.more-wrap` הועברה גם היא ל-`evtInside`** אף שאינה שבורה היום (`toggleMore`
  עוצר את ההתפשטות ואינו מרנדר מחדש) — כדי שהדפוס לא יחזור דרך הדלת האחורית.
- **לא נגענו במובייל ובמודאלים** — ראה "מה נבדק".
- **ממצא צדדי שנסגר בסשן:** בפתיחת B48a התגלה ש-`קוד שרת.txt` בריפו היה מלפני B46
  (הקוד הודבק ב-Apps Script אך לא הועלה לריפו). אבי העלה את הקוד הפרוס, ואומת בסוף
  הסשן שהריפו מכיל `b46DriveCaps` / `b46HtmlToPdf` / `b46FixPdfDocs` / שתי פעולות
  התמונה, ו-`photo_url` בשתי הטבלאות. **מסקנה לתהליך: קוד שרת שנמסר בסשן חייב לעלות
  לריפו באותו סשן — אחרת הסשן הבא בונה על קוד ישן.**

**הגדלת ראש — היכן עוד מופיע הדפוס.**
- **אקורדיון המובייל (`NAV_OPEN`)** — אותו `renderNav`, אותו ניתוק, אבל המאזין
  יוצא מוקדם ב-`if(!NAV_DD) return;` ו-`NAV_DD` ריק במובייל. **לא שבור** (אומת בבדיקה).
- **`.more-wrap`** — מוגן פעמיים: `toggleMore` קורא `ev.stopPropagation()`, ואין רינדור
  מחדש אלא החלפת class. **לא שבור** (אומת בבדיקה, לפני ואחרי).
- **סגירת מודאל** — `#modalBg` בודק `event.target===this` (זהות, לא `closest`),
  והאלמנט עצמו לעולם אינו מוחלף. **לא שבור** (אומת בבדיקה).
- **שלושת `closest` הנותרים** (כרטיס משימת כביסה, כרטיס משלוח, כרטיס החזרה) בודקים
  `closest('button,a')` על **תת-העץ של היעד עצמו**, לא על מיכל שהוחלף — ולכן חסינים
  מבנית לניתוק. לא שונו.

**מה נבדק.** 70 בדיקות jsdom על גוף העמוד האמיתי — **כולן דרך `MouseEvent` עם
`bubbles:true`**, אף בדיקה לא קוראת ל-`navToggle` ישירות (זו הבדיקה שפספסה את הבאג ב-B47).
מול הקובץ שלפני התיקון: 46 עברו / 24 נכשלו. אחרי: **70/70**.
כוסו: פתיחה, סגירה, מעבר בין קטגוריות, קליק על ה-SVG שבתוך הכותרת, קליק מבחוץ,
ניווט מתוך החלונית, Escape, שינוי רוחב חלון, כפתור "דוחות" (קטגוריה יחידה),
גבולות 899/900px, מובייל (אקורדיון + פאנל), ⋯, מודאל, ארבעת התפקידים בדסקטופ,
נהג (סרגל טאבים), שלושת הקשרי הריצה, ונפילה חלופית בדפדפן בלי `composedPath`.
`node --check` על בלוק ה-`<script>` — עבר.

**מה לא נבדק.** דפדפן אמיתי (jsdom אינו מחשב פריסה — `navFixDD` יוצא מוקדם כש-`width=0`,
ולכן מיקום החלונית בפועל, ובעיקר בקטגוריות שבקצה השורה, טרם נראה) · מסך מגע אמיתי.

### B46 — PDF אמיתי + אריחי עובד ורכב + תמונות (26.07.2026)
**מה נבנה.** (א) `invoiceSaveToDrive` נכתבה מחדש עם זיהוי יכולות: `b46DriveCaps()`
מזהה Drive v3 (`Drive.Files.create`) / v2 (`Drive.Files.insert`) / שירות כבוי, וההמרה
עברה לפונקציה משותפת `b46HtmlToPdf`. ה-fallback ל-HTML נשמר אך **מדווח על עצמו** דרך
`PDF_LAST_FALLBACK` → `pdf_fallback` בתשובה, והממשק מציג אזהרה חוסמת (`b46PdfWarn`).
(ב) `b46FixPdfDocs` — תיקון רטרואקטיבי באצוות של 30 עם `more:1`, אידמפוטנטי.
(ג) `photo_url` נוסף ל-`employees` ול-`vehicles`; פעולות `b46UploadEmpPhoto` /
`b46UploadVehPhoto`. (ד) `rStaff` ו-`rFleet` הומרו לדפוס האריח `.eqgrid`/`.eqtile`;
נוסף כרטיס רכב `openVeh`; תמונה בראש `openEmp`.

**החלטות שהתקבלו ולמה.**
- חתימת `invoiceSaveToDrive` לא שונתה (חמישה אתרי קריאה) — דיווח ה-fallback דרך דגל.
- התיקון הרטרואקטיבי הורחב גם לקבלות (`b2GetReceiptLink` משחזר מ-`payments`).
- חשבוניות מכבסה מרוכזות (`nobleCreateInvoice`, ללא `order_id`) מדולגות כ-`no_source`.
- האזהרה חוסמת ולא toast — הכישלון השקט הסתיר את הבאג חודשים.
- הפעולות החדשות ב-`MANAGER_ONLY`, אף אחת לא ב-`READ_ONLY_ACTIONS`.

**מה נבדק.** 94 בדיקות אוטומטיות (37 שרת ב-`vm.createContext` עם Drive מדומה בשלושת
המצבים; 57 ממשק ב-jsdom). אומת ש-`case 'update'` עושה מיזוג ולכן `saveEmp`/`saveVeh`
אינם מוחקים את התמונה.

**מה לא נבדק.** Drive אמיתי · המרת HTML→PDF בעברית RTL · מראה האריחים בדפדפן ·
תיקון רטרואקטיבי בהיקף אמיתי. כל אלה ממתינים לאימות אבי (ראה למעלה).

### B47 — תפריט ניווט עליון בדסקטופ (26.07.2026)
סרגל הצד (228px) הוחלף בשורת תפריט אופקית sticky עם חלוניות. 'דוחות' יצא לקטגוריה
עצמאית (הכנה ל-B49). מובייל ונהג — ללא שינוי. הוחלט: `NAV_DD` נפרד מ-`NAV_OPEN`;
פריטי קטגוריה תמיד ב-DOM (`.gmenu`). נבדק: 139 בדיקות jsdom — **אך כולן קראו
ל-`navToggle` ישירות ולכן פספסו את הבאג החוסם שתוקן ב-B48a.**

## היסטוריה (שורה לאצווה; פירוט מלא בכותרת קובץ השרת)
C1–C3, D, F1, G1 — תשתיות מוקדמות: עמודות, אימיילים, מדפים/ברקודים, תיוג, טעינה עצלה, קיצוץ טקסטים ארוכים.
NC1–NC4 — מודול מכבסה: רצפה, לוח חי, חשבונית מרוכזת, יתרות.
B1 — חשבוניות/קבלות + מספור + הקצאה (S7) + PDF ל-Drive.
B2 — מנוע תשלום ואשראי (S1/S2/S5, D7/D9/D15), אגורות, idempotency.
B3 — פקדון/אימות אשראי + ערבויות (stub סליקה; credentials ב-ScriptProperties — S4).
B4 — התראות אימייל + טריגר יומי (b4RunNow).
B5 — Driver-Lite: מסלול, הסרת כתובת, תיעוד שיחות, סטטוסים.
B7/B9/B10 — superadmin · דוח גיל חובות + חריגים · מרכז אישורים.
B13 — חתימה דיגיטלית במסירה (Bug 1 פתוח) · B14 — BI · B16b — מסירה בצילום בלי לקוח.
B20 — כרטיס עובד מרוכז (openEmp) · B23 — תפריט מובייל.
B31 — הבטחת חשבונית · B34 — סריקת העמסה · B35 — מדפים ואצוות.
B36–B37 — שעון נוכחות, מספר אישי, בורר עובד למנהל.
B38 — אבטחה: סינון pin/password_hash, אימות PIN בשרת, hard-fail, ymdLocal/dYmd, בלוק "טרם שובץ נהג".
B39–B42 — אייקוני ציוד · פיצול כביסה · סגירת איסופים תקועים · תחזוקה, תקלות, ספקי שירות.
B43 — תזרים, מועדי פירעון, חובות לקוחות (b43ReceivablesFE).
B44–B45a — כלל מע"מ אחיד.
B46 — PDF אמיתי · אריחי עובד ורכב · photo_url.
B47 — תפריט עליון בדסקטופ · B48a — תיקון פתיחת החלוניות (evtInside).
B48 — מקור אמת אחד ליתרות לקוחות (b48BalancesAg) · פנוי-על-המדף להתראת מינימום · מסנן מחסן במלאי.

## פתוח לטיפול (לפי עדיפות)
1. מרכז דוחות — B49.
2. דוחות חסרים: שעות עבודה, ניצולת השכרות, צי רכב + ייצוא CSV (כולל תזרים B43) — B50.
3. מדד ניצולת מכונות עם חלון קשיח 05:00–17:00 — להחליף לגזירה מהאירוע הראשון ביום
   (הכרעת אבי Y1) — B50.
4. ביצועים: `getAll` שולח את כל 55 הטבלאות במלואן — B51.
5. Bug 1: חתימה דיגיטלית בטאבלט — B51.
6. ניקוי גיליון `audit_log` מסיסמאות שנכתבו לפני B36 — B51.
7. מסך קיוסק — B52 (המפרט נכתב מחדש ב-BUILD_PLAN; החסימה הוסרה).
8. פריסת ניווט משולבת: שורה עליונה + סרגל צד ימני קבוע — B53.
9. כפתור השבתת ספק שירות (השדה `active` קיים, אין כפתור).
10. החלטה סופית: סדר תצוגה בחנות ובפורטל (נטו ראשי מול ברוטו ראשי).
11. דוח "עלות תחזוקה לפי חודש" מ-`fault_events` (מ-B42).
12. חשבוניות מכבסה מרוכזות (`nobleCreateInvoice`) אינן מפיקות מסמך PDF כלל. דורש
    החלטה: להפיק להן מסמך, ואם כן — מאיזה מקור נתונים (`laundry_intakes`).
13. **חדש (B48):** `customerBalance` בשרת נותרה בלי קוראים אחרי שה-aging עבר
    ל-`b48BalancesAg`. היא נשמרה בכוונה כנקודת הגישה הנקייה ללקוח בודד עבור B49–B50.
    אם B50 לא ישתמש בה — לשקול הסרה.

## מפרטים ממתינים
- `BUILD_PLAN.md` בידע הפרויקט: B49 · B50 · B51 · B52 · B53 — כתובים במלואם,
  כולל הכרעות ארכיטקטוניות. (B46 ו-B48 בוצעו; B48a לא היה במסמך ונוסף כתיקון חוסם.)
- ⚠ **לקח מ-B48 לצ'אטים הבאים:** סעיפי "מצב קיים" ב-`BUILD_PLAN` נכתבו מול תמונת
  קוד שהתיישנה. תמיד לאמת את העוגנים מול הקוד החי לפני שבונים, ולעצור ולדווח
  כשנמצא פער — במקרה של B48 היה נבנה מנגנון כפול לחלוטין ללא האימות הזה.
- ⚠ **תלות מחייבת שנוצרה ב-B48:** `b48BalancesAg` (שרת) · `custBalance`/`b48BalancesAgFE`
  (ממשק) · `b2CreditUsedAg` (מנוע האשראי) — שלושתם חייבים להחזיר אותו מספר.
  בדיקת השוויון קיימת בחבילת הבדיקות ואין למסור batch שמפר אותה.
- כלל בדיקה מחייב שנוסף ב-B48a: **כל בדיקת ניווט/תפריט מדספאטצ'ת `MouseEvent` אמיתי
  עם `bubbles:true`.** בדיקה שקוראת לפונקציית ה-handler ישירות אינה קבילה.
- כיוון עתידי (לא מתוכנן): פיצול לאפליקציות ייעודיות לפי סיווג (הנהלה/נהגים/ייצור).
