# MAPA-NOBLE — מצב המערכת

## מצב נוכחי
canary ממשק: **v4.58-B60** | canary שרת: **v4.57-B59** (לא נגע ב-B60) | עדכון אחרון: 30.07.2026

**מה עובד:** כל המערכת כפי שנמסרה עד B59, ובנוסף — לחיצה חוזרת על פריט בתפריט
מחזירה תמיד לתצוגה הראשית של המסך, ו"הקודם" בדפדפן מחזיר מסך במקום לצאת מהאתר.
**מה שבור:** אין באג פתוח ידוע שנפתח ב-B60.
**מה ממתין לאימות אבי:** T3 · T4 (B60) · T5 (B59) · ופריטי האימות מהאצוות הקודמות.

---

## ⚠ החלטת ניהול — 30.07.2026 · מיזוג התור ל-8 אצוות

**סשן תכנון בלבד. לא נגע בקוד. אין שינוי canary ואין שינוי סכימה.**

הכלל "פריט אחד לצ'אט" **בוטל**. 16 הפריטים שנותרו קובצו ל-**8 אצוות, B60–B67**,
לפי אזור קוד משותף. המספור B60–B75 מבוטל ומוחלף.

**מה שמאפשר את המיזוג — ואינו ניתן לוויתור:**
- **M3** — חמש שאלות הגדלת הראש נענות לכל פריט **בנפרד ובכתב**. אצווה שענתה
  "לכל האצווה יחד" נפסלת.
- **M5** — `PROGRESS.md` מפרט כל פריט בנפרד תחת אותו `B##`, כדי שאפשר יהיה
  לזהות מי שבר מה.
- **B61 = `BLD-08` (`tests/`) נבנה שני בתור**, לפני כל אצווה מאוחדת גדולה.
- **שינוי סכימה וסיכון גבוה אינם מתמזגים לעולם** — B66 ו-B67 נשארים לבד.

**המפה המלאה וכללי המיזוג M1–M6: `TASK_QUEUE.md` חלקים 9 ו-9א.**
⚠ **אין לפצל אצווה מאוחדת בחזרה לפריטים בלי הכרעת אבי.**

---

**✅ PERF-02 סגור סופית. `ACT-19` ו-`ACT-18` הורצו על נתוני אמת (30.07.2026, 19:07).**

> **זהות מלאה: 55 מתוך 55 טבלאות זהות ביט-אחר-ביט. המסלול המהיר פעיל.**

**המספרים הסופיים, על הגיליון האמיתי:**

| מדד | לפני | אחרי |
|---|---|---|
| `getAll` מלא, מהדפדפן | **~14 שנ׳** | **1.6 שנ׳** |
| קריאת כל הגיליונות בשרת | 12.8–16.6 שנ׳ (55 נסיעות) | **553 מ״ש** (`batchGet`) |
| יחס | — | **פי 23.2** |

**⚠ הגענו לתקרת הפלטפורמה, וזה הממצא החשוב ביותר לכל אצווה עתידית.**
קריאה שנייה **מיד** אחרי הראשונה לקחה 1.7 שנ׳ — כמעט זהה. הסיבה אינה מטמון
שלא עובד: **רצפת התקורה של Apps Script נמדדה אצל אבי ב-2.7 שנ׳ לבקשה**, גם
לזעירה ביותר. כל מה שהיה בשליטתנו ירד לאפס-פסיק-משהו:
בניית המטען **44 מ״ש** · סריאליזציה **0 מ״ש** · פענוח בדפדפן **1 מ״ש** ·
רינדור **9 מ״ש**. מטען: **49.2 KB** (מתוך 92.4 KB בגיליונות; `audit_log`
במשקל 44.4 KB אינו נשלח).
**המסקנה המעשית: אין יותר מה לסחוט מהמשקל. כל שיפור עתידי חייב להיות
צמצום מספר הבקשות — כלומר `R10`, ולא אופטימיזציה של נתונים.**

**ממצא שתועד ולא נפתחה עליו אצווה:** שיעור פגיעות המטמון **26.7%**
(4 פגיעות · 11 החטאות), ו-**47.4% מהפערים בין כתיבות קצרים מה-TTL של 120 שנ׳**
— כלומר כתיבה דורסת את המטמון הגלובלי לפני שהספיק לשרת. **זה כבר לא מזיק:**
החטאה עולה עכשיו 1.6 שנ׳ במקום 14, ובדיוק בשביל זה נבנה המטמון פר-טבלה של
B58 ②. חוזרים לזה רק אם מדידה עתידית תראה שזה נכנס לצוואר.

**B58a — תיקון שנספח לסשן אחרי שאבי הריץ את `ACT-17` (30.07.2026).**
האבחון עשה בדיוק את מה שנבנה בשבילו: תפס אי-התאמה **לפני** שהיא הזיקה,
כיבה את המסלול המהיר לבד, והמערכת המשיכה לעבוד במסלול הרגיל.

**B58 נגע בשני הקבצים ולכן שני ה-canary התיישרו ל-v4.55-B58.
אין שינוי סכימה ב-B58 — אין להריץ setupDatabase בגללו.**

**⚠ B58 = PERF-02: זהו התיקון עצמו. `getAll` בהחטאת מטמון ירד מ-55 נסיעות
לגיליון לקריאת `batchGet` אחת (נמדד בבדיקות: 55 → 1, ואפס `getValues`).
צפי: 11.9 שנ׳ → ~1 שנ׳, ועוד 2.2 שנ׳ תקורת Apps Script שאינה ניתנת להסרה.**

**⚠ פעולה נדרשת מאבי: להריץ פעם אחת *יומן פעולות ← קריאה מהירה (B58) ←
"בדוק זהות מטען ומדוד"*. זו ההוכחה על הנתונים האמיתיים ששני המסלולים
מחזירים בדיוק אותו דבר. אם תימצא אי-התאמה — המסלול המהיר נכבה לבד
והמערכת ממשיכה לעבוד כרגיל, בלי שבירה.**

**B57 נגע בשני הקבצים ולכן שני ה-canary התיישרו ל-v4.54-B57.
אין שינוי סכימה ב-B57 — אין להריץ setupDatabase בגללו.**

**⚠ B57 הוא אצוות אבחון בלבד. שום דבר במערכת לא הואץ בה.
התיקון עצמו הוא PERF-02 = B58, וההיקף שלו נגזר מהמספרים ש-B57 מציג.**

**B56 נגע בשני הקבצים ולכן שני ה-canary התיישרו ל-v4.53-B56.
אין שינוי סכימה ב-B56 — אין להריץ setupDatabase בגללו.**

**⚠ הכרעת אבי B56-א (29.07.2026) — הפרדה מלאה בין תקלות רכב לתקלות מכבסה.**
לשון אבי: *"אין קשר בין תקלות של רכב לתקלות של מכונות כביסה. תקלה של רכב
אמורה להיות מתועדת ב'רכב', לא במכונות כביסה."* ההפרדה אינה המלצה — היא כלל
מחייב. כל batch עתידי שנוגע ב-B42 חייב לשמור עליה: מסך `assets` מסונן
ל-`['מכונה','עגלה']`, מסך `fleet` ל-`['רכב']`, דרך `b42ScopeTypes`/`b42InScope`.
**אין להציג רשימת תקלות מאוחדת בשום מסך.**

**⚠ תקלת סנכרון שהתגלתה בפתיחת B51: `PROGRESS.md` של B50 מעולם לא הגיע לריפו —
מה שישב שם היה עדיין הגרסה של B49f. התוכן שוחזר ושולב כאן. הקובץ הזה מכיל את
B50 ואת B51 גם יחד. לקח: לוודא בכל סשן שגם `PROGRESS.md` נדרס, לא רק הקוד.**

**B50 היה ממשק בלבד. B51 נגע בשני הקבצים ולכן שני ה-canary התיישרו ל-v4.50-B51.
אין שינוי סכימה ב-B50 ואין שינוי סכימה ב-B51.**

⚠ **שינוי סכימה פתוח מ-B49f (לא מ-B50): שתי עמודות ב-`deliveries` —
`trip_confirmed_by` · `trip_auto_cancel`. אם `setupDatabase` טרם רצה מאז B49f —
יש להריץ אותה פעם אחת. B50 עצמו אינו דורש הרצה.**
⚠ אם `setupDatabase` של B49b/B49c/B49e טרם רצה — ההרצה הזו מכסה גם אותה:
`carts.virtual` · `laundry_tasks.intake_id` · `laundry_intakes.order_id` ·
`laundry_intakes.ready_ts` · `orders.ship_out` · `orders.ship_back` · טבלה `signatures`
· טבלה `charges` (B54) · טבלאות `payment_declarations` ו-`customer_messages` (B55) ·
**`deliveries.trip_confirmed_by` ו-`deliveries.trip_auto_cancel` (B49f)**.
כל העמודות נוספו בסוף המערכים — מיגרציה בטוחה.

**מה עובד:** כל הליבה — הזמנות, מלאי רב-מחסני, משלוחים ומסלולי נהג (Driver-Lite B5),
החזרות ושקילה, מודול מכבסה מלא (NC1–NC4), רצפת ייצור, משימות, פגישות, פורטל לקוח,
חנות ציבורית, מנוע תשלום ואשראי (B2), פקדון/ערבויות (B3), התראות אימייל (B4),
חשבוניות עם מספור רציף ומספרי הקצאה (B1/B31), דוח חובות לפי גיל ודשבורד חריגים (B9),
מרכז אישורים (B10), BI (B14), מדפים (B35), שעון נוכחות ושכר (B36–B37), אבטחת שדות
רגישים (B38), תחזוקת ציוד ותקלות (B42), תזרים וחובות עם מועדי פירעון (B43),
סריקת העמסה מרובה (B44), כלל מע"מ אחיד (B45), מסמכים כ-PDF אמיתי ואריחי עובד/רכב (B46),
תפריט עליון בדסקטופ (B47) — פתיחת החלוניות תוקנה ב-B48a,
מקור אמת אחד ליתרות לקוחות ומסנן מחסן במסך המלאי (B48),
מרכז דוחות (B49), עגלה וירטואלית (B49b), גשר לוח הכביסה↔יומן NOBLE (B49c),
איסוף עצמי ומשלוח כשני מצבים עצמאיים + חתימה מופרדת מהמשלוח (B49e),
כביסה מלקוח: הזמנת `כביסה` → קליטת NOBLE (B49d),
ספר החיובים — מקור אחד לכל חיוב לקוח, עם מקור לכל חיוב (B54),
פורטל לקוח מלא: פירוט חיובים · הודעת תשלום עם אסמכתא · פניות מקושרות (B55),
אישור הנהלה שהנסיעה בוצעה בפועל + ריפוי בדיעבד (B49f),
שלושה דוחות חדשים — שעות עבודה · ניצולת השכרות · צי רכב — בתוספת עלות תחזוקה
לפי חודש, ייצוא CSV מכל טבלת דוח וממסך התזרים, וביטול חלון השעות הקבוע
במדד ניצולת המכונות (B50),
**קיצוץ מטען `getAll` לפי `SLIM_POLICY` · יומן פעולות בעימוד שרת · ניקוי סיסמאות
היסטוריות מ-`audit_log` · אבחון משקל הנתונים על המסך (B51).**
**מסך קיוסק — הקשר ריצה רביעי `?kiosk=1`: טאבלט משותף שמחתים נוכחות במספר אישי,
בלי סשן ובלי DB, מאומת בטוקן מכשיר (B52).**
**פריסת ניווט משולבת: שורה עליונה עם קיצורי דרך מוגדרים + סרגל צד ימני קבוע
שמציג את כל המסכים מ-1100px ומעלה (B53).**

**מה שבור:**
1. ✅ **חיוב כפול/שווא ללקוח — תוקן ב-B54.** ארבעת הממצאים נסגרו: (א) שורות הזמנת
   כביסה אינן נספרות כסף (C5) → אין יותר חוב שווא; (ב) `nobleCreateInvoice` מסנן
   `order_id` (C8) → אין חיוב כפול; (ג) דמי שינוע וחיוב חוסרים הם שורות חיוב
   עצמאיות עם זהות משלהן (C6); (ד) קיים מסלול להקלדת חיוב ידני (`charges`, C3).
   ⚠ **ממתין לאימות אבי על נתוני אמת** — כפתור "בדיקת נתוני חיובים" בהגדרות.
2. ✅ **הפורטל — תוקן ב-B55.** הלקוח רואה פירוט חיובים מלא, מודיע על תשלום
   (סכום + אסמכתא), ושולח פניות מקושרות. `portalPayDebt` הוסר לחלוטין.
   ⚠ **מגבלה מוצהרת:** אין חשבון סליקה מחובר, ולכן כרטיס אשראי אינו מחייב
   בפועל — הכפתור פותח את `payment_link` ויוצר הודעת תשלום לאימות המשרד.
   כשיחובר ספק, ה-callback שלו יקרא לאותה `b55ConfirmDeclaration`. תפר אחד.
3. ✅ Bug 1 (חתימה בטאבלט) — **תוקן ב-B49e**, הוקדם מ-B51. ממתין לאימות על טאבלט אמיתי.
4. ✅ **באג שקט מ-B37 — תוקן ב-B52.** הממשק שלח ל-`clock` את `employee_id`
   (הבורר שנוסף ב-B37), אך `case 'clock'` בשרת קרא רק `p.pin` — ולכן **מנהל
   שבחר עובד מהרשימה החתים בפועל את עצמו**, בלי שום הודעת שגיאה.
   ⚠ **נוכחות היסטורית שנרשמה כך שגויה** — אם אבי השתמש בבורר, יש לתקן ידנית
   (נוכחות ← רישום ידני / פסילת שעות).

**ממתין לאימות אבי:**
0. ⭐ **חדש (B52) — טאבלט קיוסק.** מיתוג והגדרות ← "טאבלט קיוסק" ← רישום →
   לפתוח את הקישור בטאבלט → הקשת מספר אישי → כניסה/יציאה. פירוט במקטע B52 להלן.
0**a**. 🔴 **B52 — לוודא שהבורר בשעון הנוכחות מחתים את מי שנבחר.** זה הבאג
   שתוקן; לבדוק בפועל: נוכחות ← בחירת עובד אחר ← החתמה ← השם שמופיע.
0*. ⭐ **חדש (B51) — מסך יומן הפעולות נטען עכשיו מהשרת בעמודים.** הפילטרים זהים;
   מה שהשתנה הוא שהיומן כבר לא מגיע בכל רענון. לבדוק שהמסך נטען, שהסינון עובד
   ושכפתור "טען עוד" מוסיף שורות.
0**. ⭐ **חדש (B51) — "כלי תחזוקה" במסך היומן (מנהל בלבד):**
   (א) **📊 משקל הנתונים** — מודד כמה שוקל `getAll` לפני ואחרי הקיצוץ, טבלה-טבלה.
   זה המספר שאומר אם ה-batch עשה את שלו. הפעולה קוראת את כל הגיליונות ולכן
   לוקחת כמה שניות — זה תקין.
   (ב) **🧹 ניקוי סיסמאות היסטוריות** — קודם תצוגה מקדימה ("נמצאו N"), ורק אז
   כפתור ביצוע. ריצה שנייה חייבת להחזיר 0.
0***. 🟡 **B51 — מסכי היסטוריה מציגים עכשיו "מוצגים N הימים האחרונים".** יומן
   אירועי המכבסה ויומן שלבי המשימה מוגבלים ל-180 יום ברענון, שינויי מועד ל-365,
   התראות ל-90 (ממתינות ונכשלות מוצגות בכל גיל). הנתונים בגיליון לא נגעו.
0×. ⭐ **חדש (B50) — ארבעה דוחות במרכז הדוחות:** שעות עבודה (מנהל בלבד) ·
   ניצולת השכרות · צי רכב · עלות תחזוקה לפי חודש. פירוט הבדיקות במקטע B50 להלן.
0××. ⭐ **חדש (B50) — כפתור ⬇ ייצוא** בכל טבלת דוח ובשתי הטבלאות של מסך התזרים.
   ⚠ לוודא שהקובץ נפתח באקסל עם עברית תקינה (BOM) ושהסכומים ניתנים לסכימה.
0×××. 🟡 **B50 — מדד ניצולת המכונות השתנה.** האחוזים שאבי ראה עד היום ישתנו:
   החלון הקבוע בוטל והמדד נמדד מול יום העבודה של הצוות בפועל (הכרעת Y1).
   שינוי מכוון, לא באג.
0. 🔴 **B49f — שאלת "האם נהג ביצע את הנסיעה בפועל?" בשלושת מסלולי הקליטה,
   ורשימת "נסיעות שבוטלו בקליטה" במסך המשלוחים.** פירוט במקטע B49f להלן.
0א. 🔴 **B55 — כל פריטי האימות בסוף מקטע B55 להלן.**
1. 🔴 **הרצת `setupDatabase`** — חובה אחרי B49f (שתי עמודות ב-`deliveries`;
   מכסה גם את B54 ו-B55 אם טרם רצה).
2. ⚠ **הפעלת שירות Drive API** בעורך Apps Script (שירותים ← + ← Drive API).
3. הרצת "תיקון מסמכים שנשמרו כ-HTML" במסך ההגדרות (אחרי 1 ו-2).
4. 🔴 **חדש (B54) — "בדיקת נתוני חיובים"** במסך ההגדרות. זו הפעולה שמראה מה
   השתנה במספרים על נתוני האמת. כלל R1 — אין ייצוא, הכל על המסך.
5. ⭐ **חדש (B54) — לשונית "חיובים"** במסך הכספים: כל חיוב, עם המקור שלו.
6. ⭐ **חדש (B54) — "+ חיוב חדש"** בלשונית החיובים ובכרטיס הלקוח (מנהל/משרד).
7. ⭐ **חדש (B54) — "הפק חשבונית מהחיובים המסומנים"** בלשונית החיובים.
8. ⭐ **חדש (B54)** — בכרטיס הזמנת כביסה: שורות הפריטים מסומנות
   "אומדן — החיוב לפי משקל", ומקטע הכספים מפולח לפי מקור.
9. B49d — רשימת "ממתין לקליטה" · "פתח קליטה במכבסה" · "הוסף לקליטה הקיימת".
10. B49e — "מילוי אופן מסירה" בהגדרות · מקטע אופן המסירה · חתימה מכרטיס ההזמנה
   (⚠ לבדוק על הטאבלט של הדלפק) · ביטול נסיעת סרק.
11. B46/B48/B49/B49b/B49c — פריטי האימות מהאצוות הקודמות עדיין פתוחים.

## ⛔ כללי עבודה קבועים — הכרעות אבי. גוברים על כל מסמך

**R1 (28.07.2026) — אבי אינו מייצא ואינו שומר בסיס נתונים. לעולם.**
אין לבקש ממנו ייצוא גיליון, קובץ CSV, צילום מסך של טבלה או "תבדוק מה יש לך ב-X".
זה מבטל את הכלל ב-`WORKING_PROTOCOL.md` חלק 5 ("מסך ריק = לבקש ייצוא") — **הכלל
הזה מבוטל ואין לפעול לפיו.**
**מה עושים במקומו:** כשצ'אט צריך לדעת מה יש בפועל בנתונים, הוא **בונה פעולת
אבחון READ_ONLY לתוך המערכת** (ספירות, דגימות, אי-התאמות) ומציג אותה כמסך/כפתור.
אבי לוחץ, רואה על המסך, וזהו. אין קבצים, אין העברות, אין דבר שהוא צריך לשמור.
**וכלל עליון על זה:** קוד שדורש ידיעה מוקדמת על הנתונים הוא קוד שתוכנן לא נכון.
תיקון נתונים חייב להיות **מגלה-בעצמו ואידמפוטנטי**, עם תצוגה מקדימה לפני ביצוע.

## שלושת ה-batches האחרונים

### B60 — T3 + T4: מצב מסך והיסטוריית דפדפן (30.07.2026)

**האצווה המאוחדת הראשונה במבנה החדש.** שני פריטים, אזור קוד אחד — החלפת מסך.
לפי **M5** המסירה נעשתה פעם אחת בסוף, ולפי **M5** הפירוט כאן נפרד לכל פריט,
כדי שאפשר יהיה לזהות מי שבר מה אם ישבר משהו.
**ממשק בלבד. קוד השרת לא נגע. אין שינוי סכימה — אין להריץ `setupDatabase`.**
canary: **v4.57-B59 ← v4.58-B60**.

---

#### פריט 1 — `T3`: לחיצה חוזרת על "דוחות" לא חוזרת לקטלוג

**מה אבי ראה.** *"אם אני נכנס לדוח מתוך 'דוחות' ואחר כך רוצה לחזור, אני צריך
ללחוץ על לחצן ייעודי. לחיצה שוב על 'דוחות' לא מנווטת לתצוגה ראשית."*

**השורש — אומת בקוד החי.** `REPORT_ID` הוא משתנה מצב ברמת המודול.
`repBack()` איפס אותו, **`go()` לא** — ולכן `go('reports')` מ-`rReports` נכנס
לענף של הדוח הפתוח ורינדר אותו מחדש.

**מה שהמפרט דרש מעבר לתיקון עצמו,** ומה שנעשה בפועל: `grep` על כ-70 משתני מצב
ברמת המודול, ומעבר על **כל 29 פונקציות ה-render**. הממצא: `rReports` הוא
**היחיד** שמחליף את כל המסך בתת-מסך לפי משתנה מודול. כל השאר מבוססי טאבים
גלויים — למשתמש תמיד יש דרך גלויה לצאת, ולכן אין שם מלכודת.

**אבל נמצאו שתי מלכודות מאותה מחלקה שהיו נשארות:**
- **רצפת ייצור** — `FLOOR_INTAKE_ORDER` נשאר מצביע על הזמנה ישנה. כניסה מחדש
  מהתפריט הייתה נוחתת על טופס קליטה שקשור להזמנה שכבר לא רלוונטית. **זו לא
  רק אי-נוחות — זו סכנת נתונים.**
- **החזרות** — `RET` (תעודה פתוחה בשקילה) נשאר בזיכרון אחרי יציאה מהמסך.

**הכלל האחיד שנכתב — שלוש מחלקות, וכל משתנה מצב שייך לאחת:**

| מחלקה | מה זה | ב-`go()` |
|---|---|---|
| **א' — תת-מסך / סשן קשור לרשומה** | `REPORT_ID` · `REPORT_PARAMS` · `FLOOR_INTAKE_ORDER` · `FLOOR_INTAKE_CARTS` · `FLOOR_BC` · `FLOOR_INFO` · `B49F_DECL` · `RET` · `RET_CAM` | **מתאפס בכל כניסה**, כולל לחיצה חוזרת. חריג יחיד: `{keep:1}` מדריל-דאון |
| **ב' — העדפת תצוגה** | טאבים ומסננים: `FIN_TAB` · `ORD_TAB` · `DELIV_TAB` · `DRV_TAB` · `BI_TAB` · `ATT_TAB` · `FLEET_TAB` · `ASSET_TAB` · `FLOOR_TAB` · `CUST_TAB` · `EMP_TAB` · `AUDIT_FILTER` · `B54_F` · `B43_RANGE` · `FEXP_*` · `ITEMS_WH` · `SHELF_Q` · `COL_MONTH` · `B42_CUT` · `B42_CUT_SCOPE` · `UNITS_SHOW_DEL` · `NOBLE_REP_DAYS` · `B49F_DAYS` · `ATT_Y`/`ATT_M` · `CAL_Y`/`CAL_M` · `BI_DATA` · `B51_STATS` | **נשמרת בכוונה.** המשתמש בחר, זה גלוי, ויש דרך גלויה לשנות |
| **ג' — הקשר מודל** | `CONFIRM_CB` · `MGR_DEL_CB` · `SIG_CTX` · `TAG_CTX` · `MENTION_CTX` · `PAY_CTX` · `COLLECT_CTX` · `MEET_DRAFT` · `B59_TASK` · `B34_PICKS` · `B44_OSCAN` · `B35_ST` · `B35_CNT` · `B46_PDF_WARN_NEXT` | **לא נוגעים** — מאותחל בכל פתיחת מודל |

⚠ **חריג מפורש שנרשם בקוד: `UNDO_PENDING` לעולם אינו מתאפס.** הוא מחזיק פעולה
שממתינה חמש שניות לשליחה לשרת. איפוסו היה מבטל שליחה שאבי כבר אישר.

**איפה זה יושב:** `B60_VIEW_RESET` ב-`index.html`, ליד `go()`. **טבלה אחת.
אין רשימה שנייה שתתיישן.** מסך חדש עם מצב מחלקה א' מוסיף שם שורה, וזהו.

**מפת חיבורים (R2) — `T3`:**
1. **איפה הנתון נשמר?** בשום מקום. **אלה משתני זיכרון בדפדפן בלבד** — אין
   טבלה, אין עמודה, אין `TABLES`. **אין שינוי סכימה ואין להריץ `setupDatabase`.**
   החריג היחיד שנוגע באחסון הוא `sessionStorage.mn_view`, שכבר היה קיים.
2. **מי כותב אותו?** `go()` (מאפס) · `goReport` · `b49dGoIntake` · `repBack` ·
   `goFin` · `findReturn` · `floorCheck` · טופסי המסכים עצמם. **כל אתרי הכתיבה
   נסרקו; אף אחד לא נותר מחוץ לכלל.**
3. **מי קורא אותו?** `rReports` (בודק `REPORT_ID`) · `floorIntakeTab`
   (`FLOOR_INTAKE_ORDER`) · `renderReturn` ו-`doReturn` (`RET`) ·
   `floorRenderInfo`/`floorRefresh` (`FLOOR_BC`/`FLOOR_INFO`) ·
   `b49fQuestionHtml`/`b49fGuardOrder` (`B49F_DECL`). **רשימה מלאה, לא הערכה.**
4. **לאן הוא זורם?** **לשום מקום כספי.** אין חיוב, אין רשומה ב-`b54Ledger`,
   אין חשבונית או קבלה, אין `expenses`, אין השפעה על דוח הכנסות מול הוצאות,
   אין תזרים B43, אין נגיעה במסגרת האשראי S5, ואין תחולה לכלל המע"מ B45 —
   **הנימוק לכל אלה זהה: זהו מצב תצוגה בדפדפן, שאינו נשלח לשרת ואינו נשמר.**
   ⚠ החריג המהותי היחיד: איפוס `FLOOR_INTAKE_ORDER` **מונע** יצירת קליטה
   כנגד הזמנה שגויה — כלומר הוא מקטין סיכון כספי, לא יוצר אותו.
5. **מצבי קצה.** *אין נתונים:* `B60_VIEW_RESET` מוגן ב-`try/catch` ומדלג
   בשקט. *הנתון נמחק:* `floorIntakeTab` כבר מנקה בעצמו הזמנה שאינה קיימת
   ב-`DB` — התנהגות קיימת שנשמרה. *הרשומה בוטלה:* אותו מסלול. *לחיצה כפולה
   על אותו פריט תפריט:* איפוס שני על מצב שכבר אופס — no-op. *כשל באמצע:*
   האיפוס הוא השמה מקומית, אין לו "אמצע".
6. **מי רואה את זה?** **חמשת התפקידים** — מנהל · משרד · מכבסה · עובד רצפה ·
   נהג. נבדקו במפורש: המנהל (מסך הדוחות), עובד הרצפה (הקליטה) והנהג
   (טאבי המסלול). **ארבעת הקשרי הריצה:** צוות — פעיל; פורטל, חנות וקיוסק —
   `go()` אינו נקרא שם כלל (הם מסך יחיד בלי ניווט), ולכן אין לשינוי תחולה.

**סיפוח שנעשה (WORKING_PROTOCOL חלק 4א).** `goFin(tab)` הציב `VIEW='finance'`
ישירות ועקף את `go()` לגמרי — ולכן לא נשמר ב-`mn_view` (רענון לא החזיר לכספים)
ולא היה יכול להיכנס להיסטוריה. הועבר לעבור דרך `go()`. `FIN_TAB` הוא מחלקה ב'
ולכן ממשיך להישמר.

**מה במכוון לא שונה, וזו הכרעה ולא השמטה.** שלושת מסלולי המצלמה
(`RET_CAM` · סריקת ברקוד · `FLOOR_CAM`) מציבים `VIEW` ישירות. **הם אינם ניווט** —
הם חזרה למסך שהמשתמש כבר עובד בו, אחרי שכיסוי המצלמה החליף אותו זמנית.
המרתם ל-`go()` הייתה מוחקת את הקשר הקליטה **בדיוק ברגע שבו הברקוד נסרק**.

---

#### פריט 2 — `T4`: "הקודם" בדפדפן מוציא מהאתר

**מה אבי ראה.** *"לחיצה על 'הקודם' בדפדפן מוציאה לגמרי מהאתר לתצוגת כניסה."*
(חצי הרענון של התלונה הוכרע ב-B57 כתסמין של האיטיות, ונסגר ב-B58/PERF-02.)

**השורש — אומת מחדש מול הקוד החי (R9).** `grep` על `pushState`/`popstate`
החזיר **מופע אחד** בכל `index.html`: `history.replaceState` של הקיוסק.
**המנגנון אכן מעולם לא נבנה.**

**מה נבנה.**

| מצב | ההתנהגות החדשה |
|---|---|
| "הקודם" ממסך שנוסע אליו | חוזר למסך הקודם. **לא יוצא מהאתר** |
| "הקודם" כשמודל פתוח | **סוגר את המודל** ונשאר במסך |
| "הקודם" בעוגן הבסיס | יציאה אמיתית, כמו בכל אתר. **אין לכידת משתמש** |
| "הקודם" למסך שאינו מותר לתפקיד | לא מזיז |
| לחיצה חוזרת על אותו מסך | `replaceState` — ההיסטוריה לא מתנפחת |
| דריל-דאון (`goReport` · `b49dGoIntake`) | `pushState` גם באותו מסך, כדי ש"הקודם" יחזיר לקטלוג |
| לחיצה על שם האתר (`goHome`) | עבדה כבר קודם — עכשיו גם נכנסת להיסטוריה |

**ארבעת הגבולות שנשמרו בקפידה — R4:**
1. **הכתובת עצמה לא משתנה לעולם.** `pushState` מקבל `location.href` כמו שהוא.
   זה מה ששומר על `?kiosk=1` · `?portal=1` · `?shop=1`, ובפרט על
   `history.replaceState` של הקיוסק (B52) שמנקה את `kt` מהכתובת. **אין התנגשות.**
2. **המנגנון פעיל רק בהקשר צוות וכשהמערכת פתוחה בפועל.** `b60HistCtx()` מחזיר
   `false` בפורטל, בחנות, בקיוסק ובמסך הכניסה. "הקודם" בפורטל ובחנות ממשיך
   להתנהג כמו היום — הם מסך יחיד ואין להם ניווט.
3. **נעילת B7 מכבה את המנגנון מיידית** — `idleLock` מסתיר את `#app`.
   **אין נגיעה ב-`IDLE_LIMIT_MS`.** חלון 10 הדקות שאבי ביקש ונעילת האבטחה
   נשארו שני מנגנונים נפרדים, בדיוק כפי שהמפרט דרש.
4. **`closeModal` לא שונה,** ואין קריאה ל-`history.back()` בשום מקום מלבד
   עוגן הבסיס. חמישים אתרי הקריאה מהדפוס `closeModal();go('x')` עובדים כמו שהם.

**מפת חיבורים (R2) — `T4`:**
1. **איפה הנתון נשמר?** ב-`history.state` של הדפדפן בלבד — אובייקט
   `{mn:1, v:'<view>'}`. **אין טבלה ואין עמודה ב-`TABLES`. אין שינוי סכימה
   ואין להריץ `setupDatabase`.**
2. **מי כותב אותו?** `b60HistInit()` (מ-`enterApp` ומ-`enterOfflineIfPossible`)
   ו-`b60HistPush()` (מ-`go()` ומהמאזין). אלה שלושת אתרי הכתיבה, וזה הכל.
3. **מי קורא אותו?** מאזין `popstate` בלבד, ו-`b60HistPush` שמשווה מול
   `history.state` כדי להחליט push מול replace. **אין צרכן נוסף בקוד.**
4. **לאן הוא זורם?** **לשום מקום.** אין חיוב, אין `b54Ledger`, אין חשבונית או
   קבלה, אין `expenses`, אין השפעה על רווח, אין תזרים B43, אין מסגרת אשראי S5,
   ואין תחולה לכלל המע"מ B45. **הנימוק לכל אלה זהה: מצב ניווט בדפדפן שאינו
   נשלח לשרת.** ⚠ **R10: אפס בקשות שרת נוספו.** הכל בצד הלקוח.
5. **מצבי קצה.** *אין נתונים / `state` ריק:* מעגנים מחדש ב-`replaceState` ולא
   מזיזים את המשתמש. *מסך שנמחק מההרשאות:* נבדק מול `allowedViews()` — נשארים
   במקום. *לחיצה כפולה על "הקודם":* כל `popstate` מטופל עצמאית; מודל פתוח נסגר
   פעם אחת ואז הניווט ממשיך כרגיל. *כשל באמצע:* כל קריאות ה-History API עטופות
   ב-`try/catch`; דפדפן שחוסם אותן משאיר את המערכת בדיוק כפי שהיא היום.
   *יציאה:* `doLogout` עושה `location.reload()` — מסמך חדש, היסטוריה נקייה.
6. **מי רואה את זה?** **חמשת התפקידים.** נבדק במפורש למנהל ולנהג (טאבי המסלול
   שלו הם `go()` לכל דבר). **ארבעת הקשרי הריצה:** צוות — פעיל; פורטל · חנות ·
   קיוסק — מכובה לחלוטין, **ויש בדיקה אוטומטית לכל אחד מהשלושה** שמוודאת שאין
   נגיעה בהיסטוריה, ובדיקה נוספת שמוכיחה שה-`replaceState` של הקיוסק ממשיך
   לנקות את הטוקן מהכתובת.

---

#### הכרעה מרכזית: איך `T3` ו-`T4` לא מבטלים זה את זה

זו הסיבה שה-OPENER הזהיר מפני איחודם, והיא אמיתית. שתי נקודות חיכוך מדויקות,
ושתיהן נפתרו בדגלים מפורשים ב-`go(v, o)`:

- **`o.keep`** — דריל-דאון: המצב הוגדר **לפני** הקריאה ל-`go()`, ולכן אסור
  לאפס אותו (T3); ובמקביל הרשומה בהיסטוריה **כן** נדחפת גם כשהמסך זהה (T4),
  אחרת "הקודם" מתוך דוח לא היה מחזיר לקטלוג.
- **`o.fromHistory`** — הניווט הגיע מ"הקודם": מאפסים מצב מסך כרגיל, אבל **לא**
  דוחפים רשומה חדשה — אחרת כל לחיצת "הקודם" הייתה יוצרת לולאה.

**התוצאה הרצויה, ויש לה בדיקה ייעודית:** "הקודם" מתוך דוח פתוח מחזיר **לקטלוג
הדוחות**, כלומר כלל האיפוס של T3 חל גם על ניווט היסטוריה.

**סדר הבנייה שנדרש נשמר:** T3 נבנה עד הסוף, ורק אז T4 מעליו.

---

**מה נבדק.** `node --check` על בלוק ה-`<script>` המלא (862 KB).
**60 בדיקות אוטומטיות · 60 עברו · 0 נכשלו**, ב-jsdom מול הקוד האמיתי:
28 ל-T3 · 18 ל-T4 · 8 להקשרי ריצה (פורטל · חנות · קיוסק) · 6 לתפקידים
(נהג · עובד רצפה). לפי **R7**, בדיקת הניווט מדספאטצ'ת `MouseEvent` אמיתי עם
`bubbles:true` על כפתור אמיתי מ-`#nav`, דרך עוזר `wire()`.
**בדיקות R4 שמוכיחות שלא נשבר מה שלא היה אמור להשתנות:** `mn_view` עדיין
נשמר · חלונית הניווט עדיין נסגרת · `NAV_OPEN` עדיין נקבע לפי הקטגוריה ·
מחלקה ב' נשמרת בין מסכים · `UNDO_PENDING` לא מתאפס · `replaceState` של הקיוסק
עובד · מסך אסור לתפקיד לא נפתח דרך ההיסטוריה.

**שתי הנחות שלי התבררו כשגויות ותוקנו מול הקוד החי, לא מול הציפייה (R9):**
דוח עם `autoRun` דורס את `REPORT_PARAMS` מהטופס, ו-`floorIntakeTab` מנקה
בעצמו הזמנת קליטה שאינה קיימת ב-`DB`. **שתיהן התנהגות קיימת ותקינה** —
הבדיקות תוקנו, לא הקוד.

**מה לא נבדק.** ההתנהגות בפועל של כפתור "הקודם" הפיזי בדפדפן של אבי —
jsdom מדמה את ה-History API אך אינו דפדפן אמיתי. **זה פריט האימות המרכזי
של האצווה.** כמו כן לא נבדק על מכשיר נייד אמיתי (מחוות החלקה לאחור ב-iOS
ובאנדרואיד עוברות דרך אותו `popstate`, אבל לא נצפו בעין).

### B59 — T5: קידום שלב לפי בחירה, לא לפי הסדר (30.07.2026)

**מה אבי ראה.** *"בכביסות, אם לא סורקים את העגלה ואת המכונה אלא מקדמים
שלב על ידי התוכנה — אם אני בוחר שלב שלא לפי הסדר (אחרי כביסה מיד
גיהוץ או אריזה) — זה לא עובר לשם. תתקן שלפי השלב שנבחר זה יעבור."*

**השורש — אומת בקוד החי.** למעשה **לא היה במערכת בורר שלבים בכלל.**
מה שנראה כמו תפריט שלבים הוא **בורר המכונה** — כל אפשרות בו מוצגת כ-
`מעגילה · M-3 (בגיהוץ וקיפול)`, ולכן נראה כאילו בוחרים שלב. שלושת אתרי הקוד:
- `advanceForm()` — חישבה `STAGES[idx+1]` והציגה אותו בכותרת
- `doAdvance()` — שלחה `cart_id` + `machine_id` בלבד, בלי שלב יעד
- `advanceLaundry()` (שרת) — `toStage = STAGES[idx + 1]`, קשיח

כלומר: בחירת מכונת גיהוץ הותירה את שלב המכונה **ללא כל השפעה** — המשימה
התקדמה צעד אחד לפי האינדקס ונחתה ב"בייבוש".

**ממצא שני.** `STAGES` (לוח הכביסה) = `התקבל · בכביסה · בייבוש · בגיהוץ וקיפול · מוכן`
— **אין בו "באריזה" כלל**, אבל למכונות **כן** ניתן להגדיר `auto_stage = 'באריזה'`
(`NOBLE_MACHINE_STAGES`). הכנסת שלב המכונה כמות שהוא היתה מעלימה משימות מהלוח.
**הכרעה:** מכונת אריזה נחשבת מתאימה ל-"בגיהוץ וקיפול" (`b59MachStage`).
אין עמודה חדשה בלוח, אין שינוי סכימה ואין שינוי בערכים הנשמרים.

**התיקון.**
- בורר **"שלב יעד"** בראש טופס הקידום. ברירת המחדל = השלב הבא
  (ההרגל לא משתנה למי שעובד לפי הסדר). כל אפשרות מתוייגת:
  *השלב הבא* · *דילוג שלבים* · *חזרה אחורה*.
- **דילוג קדימה וחזרה אחורה מותרים** (כביסה חוזרת = חזרה ל"בכביסה").
- בחירת "מוכן" מחליפה את גוף הטופס להכרעת המידוף — אפשר לקפוץ לסיום
  מכל שלב, והמידוף נשאר חובה כמו קודם.
- רשימת המכונות **מקובצת**: "מתאימות ל<שלב>" קודם, "מכונות אחרות" אחריהן,
  ומכונה מתאימה נבחרת מראש. **אף מכונה לא הוסתרה** — אין מבוי סתום.
- השרת מקבל `opts.to_stage`. **בהעדרו — התנהגות זההה לחלוטין ל-B40** (R4).

**מה נשמר במכוון.** דרישת B40 (עגלה **וגם** מכונה בכל מעבר) **לא נגעה** —
ההכרעה ההפוכה היא על **שלבים**, לא על שיוך. משימה שהגיעה ל"מוכן" **אינה
נפתחת מחדש** — השרת חוסם, והמסך מסכים איתו (העגלה כבר שוחררה ו-`done_date` נרשם).

**מפת חיבורים (R2).**
1. **איפה נשמר?** `laundry_tasks.stage` — **עמודה קיימת**, אותם ערכי `STAGES`.
   המעבר נרשם ב-`laundry_task_events` (`stage_from` · `stage_to` · `note`) — גם הן קיימות.
   `to_stage` הוא **פרמטר בקשה בלבד — אינו נשמר בשום עמודה.** אין שינוי סכימה.
2. **מי כותב?** `advanceLaundry` בלבד (קורא אחד: `case 'advanceLaundry'`).
   בממשק: `advanceForm` ← `b59StageChg` ← `doAdvance`. שני כפתורים קוראים ל-`advanceForm`:
   כרטיס בלוח הכביסה וכפתור במודל ההיסטוריה. שניהם עוברים באותו נתיב.
3. **מי קורא?** `grep` מלא על `שלב`/`stage`: `rLaundry` (עמודות הלוח) ·
   `openLaundryTask` (כרטיס ויומן) · `b40Siblings`/`b40CartMates` · `b40SplitLaundry` ·
   `b49cAfterAdvance` (גשר NOBLE) · דוח ביצועי המכבסה · `nobleBoard`. **כולם נבדקו.**
4. **לאן זורם?** ללוח הכביסה, ללוח החי, לדוח ביצועי המכבסה ולסטטוס הקליטה.
   **כסף: אין.** שלב כביסה אינו מחולל חיוב — החיוב נולד בשקילה (`nobleWeigh`)
   ובחשבונית המכבסה, לא במעבר השלב. לא נוצר חיוב · לא נגע `b54Ledger` ·
   לא נוצרה חשבונית/קבלה · לא הוצאה · לא נוגע ברווח · לא בתזרים ·
   לא במסגרת האשראי · לא בכלל המע"מ. **הנימוק זהה לכולן: המעבר משנה
   עמודת סטטוס וכותב שורת יומן — ולא נוגע בשום סכום.**
5. **מצבי קצה.** שלב לא מוכר → נדחה · "התקבל" כיעד → נדחה · השלב הנוכחי →
   נדחה · משימה ב"מוכן" → לא נפתחת · דילוג בלי עגלה/מכונה → נחסם (B40) ·
   מכונה בתיקון → נדחית · קפיצה ל"מוכן" בלי הכרעת מידוף → נדחית ·
   מדף לא קיים → נדחה · אין מכונות כלל → הטופס נפתח עם קישור ליצירה ·
   לחיצה כפולה → השנייה נדחית ב-"המשימה כבר בשלב X". **כל אלה בבדיקות.**
6. **מי רואה?** לוח הכביסה זמין ל**מנהל · משרד · מכבסה · עובד רצפה** דרך
   `allowedViews` — **לא נגעה.** נהג אינו רואה את המסך ולא נגע בו.
   הקשרי הריצה: **צוות בלבד.** הפורטל והחנות אינם מציגים קידום שלב;
   הקיוסק — `B52_KIOSK_ACTIONS` רשימה סגורה ו-`advanceLaundry` **אינה בה ולא נוספה אליה.**

**בדיקות: 85 — כולן עברו.**
39 שרת ב-`vm.createContext` מול קוד השרת האמיתי + 46 ממשק ב-jsdom באירועי DOM אמיתיים (R7).
בתוכן: **רגרסיה שמשחזרת את התקלה שאבי ראה** (בכביסה + מעגילה → היה "בייבוש",
עכשיו "בגיהוץ וקיפול") · **כל עשרת צירופי הדילוג החוקיים** · **מבחן R4:
בהעדר `to_stage` (וגם `null`/`''`/רווחים) התוצאה זההה ביט-אחר-ביט ל-B40** ·
הגשר B49c · הפיצול · חמש עמודות הלוח ללא "באריזה".

**⭐ R10 — נבדק מחדש מול ממצאי `ACT-18` (רצפת תקורה 2.7 שנ׳ לבקשה).**
התיקון אינו מוסיף אף בקשה: `b59StageChg` פועל כולו על `DB` שבזיכרון
ואינו פונה לשרת כלל; קידום שלב = **בקשה אחת**, כמו קודם. ובפועל הוא
**מוריד בקשות**: מעבר מ"בכביסה" ל"בגיהוץ וקיפול" דרש עד היום שתי
קריאות `advanceLaundry`, ועכשיו אחת — **חיסכון של כ-2.7 שנ׳ לכל דילוג.**
שלוש בדיקות נוספות סופרות את קריאות `act` ומוכיחות זאת.
לא נגעתי במשקל המטען, במטמון וב-`pollDataVersion` — בדיוק כפי שהממצא מורה.

**⚠ אין שינוי סכימה — אין להריץ `setupDatabase`.**

### B58a — תיקון אי-ההתאמה שאבחון ACT-17 גילה (30.07.2026)

**מה אבי ראה.** *יומן פעולות ← קריאה מהירה (B58) ← "בדוק זהות מטען ומדוד"*:
**51 מתוך 55 טבלאות זהות.** ארבע לא: `tasks` · `task_comments` · `meetings` ·
`credit_profiles` — **עם מספר שורות זהה בכל אחת.** כלומר ההבדל אינו בשורות
אלא בתוך תא. במקביל נמדד השיפור: **`batchGet` 486 מ״ש מול 13.3 שניות
ב-55 נסיעות — פי 27.3.**

**השורש.** `getValues()` מחזיר תא-תאריך כאובייקט `Date`, ו-`readTable` הופך
אותו ל-`yyyy-MM-dd`. `batchGet` עם `FORMATTED_STRING` החזיר את **מחרוזת
התצוגה של הגיליון** (למשל `30/07/2026`) — אותו נתון בייצוג אחר. ארבע הטבלאות
הן בדיוק אלה שיש בהן עמודות תאריך/שעה עם תאים שלא נכתבו בפורמט טקסט של B20:
`tasks.due_date`/`created_at` · `task_comments.ts`/`handled_ts` ·
`meetings.date`/**`time`** · `credit_profiles.verified_at`.

**התיקון — זיהוי ודאי, לא ניחוש לפי תבנית.**
דחיתי במכוון את הפתרון המתבקש (נורמליזציה של מחרוזות שנראות כמו תאריך) — הוא
היה משבש טקסט אמיתי שנכתב במקרה בצורת תאריך. במקומו: **כל טווח נקרא בשני
ייצוגים, בשתי קריאות `batchGet`** — `SERIAL_NUMBER` ו-`FORMATTED_STRING`.
ההפרש היחיד בין שתי הקריאות הוא `dateTimeRenderOption`, ולכן:

> **תא הוא תאריך/שעה ⇔ הייצוג הסידורי הוא `number` והמעוצב הוא `string`.**

מהמספר הסידורי מחושב התאריך האזרחי באריתמטיקה טהורה ב-UTC (אפס הלוח:
30.12.1899) — חסין לאזור זמן, ומחזיר בדיוק את מה ש-`formatDate(…,'yyyy-MM-dd')`
מחזיר. **שתי קריאות במקום 55 — עדיין ~1 שנ׳.**

**שתי תוספות שנספחו:**
1. **האבחון מצביע על התא.** במקום "לא זהה", הוא מציג שורה · עמודה · שני
   הערכים · **שני הטיפוסים**, עד שלושה תאים לכל טבלה. זה מה שהופך דיווח
   לאבחנה שאפשר לתקן לפיה (R1: הכל על המסך, בלי ייצוא).
2. **זהות מלאה מפעילה מחדש את המסלול המהיר לבד.** אבי לא צריך ללחוץ על
   מתג נוסף אחרי תיקון.

**מה נבדק — 97 בדיקות, כולן עברו** (64 שרת + 33 ממשק):
- **בדיקת רגרסיה שמשחזרת את התקלה של אבי:** אותם נתונים מריצים את **B58 כפי
  שנמסר** ואת **B58a**. הראשון נכשל ב-**בדיוק ארבע הטבלאות שאבי ראה**, עם
  מספר שורות זהה — והשני עובר ב-100%. בלי השחזור הזה התיקון היה השערה.
- מטען `getAll` זהה ביט-אחר-ביט לחמשת התפקידים **גם עם תאי `Date` אמיתיים**.
- המרת התא נבדקה ישירות: תאריך · תאריך+שעה · **שעה בלבד** (`1899-12-30`,
  בדיוק כמו `readTable`) · 01.01.2000.
- מדידת נסיעות: 55 → **2 `batchGet` + 0 `getValues`**.
- ⚠ **תקלה שנמצאה בסביבת הבדיקה עצמה, לא בקוד:** `Date` שנוצר ב-realm של
  Node אינו עובר `instanceof Date` בתוך `vm.createContext`, ולכן המדומה
  הראה "אי-התאמה" מדומה. **האבחון החדש הוא שחשף את זה** — הוא הדפיס
  `slow_type: object`. המדומה תוקן להחזיר `Date` מה-realm של ה-vm.

### B58 — PERF-02: תיקון האיטיות (29.07.2026)

**הבסיס.** המספרים של B57 (29.07.2026 20:41) הכריעו את הכיוון ולא הותירו מקום
לניחוש: הצוואר אינו הנפח (40.7 KB בסך הכול) ואינו הרינדור (6 מ״ש) — הוא
**מספר הנסיעות לשרתי Google**. ההוכחה בשורה אחת: `invoices`, שלוש שורות,
1.3 KB, **1.1 שניות**. כל `readTable` הוא `getRange().getValues()` נפרד;
55 נסיעות × ~215 מ״ש = 11.9 שניות.

**מה נבנה — ארבעה תיקונים.**

**① קריאה אחת במקום 55 — העיקר.**
מעבר ל-Advanced Sheets Service: `Sheets.Spreadsheets.Values.batchGet` קורא
את כל הטווחים בקריאת HTTP אחת. **החוזה כלפי כל שאר הקוד לא השתנה בכלל** —
`readTable` ו-`loadDb` נשארו כפי שהם, ו-`db.orders` מתנהג בדיוק כמו קודם.
המנגנון החדש (`b58Prefetch`) רק **ממלא מראש** את מטמון הטעינה העצלה של
`loadDb`, ולעולם אינו דורס טבלה שכבר נטענה או שונתה בזיכרון.
נמדד בבדיקות אוטומטיות: **55 קריאות `getValues` → קריאת `batchGet` אחת ואפס
`getValues`.**

**② מטמון פר-טבלה במקום `data_version` גלובלי אחד.**
עד היום כתיבה להזמנות מחקה גם את `settings` וגם את `employees`. עכשיו לכל
טבלה יש גרסה משלה (`b58_table_versions` ב-`CacheService`), ו-`writeTable`/
`appendRowToTable` מסמנים איזו טבלה נכתבה בפועל.
⚠ **`bumpDataVersion` (מנגנון B7) ממשיך לעדכן את `data_version` בכל כתיבה
בדיוק כמו קודם, ו-`checkVersion` מחזיר את אותו `version` בדיוק** — נוסף
שדה `tables` בלבד. הסמנטיקה כלפי הממשק זהה.
**ברירת המחדל הבטוחה:** כתיבה שאינה עוברת ב-`writeTable`/`appendRowToTable`
מסמנת את **כל** הטבלאות כמשתנות, כדי שנתון לעולם לא ייתקע ישן במסך אחר.
שני אתרי הכתיבה הישירה שקיימים בקוד (`repairTimeFields` על `payroll`,
`b51ScrubAudit` על `audit_log`) קיבלו סימון מפורש.

**③ חיווי "טוען נתונים…" במקום מסך כניסה שותק.**
זה החצי של `T4` שעבר לכאן. אחרי רענון, `autoLogin` קרא ל-`enterApp` שהמתין
ל-`getAll` (עד 14 שניות) בזמן שמסך הכניסה מוצג בשקט — ואבי חווה את זה
כ"רענון מוציא אותי מהמערכת". **המערכת מעולם לא זרקה אותו; היא פשוט לא
אמרה כלום.** החיווי יושב ב-`enterApp` ולכן מכסה את **שני** מסלולי
`autoLogin`, כולל מסלול טוקן-הסשן ששתק לגמרי, וגם את מסלול "זכור אותי".
⚠ **לא נגענו ב-`IDLE_LIMIT_MS` ובנעילת B7** — מנגנונים נפרדים לגמרי.

**④ `pollDataVersion` לא מושך `getAll` מלא בכל bump.**
המדידה חידדה את הנקודה: רצפת התקורה של Apps Script היא **2.2 שניות לכל
בקשה**, גם לזעירה. לכן צומצם **מספר** הבקשות, לא רק משקלן:
- לשונית ברקע (`document.hidden`) **אינה שולחת דבר**; חזרה לחזית בודקת מיד
  (מאזין `visibilitychange`), כדי שהדילוג לא ייצור פיגור של עד 30 שניות.
- `checkVersion` מחזיר גם גרסה לכל טבלה, ולכן נמשכות **רק** הטבלאות שהשתנו
  דרך פעולה חדשה `b58Tables` (`READ_ONLY`).
- ⚠ **הכלל שנשמר במלואו: נתון שהשתנה במסך אחד חייב להופיע במסך אחר.**
  כל מצב לא-ודאי נופל למטען מלא: נהג (B17 — מטענו נבנה פר-משתמש ואינו עובר
  במטמון המשותף) · אין מפת טבלאות (שרת ישן) · מפה ריקה · כשל בשליפה החלקית.

**החלטות שהתקבלו ולמה.**

1. **ה-prefetch יושב ב-`getAllPayload`, לא ב-`loadDb`.** `loadDb` נקרא בכל
   פעולה; פעולת כתיבה קטנה קוראת 2–3 טבלאות בלבד. `batchGet` גורף של 55
   טבלאות היה **מאט** אותה. ה-prefetch מופעל בדיוק במקום שבו ידוע מראש
   שנדרשות כל הטבלאות. R3: זו החלטה מדודה, לא דילוג.
2. **אימות עצמי על נתוני אמת — תוספת שלא הייתה במפרט.**
   אי אפשר להוכיח מהסביבה הזו ש-`batchGet` מחזיר בדיוק את מה ש-`getValues`
   מחזיר על הגיליון של אבי (סוגי תאים, תאריכים, פורמט טקסט של B20). לכן
   **פעם ב-6 שעות המערכת מריצה את שני המסלולים ומשווה JSON טבלה-טבלה לבד.**
   אי-התאמה → כיבוי אוטומטי של המסלול המהיר + דיווח במסך האבחון, והמערכת
   ממשיכה ב-`readTable` בלי לשבור דבר. עלות: קריאה איטית אחת ב-6 שעות.
3. **שלוש רמות נפילה חיננית, כולן נבדקו:** שירות Sheets אינו מופעל · `batchGet`
   זורק · אי-התאמה בין המסלולים. בכל אחת מהן המטען יוצא **זהה** למטען הישן.
4. **`b58SetFast` — מתג כיבוי ידני.** כותב ל-`ScriptProperties` בלבד (לא
   לגיליון), ולכן **אינו** ב-`READ_ONLY_ACTIONS` ומחזיק נעילה ורישום ביומן.
5. **הקיוסק לא נפתח לשום פעולה חדשה.** `B52_KIOSK_ACTIONS` נשארה
   `['b52KioskClock','checkVersion']` — נבדק אוטומטית.

**מה נבדק — 80 בדיקות אוטומטיות, כולן עברו.**

*שרת (47 בדיקות, `vm.createContext` מול קוד השרת האמיתי, גיליון מדומה של
59 טבלאות):*
- **הבדיקה המרכזית של האצווה: מטען `getAll` זהה ביט-אחר-ביט לפני ואחרי,
  לכל אחד מחמשת התפקידים** (מנהל · משרד · מכבסה · עובד רצפה · נהג).
- **R6 — תלות הכסף:** `b48BalancesAg` מחזיר תוצאה זהה לפני ואחרי.
- מטען הנהג (`getDriverFilteredPayload`) זהה · `_slim` זהה · `audit_log`
  נשאר ריק (`drop`) · קיצוץ 300 תווים ודגל `_trunc` זהה · **סדר המפתחות
  במטען זהה**.
- שלוש הנפילות החיננוֹת מחזירות מטען זהה, והאי-התאמה מכבה את המסלול לבד.
- מטמון פר-טבלה: כתיבה ל-`orders` מסמנת רק אותה, `settings` לא מסומנת,
  והמטען שנבנה מהמטמון **זהה לחישוב מלא מאפס**.
- `checkVersion`: `version` זהה, `tables` שדה נוסף בלבד.
- `b58Tables`: פרוסה זהה בדיוק ל-`getAll`; `employees` לא נדרס כשלא ביקשו
  אותו; מנהל מקבל `pin`, משרד לא — גם בשליפה חלקית; נהג נדחה.
- רשימות ההרשאה, והקיוסק שלא נפתח.
- **מדידת נסיעות בפועל: ישן 55 · חדש 1 `batchGet` + 0 `getValues`.**

*ממשק (33 בדיקות, jsdom, אירועי DOM אמיתיים — R7):*
- החיווי מוצג בזמן ההמתנה ל-`getAll` ומוסתר בהצלחה, בכשל שרת ובנפילת רשת.
- שליפה חלקית מושכת **רק** את הטבלה שהשתנתה, והנתון שלא השתנה נשמר בזיכרון.
- ארבעת מצבי הנפילה למטען מלא.
- לשונית מוסתרת = אפס בקשות; `visibilitychange` אמיתי מפעיל בדיקה מיידית.
- `IDLE_LIMIT_MS` נשאר 10 דקות, `idleLock` לא הוחלף, canary עודכן.

**מה לא נבדק — וצריך את אבי.**
- **הזהות על הנתונים האמיתיים.** זו הסיבה לכפתור האבחון ולאימות העצמי.
- **השיפור בפועל בשניות** — נמדד רק על גיליון מדומה. המדידה המקורית נעשתה
  על נתוני בדיקה, ואין להניח שהיחסים יישמרו על נתוני אמת.
- שירות Sheets בעורך Apps Script (`ACT-16`) — אם אינו מופעל, המערכת פשוט
  ממשיכה במסלול הישן ולא נשברת.

**מה לא נכנס, ולמה.**
- **snapshot ליתרות מלאי (`BLD-11`):** נדחה במכוון — `stock_moves` שוקלת אפס
  ואינה בעשרים הכבדות. חוזרים אליה רק אם מדידה עתידית תראה שהיא נכנסה לצוואר.
- **חיתוך נוסף ב-`SLIM_POLICY`:** מיותר. 40.7 KB בסך הכול.
- **שכתוב מסכים לשליפה ממוקדת:** תיקון ① מייתר את הדחיפות. נשאר כלל מחייב
  למסכים **חדשים** (`R10`).

### B57 — PERF-01: אבחון האיטיות (29.07.2026)

**מה זו האצווה הזו.** מדידה, לא תיקון. אבי דיווח שהמערכת איטית; עד היום לא היה
לאיש מספר אחד אמיתי, ולכן כל דיון על הפתרון היה ניחוש. B57 בונה את המכשיר.
**היא אינה מאיצה שום דבר, במכוון.** התיקון הוא `PERF-02` = B58.

**מה נבנה — פעולה אחת בממשק אחד.**
מסך *יומן פעולות* ← כרטיס **“אבחון ביצועים (B57 · PERF-01)”** (מנהל בלבד),
מתחת לכרטיס כלי התחזוקה של B51. שלושה כפתורים:

| כפתור | מה הוא מודד |
|---|---|
| ⏱ הרץ אבחון מלא | רצפת רשת (`checkVersion` ×3) · `getAll` פעמיים ברצף · רינדור · אבחון שרת |
| 🖼 מדוד רינדור לכל מסך | זמן `render()` לכל מסך מותר, על נתונים שכבר בזיכרון |
| ↺ אפס מוני מטמון | פותח חלון מדידה חדש לשיעור פגיעות המטמון |

**בשרת:** פעולה חדשה `b57PerfProbe` — `READ_ONLY`, מנהל בלבד. מחזירה:
זמן קריאה ומשקל **לכל טבלה בנפרד**, זמן בניית המטען, זמן סריאליזציה,
עובדות המטמון (כולל **מבחן ישיר: האם המטען בכלל נכנס למגבלת `CacheService`**),
ומקצב הכתיבות.

**⚠ החלטה שחסכה מכשור:** מקצב דריסת המטמון נמדד **בלי לגעת ב-`bumpDataVersion`**.
ב-`route`, כל פעולת כתיבה מוצלחת כותבת שורה ל-`audit_log` **ומיד אחריה** קוראת
ל-`bumpDataVersion`. כלומר חותמות הזמן ב-`audit_log` הן בדיוק יומן הדריסות —
רטרואקטיבית, בחינם, ובלי לגעת במנגנון B7. R4 נשמר.

**ששת הממצאים שכבר הוכחו מהקוד — לא נמדדו, אלא נקראו:**

1. **`getAll` קורא 55 מתוך 59 גיליונות בכל החטאת מטמון.** `SLIM_POLICY` של B51
   מפילה 4 בלבד (`audit_log`, `call_logs`, `payment_declarations`,
   `customer_messages`) וחותכת 4 לפי ימים. **אומת:** `stock_moves`,
   `package_moves`, `fault_events`, `attendance`, `payroll` **לא נחתכו** —
   בדיוק כפי ש-B51 תיעד. הן צומחות ללא גבול.
2. **המטמון נמחק בכל כתיבה במערכת.** מפתח `getAllCached` הוא
   `'getAll_payload_' + data_version`, ו-`route` קורא ל-`bumpDataVersion()`
   אחרי **כל** פעולה שאינה ב-`READ_ONLY_ACTIONS`. אורך החיים האפקטיבי של
   המטמון = הפער בין שתי כתיבות, לא ה-TTL של 120 שניות. **חשד B12 מאומת.**
3. **ייתכן שהמטמון מעולם לא אחסן דבר.** `cache.put` ב-`getAllCached` עטוף
   ב-`catch` ריק. ל-`CacheService` יש תקרה של ~100KB לערך יחיד; מטען שחורג
   ממנה גורם לזריקה שנבלעת בשקט. **זו שאלה בינארית שאי אפשר להכריע מקריאת
   קוד** — ולכן האבחון מנסה לאחסן את המטען האמיתי ומדווח הצלחה/כישלון מפורשות.
4. **`enterApp()` ממתין ל-`getAll` מלא לפני שהוא מציג משהו.** בזמן ההמתנה
   מסך הכניסה עדיין גלוי, ובמסלול טוקן-הסשן **אין אפילו הודעת “טוען”**.
   → זו ההכרעה על `T4`, למטה.
5. **כל לקוח מחובר מושך `getAll` מלא כל 30 שניות ביום עבודה פעיל.**
   `pollDataVersion` בודק `checkVersion` כל 30 שניות ומושך `getAll` שלם בכל
   פעם ש-`data_version` השתנה. בשילוב עם ממצא 2: בזמן עבודה ה-`data_version`
   משתנה כל הזמן, ולכן זו למעשה משיכה מלאה כל 30 שניות, לכל משתמש.
6. **הנהג לא נהנה מהמטמון כלל, ומשלם יותר מכולם.**
   `getDriverFilteredPayload` בונה `getAllPayload` **מלא** ואז מסנן — ואינו
   עובר דרך `getAllCached` בכלל (מכוון, B17: המטען ייחודי-משתמש). כל רענון
   של נהג = בנייה מאפס.

**✅ ההכרעה על `T4` — פיצול, לא איחוד.** אבי דיווח שני תסמינים במשפט אחד,
ובקוד הם שני דברים שונים לחלוטין:

- **“רענון מוציא מהמערכת” = תסמין של PERF-01.** `sessionStorage.mn_token` שורד
  רענון; `autoLogin()` משחזר את הסשן; `sessionStorage.mn_view` מחזיר לאותו מסך;
  ו-`idleLock` **אינו יכול** לירות ברענון, כי `LAST_ACTIVITY` מאותחל בטעינת הדף.
  המערכת אינה זורקת את אבי. היא מציגה לו את מסך הכניסה, בשקט וללא הודעה, כל עוד
  `getAll` רץ. **התיקון אינו בסשן — הוא מהירות + חיווי “טוען”.** עובר ל-`PERF-02`.
- **“הקודם מוציא מהאתר” = באג נפרד ואמיתי.** `grep` על `pushState`/`popstate`
  בכל `index.html` מחזיר **מופע אחד בלבד** — `history.replaceState` של הקיוסק.
  אין שום אינטגרציה עם היסטוריית הדפדפן. המנגנון מעולם לא נבנה, ולביצועים אין
  לזה שום קשר.

**לכן התור אינו מתקצר ל-17.** `T4` נשאר אצווה, אך היקפו נחתך בכ-חצי: רק
`pushState`/`popstate`. חצי הרענון עבר ל-`PERF-02`.

**מה לא נעשה, במכוון:** שום אופטימיזציה. לא הוגדל TTL, לא שונתה `SLIM_POLICY`,
לא נגעה `bumpDataVersion`, לא שונה `pollDataVersion`. R3: זו אינה התחמקות —
זה גבול האצווה שנקבע מראש ב-`TASK_QUEUE.md`, והפריט שממשיך אותה מוגדר ב-`PERF-02`.

**בדיקות:** 44 אוטומטיות עברו (30 שרת מול הקוד האמיתי ב-`vm.createContext`,
14 ממשק מול הבלוק שבתוך `index.html`). כולל הוכחת רגרסיה: `getAllCached` מחזיר
בדיוק את אותו מטען לפני ואחרי; `getAll` ו-`checkVersion` ממשיכים לעבוד;
`READ_ONLY_ACTIONS` קיבלה **בדיוק** פריט אחד ולא איבדה כלום; ובדיקה מפורשת
ש-**האבחון אינו כותב אף שורה לאף גיליון** (R1).

**⚠ אין שינוי סכימה — אין להריץ `setupDatabase` בגלל B57.**

---

### B56 — סגירת רגל החזור של הזמנת כביסה + תקלות רכב (29.07.2026)

**הרקע.** שני פריטים מ"פתוח לטיפול" שאבי ביקש לאחד לצ'אט אחד אחרי שהצגתי לו
הערכת גודל. הפריט השלישי שהוצע (snapshot ל-`stock_moves`) **נדחה במכוון** —
הוא נוגע בליבת המלאי, טעות בו שקטה ומזיקה, וזו בעיה עתידית ולא כאב נוכחי.

**חלק 1 — רגל החזור של הזמנת כביסה (שרת)**

הבאג: `nobleDeliver` סגר רק את רשומת המשלוח הפנימית של המכבסה
(`intake.delivery_id`, `kind='משלוח מכבסה'`, בלי `order_id`). רגל החזור של
ההזמנה עצמה — רשומת `אספקה` שנוצרה מ-`b38PlannedDeliveries` — נשארה `מתוכנן`
לנצח. B49d סגר את רגל ההלוך בלבד, במכוון, והחזור נשאר פתוח.

- `b56CloseLaundryReturnLeg(db, order, user)` — נשענת על `b49fCloseLegs` הקיימת.
  **אין לוגיקת סגירה חדשה.**
- **`declared` = `'כן'` תמיד, וזו הכרעה מנומקת:** `nobleDeliver` כבר חוסם מסירה
  בלי חתימת לקוח או צילום (B19d/L6). מרגע שהגענו לשורה הזו המסירה הוכחה, ולכן
  הרגל נסגרת עם `trip_confirmed_by` ולא מבוטלת. ביטול היה מוחק נסיעה שבוצעה.
- **איסוף עצמי ברגל החזור** (`ship_back='איסוף עצמי'`) → אין רגל, יוצאים בשקט.
  `b49eShipMode` הוא מקור האמת, לא `delivery_fee` (כלל P2 מ-B49e).
- `b56CloseLaundryOrder(db, order)` — ההזמנה עוברת ל-`הושלמה` רק כשאין קליטה
  פתוחה שלה (`B56_OPEN_INTAKE`) **וגם** אין רגל משלוח פתוחה. הזמנה עם שתי
  קליטות לא נסגרת על הראשונה. אידמפוטנטי.
- **בדיקת הכסף שקדמה למימוש:** `B54_SKIP_ORDER = ['בוטלה','טיוטה','הצעת מחיר']`.
  `מאושרת` ו-`הושלמה` נספרות **זהה** בספר החיובים, ולכן שינוי הסטטוס לא מזיז
  יתרה באגורה. נבדק אוטומטית ונשמר כבדיקת רגרסיה — אם מישהו יוסיף `הושלמה`
  ל-`B54_SKIP_ORDER` בעתיד, הבדיקה תיפול.

**חלק 2 — רכב כסוג נכס שלישי ב-B42**

הממצא שהוזיל את ה-batch: `asset_faults` ו-`fault_events` היו **גנריות
מלכתחילה** (`asset_type`/`asset_id`). מה שהגביל אותן לשני סוגים היה שתי
פונקציות בלבד — `b42Asset` בשרת ו-`b42Meta` בממשק. **לא נוספה טבלה, לא נוספה
עמודה, אין שינוי סכימה.** ההערכה הראשונית שלי ("בינוני + שינוי סכימה") נסמכה
על `BUILD_PLAN` במקום על הקוד והייתה שגויה — תיקנתי אותה מול אבי לפני הבנייה.

- **שרת:** ענף `'רכב'` ב-`b42Asset` → `vehicles.status`, מצבים
  `פעיל/בטיפול/מושבת`. מרגע זה ארבע פעולות התקלה (`b42ReportFault`,
  `b42AddEvent`, `b42SaveService`, `b42CloseFault`) עובדות על רכב **בלי שנגעתי
  בהן** — כולן עוברות דרך `b42Asset`. `b42SaveService` קיבל ענף רכב שכותב
  ל-`last_service_date`/`next_service_date`/`next_service_km`/`mileage_km`
  (שמות שונים משל מכונה, ולכן ענף נפרד ולא איחוד). שדות אלה ישבו בסכימה מאז
  B42 ואיש לא כתב אליהם. הודעת "נכס לא נמצא" אוחדה ל-`B56_NO_ASSET`.
- **ההפרדה (הכרעת אבי):** `b42ScopeTypes`/`b42InScope` בממשק. `b42Faults`
  ו-`b42OpenFaults` מקבלים תחום אופציונלי (בלי תחום = הכל, תאימות לאחור).
  `b42FaultsHtml(scope)`, מוני הטאבים, וחתכי העלויות — כולם מסוננים.
  **אירוע בלי נכס ("תשתיות/אחר") שייך לתחום המכבסה בלבד** — לרכב אין נושא אחר,
  ולולא הסינון הוא היה נספר פעמיים.
- **מסך `fleet`:** קיבל טאבים בדפוס `rAssets` — רכבים · יומן נסיעות ·
  **תקלות רכב** (עם מונה). `FLEET_TAB` + `b56SetFleetTab`. כפתור "+ רכב" מוצג
  רק בטאב הרכבים. אריח רכב עם תקלה פתוחה מסומן 🛠 באדום עם צ'יפ.
- **כרטיס הרכב:** כפתור "תיקונים / תקלות" (מנהל/משרד) + באנר תקלה פתוחה,
  באותו דפוס של עגלה ומכונה.
- **`b42CloseForm`:** `'הושבתה סופית'` היה ממופה למחרוזת קשיחה `'מושבתת'`.
  אצל רכב הערך הוא `'מושבת'`, ולכן הרמז למשתמש הציג מצב שאינו קיים.
  עבר ל-`meta.states[2]`. **תיקון שנלווה, לא היה בהיקף המקורי.**

**חלק 3 — דוח הצי (נלווה, סוגר פריט פתוח 1א)**

בדוח ישב משפט: *"תקלות רכב אינן נרשמות במערכת, ולכן אין כאן עמודת תקלות."*
מרגע ש-B56 עולה המשפט הזה שקר, ולכן הוסרו והוחלפו: הדוח קיבל עמודת **תקלות**
(פתוחות = מצב נוכחי, לא תלוי טווח; סגורות כשורת משנה) ועמודת **עלות תחזוקה**
(סך `fault_events` של הרכב בתוך הטווח בלבד). שתיהן נכנסו גם לייצוא ה-CSV.
תקלה פתוחה נוספה לדגלי האזהרה של הרכב.

**מה נבדק — 74 בדיקות אוטומטיות, כולן עברו**
- **שרת (31, `vm.createContext` מול הקוד האמיתי):** b42Asset לשלושת הסוגים +
  סוג לא מוכר · מחזור תקלת רכב מלא (דיווח → סטטוס `בטיפול` → סגירה → `פעיל`) ·
  סגירה כ"הושבתה סופית" → `מושבת` · חסימת תקלה כפולה · רישום טיפול לרכב כותב
  לארבעת השדות הנכונים ו**לא** לשדות של מכונה · רגרסיה שמכונה עדיין כותבת
  ל-`last_service` · עובד רצפה נחסם · סגירת רגל החזור עם `trip_confirmed_by` ·
  איסוף עצמי מדולג · הזמנת השכרה לא מושפעת · `b56CloseLaundryOrder` בארבעה
  תרחישים כולל אידמפוטנטיות והזמנה מבוטלת · `nobleDeliver` מקצה-לקצה ·
  קליטה פנימית בלי הזמנה · רגרסיה לחסימת מסירה בלי חתימה (B19d) ·
  `TABLES.vehicles` עדיין 22 עמודות ו-`asset_faults`/`fault_events` 15.
- **ממשק (43, jsdom מול `index.html` האמיתי):** ההפרדה בשני הכיוונים (תקלת רכב
  לא נראית במסך המכבסה ולהפך — כולל סגורות, לוחיות רישוי וחתכי עלויות) ·
  מוני הטאבים סופרים נכון בכל מסך · טאבי הצי · אריח וכרטיס הרכב · דוח הצי
  כולל אי-קיום המשפט שהתיישן · **ארבעת הקשרי הריצה** (צוות · פורטל · חנות ·
  קיוסק) נטענים בלי חריגה · חמשת התפקידים (`b42Can`) · נהג לא רואה טאב תקלות
  ולא כפתור תקלות · מכבסה לא רואה מונה · רגרסיה ל-B53 (`navIsSide`/`navMode`)
  ול-B47 (`NAV_DD`) · `API_URL` הושווה בית-בית מול הריפו וזהה.

**מה לא נבדק:** המראה בדפדפן אמיתי. גלישה אופקית בדוח הצי אחרי הוספת שתי
עמודות — הוספתי `overflow-x:auto` לכרטיס כאמצעי מניעה, אבל זה טעון עין.

### B53 — פריסת ניווט משולבת: שורה עליונה + סרגל צד ימני קבוע (29.07.2026)

**הרקע.** תפריט B47 (שורה אופקית עם חלוניות נפתחות) לא התאים לעבודה בפועל: כל מעבר
מסך דרש לחיצה, המתנה וחיפוש בתוך חלונית. אבי ביקש את הדפוס המקובל בתוכנות ניהול —
כל המסכים גלויים בסרגל צד קבוע, ובשורה העליונה קיצורים למסכים שנכנסים אליהם הרבה
(הכרעות N1–N3, 26.07.2026).

**מה נבנה**
- **שלושה מצבי תצוגה לאותו `#nav`.** `navIsSide()` = `min-width:1100px` → סרגל צד;
  `navIsTop()` = 900–1099 → תפריט B47 עם החלוניות, בדיוק כמו קודם; מתחת ל-900 →
  אקורדיון B23, בלי שינוי כלל. `navMode()` מחזיר `side/top/mob`, ו-`NAV_MODE` נשמר
  כדי שמעבר רוחב חלון ירנדר מחדש. **אין אלמנט ניווט שני ואין דפוס רינדור רביעי.**
- **הסרגל (N2/N3):** רשימה שטוחה מקובצת לפי `NAV_GROUPS`, כל המסכים המותרים תמיד
  ב-DOM, כותרת הקטגוריה היא `.gcap` — תווית, לא כפתור. רוחב 228px (198px בטווח
  1100–1279), `position:sticky` מתחת ל-header, גלילה פנימית בלבד.
- **קיצורי הדרך (N1):** `renderTopbar` נגזר מהגדרה אחת `nav_quick` במקום שלושה
  מסכים מקודדים. ברירת מחדל `orders,customers,deliveries,laundry,calendar`,
  סינון לפי `allowedViews()`, מקסימום 6, ניקוי כפילויות ומפתחות שגויים.
  האייקונים נלקחים מ-`NAV_GROUPS`/`DRIVER_TAB_ICONS` הקיימים — לא צויר אייקון חדש.
- **פאנל בהגדרות:** בחירת הקיצורים בסימון תיבות בעברית, מקובצות לפי קטגוריה,
  עם שדה מוסתר `set_nav_quick` ש-`saveSettings` קורא. **אבי לא מקליד מפתח טכני אחד.**

**החלטות שהתקבלו ולמה**
- **הפריסה נעשית ב-`#shell` (div עוטף שנוסף סביב `nav`+`main`) ולא ב-`#app`,
  בניגוד ל-BUILD_PLAN סעיף B53 הכרעה 4.** שלוש סיבות קונקרטיות שהתגלו בקוד החי:
  (א) `position:sticky` של אלמנט בתוך grid מוגבל לתא שלו — `header` היה מפסיק
  להיות דבוק לראש המסך; (ב) `#app` מקבל `style.display='block'` **inline** משני
  מקומות ב-JS (`applyLogin`, `enterOfflineIfPossible`), ו-inline גובר על כל כלל
  גיליון — ה-grid פשוט לא היה נכנס לתוקף; (ג) עמודה ריקה בפורטל/חנות/קיוסק —
  הסיכון נמחק לגמרי כי `#app` שם `display:none` ולא נגענו בו.
  **התוצאה הוויזואלית זהה לחלוטין להכרעת התכנון, וכל שלושת הסיכונים שה-BUILD_PLAN
  עצמו הזהיר מהם נעלמו.** `margin-right` לא הוחזר — האיסור נשמר כלשונו.
- **נקודת השבירה 1100px** כפי שנקבע בתכנון: בין 900 ל-1099 אין מקום לסרגל
  ולטבלאות הרחבות יחד, ושם נשאר תפריט B47. `NAV_DD`/`.gmenu` **לא נמחקו** —
  הם המנגנון של הטווח הזה, ותיקון B48a לא הלך לפח.
- **נהג:** נשאר עם סרגל הטאבים התחתון בכל רוחב. `body:not(.driver-mode)` מסייג את
  כל כללי הפריסה החדשים, ו-`renderNav` יוצא מוקדם לנהג כמו קודם.
- **הקיצורים מקבלים תווית טקסט** מ-1280px ומעלה ואייקון בלבד מתחת — שישה
  אייקונים בלי מילים אינם ניווט.

**מה נבדק** — 106 בדיקות אוטומטיות (jsdom מול `work.html` האמיתי), כולן עברו:
שלד ה-DOM ומיקום ה-header מחוץ ל-`#shell` · שלושת מצבי הניווט · כל המסכים המותרים
בסרגל ואף אחד מעבר · רגרסיה מלאה ל-1000px (פתיחת חלונית B47/B48a) ול-800px
(אקורדיון) · חמשת התפקידים · ארבעת הקשרי הריצה כולל `kioskBoot` · קיצורים:
ברירת מחדל, הגדרה מותאמת, מפתח שגוי, מסך אסור לתפקיד, תקרת 6, כפילויות ·
פאנל ההגדרות: סימון, ביטול, תקרה, שדה מוסתר · מעבר רוחב חלון בשני הכיוונים ·
`API_URL`/`NAV_GROUPS`/`DRIVER_TAB_ORDER`/`allowedViews`/`VIEWS` זהים בית-בית ·
פאנל הקיוסק של B52 עדיין נקרא ב-`rSettings`. `node --check` עבר.

**מה לא נבדק**
- **דפדפן אמיתי.** jsdom אינו מנוע פריסה — הסרגל, ה-sticky וגלישת הטבלאות
  נבדקו כקוד, לא בעין. זו הבדיקה הידנית הקריטית של האצווה.
- **גלישה אופקית בששת המסכים הרחבים** (`items`, `orders`, `finance`, `payroll`,
  `audit`, `reports`) ב-1100px / 1280px / 1440px. הריפוד צומצם והסרגל הוצר
  ל-198px מתחת ל-1280px כדי להקדים תרופה, אבל צריך עין.
- טאבלט בשכיבה (בדיוק 1024px → נשאר תפריט B47, כמתוכנן).

### B52 — מסך קיוסק: הקשר ריצה רביעי (29.07.2026)

**מה נבנה**

*שרת (`קוד שרת.txt`)*
- **`clockCore(db, emp)`** — ליבת הנוכחות חולצה למקום אחד יחיד. גם `case 'clock'`
  וגם `b52KioskClock` קוראים לה. אין שכפול של לוגיקת נוכחות במערכת.
- **`b52RegisterKiosk`** (מנהל, כתיבה) — מגריל טוקן 32 תווים, שומר **hash בלבד**
  ב-ScriptProperties תחת `kiosk_token_hash`, ומחזיר את הטוקן הגלוי **פעם אחת**.
  מכשיר יחיד בשלב ההרצה — רישום חדש דורס את הקודם (K1).
- **`b52RevokeKiosk`** (מנהל, כתיבה) — מוחק את ה-hash. הקיוסק מת מיד.
- **`b52KioskStatus`** (מנהל, READ_ONLY) — אבחון על המסך לפי כלל R1: האם יש מכשיר
  רשום, כמה עובדים פעילים בכלל יכולים להחתים, מי בלי מספר אישי, ואילו מספרים
  משוכפלים.
- **`b52KioskClock`** (כתיבה) — מאותרת בזכות טוקן המכשיר בלבד. מחזירה
  `{ok, name, direction, time}` ו**שום דבר מעבר לזה** (K3).
- **ענף אימות רביעי ב-`doPost`** — רשימה **סגורה ומפורשת** `B52_KIOSK_ACTIONS`
  (`b52KioskClock` · `checkVersion`), לא התאמת קידומת. סשן רגיל אינו נכנס לענף
  הזה, וטוקן קיוסק אינו פותח שום פעולה אחרת.
- **הגבלת קצב פר-מכשיר** — 5 ניסיונות ל-10 דקות ב-CacheService, דפוס
  `b38VerifyManagerPin`. הצלחה מאפסת את המונה.

*ממשק (`index.html`)*
- **מצב `?kiosk=1`** — יציאה מוקדמת בטעינה, בדיוק כמו `portal=1`/`shop=1`:
  בלי `renderNav`, בלי `getAll`, `DB` נשאר `null`. `autoLogin` יוצא מוקדם,
  ה-Service Worker **מוחרג בכוונה** (מטמון shell ישן היה מסכל את הרענון העצמי).
- **מסך השעון** — שעון חי גדול (HH:MM:SS), תאריך לועזי + עברי, מקלדת ספרות
  בגובה 68px לכפתור, שדה ●●●● (הספרות עצמן לעולם אינן מוצגות), אישור/ניקוי.
  תמיכה גם במקלדת ספרות פיזית (ספרות · Enter · Backspace · Esc).
- **מסך משוב** — "שלום <שם> ✓ נרשמה כניסה 07:42" (ירוק) / "נרשמה יציאה" (כחול),
  5 שניות וחזרה לשעון. פרק הזמן הזה הוא גם מנעול ה-UI מפני הקשה כפולה.
- **פאנל ניהול** במסך ההגדרות (מנהל בלבד): רישום · ביטול · קישור להעתקה
  (מוצג פעם אחת) · אבחון המספרים האישיים · הערת תפעול על מצב קיוסק במכשיר.

**החלטות שהתקבלו ולמה**
1. **רשימה סגורה במקום קידומת.** ה-BUILD_PLAN הציע `action.startsWith('b52Kiosk')`.
   דחיתי: כל פעולה עתידית שתיקרא כך תיפתח אוטומטית למי שמחזיק טוקן מכשיר.
   `B52_KIOSK_ACTIONS` מחייבת החלטה מודעת לכל הוספה.
2. **`checkVersion` נכנסה לרשימה — אבל לא לשימוש שתואר במפרט.** `data_version`
   מתעדכן בכל **כתיבה** במערכת, לא בפריסת גרסה חדשה; הסתמכות עליו הייתה מרעננת
   את הטאבלט כמה פעמים בשעה. במקום זה: רענון עצמי פעם בשעה, רק כשהמסך במנוחה,
   ו-`checkVersion` משמשת **בדיקת קישוריות לפני הרענון** — כדי שטאבלט בלי רשת לא
   יישאר תקוע על שגיאת דפדפן עד שמישהו יבחין.
3. **מספר אישי משוכפל = סירוב, לא ניחוש.** `find` היה מחתים את הראשון ברשימה.
   שני העובדים היו מקבלים "כניסה נרשמה" והנתון היה שגוי בשקט. עכשיו שני המסלולים
   (קיוסק ושעון הצוות) מסרבים, והאבחון בהגדרות מציג את השמות.
4. **תיקון באג B37 (סופח, כלל 4א).** ראה "מה שבור" סעיף 4 למעלה. שני האופנים
   (`employee_id` ו-`pin`) נתמכים עכשיו, שניהם למנהל בלבד.
5. **הקיוסק אינו מקבל את טבלת העובדים כלל** — הזיהוי נעשה בשרת בלבד. `b38ScrubEmployees`
   אינה רלוונטית כאן כי אין payload של עובדים.

**מה נבדק** — 68 בדיקות שרת (`vm.createContext`) + 71 בדיקות ממשק (jsdom) = 139, כולן עברו.
`node --check` על שני הקבצים. ארבעת הקשרי הריצה. חמשת התפקידים.
נבדק במפורש: טוקן שגוי/חסר/מבוטל נדחה · `getAll`/`delete`/`b52RegisterKiosk` עם טוקן
קיוסק חסומות · 5 כשלונות חוסמים · היומן אינו מכיל את הטוקן ולא את המספר האישי ·
ההחתמה נכתבת לאותה טבלת `attendance` בדיוק.

**מה לא נבדק** — טאבלט פיזי אמיתי (מגע, גודל כפתורים עם כפפות, מצב Guided Access),
והתנהגות המכשיר אחרי שבועות של הפעלה רצופה.

### B51 — ביצועים: קיצוץ מטען getAll + עימוד יומן הפעולות (29.07.2026)

**שני הקבצים. אין שינוי סכימה — אין להריץ `setupDatabase` בגלל B51.**

**הרקע.** `getAllPayload` עבר על כל `TMAP` ושלח את כל 55 הטבלאות במלואן בכל רענון,
לכל משתמש. בגלל הטעינה העצלה (F1) זה גם אילץ **קריאה של כל גיליון** — כולל
`audit_log`, הטבלה שגדלה הכי מהר במערכת (שורה לכל פעולת כתיבה).

**מה נבנה**

*שרת:*
- **`SLIM_POLICY`** — אובייקט מדיניות אחד בראש הקובץ, ליד `LONG_TEXT_FIELDS`.
  לא if-ים פזורים. שני מצבים בלבד:
  `drop` — הטבלה **כלל אינה נקראת מהגיליון** ונשלחת כמערך ריק (`[]` ולא
  `undefined`, כי הממשק מניח שכל המערכים קיימים);
  `days` — רק N הימים האחרונים, עם `keep()` אופציונלי לרשומות שחייבות לעבור בכל גיל.
- **`out._slim`** — תיאור מה נחתך, נשלח לממשק כדי שמסכי היסטוריה יגידו
  "מוצגים N הימים האחרונים" ולא ייראו כאילו נתונים נעלמו.
- **`b51AuditPage {offset, limit, q, user, action, from, to}`** — עימוד שרת ליומן
  (READ_ONLY, מנהל בלבד). **קריאה חסומה בגודל בכל מסלול:** בלי סינון — חלון ישיר
  מסוף הגיליון (היומן נכתב ב-`appendRow`, כלומר כרונולוגי); עם סינון — סריקה
  לאחור באצוות של 2,000 עם תקרה של 20,000 שורות לבקשה. רשימות הסינון נבנות
  משתי עמודות ומ-5,000 השורות האחרונות בלבד.
- **`b51ScrubAudit {offset, preview}`** — ניקוי ערכים רגישים שנכתבו לפני B36.
  אצוות 500 שורות, כתיבה ישירה לעמודת `details` בלבד (לא `writeTable` על טבלה
  שאינה נטענת), **אידמפוטנטי**, ו**תצוגה מקדימה לפני ביצוע** לפי כלל R1.
- **`b51PayloadStats`** — אבחון משקל הנתונים על המסך (READ_ONLY, מנהל בלבד).
  זו התשובה של כלל R1 לדרישת ה-BUILD_PLAN "למדוד לפני/אחרי": אבי לוחץ ורואה,
  בלי לייצא כלום.

*ממשק:*
- `rAudit` נכתב מחדש מול השרת. אותם פילטרים, אותה תצוגה, מקור אחר.
  `AUDIT_SEQ` מונע מרוץ: שינוי פילטר תוך כדי טעינה **מנצח** את הבקשה הקודמת
  (הגרסה הראשונה חסמה כניסה חוזרת ואיבדה את השינוי — תוקן לפני מסירה).
- כרטיס "כלי תחזוקה (B51)" במסך היומן, מנהל בלבד.
- `b51SlimNote(table)` — הודעת חיתוך אחידה, מוזרקת ל-4 מסכי היסטוריה.

**החלטות שהתקבלו ולמה**

1. **שלוש טבלאות שה-BUILD_PLAN הציע לחתוך — אינן נחתכות.** ה-BUILD_PLAN עצמו
   דרש grep על כל צרכן לפני קביעת הרשימה, וה-grep הפיל שלוש:
   - `stock_moves` — **יתרת המלאי היא סכום כל התנועות מאז ומעולם**
     (`rDash`, `rItems`, `b40ItemBreakdown`, `whQty`). חיתוך = מספרי מלאי שגויים.
   - `package_moves` — יתרות האריזות מחושבות באותה שיטה (`pkgBalance`, `pkgLedger`).
   - `fault_events` — דוח "עלות תחזוקה לפי חודש" של B50 רץ על **טווח חופשי**,
     כולל שנים אחורה. חיתוך = עלויות שנעלמות בשקט.
   `attendance` ו-`payroll` לא נחתכו גם הן (דוח השעות של B50 ומדד Y1).
   **זה בדיוק המקרה שה-BUILD_PLAN התכוון אליו כשכתב "אין חיתוך בלי רשימת צרכנים".**
2. **`call_logs`, `payment_declarations`, `customer_messages` — אפס צרכנים ב-`index.html`.**
   B55 עובד מולן דרך פעולות שרת (`b10ApprovalsInbox`, `portalGetData`) ולא מ-`DB`.
   לכן `drop` מלא — לא חיתוך לפי ימים.
3. **`notifications` — ממתינות ונכשלות עוברות בכל גיל.** ווידג'ט ההתראות סופר אותן;
   חיתוך עיוור לפי תאריך היה מסתיר התראה תקועה מלפני חצי שנה. בדיוק המקרה שבגללו
   חיתוך "פשוט" הוא מסוכן.
4. **רשומה בלי חותמת זמן נשמרת תמיד.** שדה `ts` ריק לא יגרום לרשומה להיעלם בשקט.
5. **`laundry_events` ו-`laundry_task_events` — 180 יום ולא 90.** הכפתור הארוך
   ביותר בדוח ביצועי המכבסה הוא 90 יום; 180 נותן מרווח ולא נוגע בגבול.
6. **חלק 2 של B51 (באג החתימה, Bug 1) לא נבנה** — בוצע כבר ב-B49e.

**מה נבדק**
86 בדיקות שרת ב-`vm.createContext` מול הקוד האמיתי + 54 בדיקות ממשק ב-jsdom.
בין השאר: `audit_log` **לא נקרא כלל** (ריגול על קריאות הגיליון) · חיתוך לפי ימים ·
רשומה בלי `ts` נשמרת · `notifications` ממתין/נכשל שורדות · חמש הטבלאות המצטברות
חוזרות מלאות · `_slim` שורד את `b38ScrubEmployees` ואת `getDriverFilteredPayload` ·
עימוד היומן (שלושה עמודים, סדר יורד) · חמישה מסלולי סינון + offset · יומן ריק ·
`b51ScrubText` בשתי הצורות (JSON וטקסט חופשי) ואידמפוטנטיות · preview לא כותב ·
ריצה שנייה מחזירה 0 · הרשאות (משרד נחסם, מנהל עובר) · מרוץ בקשות · G1 לא נשבר ·
שלושת הקשרי הריצה · חמשת התפקידים.

**מה לא נבדק**
דפדפן אמיתי · **החיסכון בפועל על נתוני הייצור** — זה בדיוק מה ש"📊 משקל הנתונים"
נועד להראות · התנהגות `b51AuditPage` על יומן בן עשרות אלפי שורות (הקוד חסום בגודל,
אבל לא נמדד על נפח אמיתי) · ניקוי הסיסמאות על הגיליון החי.

### B50 — דוחות חסרים + ייצוא CSV + הכרעת Y1 (28.07.2026)

**ממשק בלבד. קובץ השרת לא נפתח ולא נמסר. אין שינוי סכימה.**

**מה נבנה**

*ייצוא:*
- `exportCsv(filename, headers, rows)` — פונקציה גנרית אחת. Blob עם **BOM (`\uFEFF`)**
  בתחילתו (חובה לעברית באקסל), נפילה ל-`data:` URI אם `Blob`/`URL` אינם זמינים.
  תאריכים `yyyy-mm-dd`, סכומים כמספר בלי `₪` — שאקסל יוכל לסכם.
- `B50_EXPORTS` + `b50Reg`/`b50Export`/`b50ExportBtn` — כל טבלה רושמת את המטען שלה
  תחת מפתח, והכפתור קורא למפתח. **בלי לשרשר נתונים לתוך `onclick`.**
  אין נתונים → הכפתור מנוטרל עם title מסביר.
- כפתור ⬇ ייצוא על כל טבלת דוח בקטלוג **וגם על שתי הטבלאות במסך התזרים (B43)** —
  פריט פתוח ישן, נסגר כאן.

*ארבעה דוחות חדשים ב-`REPORTS_CATALOG` (רשומה + `REPORT_RENDERERS[id]`, בלי לגעת ב-`rReports`):*
- **`hours` — שעות עבודה** (מנהל בלבד; שכר = מידע רגיש). פר עובד: ימי עבודה,
  שעות מזכות דרך **`attEff` הקיימת** (מכבדת פסילות — אין חישוב שכר חדש),
  שעות שנפסלו, ממוצע ליום, שעת התחלה ממוצעת. עובד בודד → פירוט יומי.
- **`utilization` — ניצולת השכרות** (מנהל+משרד). ימי השכרה = חפיפת
  `start_date..end_date` עם הטווח × qty; ניצולת = ימי השכרה / (מלאי כולל × ימי הטווח).
- **`fleet` — צי רכב** (מנהל+משרד). ראה הכרעה 1 להלן.
- **`maint` — עלות תחזוקה לפי חודש** (מנהל+משרד). הפריט הפתוח מ-B42, נסגר כאן.

*תשתית קטלוג:*
- `repParamsForm` קיבל סוג שדה `employee` (אותו דפוס בדיוק כמו `customer`)
  ותמיכה ב-`def` — ברירת מחדל לשדה ריק, כדי שדוח `autoRun` ייפתח על טווח הגיוני
  ולא ריק. שתי תוספות זעירות, בלי דפוס טופס שני.

*הכרעת Y1:*
- מדד ניצולת המכונות ב-`renderNobleReports` — חלון השעות הקבוע **בוטל**.
  המדד הראשון נמדד עכשיו מול **יום העבודה של הצוות בפועל**: `b50TeamDayWindow()`
  גוזרת לכל יום קלנדרי (מ-00:00) את החלון מההחתמה הראשונה של עובד מכבסה/רצפה
  ועד האחרונה. שני המדדים האחרים לא נגעו.

**החלטות שהתקבלו ולמה**

- **הכרעה 1 — דוח הצי מפלח לשתי עמודות, כפי ש-B49f דרש.**
  "משלוחים שבוצעו" מפוצל ל"הנהג תיעד בשטח" (סריקה/צילום — מוכח) מול
  "אישור הנהלה בדיעבד" (`trip_confirmed_by` מלא — הצהרה). עמודה שלישית:
  "נסיעות שבוטלו בקליטה" (`trip_auto_cancel` בלבד — **ביטול לגיטימי אינו נספר**).
  איחוד השלוש היה מוחק את כל הערך של ההפרדה שנבנתה ב-B49f.
- **הכרעה 2 — אין עמודת תקלות בדוח הצי, ודוח התחזוקה הוא דוח ציוד.**
  ⚠ **ממצא מהקוד החי:** `b42Meta` תומך ב-**עגלה ומכונה בלבד**, ול-`expenses`
  אין שיוך לרכב — כלומר **תקלות רכב אינן נרשמות במערכת כלל**. BUILD_PLAN B50 סעיף 4
  צפה את האפשרות והורה להשמיט ולציין. לכן: אין עמודת תקלות בדוח הצי, ו"עלות
  תחזוקה לפי חודש" נבנה כ**רשומת קטלוג נפרדת** ולא כדוח-משנה בתוך דוח הצי —
  עלות תחזוקת עגלות בתוך דוח צי רכב היא הטעיה. שני הדוחות מציינים זאת בכותרת.
- **הכרעה 3 — סתירה פנימית ב-BUILD_PLAN סעיף 1 (Y1) הוכרעה לפי הניסוח הסופי שלו.**
  הסעיף הציע תחילה להגדיר מחדש את המדד הראשון כ"מהאירוע הראשון עד האחרון" —
  אבל זה **בדיוק המדד השני שכבר קיים**. הניסוח הסופי באותו סעיף הוא הנכון,
  והוא שמומש: להסיר את מדד החלון הקבוע ולהוסיף במקומו "יחסית ליום העבודה של הצוות".
- **הכרעה 4 — הכנסה בדוח הניצולת נגזרת מספר החיובים (B54), לא מ-`order_lines`.**
  לכל הזמנה נלקח סכום שורת `'השכרה'` מ-`b54LedgerFE`, מוקצה לשורות ההזמנה
  דרך **`b54Alloc` הקיימת** לפי משקל `qty × unit_price`, ומוכפל בשיעור החפיפה של
  ההזמנה עם הטווח. כך הדוח מכבד חשבונית שהסכום בה שונה מסכום השורות, ולא מייצר
  מספר כספי חמישי במערכת. **אין בדוח שום `reduce` על כסף מחוץ לספר.**
- **הכרעה 5 — פריט בלי רישום תנועות מלאי מוצג בלי אחוז ניצולת (`—`), לא כ-0%.**
  אין לו מכנה. 0% היה נראה כמו פריט שלא הושכר.
- **הכרעה 6 — הנהג אינו מגדיר את יום העבודה של המכבסה.**
  `b50TeamDayWindow` סופרת עובדי `מכבסה`/`עובד רצפה` בלבד. נהג שמחתים ב-04:00
  היה מותח את המכנה ומדלל את אחוז הניצולת של כל המכונות. עובד שאינו מזוהה
  בטבלת העובדים **כן** נספר — עדיף להכליל מאשר לאבד יום.
- **הכרעה 7 — יום בלי נוכחות אינו נספר בשני הצדדים.**
  אם ליום יש פעילות מכונות אך אין החתמות, הוא יוצא גם מהמונה וגם מהמכנה
  (אחרת המדד היה קופץ מעל 100%). אין נוכחות כלל בתקופה → המדד מוצג `—`
  עם הסבר, ולא 0%.
- **הכרעה 8 — משלוח בלי `vehicle_id` נופל לרכב של הנסיעה המשולבת (`trip_id`),
  ואם גם זה חסר — לשורת "ללא שיוך רכב".** לא מושמט בשקט. הדוח מגלה בעצמו
  כמה משלוחים אינם משויכים (כלל R1 — אין ייצוא, המסך מראה).

**מה נבדק — 56/56 בדיקות אוטומטיות (jsdom, מול הקוד האמיתי)**
- ייצוא: escaping של פסיקים ומרכאות · BOM · שם קובץ · כפתור מנוטרל בלי נתונים ·
  `b50Export` על מפתח לא קיים אינו זורק.
- שעות: `attEff` מכובדת (9−1=8) · ממוצע ליום · שעת התחלה ממוצעת מה-`in_time`
  הראשון בכל יום · סינון לעובד בודד · פירוט יומי · טווח ריק · טווח הפוך.
- ניצולת: ימי השכרה = חפיפה × qty · הזמנה **מבוטלת** לא נספרת · הזמנת **כביסה**
  לא נספרת כהשכרה (C5) · אחוז מול המלאי · הכנסה מהספר · חפיפה חלקית מקצה יחסית.
- צי: **הפילוח של B49f** (2 בוצעו, מהם 1 אישור הנהלה ו-1 הנהג תיעד) ·
  `trip_auto_cancel` נספר וביטול לגיטימי לא · נפילה ל-`trip_id` · שורת "ללא שיוך" ·
  משלוח מחוץ לטווח · דגלי טסט/ביטוח/טיפול (פג מול מתקרב).
- תחזוקה: קיבוץ לפי חודש · אירוע בעלות 0 לא נספר · פילוח לפי נכס.
- Y1: אין יותר חיתוך לשעה קבועה בקוד · חלון הצוות מתעלם מהנהג · משמרת פתוחה ·
  האחוז המחושב נכון (240 דק' מתוך 690) · בלי נוכחות → `—` עם הסבר.
- הרשאות: מנהל רואה הכל · **משרד אינו רואה את דוח השעות** · מכבסה/רצפה לא ·
  נהג — `reports` אינו ב-`allowedViews` שלו.
- קטלוג: תשעה דוחות, לכל אחד `render` · ברירות המחדל ממלאות את הטופס והדוח רץ ·
  `goReport` עם פרמטר מאוכלס.
- תזרים B43: שני כפתורי ייצוא · סכומים כמספר בלי `₪`.
- שלושת הקשרי הריצה: צוות · `?portal=1` · `?shop=1` — נטענים בלי חריגה.
- רגרסיה: `b54LedgerFE` · `attEff` · `b40ItemBreakdown` · `dYmd` · קטלוג B49
  (דוח aging עם `customer_id`).
- `node --check` על בלוק ה-`<script>` — עבר.

**מה לא נבדק**
דפדפן אמיתי · פתיחת קובץ CSV באקסל בפועל (ה-BOM נבדק בקוד, לא בעין) ·
הדוחות על נפח נתוני ייצור · שעת ההתחלה הממוצעת מול נתוני נוכחות אמיתיים
(הפיקסצ'ר סינתטי) · האחוזים ההיסטוריים של ניצולת המכונות **ישתנו** — זו
ההכרעה, לא באג.

## היסטוריה (שורה לאצווה; פירוט מלא בכותרת קובץ השרת)
B60 — T3+T4: מצב מסך והיסטוריית דפדפן. **האצווה המאוחדת הראשונה.** ממשק בלבד, אין שינוי סכימה · T3: `REPORT_ID` לא אופס ב-`go()` — נכתב כלל אחיד בשלוש מחלקות (תת-מסך מתאפס · העדפת תצוגה נשמרת · הקשר מודל לא נוגעים), מרוכז ב-`B60_VIEW_RESET`; `UNDO_PENDING` חריג מפורש שלעולם לא מתאפס; נמצאו ותוקנו שתי מלכודות נוספות (`FLOOR_INTAKE_ORDER` · `RET`); `goFin` הועבר לעבור דרך `go()` · T4: `pushState`/`popstate` נבנו מאפס — "הקודם" מחזיר מסך, סוגר מודל, ולא יוצא מהאתר; הכתובת לא משתנה לעולם ולכן `replaceState` של הקיוסק לא נגע; מכובה לחלוטין בפורטל/חנות/קיוסק/מסך כניסה; אין נגיעה ב-`IDLE_LIMIT_MS` · החיכוך בין השניים נפתר בדגלים `keep`/`fromHistory` ב-`go(v,o)`. 60 בדיקות (28 T3 · 18 T4 · 8 הקשרי ריצה · 6 תפקידים), R7 באירועי DOM אמיתיים. R10: אפס בקשות שרת נוספו. (30.07.2026)
B59 — T5: קידום שלב לפי בחירה ולא לפי הסדר. השורש: לא היה בורר שלבים — בורר המכונה נראה כמוהו, ו-`advanceLaundry` חישבה `STAGES[idx+1]` קשיח · בורר "שלב יעד" עם תוויות השלב-הבא/דילוג/חזרה · `opts.to_stage` בשרת, ובהעדרו התנהגות B40 זההה ביט-אחר-ביט · מכונת אריזה ממופתת ל"בגיהוץ וקיפול" בלי עמודה חדשה · דרישת עגלה+מכונה של B40 לא נגעה. 85 בדיקות (39 שרת + 46 ממשק), כולל רגרסיה שמשחזרת את התקלה ובדיקת R10 (בקשה אחת לקידום; דילוג חוסך בקשה שלמה = 2.7 שנ׳). (30.07.2026)
B58 · אימות סופי (ACT-19 + ACT-18, 30.07.2026 19:07) — זהות מלאה 55/55, המסלול המהיר פעיל. `getAll` מלא ירד מ-~14 שנ׳ ל-**1.6 שנ׳**; `batchGet` 553 מ״ש מול 12.8 שנ׳ = **פי 23.2**. הגענו לרצפת התקורה של Apps Script (2.7 שנ׳ לבקשה) — כל שיפור עתידי חייב להיות צמצום **מספר** הבקשות (R10), לא אופטימיזציה של משקל. PERF-02 סגור.
B58a — תיקון אי-ההתאמה שאבחון ACT-17 גילה בנתוני אמת (4 טבלאות עם עמודות תאריך): קריאה בשני ייצוגים (`SERIAL_NUMBER` + `FORMATTED_STRING`) מזהה תא-תאריך בוודאות במקום לנחש לפי תבנית · `b58SerialYmd` באריתמטיקה טהורה · האבחון מצביע על התא המדויק (שורה·עמודה·ערכים·טיפוסים) · זהות מלאה מפעילה מחדש את המסלול המהיר לבד. נמדד אצל אבי: 486 מ״ש מול 13.3 שנ׳ — פי 27.3. 97 בדיקות, כולל רגרסיה שמשחזרת את התקלה ובדיקת R10 (בקשה אחת לקידום; דילוג חוסך בקשה שלמה = 2.7 שנ׳). (30.07.2026)
B58 — PERF-02 תיקון האיטיות: `batchGet` אחד במקום 55 נסיעות (`b58Prefetch`, עם אימות עצמי מול `readTable` פעם ב-6 שעות וכיבוי אוטומטי באי-התאמה) · מטמון פר-טבלה (`b58_table_versions`) בלי לשנות את סמנטיקת `checkVersion` · חיווי טעינה בשני מסלולי `autoLogin` · `pollDataVersion` מושך רק טבלאות שהשתנו (`b58Tables`) ואינו שולח דבר כשהלשונית ברקע. 80 בדיקות אוטומטיות, מטען זהה ביט-אחר-ביט לחמשת התפקידים. (29.07.2026)
B57 — PERF-01 אבחון האיטיות (READ_ONLY, בלי תיקון): `b57PerfProbe` — זמן ומשקל לכל טבלה · זמן שליפה/בנייה/סריאליזציה · מבחן ישיר האם המטען נכנס למגבלת CacheService · מוני hit/miss ב-`getAllCached` · מקצב דריסת המטמון מחושב מחותמות `audit_log` בלי לגעת ב-`bumpDataVersion`. הכרעת T4: הרענון תסמין, “הקודם” באג נפרד. (29.07.2026)
B56 — רגל החזור של הזמנת כביסה נסגרת במסירה · רכב כסוג נכס שלישי בתקלות, בהפרדה מלאה מהמכבסה · דוח הצי קיבל תקלות ועלות תחזוקה (29.07.2026)
B53 — פריסת ניווט משולבת: `navIsSide()` 1100px+ · סרגל צד ימני קבוע עם כל המסכים (`.gcap` תווית, לא כפתור) · `#shell` כעוטף פריסה במקום grid על `#app` · קיצורי `nav_quick` מוגדרים בהגדרות בסימון תיבות · תפריט B47 נשאר לטווח 900–1099.
B52 — מסך קיוסק: הקשר ריצה רביעי `?kiosk=1` · טוקן מכשיר (hash ב-ScriptProperties) · `B52_KIOSK_ACTIONS` רשימה סגורה · `clockCore` כליבת נוכחות יחידה · הגבלת 5 ניסיונות/10 דק' · אבחון מספרים אישיים · **תיקון באג ההחתמה של B37**.
B51 — SLIM_POLICY לקיצוץ getAll (audit_log/call_logs/B55 לא נשלחות כלל; laundry 180י'; date_changes 365י'; notifications 90י' + ממתין/נכשל) · b51AuditPage עימוד שרת · b51ScrubAudit עם תצוגה מקדימה · b51PayloadStats. stock_moves/package_moves/fault_events/attendance/payroll **לא נחתכו** — יתרות והיסטוריה מצטברת.
B50 — דוחות שעות עבודה / ניצולת השכרות / צי רכב + עלות תחזוקה לפי חודש · exportCsv גנרי עם BOM (כולל תזרים B43) · הכרעת Y1: חלון השעות הקבוע במדד ניצולת המכונות בוטל.
B49f — אישור הנהלה שהנסיעה בוצעה: b49fCloseLegs מאחדת שלושה אתרי הכרעה · שאלה בשער הקליטה · trip_confirmed_by/trip_auto_cancel · b49fRestoreTrip לריפוי בדיעבד.
B55 — פורטל הלקוח המלא: פירוט חיובים · הודעת תשלום עם אסמכתא + אישור במשרד · שיק ממתין לפירעון · פניות מקושרות · חיוב ידני הפך לגָביל.
B54 — ספר החיובים: b54Ledger כמקור יחיד · טבלה charges · חמישה מקורות · C8 מוציא קליטה מקושרת מהמרוכזת · C9 מובטח בהגדרה · C10 תשלום נספר רק אם נגבה.
B49d — כביסה מלקוח: nobleIntake מקבל order_id · b49dAddCarts (L2, בלי תקרה) · פתיחת קליטה סוגרת את רגל ההלוך.
B49e — איסוף עצמי מול משלוח כשני מצבים עצמאיים · חתימה מופרדת (signatures) · P7 ביטול נסיעה שלא בוצעה · תיקון פד החתימה (הוקדם מ-B51)
C1–C3, D, F1, G1 — תשתיות מוקדמות: עמודות, אימיילים, מדפים/ברקודים, תיוג, טעינה עצלה, קיצוץ טקסטים ארוכים.
NC1–NC4 — מודול מכבסה: רצפה, לוח חי, חשבונית מרוכזת, יתרות.
B1 — חשבוניות/קבלות + מספור + הקצאה (S7) + PDF ל-Drive.
B2 — מנוע תשלום ואשראי (S1/S2/S5, D7/D9/D15), אגורות, idempotency.
B3 — פקדון/אימות אשראי + ערבויות (stub סליקה; credentials ב-ScriptProperties — S4).
B4 — התראות אימייל + טריגר יומי (b4RunNow).
B5 — Driver-Lite: מסלול, הסרת כתובת, תיעוד שיחות, סטטוסים.
B7/B9/B10 — superadmin · דוח גיל חובות + חריגים · מרכז אישורים.
B13 — חתימה דיגיטלית במסירה (Bug 1 פתוח) · B14 — BI · B16b — מסירה בצילום בלי לקוח.
B20 — כרטיס עובד מרוכז (openEmp) · B23 — תפריט מובייל.
B31 — הבטחת חשבונית · B34 — סריקת העמסה · B35 — מדפים ואצוות.
B36–B37 — שעון נוכחות, מספר אישי, בורר עובד למנהל.
B38 — אבטחה: סינון pin/password_hash, אימות PIN בשרת, hard-fail, ymdLocal/dYmd, בלוק "טרם שובץ נהג".
B39–B42 — אייקוני ציוד · פיצול כביסה · סגירת איסופים תקועים · תחזוקה, תקלות, ספקי שירות.
B43 — תזרים, מועדי פירעון, חובות לקוחות (b43ReceivablesFE).
B44–B45a — כלל מע"מ אחיד.
B46 — PDF אמיתי · אריחי עובד ורכב · photo_url.
B47 — תפריט עליון בדסקטופ · B48a — תיקון פתיחת החלוניות (evtInside).
B48 — מקור אמת אחד ליתרות לקוחות (b48BalancesAg) · פנוי-על-המדף להתראת מינימום · מסנן מחסן במלאי.
B49 — מרכז דוחות: קטלוג REPORTS_CATALOG, טופס פרמטרים אחיד, goReport לדריל-דאון.
B49b — קליטת מכבסה ללא עגלה פיזית: עגלה וירטואלית (carts.virtual, tare_kg=0), בלי שינוי בשקילה/שלבים/מסירה.
B49c — גשר לוח הכביסה ← יומן NOBLE: החזרה פותחת קליטה פנימית, קידום שלב כותב לשני היומנים, L3 (ready_ts).

## פתוח לטיפול (לפי עדיפות)

000. ✅ **`T3` + `T4` = B60. בוצע 30.07.2026.** נותר פריט אימות אחד שרק אבי
    יכול לסגור, והוא **חייב להיעשות בדפדפן אמיתי**: ללחוץ "הקודם" ולוודא
    שהוא מחזיר מסך ולא יוצא מהאתר, ושהוא סוגר מודל פתוח. **jsdom אינו דפדפן.**
    ⚠ לבדוק גם בטלפון — מחוות ההחלקה לאחור עוברות דרך אותו `popstate`.

00. ✅ **PERF-02 = B58 — תיקון האיטיות. בוצע 29.07.2026.** נותרו שני פריטי
    אימות שרק אבי יכול לסגור, ושניהם על **נתוני אמת**:
    (א) ✅ **סגור.** `ACT-17` (30.07) מצא תקלה בארבע טבלאות תאריך → תוקן ב-B58a.
    `ACT-19` (30.07, 19:07) אישר: **זהות מלאה 55/55, המסלול המהיר פעיל.**
    `batchGet` **553 מ״ש** מול **12.8 שנ׳** — **פי 23.2**.
    (ב) ✅ **סגור.** `ACT-18` — קו הבסיס החדש נמדד: `getAll` מלא **1.6 שנ׳**
    מהדפדפן. **הגענו לרצפת התקורה של Apps Script (2.7 שנ׳ לבקשה).**
    ⚠ **אין פריטים פתוחים ב-PERF-02. האצווה סגורה במלואה.**

00א. 🟡 **`BLD-11` — snapshot ליתרות מלאי. נדחה במכוון ב-B58.**
    `stock_moves` שוקלת אפס ואינה בעשרים הטבלאות הכבדות. **חוזרים אליה רק
    אם מדידה עתידית תראה שהיא נכנסה לצוואר** — לא לפני כן.

0. ✅ **B54 — ספר החיובים. בוצע 28.07.2026.** נותר פריט המשך אחד:
   (א) ✅ **נסגר ב-B55** — חיוב ידני עצמאי הפך לגָביל: `b55EnsureDocsForDebt`
   מפיק לו חשבונית לפני הגבייה, ובמסך הגביה יש כפתור "הפק חשבונית וגבה".
   `payments` לא שונתה ו-C7 נשמר.
   (ב) קליטה שנקשרה להזמנה **אחרי** שהופקה לה חשבונית — הסכום אינו כלול במסמך.
   `b54LedgerAudit` מדווח על מקרים כאלה; אם יימצאו — להכריע איך מטפלים.
0א. ✅ **B55 — פורטל הלקוח המלא. בוצע 28.07.2026.** נותרו שני פריטי המשך:
   (א) **חיבור ספק סליקה** — כשיהיה חשבון, ה-callback שלו קורא ל-
   `b55ConfirmDeclaration` עם אותו `txn_id`. תפר אחד, בלי שכתוב.
   (ב) `b55EnsureDocsForDebt` מפיק חשבוניות ל**כל** החוב הפתוח של הלקוח בעת
   האישור, לא רק לחלק שמשולם — זהה להתנהגות `b31EnsureInvoice` הקיימת.
   אם אבי ירצה הפקה סלקטיבית — batch קטן.
0ב. ✅ **B49f — אישור הנהלה שהנסיעה בוצעה. בוצע 28.07.2026.** נותר פריט המשך אחד:
   **דוח צי הרכב ב-B50 חייב לפלח לפי `trip_confirmed_by`** — נסיעה שהנהג הוכיח
   ונסיעה שההנהלה אישרה בדיעבד אינן אותו דבר, ואיחוד שלהן יאבד את כל הערך
   של ההפרדה שנבנתה כאן.
1. ✅ **B50 — דוחות חסרים + ייצוא CSV. בוצע 28.07.2026.** דוח הצי מפלח לפי
   `trip_confirmed_by` כנדרש; הכנסות דוח הניצולת נגזרות מספר החיובים (B54).
   נותרו שני פריטי המשך:
   (א) ✅ **נסגר ב-B56 (29.07.2026).** רכב הוא סוג נכס שלישי ב-B42, ודוח הצי קיבל
   עמודת תקלות ועמודת עלות תחזוקה. ⚠ **בהפרדה מלאה מתקלות המכבסה — הכרעת אבי.**
   (ב) דוח הניצולת מודד ניצולת **פריטים**, לא ניצולת יחידות ממוספרות (`units`).
   מספיק להחלטות רכש; אם יידרש דיוק ברמת יחידה — batch נפרד.
2. ✅ **B50 — הכרעת Y1 יושמה.** חלון השעות הקבוע במדד ניצולת המכונות בוטל;
   המדד נמדד מול יום העבודה של הצוות בפועל (`b50TeamDayWindow`).
3. ✅ **B51 — ביצועים ויומן הפעולות. בוצע 29.07.2026.** נותרו שלושה פריטי המשך:
   (א) **החיסכון בפועל לא נמדד על נתוני ייצור** — יש כפתור "📊 משקל הנתונים"
   במסך היומן. אחרי שאבי ילחץ, לרשום כאן את המספר האמיתי.
   (ב) **`attendance` ו-`payroll` לא נחתכו** ויגדלו בקצב קבוע. כשיתחילו להכביד,
   הפתרון אינו חיתוך לפי ימים — דוח השעות של B50 ומדד Y1 נשענים עליהן ישירות —
   אלא העברת שני הדוחות לפעולת שרת ייעודית. batch נפרד.
   (ג) **`stock_moves` תגדל ללא גבול ואי אפשר לחתוך אותה** כי יתרת המלאי היא
   סכום כל התנועות. הפתרון הנכון הוא **יתרת פתיחה תקופתית** (snapshot חודשי
   ב-`items` או טבלה חדשה) — batch בפני עצמו, נוגע בליבת המלאי.
4. ✅ **B51 — ניקוי `audit_log` מסיסמאות. נבנה 29.07.2026.**
   ⚠ **ממתין לביצוע בפועל ע"י אבי** — הכפתור קיים, ההרצה לא בוצעה.
   (Bug 1, חתימה בטאבלט, הוקדם ובוצע ב-B49e.)
5. ✅ **B52 — מסך קיוסק. בוצע 29.07.2026.** נותרו שלושה פריטי המשך:
   (א) **מכשיר יחיד בלבד** — `kiosk_token_hash` הוא מפתח בודד, ורישום שני דורס
   את הראשון. אם יידרשו שני טאבלטים (למשל שער נוסף) — מעבר למפתח לכל מכשיר
   (`kiosk_token_hash_<n>`) עם שם ידידותי. batch קטן.
   (ב) **תיקון נוכחות היסטורית מבאג B37** — נוכחות שנרשמה דרך בורר העובד לפני
   B52 שויכה למנהל במקום לעובד. אין דרך אוטומטית לזהות אילו רשומות — אבי צריך
   להחליט אם לתקן ידנית ואילו חודשים.
   (ג) **עובדים בלי מספר אישי אינם יכולים להשתמש בקיוסק.** האבחון בהגדרות מציג
   את הרשימה; מילוי המספרים הוא פעולה של אבי בכרטיסי העובדים.
6. ✅ **B53 — פריסת ניווט משולבת. בוצע 29.07.2026.** נותרו שלושה פריטי המשך:
   (א) **ששת המסכים הרחבים לא נבדקו בדפדפן** — `items`, `orders`, `finance`,
   `payroll`, `audit`, `reports` מאבדים 228px (198px מתחת ל-1280). אם אבי מדווח
   על גלישה אופקית — הפתרון הוא גלילה בתוך הכרטיס למסך הספציפי, לא הרחבת הסרגל.
   (ב) **מובייל וטווח 900–1099 לא שונו במכוון.** אם אבי ירצה את הסרגל גם בטאבלט
   בשכיבה (1024px) — זו הורדת נקודת השבירה, batch קטן, אבל צריך קודם לראות
   שהטבלאות נכנסות.
   (ג) **סדר הקיצורים** נקבע לפי סדר הסימון בפאנל. אם יידרש סידור מחדש בגרירה —
   batch קטן ונפרד.
7. ✅ **נסגר ב-B56 (29.07.2026).** מסירת הכביסה הנקייה סוגרת עכשיו את רגל החזור
   של ההזמנה (`b56CloseLaundryReturnLeg`) ואת ההזמנה עצמה (`b56CloseLaundryOrder`).
   ⚠ **ממתין לאימות אבי על נתוני ייצור:** הזמנות כביסה **היסטוריות** שנמסרו לפני
   B56 נשארו עם רגל חזור פתוחה — התיקון חל מכאן והלאה בלבד ואינו רטרואקטיבי.
   אם יתברר שיש הרבה כאלה, פעולת ריפוי אידמפוטנטית עם תצוגה מקדימה (כלל R1)
   היא batch קטן בפני עצמו.
8. (נבלע ב-B54 — בוצע.)
8א. **מ-B54:** קובצי הבדיקות אינם נשמרים בריפו, ולכן כל batch כותב את הרגרסיה
    מאפס. לשקול `tests/` בריפו — יחסוך זמן וימנע אובדן כיסוי.
8ב. **מ-B56:** `technician_visits` (ביקורי טכנאי) נשאר **בתחום המכבסה בלבד** —
    לרכב אין "ביקור טכנאי" אלא מוסך, ותקלת רכב נסגרת עם עלות ישירות בלי ביקור.
    אם אבי ירצה יומן ביקורי מוסך — batch קטן ונפרד. **החלטה מכוונת, לא השמטה.**
8ג. **מ-B56:** הוצאות תחזוקת רכב נכנסות למנגנון ההוצאות הקיים דרך `b42CreateExpense`
    כמו כל תקלה, אבל ל-`expenses` עדיין אין שיוך ישיר לרכב. עלות התחזוקה בדוח הצי
    נגזרת מ-`fault_events`, לא מ-`expenses`. מספיק להיום; אם יידרש דוח הוצאות
    לפי רכב מתוך הנהלת החשבונות — צריך עמודה ב-`expenses` (שינוי סכימה).
9. כפתור השבתת ספק שירות (השדה `active` קיים, אין כפתור).
10. החלטה סופית: סדר תצוגה בחנות ובפורטל (נטו ראשי מול ברוטו ראשי).
11. חשבוניות מכבסה מרוכזות (`nobleCreateInvoice`) אינן מפיקות מסמך PDF כלל.
    **נסגר ב-B54:** כל חשבונית — מכל מקור — עוברת דרך נתיב מסמך אחד.
12. ✅ **נסגר ב-B50:** דוח "עלות תחזוקה לפי חודש" נבנה כרשומת קטלוג נפרדת
    (`maint`) ולא כדוח-משנה בדוח הצי — כי יומן התקלות מכסה עגלות ומכונות בלבד.
13. ✅ **נסגר ב-B54:** `customerBalance` נשארה כעטיפה דקה מעל הספר
    (`agIls(b48CustomerBalanceAg)`). לא הוסרה — היא נקראת מ-`case 'reports'`.
14. מסך `bi` לא עבר לדפוס הקטלוג של B49 — נשאר כך בכוונה (BUILD_PLAN B49 סעיף 3).
    לתיעוד בלבד, לא פריט לטיפול.

## מפרטים ממתינים
- ✅ **מפרט B54** — **בוצע 28.07.2026.** נשמר בסוף מסמך זה לתיעוד ההכרעות C1–C11.
- ✅ **מפרט B55** — **בוצע 28.07.2026.** נשמר בסוף מסמך זה לתיעוד ההכרעות P1–P7
  ולתיעוד שתי הסטיות המודעות ממנו (טבלה נפרדת להצהרות; סכום במקום allocations).
- ✅ **מפרט B49f** — **בוצע 28.07.2026.** נשמר בסוף מסמך זה. שני הסעיפים שהמפרט
  השאיר פתוחים לצ'אט המבצע הוכרעו ע"י אבי: עמודה חדשה (לא מיחזור `manual_intake_by`)
  וריפוי בדיעבד — כן, 30 יום. **העוגן השלישי (`b49dClosePickupAfterIntake`) טופל
  ע"י איחוד שלושת אתרי ההכרעה לפונקציה אחת, כפי שהמפרט הציע כחלופה.**
- ❌ **מפרט B49g — בוטל 28.07.2026.** נבלע ב-B54. שני הממצאים שלו נשמרו שם
  במלואם (חשבונית מרוכזת בלי סינון `order_id` · `orderTotal` סופר שורות כביסה).
  אין לבנות אותו בנפרד.
- `BUILD_PLAN.md` בידע הפרויקט: B53 — כתוב במלואו. **B50, B51 ו-B52 בוצעו.**
- ⚠ **לקח מ-B52, רביעי במספר:** סעיף "רענון עצמי" של B52 ב-`BUILD_PLAN` הציע
  להשתמש ב-`checkVersion` כדי לזהות גרסה חדשה — אבל `data_version` מתעדכן בכל
  כתיבה במערכת, לא בפריסה. המפרט תיאר כוונה נכונה במנגנון לא נכון. **תמיד לאמת
  מה הפונקציה שהמפרט מזכיר עושה בפועל.**
- ⚠ **לקח מ-B50, שלישי במספר:** סעיף 1 של B50 ב-`BUILD_PLAN` סתר את עצמו — הגדיר
  מדד "חדש" שהיה זהה למדד קיים, ורק הניסוח האחרון באותו סעיף היה הנכון. סעיף 4
  שלו הניח אולי שתקלות רכב נרשמות — הן לא. **מפרט תכנון הוא כוונה, לא מציאות.**
- ⚠ **לקח מ-B48, אומת מחדש ב-B49, ב-B49d ושוב בסשן התכנון של B54:** סעיפי
  "מצב קיים" במסמכי מפרט עלולים לתאר קוד שהתיישן, ולעיתים גם טריגר שאינו קיים
  בקוד כלל. תמיד לאמת את העוגנים מול הקוד החי לפני שבונים, ולעצור מול אבי
  כשנמצאת סתירה.
- ⚠ **תלות מחייבת מ-B48:** `b48BalancesAg` (שרת) · `custBalance` (ממשק) ·
  `b2CreditUsedAg` (מנוע האשראי) — שלושתם חייבים להחזיר אותו מספר.
  **ב-B54 שלושתם נגזרים מספר החיובים, והבדיקה האוטומטית של השוויון נשמרת.**
- כלל בדיקה מחייב מ-B48a: **כל בדיקת ניווט/קליק מדספאטצ'ת אירוע DOM אמיתי**
  (`MouseEvent`/`change` עם `bubbles:true`). ב-jsdom במצב `outside-only` מטפלי
  `on*` בתגית אינם מקומפלים — יש לחבר אותם כמאזינים אמיתיים לפני הדיספאטץ'
  (עוזר `wire()`). קריאה ישירה ל-handler אינה קבילה.
- **לב ההרחבה (הוכח ב-B50):** `REPORTS_CATALOG`/`REPORT_RENDERERS`/`repParamsForm` —
  דוח נוסף = רשומה בקטלוג + פונקציית render, בלי לגעת ב-`rReports`. ב-B50 נוספו
  ארבעה דוחות כך. הטופס תומך עכשיו גם בסוג שדה `employee` ובברירת מחדל (`def`).
- **ייצוא CSV (B50):** `exportCsv(filename, headers, rows)` היא הפונקציה היחידה.
  טבלה חדשה שרוצה ייצוא = `b50Reg(key, filename, headers, rows)` + `b50ExportBtn(key)`.
  **אין לשרשר נתונים לתוך `onclick`.**
- כיוון עתידי (לא מתוכנן): פיצול לאפליקציות ייעודיות לפי סיווג (הנהלה/נהגים/ייצור).

---

---

## מפרט B54 — ספר החיובים: מקור אחד לכל חיוב לקוח

נכתב 28.07.2026 בסשן תכנון על מודל חזק, אחרי חקירת כל שרשרת הכסף בקוד החי
(v4.45-B49d). **מחליף ומבטל את מפרט B49g** — שני הממצאים שלו נשמרו כאן.
**דחיפות: גבוהה — חשיפה כספית פעילה.**

### הכרעת אבי (28.07.2026) — מילה במילה

> "שיהיה רק מקום אחד שיירשמו בו חיובים ללקוח. לא רוצה כפילויות ובלבולים.
> לכל חיוב יהיה המקור של החיוב (כביסה/השכרה/חיוב חוסרים/משלוח/חיוב כללי).
> חיוב כביסה — רק אחרי שקילה או באמצעות הקלדה שלנו במשרד/הנהלה."

### הממצא: חמישה מקורות כסף, אף אחד לא יודע על האחרים

אומת בקוד החי, כולל מספרי שורות:

| מקור הכסף | מי מחזיק אותו | איך נכנס לחוב היום |
|---|---|---|
| שורות הזמנה (השכרה) | `order_lines.unit_price`, תמחור ב-`linePrice` (שרת 3594) | `orderTotal` (4275) → `b2OrderBalanceAg` (5445) → `b43Receivables` (6405) |
| שורות הזמנה (כביסה) | אותו מקום, `items.wash_price` | **אותו מסלול — ולכן חוב שווא** |
| דמי שינוע | `orders.delivery_fee` | נבלע בתוך `orderTotal`, בלי זהות משלו |
| חיוב חוסרים | `orders.shortage_charge` | נבלע בתוך `orderTotal`, בלי זהות משלו |
| כביסה לפי משקל | `laundry_intakes.total_charge`, נקבע ב-`nobleWeigh` (4699) | חשבונית מרוכזת `nobleCreateInvoice` (5010) → בלוק החשבוניות ב-`b43Receivables` |
| חיוב כללי / ידני | **אינו קיים כלל** | — |

**שני הממצאים של B49g, שנשמרים כאן:**
1. `nobleCreateInvoice` (5013) והמסך המקביל בממשק (שורה 4639) מסננים חמישה תנאים —
   לקוח · `internal !== 'כן'` · `status === 'נמסר'` · אין `invoice_id` · `total_charge > 0` —
   **ואין בהם `order_id`.** קליטת B49d עם `order_id` תיכנס לחשבונית המרוכזת גם אם
   ההזמנה חויבה. חיוב כפול.
2. `orderTotal` סופר `qty × unit_price` **בלי לבדוק סוג הזמנה**. כל הזמנת כביסה
   מאושרת = חוב שווא בכרטיס הלקוח, בגיל החובות, בתזרים, במסגרת האשראי ובפורטל.

**ממצא שלישי, חדש:** דמי השינוע וחיוב החוסרים אינם ניתנים לזיהוי בשום דוח —
הם מספר אנונימי בתוך `orderTotal`. אבי ביקש מקור לכל חיוב; זה חסר גם בהשכרה.

**ממצא רביעי:** אין שום מסלול להקלדת חיוב ידני. הדרך היחידה היום היא חשבונית
ללא הזמנה — כלומר לייצר מסמך רק כדי לרשום חיוב. זה בדיוק ה"טלאי" שאבי ביקש למנוע.

### עקרון-העל של המפרט הזה

**כסף לא נשמר פעמיים.** מקום אחד שממנו **קוראים** את כל החיובים, ומקום אחד
לכל חיוב שממנו הוא **נגזר**. טבלה חדשה שמעתיקה כסף שקיים כבר בטבלה אחרת
היא בדיוק המחלה שהכרעה S5 ו-B48 נועדו לרפא (drift בין שני מקורות). לכן:

> **persist רק את מה שאין לו טבלת אב. את השאר לגזור.**

### ההכרעות הארכיטקטוניות C1–C11 (מחייבות; אין לצ'אט המבצע שיקול דעת בהן)

**C1 — ספר אחד, פונקציה אחת.** `b54Ledger(db)` בשרת ו-`b54LedgerFE()` בממשק
הן **המקום היחיד** שבו חיוב לקוח מחושב. כל צרכן — חוב, גיל חובות, אשראי, תזרים,
כרטיס לקוח, פורטל, דוחות, חשבונית — קורא משם ורק משם. אין חישוב כסף מקומי בשום
מסך. `b43Receivables` נשארת בשם שלה כ**עטיפה** שמחזירה את שורות הספר עם יתרה
חיובית (חתימה זהה, כדי שלא לשבור את שמונת הקוראים שלה), אבל הלוגיקה עוברת ל-`b54Ledger`.

**C2 — חמישה מקורות, אוצר מילים סגור.** קבוע יחיד בשני הקבצים:
`var CHARGE_SOURCES = ['השכרה','כביסה','חוסרים','משלוח','כללי'];`
כל שורת חיוב נושאת בדיוק אחד מהם. אין מקור שישי בלי הכרעת אבי.

**C3 — persistence מינימלי: טבלה `charges` לחיוב ידני בלבד.**
טבלה חדשה, ורק לחיוב שאין לו טבלת אב — כלומר חיוב שהמשרד/ההנהלה מקלידים.
כל שאר החיובים **נגזרים** מהאב שלהם (`order_lines` · `orders.delivery_fee` ·
`orders.shortage_charge` · `laundry_intakes.total_charge`) ואינם משוכפלים לטבלה.
סכימה (להוסיף ל-`TABLES`, בסוף המערך של הטבלאות):
```
charges: ['id','customer_id','source','date','amount','description',
          'order_id','intake_id','invoice_id','status','created_by','created_at','cancel_reason']
```
- `source` — מתוך `CHARGE_SOURCES`. חיוב כביסה שהוקלד ידנית = `'כביסה'`;
  חיוב שאינו נופל לאף מקור = `'כללי'`.
- `amount` — **נטו בשקלים**, כמו כל עמודת כסף בכל 56 הטבלאות (עקביות לייצוא
  ולקריאה של אבי). החישוב עצמו באגורות דרך `toAg`/`agIls`, ככלל הברזל.
- `order_id` / `intake_id` — אופציונליים. חיוב ידני שקושר להזמנה ייכנס לחשבונית
  שלה; חיוב בלי קישור עומד בפני עצמו.
- `status` — `'פתוח'` / `'בוטל'`. **אין מחיקה** (אותו עקרון כמו חשבוניות).
  ביטול דורש `cancel_reason`.
- חיוב ידני **אינו** מפיק חשבונית מעצמו. הוא חיוב שממתין לחיוב/גבייה.

**C4 — חשבונית = מעטפת, לא חיוב.** חשבונית לעולם אינה מוסיפה כסף לספר. חוב =
Σ יתרות שורות החיוב, ובלבד. שורת חיוב שחויבה נושאת `invoice_id`; החשבונית קובעת
את זהות המסמך ואת מועד הפירעון של השורה.
**C4א — הקצאת סכום החשבונית לשורותיה.** `b2OrderBalanceAg` היום מחליף את בסיס
היתרה לסכום החשבונית (ברוטו, כולל מע"מ) כשקיימת חשבונית פעילה. ההתנהגות הזו
**נשמרת** — הלקוח חייב את סכום המסמך. בספר: סכום החשבונית באגורות מתחלק בין שורות
החיוב שלה **לפי חלקן היחסי בנטו**, והשורה האחרונה סופגת את שארית העיגול. כך
Σ השורות = `inv.total` בדיוק, והפילוח לפי מקור נשמר.
**C4ב — חשבונית מדור קודם ללא שורות ניתנות לגזירה** (חשבונית ידנית ללא הזמנה
וללא קליטות מסומנות) → הספר מייצר לה שורה סינתטית אחת `source:'כללי'`,
`origin_type:'invoice'`. **כך אין מיגרציה של נתונים היסטוריים ואין כסף שנעלם.**

**C5 — שורות הזמנת כביסה הן אומדן כמותי, לא כסף.** `orderTotal` לסוג `'כביסה'`
מחזירה `delivery_fee + shortage_charge` בלבד. `unit_price` בשורות **לא משתנה
ולא מאופס** — הוא נשאר כאומדן להצגה, מסומן בממשק "אומדן — החיוב לפי משקל".
**מכאן: אין תיקון רטרואקטיבי לנתונים.** ברגע שהמנוע מפסיק לספור את השורות,
החוב השווא ההיסטורי נעלם מעצמו. זו הסיבה שהמפרט הזה עדיף על אפשרות "לאפס
`unit_price`" שנשקלה ב-B49g: אפס כתיבה לנתוני עבר, אפס אובדן מידע.

**C6 — משלוח וחוסרים הם שורות חיוב עצמאיות.** לכל הזמנה עם `delivery_fee > 0`
נגזרת שורה `source:'משלוח'`; עם `shortage_charge > 0` — שורה `source:'חוסרים'`.
זה מה שסוגר את "דמי שינוע יתומים" (הבעיה שאפשרות 1א של B49g נכשלה בה) ומאפשר
פילוח הכנסות לפי מקור בדוחות B50.

**C7 — תשלומים נשארים כפי שהם. ההקצאה בתוך המקור דטרמיניסטית.**
`payments` **לא משתנה בסכימה ולא במיגרציה.** תשלום ממשיך לשאת `order_id`/`invoice_id`.
בתוך מקור אחד, סך התשלומים מוקצה לשורות החיוב שלו לפי הסדר הקבוע של
`CHARGE_SOURCES` (השכרה → כביסה → חוסרים → משלוח → כללי). הסדר שרירותי אך קבוע,
ומתועד בקוד. **Σ לכל מקור אינו משתנה** — ולכן זהות B48 נשמרת (C9).

**C8 — קליטה מקושרת להזמנה יוצאת מהחשבונית המרוכזת.** `nobleCreateInvoice`
(שרת 5010) והמסך המקביל (ממשק 4639) מסננים גם `!String(i.order_id||'').trim()`,
**או** `order_id` שמצביע על הזמנה שאינה קיימת/בוטלה. הקליטה המקושרת מחויבת דרך
חשבונית ההזמנה: `b1CreateInvoice` מרכיבה את שורות המסמך מ**כל** שורות הספר של
ההזמנה — השכרה/כביסה/משלוח/חוסרים/כללי מקושר. **אין שינוי סכימה בשביל זה**:
`laundry_intakes.order_id` כבר קיים (שרת 227). קליטה **פנימית** — ללא שינוי
(מסוננת ב-`internal !== 'כן'` כמו היום). קליטה **חופשית בלי `order_id`** — ללא
שינוי כלל, ממשיכה למרוכזת.

**C9 — זהות B48 נשמרת ונבדקת.** `b48BalancesAg` ו-`b2CreditUsedAg` **שתיהן**
נגזרות מ-`b54Ledger` (ולא אחת מהשנייה), וחייבות להחזיר אותו מספר לכל לקוח.
בדיקת השוויון הקיימת של B48 נשמרת כלשונה ומורחבת: fixture עם חמשת המקורות יחד.
⚠ אם אחרי המעבר מתגלה פער בין `b2CreditUsedAg` לפני ואחרי על נתוני אמת —
**עצור ודווח לאבי לפני מסירה. זה כסף.**

**C10 — תשלום נספר רק אם הוא נגבה.** כל קוראי `payments` היום סוכמים בלי לבדוק
`status` (אומת: שרת 4282, 5448, 5455; ממשק 934, 1793, 2246). B55 יוסיף שורות
תשלום במצב `'ממתין לאישור'`, ולכן B54 מקדים ומכניס עוזר יחיד:
`function b54PayCounts(p){ return String(p.status||'שולם') === 'שולם'; }`
(status ריק = נגבה, לתאימות היסטורית). **חובה grep על כל סכימת תשלומים בשני
הקבצים והעברת כולם לעוזר הזה.** בלי זה B55 יקטין חוב על תשלום שלא התקבל.

**C11 — מע"מ.** כלל B45 בעינו: שורות הספר נטו, המע"מ מחושב ברמת המסמך.
היחיד שמציג ברוטו הוא מסמך חשבונית/קבלה — ושורת ספר שכבר חויבה, לפי C4א.

### פעולות שרת חדשות
- `b54ChargeAdd {customer_id, source, amount, date?, description, order_id?, intake_id?}` —
  כתיבה, **מנהל + משרד**. ולידציה: לקוח קיים · `source` מתוך הקבוע · `amount > 0` ·
  `order_id` (אם ניתן) שייך לאותו לקוח. לא נכנס ל-`READ_ONLY_ACTIONS`.
- `b54ChargeCancel {charge_id, reason}` — כתיבה, **מנהל בלבד**. `status:'בוטל'`.
  חסום אם `invoice_id` מאוכלס (יש לבטל/לזכות את החשבונית קודם) — הודעה עברית ברורה.
- `b54LedgerAudit {}` — **קריאה** (נכנס ל-`READ_ONLY_ACTIONS`). דוח אבחון שאינו
  משנה כלום: הזמנות כביסה עם שורות מתומחרות וכמה חוב שווא הן ייצרו · קליטות עם
  `order_id` שנכנסו לחשבונית מרוכזת · חשבוניות ללא שורות ניתנות לגזירה (C4ב) ·
  תשלומים בלי `status`. זה מה שאבי מריץ אחרי הפריסה כדי לראות מה השתנה במספרים.

### צרכנים שחייבים לעבור לספר — רשימה מלאה (grep בוצע בסשן התכנון)
**שרת:** `b43Receivables` (6405) · `b48BalancesAg` (4309) · `b48CustomerBalanceAg` (4323) ·
`customerBalance` (4327) · `b2CreditUsedAg` (5461) · `b2OrderBalanceAg` (5445) ·
`b2InvoiceBalanceAg` (5453) · `b2ProcessPayment` (5580, מסלול קיזוז FIFO) ·
`b2PaymentContext` · `b9AgingReport` (6466+) · `case 'reports'` · תזרים (6750+) ·
תזכורות חוב (6108+) · `portalGetData` (2488) · `nobleCreateInvoice` (5010) ·
`b1CreateInvoice` (5324) · `orderTotal` (4275) · `orderBalance` (4285).
**ממשק:** `orderTotal` (934) · `orderPaid` (935) · `orderBalance` (936) ·
`custBalance` (1763) · `b43ReceivablesFE` (3814) · מסך הכספים ולשוניותיו (3673) ·
"חיובי מכבסה" (4637–4652) · כרטיס הזמנה — מקטע כספים (2323) · `b49dOrderIntakeHtml` (2064) ·
כרטיס לקוח — לשונית `finance` · `nobleSummaryData` (6063) · `renderNobleReports` (6238).
⚠ **הכלל: אחרי B54 אין בשום מסך `reduce` על כסף. יש קריאה לספר.**

### ממשק — מה אבי רואה
1. **מסך "כספים" → לשונית חדשה "חיובים"** לפני "תזרים". טבלה אחת: תאריך · לקוח ·
   **מקור** (צ'יפ צבעוני) · תיאור · סכום נטו (כולל מע"מ בכתב קטן, B45) · חשבונית ·
   שולם · יתרה · מועד פירעון. מסננים: לקוח · מקור · טווח תאריכים · רק פתוחים.
   סיכום עליון: סה"כ פתוח, ופילוח לפי מקור. **זה ה"מקום אחד" שאבי ביקש לראות.**
2. **כפתור "+ חיוב חדש"** באותה לשונית ובכרטיס הלקוח → מודאל: לקוח · מקור
   (בורר מ-`CHARGE_SOURCES`) · סכום נטו · תיאור · הזמנה לשיוך (אופציונלי).
   מנהל+משרד. אחרי שמירה — האריח מתרענן והחיוב מופיע מיד גם בכרטיס הלקוח ובפורטל.
3. **בחירה מרובה → "הפק חשבונית מהחיובים המסומנים"** — נתיב מסמך אחד (`b1CreateInvoice`
   מורחב) לכל המקורות, וכך נסגר גם פריט 11 (לחשבונית מכבסה לא היה PDF).
4. **כרטיס הזמנה, מקטע כספים** מוצג פר-מקור: השכרה/כביסה · שינוע · חוסרים · כללי,
   ותחתיהם "סה"כ לחיוב". בהזמנת כביסה: שורות הפריטים מסומנות **"אומדן — החיוב לפי משקל"**,
   ולצידן "החיוב בפועל" מהקליטה המקושרת (העיגון הקיים `b49dOrderIntakeHtml` 2064).
5. **כרטיס לקוח → לשונית כספים:** אותה טבלת חיובים מסוננת ללקוח. מספר אחד, בכל מקום.
6. **כפתור "בדיקת נתוני חיובים"** במסך ההגדרות (מנהל) → מריץ `b54LedgerAudit`
   ומציג את התוצאה **על המסך** כטבלה. זה מחליף את בקשת הייצוא מאבי (כלל R1):
   הוא לוחץ, רואה, ואם משהו חריג — פותח צ'אט ומספר. אין קובץ ואין מה לשמור.

### חמש השאלות (הגדלת ראש)
1. **איפה עוד נוגע:** כל הרשימה בסעיף "צרכנים" למעלה. בנוסף: דוח גיל החובות, דשבורד
   החריגים (ניצול אשראי), BI, תזכורות המייל של B4, ומסך הפורטל. `grep` על
   `orderTotal`, `total_charge`, `delivery_fee`, `shortage_charge`, `b43Receivables`
   בשני הקבצים — **חובה, ולדווח את רשימת אתרי הקריאה בתגובת ה-batch.**
2. **מה בכל שלב:** חיוב ידני על לקוח לא קיים → נדחה · סכום 0 או שלילי → נדחה ·
   ביטול חיוב שחויב → נדחה עם הסבר · לקוח בלי חיובים → מצב ריק "אין חיובים פתוחים ✓" ·
   לחיצה כפולה על "+ חיוב חדש" → `charges` בלי idempotency, לכן **מנעול UI**
   ו-`created_at`+`description` בבדיקת כפילות רכה (אזהרה, לא חסימה).
3. **עקביות:** צ'יפ מקור באותה שפת צבעים של המערכת · כלל מע"מ B45 · `ymdLocal`
   לכל השוואת תאריך · אגורות בחישוב, שקלים בגיליון.
4. **מי רואה:** לשונית חיובים — מנהל+משרד · "+ חיוב חדש" — מנהל+משרד ·
   ביטול חיוב — מנהל בלבד · לקוח בפורטל — החיובים שלו בלבד (B55) · נהג/מכבסה/רצפה —
   לא רואים כספים. **שלושת הקשרי הריצה חובה** (הפורטל והחנות עם `DB=null`).
5. **מה נשבר:** הסיכון המרכזי — מסך שמסכם כסף בעצמו וממשיך להציג מספר שונה.
   ההגנה: הכלל בסוף רשימת הצרכנים + בדיקת הצלבה אוטומטית שרת↔ממשק על אותו fixture,
   שדה-שדה, כמו ב-B48. סיכון שני: `b2CreditUsedAg` יזוז ויחסום הזמנות ללקוחות
   (C9 — לעצור ולדווח אם יש פער).

### בדיקות קבלה
- fixture עם חמשת המקורות על אותו לקוח → **מספר אחד זהה** בכרטיס הלקוח, בלשונית
  החיובים, בגיל החובות, בתזרים, במסגרת האשראי ובפורטל.
- הזמנת כביסה עם שורות מתומחרות + קליטה מקושרת שנמסרה → **חיוב אחד בלבד**
  (מהמשקל), דמי השינוע כשורה נפרדת, השורות מסומנות "אומדן".
- הקליטה המקושרת **אינה** מופיעה ב"חיובי מכבסה פתוחים" ואינה נכנסת למרוכזת.
- קליטה חופשית (בלי `order_id`) — **אפס שינוי**, נכנסת למרוכזת כמו היום.
- קליטה פנימית — **אפס שינוי**.
- הזמנת **השכרה** — אפס שינוי בכל החישובים, לפני ואחרי, על אותו fixture.
- חשבונית פעילה על הזמנה → Σ שורות הספר = `inv.total` בדיוק, כולל שארית עיגול (C4א).
- חשבונית היסטורית ללא הזמנה וללא קליטות → שורה סינתטית `כללי`, הכסף לא נעלם (C4ב).
- `b48BalancesAg` == `b2CreditUsedAg` לכל לקוח ב-fixture — הבדיקה של B48 עוברת כלשונה.
- הצלבת שרת↔ממשק: אותו fixture, שורות זהות שדה-שדה.
- חיוב ידני: נוסף · מופיע בשלושת המסכים · חויב בחשבונית · בוטל · ביטול אחרי חיוב נדחה.
- `b54LedgerAudit` מחזיר דוח על נתוני אמת בלי לשנות כלום; ריצה שנייה — תוצאה זהה.
- 288 הבדיקות של B49b/B49c/B49d/B49e נכתבות מחדש, מורצות, ועוברות כלשונן.

### מה אסור לגעת בו
`nobleWeigh` וחישוב `total_charge` (זה המקור, הוא נכון) · `customers.price_per_kg` ·
מנוע הסליקה/הפקדון (B3) · `b2ProcessPayment` **בזרימה** (רק מסלול הקיזוז עובר לספר,
ה-idempotency וה-`txn_id` וההפקת הקבלה — לא נוגעים) · `b41DriverHandled` ·
`b49dAddCarts`/`b49dResolveOrder` · `API_URL` · כלל מע"מ B45 · תפריט B47 ·
`READ_ONLY_ACTIONS` (רק `b54LedgerAudit` נכנסת) · **מחיקת נתונים היסטוריים —
אסורה לחלוטין; C5 מייתר אותה.**

**שינוי סכימה: יש — טבלה `charges`. להריץ `setupDatabase` פעם אחת.**
היקף: **גדול** (מנוע כסף) | מודל: **מודל חזק** | קבצים: שניהם.
### אבחון הנתונים — בלי לערב את אבי (כלל R1)

**אין לבקש ייצוא.** המפרט הזה תוכנן כך שאינו זקוק לו:
- **C5 מבטיח אפס כתיבה לנתוני עבר.** החוב השווא נעלם ברגע שהמנוע מפסיק לספור
  שורות כביסה — אין תיקון רטרואקטיבי, ולכן אין מה "לבדוק לפני" כדי לא להרוס.
- **C4ב מבטיח שאף שקל היסטורי לא נעלם** — חשבונית ישנה שאין לה שורות ניתנות
  לגזירה מקבלת שורה סינתטית `כללי`. הקוד עובד נכון על כל צורת נתונים שתימצא.
- מה שנשאר לדעת (כמה חוב שווא היה, אילו קליטות נגעו בחשבונית מרוכזת, אילו
  תשלומים בלי `status`) הוא **מידע, לא תנאי בנייה** — ולכן הוא נמסר כפעולת
  קריאה `b54LedgerAudit` + כפתור **"בדיקת נתוני חיובים"** במסך ההגדרות.
  אבי לוחץ אחרי הפריסה, רואה טבלה על המסך, ואם משהו נראה חריג — פותח צ'אט ומספר.
- ידוע מתיעוד קודם: ב-27.07.2026 `laundry_intakes` הכיל **0 רשומות** — מסלול
  הכביסה מעולם לא היה בשימוש בפועל. `b54LedgerAudit` יאמת זאת מול הנתונים החיים.
**הבדיקות נכתבות מול fixtures מלאכותיים שמכסים את כל הצורות** — כולל טבלאות
ריקות, הזמנת כביסה בלי קליטה, קליטה בלי הזמנה, וחשבונית יתומה.

---

---

## מפרט B55 — פורטל הלקוח המלא — בוצע 28.07.2026

⚠ שתי סטיות מודעות מומשו במקום המפרט המקורי — מתועדות במקטע B55 למעלה.

נכתב 28.07.2026, באותו סשן תכנון. **תלוי ב-B54 — אינו ניתן לבנייה לפניו**
(הוא מציג את ספר החיובים). הכרעת אבי:

> "לקוח תמיד יכול לראות מה החיובים שלו מתוך הקישור שלו, ויכול גם לראות פירוט,
> להוריד חשבוניות, לשלם חובות, לשלם חלק מחובות, ולשלוח פניה למשרד/הנהלה.
> אם אין משהו מתוך זה — צור אותו עכשיו. שכל דבר יהיה מקושר, בלי דברים יתומים."

### מה קיים ומה חסר (אומת בקוד החי)

| דרישה | מצב | הערה |
|---|---|---|
| רואה חוב | ✅ חלקי | `portalGetData` מחזיר `debt` — **מספר אחד מצטבר בלבד** |
| רואה **פירוט** חיובים | ❌ **חסר** | אין שורות. `b43Receivables` לא מגיעה לפורטל |
| מוריד חשבוניות | ✅ | `pgInvoices` עם `pdf_url` (20 אחרונות) |
| מוריד קבלות | ✅ | `pgReceipts` עם `pdf_url` (20 אחרונות) |
| **משלם חוב** | ❌ **חסר לחלוטין** | `portalPayDebt` (ממשק 7854) = מודאל עם קישור חיצוני וטלפון. לא נרשם, לא מקוזז, אין קבלה |
| **משלם חוב חלקי** | ❌ **חסר** | — |
| **פניה למשרד** | ❌ **חסר לחלוטין** | אין טבלה, אין מסך, אין התראה |
| מצב כביסה / הזמנות / מסגרת / פקדון | ✅ | NC2 · B2 · B3 — לא נוגעים |

### מציאות שחייבים לומר לאבי במפורש
**אין חשבון סליקה מחובר** (הכרעה D5 — מצב stub, `b3GatewayRedirectUrl` הוא נקודת
החיבור העתידית). לכן "הלקוח משלם בפורטל" **אינו יכול לחייב כרטיס היום**.
המפרט בונה את השרשרת כך שהיא נכונה עכשיו **ולא תיכתב מחדש** כשיתחבר ספק:

```
הלקוח בוחר מה לשלם  →  P1 הצהרת תשלום (payments, status='ממתין לאישור')
                        + התראה למשרד + הודעה ללקוח
                     →  P2 המשרד/ההנהלה מאשר בלחיצה  →  b2ProcessPayment
                        (אותו txn_id, idempotent) → חוב מקוזז + קבלה + מסגרת מתעדכנת
```
כשיתחבר ספק סליקה — ה-callback שלו קורא לאותה פונקציית אישור (P2) עם אותו
`txn_id`. **תפר אחד, לא שכתוב.** זו הסיבה ש-C10 (סינון `status`) נבנה כבר ב-B54.

### ההכרעות P1–P7

**P1 — הצהרת תשלום, לא תשלום.** `portalPayDeclare {code, allocations[], method_intent, note}`.
`allocations` = מערך `{charge_id, amount}` שהלקוח סימן, או `{mode:'all'}` /
`{mode:'partial', amount}` שהשרת מקצה FIFO לפי מועד פירעון. נוצרות שורות `payments`
עם `status:'ממתין לאישור'`, `type:'תשלום מהפורטל'`, `txn_id` שנוצר **בשרת**
(`genId('PTXN')`) ומוחזר ללקוח. **החוב אינו מוקטן** — C10 מבטיח שהשורות האלה לא
נספרות. ולידציות: סכום ≤ יתרת החיוב · סכום > 0 · לקוח פעיל · **הגבלת קצב**
(עד 5 הצהרות ל-10 דקות פר-לקוח ב-`CacheService`, דפוס `b38VerifyManagerPin`).
idempotency: הצהרה פתוחה זהה קיימת → מחזירים אותה, לא יוצרים שנייה.

**P2 — אישור בהנהלה, נקודה אחת.** `b55ConfirmPortalPayment {txn_id, method, note?}` —
כתיבה, **מנהל+משרד**. מוחקת את שורות ההצהרה וקוראת ל-`b2ProcessPayment` עם
אותו `txn_id`, אותו לקוח ואותה הקצאה → קבלה אחת, קיזוז FIFO, עדכון מסגרת, audit.
דחייה: `b55RejectPortalPayment {txn_id, reason}` → ההצהרה מסומנת `'נדחה'`
(לא נמחקת) + פניה אוטומטית ללקוח (P5) עם הסיבה. **אין הצהרה יתומה.**
⚠ העברת ההצהרה ל-`b2ProcessPayment` **חייבת** לעבור דרך המנוע הקיים ולא לשכפל
לוגיקת קבלה. אם מסתבר שהחתימה של `b2ProcessPayment` לא מכילה הקצאה לשורות ספר —
להרחיב אותה בפרמטר אופציונלי, **בלי לשנות את התנהגות ברירת המחדל.**

**P3 — מסך "אישורי תשלום מהפורטל"** — לשונית בתוך מסך הכספים (לא מסך חדש),
או שורה במרכז האישורים הקיים `b10ApprovalsInbox`. **הכרעה: מרכז האישורים** —
הוא בדיוק הכלי לזה, וכך אין דפוס שני. פריט = לקוח · סכום · מה סומן · אמצעי
מוצהר · תאריך · כפתורי אשר/דחה. התראת מייל לפי B4/S6.

**P4 — פירוט החיובים בפורטל.** `portalGetData` מחזיר `charges` = שורות ספר B54
של הלקוח (מקור · תיאור · תאריך · מועד פירעון · סכום · שולם · יתרה · קישור
לחשבונית אם קיימת). הפורטל מציג טבלה עם צ'יפ מקור, ובראשה "סה"כ לתשלום"
**שהוא בדיוק `debt` הקיים** — אותו מספר, שתי תצוגות.
⚠ **חובה:** להסיר את הכפילות שקיימת היום — `myOrders[].total` ו-`laundry[].charge`
מוצגים כסכומים לצד `debt`. אחרי B54 הזמנת כביסה אינה כסף, ולכן הפורטל
מציג **מקור אחד לכסף: טבלת החיובים.** ההזמנות והכביסה נשארות כמצב/מעקב בלבד.

**P5 — פניות.** טבלה חדשה:
```
customer_messages: ['id','customer_id','direction','subject','body','ref_type','ref_id',
                    'status','created_at','created_by','answer','answered_by','answered_at']
```
- `direction`: `'מלקוח'` / `'למשרד'`(תשובה). `ref_type`/`ref_id`: `'charge'`/`'invoice'`/
  `'order'`/`''` — כך פניה "למה חויבתי על זה?" **מקושרת לשורת החיוב**, לא יתומה.
- `status`: `'חדשה'` / `'בטיפול'` / `'נענתה'` / `'נסגרה'`.
- פעולות: `portalMessageSend {code, subject, body, ref_type?, ref_id?}` (הגבלת קצב
  כמו P1) · `b55MessageReply {message_id, answer}` (מנהל+משרד) · `b55MessageClose`.
- הלקוח רואה את השרשור והתשובה בפורטל. המשרד רואה במרכז האישורים/תיבה.
  התראת מייל ל-`notify_email` בפניה חדשה, ולפי `notify_pref` של הלקוח בתשובה.

**P6 — כפתור "פניה למשרד" בכל מקום רלוונטי:** ליד כל שורת חיוב, ליד כל חשבונית,
ובראש הפורטל כפניה כללית. `ref_id` מאוכלס אוטומטית — הלקוח לא מקליד מזהים.

**P7 — אין דבר יתום. הכלל המחייב של ה-batch:**
תשלום → מקושר ללקוח, לשורות חיוב, לקבלה, למסגרת האשראי, ל-audit.
הצהרה → מקושרת לתשלום או לדחייה מתועדת עם סיבה.
פניה → מקושרת ללקוח ולישות שעליה נשאלה, ונסגרת בתשובה.
כל אלה נבדקים בבדיקות הקבלה.

### חמש השאלות (הגדלת ראש)
1. **איפה עוד נוגע:** `portalGetData` (2488) · `portalPayDebt` (ממשק 7854) ·
   `renderPortal` (7816) · `b2ProcessPayment` (5580, פרמטר הקצאה אופציונלי) ·
   `b10ApprovalsInbox` · B4 (התראות) · מסך הכספים · `audit`.
2. **מה בכל שלב:** אין חוב → "אין חיובים פתוחים ✓" ואין כפתור תשלום · סכום מעל
   היתרה → נדחה · לחיצה כפולה → אותה הצהרה חוזרת (idempotent) · אין רשת → הודעה
   בלי אובדן הטופס · הצהרה שאושרה פעמיים → `b2ProcessPayment` מחזיר `duplicate`
   ולא מחייב שוב · פניה ריקה → נדחית.
3. **עקביות:** אותם צ'יפים ומקורות של B54 · B45 · אותו פורמט תאריך/מטבע ·
   הפורטל נשאר `DB=null`, כל נתון מגיע מ-`portalGetData`.
4. **מי רואה:** לקוח — רק את שלו, לפי `portal_code` · אישור תשלום — מנהל+משרד ·
   דחייה עם סיבה — מנהל+משרד · נהג/מכבסה/רצפה — לא. **שלושת הקשרי הריצה חובה.**
5. **מה נשבר:** הסיכון המרכזי — הצהרת תשלום שתיספר כתשלום ותקטין חוב על כסף
   שלא התקבל. ההגנה: C10 מ-B54 + בדיקה מפורשת שהחוב **אינו** משתנה בין הצהרה
   לאישור. סיכון שני: הרחבת `b2ProcessPayment` תשבור את מסלול התשלום במשרד —
   בדיקות התשלום הקיימות מורצות כלשונן.

### בדיקות קבלה
- לקוח עם חיובים מחמשת המקורות → הפורטל מציג את כולם, וסה"כ = `debt` = מסך ההנהלה.
- הצהרת תשלום מלא → החוב **לא** משתנה · פריט צץ במרכז האישורים · מייל נשלח.
- אישור → קבלה מופקת, החוב יורד בדיוק בסכום, המסגרת מתעדכנת, הקבלה מופיעה בפורטל.
- אישור פעמיים / רענון כפול → קבלה אחת (`duplicate`), חוב יורד פעם אחת.
- הצהרה חלקית של 30% מהחוב → קיזוז FIFO לפי מועד פירעון, יתרה נכונה.
- דחייה → אין תשלום, ההצהרה מתועדת, הלקוח רואה את הסיבה בפורטל.
- פניה על שורת חיוב → מקושרת ל-`charge_id`, המשרד עונה, הלקוח רואה תשובה.
- הגבלת קצב: 6 הצהרות/פניות רצופות → החמישית עוברת, השישית נחסמת בהודעה.
- קוד פורטל שגוי / לקוח לא פעיל → נדחה בכל הפעולות החדשות.
- שלושת הקשרי הריצה נטענים בלי חריגה; לפורטל לא מגיע שום נתון של לקוח אחר.

### מה אסור לגעת בו
`b2ProcessPayment` בהתנהגות ברירת המחדל · מנגנון הקבלות והמספור · B3 (פקדון) ·
`portal_code` ומנגנון הזיהוי · `API_URL` · ספר החיובים של B54 (קוראים בלבד) ·
`READ_ONLY_ACTIONS` (הפעולות החדשות כותבות, פרט לקריאת הפורטל).

**שינוי סכימה: יש — טבלה `customer_messages`. להריץ `setupDatabase`.**
היקף: **גדול** | מודל: **מודל חזק** | קבצים: שניהם.

---

---

## מפרט B49f — אישור הנהלה שהנסיעה בוצעה — בוצע 28.07.2026

⚠ **סדר: אחרי B54 ואחרי B55.** אינו תלוי בהם טכנית ואינו נוגע בכסף,
אבל B54/B55 קודמים בעדיפות (חשיפה כספית פעילה).

**הרקע.** B49e (P7) מבטל נסיעת איסוף שהנהג המשובץ לא תיעד בשטח
(b41DriverHandled = סריקה או צילום מהחשבון שלו). זה נכון ברוב המקרים,
אבל יוצר פער: נהג שנסע ואסף בפועל והטלפון שלו מת — הנסיעה מבוטלת,
הדלק והשעות נעלמים מדוח הצי, ואין דרך לתקן (נסיעה מבוטלת אינה
"פתוחה" ולכן גם b41CloseStalePickups לא מחזירה אותה).

**הכרעת אבי (28.07.2026).** בשער הקליטה, במקום שדה סיבה בלבד, תישאל
שאלה מפורשת: "האם נהג ביצע את הנסיעה בפועל?"
- כן → הנסיעה נסגרת 'בוצע' + תעודת משלוח, ומסומנת כאישור הנהלה
- לא → מבוטלת, כמו היום

**רשאים להצהיר: מנהל ומשרד.**

**עיקרון מחייב.** אין לשנות את b41DriverHandled ואת סמנטיקת
photo_by/scan_by (הכרעת B41b). ההצהרה היא שכבה נפרדת — הצהרה מתועדת,
לא ראיה. דוח הצי חייב להישאר מסוגל להפריד בין נסיעה שהנהג הוכיח
לבין נסיעה שההנהלה אישרה בדיעבד.

**עוגנים בקוד (אומתו מול v4.44-B49e).**
- שרת: b49eClosePickupsAfterReturn — הפונקציה שמכריעה בוצע/בוטל.
  שני אתרי קריאה: case 'receiveReturn' ו-מסלול הקליטה בשקילה.
  שניהם חייבים לקבל את אותו טיפול בדיוק.
- שרת: b41IntakeGate / b41IntakeState — שער הסיבה הקיים.
- ממשק: B41_REASONS · טופס קליטת ההחזרה.

**פתוח לתכנון בצ'אט המבצע.** איך נשמר סימון "אושר ע"י ההנהלה" —
עמודה חדשה על deliveries או שימוש ב-manual_intake_by הקיים;
והאם ניתן לתקן נסיעה שכבר בוטלה בעבר (ריפוי רטרואקטיבי).
להציג לאבי בהודעה אחת לפי חלק 4, ורק אחרי אישור לבנות.

⚠ נוגע ב-receiveReturn — תהליך ייצור קריטי. כל בדיקות הרגרסיה של
B49c ו-B49e נכתבות מחדש ומורצות לצד החדשות.

היקף: קטן-בינוני | מודל: Sonnet | קבצים: שניהם

⚠ **עוגן שלישי שנוסף ב-B49d (28.07.2026), חובה לכלול אותו בהיקף:**
`b49dClosePickupAfterIntake` בשרת מכריעה בוצע/בוטל **באותה סמנטיקה בדיוק**,
עבור רגל ההלוך של הזמנת כביסה שנפתחה לה קליטה. B49f חייב לטפל בשלושת
אתרי ההכרעה — `b49eClosePickupsAfterReturn` (שני קוראים) ו-
`b49dClosePickupAfterIntake` — או לאחד אותם לפונקציה משותפת אחת.
בממשק: הקריאה מגיעה מ-`floorSaveIntake` דרך `nobleIntake`, ולכן השאלה
"האם נהג ביצע את הנסיעה בפועל?" צריכה להישאל **בטופס הקליטה** ולא רק
בשער קליטת ההחזרה. `b49dLegState` כבר מציג את מצב הרגל לעובד — זו נקודת
העגינה הטבעית בממשק.

---

## מפרט B49d — בוצע 28.07.2026

המפרט מומש במלואו. שלושת סעיפיו:
(1) הזמנת כביסה שנאספת → קליטה עם `customer_id` ו-`price_per_kg` מכרטיס הלקוח —
בוצע, עם טריגר שנקבע מחדש בהכרעת אבי (ראה סעיף B49d למעלה);
(2) כפתור ייעודי לפתיחת קליטה בשימוש חוזר ב-`floorIntakeTab` — בוצע, בארבע נקודות כניסה;
(3) הוספה לקליטה פתוחה קיימת (L2) — בוצע ב-`b49dAddCarts`, בלי תקרה על מספר עגלות.

**הכרעות אבי L1–L3 נשמרות כתיעוד היסטורי** (L1 — שלוש נקודות כניסה לקליטת NOBLE;
L2 — קליטה אחת לאירוע עם אפשרות הוספה, בלי תקרה; L3 — כביסה פנימית נספרת בהגעה
ל"מוכן", כביסת לקוח רק במסירה). L3 מומש ב-B49c ולא שונה ב-B49d.

**הראיה מייצוא בסיס הנתונים (27.07.2026) נשמרת כתיעוד:**
`laundry_tasks`=3 · `laundry_task_events`=5 · `laundry_intakes`=0 · `intake_carts`=0 ·
`laundry_events`=0 — מסך רצפת הייצור מעולם לא היה בשימוש בפועל. שתי משימות נמצאו
בשלב "מוכן" בלי אף אירוע ובלי `cart_id`/`machine_id`; כל דוח חייב לסבול משימות
חסרות היסטוריה בלי ליפול. הבדיקות של B49c ו-B49d מכסות את המקרה הזה.
