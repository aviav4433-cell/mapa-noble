# MAPA-NOBLE — מצב המערכת

## מצב נוכחי
canary ממשק: **v4.44-B49e** | canary שרת: **v4.44-B49e** | עדכון אחרון: 28.07.2026

⚠ **יש להריץ `setupDatabase`** — ריצה אחת מכסה את B49b, B49c ו-B49e:
`carts.virtual` (B49b) · `laundry_tasks.intake_id` · `laundry_intakes.order_id` ·
`laundry_intakes.ready_ts` (B49c) · **`orders.ship_out` · `orders.ship_back` ·
טבלה חדשה `signatures` (B49e)**. כל העמודות נוספו **בסוף** המערכים — מיגרציה בטוחה.

**מה עובד:** כל הליבה — הזמנות, מלאי רב-מחסני, משלוחים ומסלולי נהג (Driver-Lite B5),
החזרות ושקילה, מודול מכבסה מלא (NC1–NC4), רצפת ייצור, משימות, פגישות, פורטל לקוח,
חנות ציבורית, מנוע תשלום ואשראי (B2), פקדון/ערבויות (B3), התראות אימייל (B4),
חשבוניות עם מספור רציף ומספרי הקצאה (B1/B31), דוח חובות לפי גיל ודשבורד חריגים (B9),
מרכז אישורים (B10), BI (B14), מדפים (B35), שעון נוכחות ושכר (B36–B37), אבטחת שדות
רגישים (B38), תחזוקת ציוד ותקלות (B42), תזרים וחובות עם מועדי פירעון (B43),
סריקת העמסה מרובה (B44), כלל מע"מ אחיד (B45), מסמכים כ-PDF אמיתי ואריחי עובד/רכב (B46),
תפריט עליון בדסקטופ (B47) — פתיחת החלוניות תוקנה ב-B48a,
מקור אמת אחד ליתרות לקוחות ומסנן מחסן במסך המלאי (B48),
מרכז דוחות (B49), עגלה וירטואלית (B49b), גשר לוח הכביסה↔יומן NOBLE (B49c),
**איסוף עצמי ומשלוח כשני מצבים עצמאיים + חתימה מופרדת מהמשלוח (B49e).**

**מה שבור:**
1. ✅ Bug 1 (חתימה בטאבלט) — **תוקן ב-B49e**, הוקדם מ-B51. ממתין לאימות על טאבלט אמיתי.

**ממתין לאימות אבי:**
1. ⚠ **הרצת `setupDatabase`** — חובה לפני הכול.
2. ⚠ **הפעלת שירות Drive API** בעורך Apps Script (שירותים ← + ← Drive API).
3. הרצת "תיקון מסמכים שנשמרו כ-HTML" במסך ההגדרות (אחרי 1 ו-2).
4. ⭐ **חדש (B49e) — "מילוי אופן מסירה"** במסך ההגדרות. ריצה אחת, אחריה מחיר
   השינוע כספי בלבד ואף חישוב אינו נגזר ממנו.
5. ⭐ **חדש (B49e) — מקטע "אופן המסירה והוכחת מסירה"** בכרטיס ההזמנה: מעבר בין
   משלוח לאיסוף עצמי לכל רגל בנפרד, ובשני הכיוונים.
6. ⭐ **חדש (B49e) — חתימה מכרטיס ההזמנה**, בלי מעבר למסך המשלוחים (P8).
   ⚠ לבדוק על הטאבלט של הדלפק — שם היה הבאג.
7. ⭐ **חדש (B49e) — הזמנה באיסוף עצמי אינה מייצרת נסיעת סרק** ביומן הנהג.
8. ⭐ **חדש (B49e) — נסיעת איסוף שאף נהג לא ביצע מבוטלת** בקליטת ההחזרה (P7).
9. B46/B48/B49/B49b/B49c — פריטי האימות מהאצוות הקודמות עדיין פתוחים.

## שלושת ה-batches האחרונים

### B49e — איסוף עצמי מול משלוח + חתימה מופרדת (28.07.2026)

**המטרה.** לקוח שאוסף בעצמו אינו נסיעה. עד היום כל הזמנת השכרה ייצרה **בכוח** נסיעת
אספקה ונסיעת איסוף, וכלל L6 חסם את "סמן כסופקה" עד שמישהו הוסיף חתימה או צילום דרך
כרטיס המשלוח. התוצאה: נסיעות סרק ביומן הנהגים ומשתמש תקוע בכל הזמנה שנאספת מהדלפק.

**הכרעות אבי בסשן (מענה לארבע שאלות התכנון).**
- **שני שדות, לא אחד** — "לאסוף עצמאית ולהחזיר במשלוח וכן להפך". רגל ההלוך ורגל
  החזור עצמאיות לחלוטין (P1+P3).
- **הזמנות קיימות** — "ניתן לקשר כל הזמנה קיימת למשלוח וניתן לאסוף עצמאית": מילוי
  לפי המצב בפועל, והכול ניתן להחלפה אחר כך.
- **חתימה** — "לא יהיה קשור רק למשלוח": טבלת `signatures` עצמאית (P4).
- **תוספת של אבי** — תיעוד גם ב**החזרה**, לא רק באספקה. מומש כ**אפשרות**, לא כחסימה:
  לא הוספה שום חסימה חדשה ל-`receiveReturn` (תהליך ייצור קריטי). שער B41 נשאר האכיפה.
- **פד החתימה** — לא נענתה. נספחה לפי פרוטוקול חלק 4א (תיקון קטן באזור שכבר נוגעים בו).

**מה נבנה — שרת.**
- סכימה: `orders.ship_out` · `orders.ship_back` (`'משלוח'`/`'איסוף עצמי'`) + טבלה
  חדשה `signatures` (`entity_type`, `entity_id`, `order_id`, `leg`, `kind`, `url`,
  `signer_name`, `ts`, `user`, `note`). הכול בסוף המערכים.
- `b49eShipMode` / `b49eLegKind` / `b49eLegDate` — נקודה אחת שמכריעה מה קורה בכל רגל.
- `b38PlannedDeliveries` נכתבה מחדש: נגזרת מהמצב בלבד. **`delivery_fee` איבד כל
  משמעות תפעולית** (P2).
- `b49eApplyLegMode` — מעבר דו-כיווני. לאיסוף עצמי: ביטול נסיעות פתוחות בלבד.
  למשלוח: השלמת הנסיעה החסרה.
- `b49eClosePickupsAfterReturn` (P7) — נסיעת איסוף שהנהג המשובץ תיעד בשטח →
  `'בוצע'` + תעודת משלוח; אחרת → `'בוטל'` בלי תעודה. מוחל **זהה** בשני מסלולי הקליטה.
- `b49eProofsOf` / `b49eHasProof` — הוכחה מטבלת `signatures` **וגם** מהתיעוד
  ההיסטורי על רשומת המשלוח. קיום מקביל, בלי מיגרציה.
- L6 הורחב: רגל הלוך באיסוף עצמי בלי הוכחה → `deliverOrder` נחסם.
- שלוש פעולות: `b49eSetShipMode` (מנהל/משרד) · `b49eSaveProof` · `b49eBackfillShipModes`
  (מנהל בלבד). אף אחת אינה `READ_ONLY`. יצירה ישירה ל-`signatures` חסומה.
- **הוסר:** ענף `delivery_fee` ב-`case 'update'` שביטל נסיעות כביסה כשהעמלה ירדה ל-0.

**מה נבנה — ממשק.**
- מראה מדויקת של הלוגיקה: `b49eShipMode`, `b49eLegKind`, `b38PlannedForOrder`.
- מקטע "אופן המסירה והוכחת מסירה" בכרטיס ההזמנה, עם מעבר לכל רגל והצגת ההוכחות.
- מודאל חתימה/צילום **מכרטיס ההזמנה** (P8), עם שם חותם אופציונלי.
- טופס ההזמנה: התווית השגויה ("ריק/0 = איסוף עצמי") הוסרה, נוספו שתי בחירות.
- תוויות היומן נגזרות מהמצב ולא מהעמלה.
- כפתור "מילוי אופן מסירה" במסך ההגדרות.
- **תיקון פד החתימה (הוקדם מ-B51 חלק 2):** קנבס ברזולוציית `devicePixelRatio`,
  סקיילינג אמיתי בין רוחב ה-CSS לרזולוציה, `preventDefault` על pointer events,
  ושחזור התוכן ב-`resize`/`orientationchange`. `sigTeardown` נקרא מ-`closeModal`.

**החלטות שהתקבלו ולמה.**
- **המלכודת המרכזית — נסיעה מבוטלת.** `b38EnsureOrderDeliveries` בדקה קיום לפי
  `kind` **בלי קשר לסטטוס**, ולכן מעבר חזרה למשלוח לא היה יוצר נסיעה לעולם. נוספה
  `b49eLiveDeliv` — רק `מתוכנן/בדרך/בוצע` נחשבים "קיים". `בוטל` נשאר כהיסטוריה.
  אותו תיקון הוחל גם על `b38FixPickups`.
- **נסיעה ב-`'בוצע'` לעולם לא נוגעים בה.** מעבר לאיסוף עצמי אחרי שהמשלוח כבר קרה
  אינו משכתב היסטוריה.
- **תאימות לאחור בקריאה, לא במיגרציה כפויה.** הזמנה ותיקה עם שדה ריק נגזרת פעם
  אחת מהמצב הקודם, כך שהמערכת נכונה כבר מהרגע הראשון גם בלי לחיצה. כפתור המילוי
  מוחק את הגזירה הזו לתמיד. שני מנגנונים, אפס סיכון.
- **חתימות קיימות — קיום מקביל.** `deliveries.signature_url` נשאר בתוקף ונספר
  כהוכחה. **מסכי הנהג לא ראו שום שינוי** — זו הייתה דרישה מפורשת.
- **P7 הוא שינוי מכוון של התנהגות קיימת.** עד היום כל איסוף פתוח סומן `'בוצע'`
  בעיוורון. מספרי דוח הצי ישתנו — לציין לאבי.
- **`receiveReturn` עצמו לא שונה.** רק לולאת סגירת הנסיעות שאחריו. הליבה
  שנבנתה ב-B49c נשארה כלשונה.

**מה נבדק. 205 בדיקות שרת ב-`vm.createContext` + 102 בדיקות ממשק ב-jsdom —
307/307 עברו.** מתוכן **71 בדיקות הרגרסיה של B49c נכתבו מחדש ועברו כלשונן**
(שישה אירועי מעבר בשלושה זוגות סגורים, MACH-002/003/006, העובד אברהם, יומן
המשימה ללא שינוי, ירושת `intake_id` בפיצול, הגשר שקט בלי `intake_id`, סגירת
קליטה בשתי משימות, ניתוק העגלה, L3). ועוד: P2 בשני הכיוונים · מלכודת הנסיעה
המבוטלת · אידמפוטנטיות · L6 בשני המסלולים · P7 בארבעה תרחישי תיעוד · סמנטיקת
B41b · מילוי חד-פעמי · סקיילינג פד החתימה כולל `rect` שונה מרוחב הקנבס ו-`rect=0` ·
שלושת הקשרי הריצה · חמשת התפקידים. `node --check` על שני הקבצים — עבר.

**מה לא נבדק.** דפדפן אמיתי · טאבלט פיזי (תיקון פד החתימה — **זו הבדיקה החשובה**) ·
נתוני ייצור אחרי `setupDatabase` בפועל · העלאה אמיתית ל-Drive.

### B49c — גשר בין לוח הכביסה ליומן NOBLE (27.07.2026)

**הבעיה, כפי שנחשפה מייצוא הגיליון החי.** דוח "ביצועי מכבסה" היה ריק — ולא בגלל באג.
הספירה בפועל: `laundry_tasks`=3 · `laundry_task_events`=5 · `laundry_intakes`=0 ·
`intake_carts`=0 · `laundry_events`=0. מסך "רצפת ייצור" (NOBLE) מעולם לא היה בשימוש;
כל העבודה נעשית בלוח הכביסה. `receiveReturn` הוא המקור היחיד ל-`laundry_tasks`,
`advanceLaundry` כותב **רק** ל-`laundry_task_events`, והדוח קורא **רק** מ-`laundry_events`.
לטובתנו: B40 כבר אוכף עגלה+מכונה בכל מעבר — כל הנתונים שהדוח צריך (מי · איזו מכונה ·
מתי) כבר נאספו, רק ליומן הלא-נכון.

**מה נבנה — שרת.**
- סכימה: `laundry_tasks.intake_id` · `laundry_intakes.order_id` · `laundry_intakes.ready_ts`,
  כולן **בסוף** המערך. **שינוי סכימה — יש להריץ `setupDatabase`.**
- `b49cOpenReturnIntake` — `receiveReturn` שיצרה משימות פותחת קליטת NOBLE פנימית אחת
  (`internal='כן'`, `customer_id=''`, `order_id`), על **עגלה וירטואלית של B49b**
  (`virtual='כן'`, `tare_kg=0`), וכל המשימות מקבלות את `intake_id` שלה.
- `b49cAfterAdvance` — **הגשר**. קריאה **אחת** בסוף `advanceLaundry` הקיימת, אחרי
  שהמעבר כבר הוכרע ונרשם ביומן המשימה. סוגרת את השלב הפתוח של העגלה ('סיום')
  ופותחת את החדש ('התחלה'), מנתקת את העגלה בסיום, וסוגרת את הקליטה כשכל משימות
  ההזמנה הגיעו ל"מוכן".
- `b49cBindRealCart` — בתוך `assignCart`: ברגע שהלוח בוחר עגלה פיזית, הקליטה עוברת
  אליה והעגלה הווירטואלית משוחררת. מכסה גם את הקריאה הפנימית מ-`advanceLaundry`.
- `b40SplitLaundry` מוריש `intake_id` למשימה המפוצלת. `nobleMarkReady` חותם `ready_ts`.
  `nobleBoard` מוריד קליטה פנימית שהושלמה מהלוח החי.

**מה נבנה — ממשק.**
- `b49cCompletedTs(i)` — נקודה אחת בקוד שמכריעה מתי קליטה "הושלמה" (L3). כל הצרכנים
  עוברים דרכה: `renderNobleReports`, `nobleSummaryData` (כרטיס BI), `nobleDashCard`.
- `b49cActiveIntakes` — קליטה פנימית שהגיעה ל"מוכן" יורדת מ"קליטות פעילות".
- הדוח: התוויות עברו מ"נמסרו" ל"הושלמו", נוספה שורת הסבר שמתעדת את הגדרת L3
  ופילוח פנימי/חיצוני בתקופה.

**החלטות שהתקבלו ולמה.**
- **מודל האירועים: זוג לכל מעבר.** מעבר לשלב עבודה מייצר 'סיום' לשלב הקודם +
  'התחלה' לחדש, באותה חותמת זמן. ארבעה מעברי לוח = **שש** שורות ב-`laundry_events`
  (3+3). המפרט כתב "ארבעה אירועים" — זו טעות שהתגלתה מול הקוד: ניצולת המכונות
  בדוח מחושבת מזוגות התחלה→סיום, וארבע שורות היו משאירות את המכונה הראשונה
  "פתוחה" ונספרת 24 שעות. המהות של בדיקת הרגרסיה נשמרה במלואה.
- **`ready_ts` כעמודה ולא כגזירה מהיומן.** מקבילה מדויקת ל-`delivered_ts`, נחתמת
  בשני המסלולים (הגשר ו-`nobleMarkReady`), ומייתרת סריקת יומן בכל רינדור של הדוח.
- **הקליטה הפנימית לא נסגרת דרך `nobleMarkReady`** — היא דורשת שקילה לכל עגלה,
  וכביסה פנימית של MAPA אינה נשקלת ואינה מחויבת. נבנתה `b49cCloseIntake` נפרדת.
  זו הבחנה עסקית אמיתית, לא שכפול לוגיקה.
- **`intake_id` על המשימה, לא `task_id` על הקליטה** — יחס רבים-לאחד; כך הפיצול
  (`b40SplitLaundry`) יורש את הקישור בשורה אחת.
- **הגשר שקט למשימות בלי `intake_id`** — כל המשימות ההיסטוריות (כולל PKG-1/PKG-2
  שיושבות ב"מוכן" בלי אף אירוע ובלי עגלה) ממשיכות לעבוד בדיוק כקודם, בלי לכתוב
  ליומן NOBLE ובלי ליפול.
- **ניתוק העגלה בסיום המשימה** — בלעדיו העגלה הייתה חוזרת ל"פנויה" בלוח אך נשארת
  משויכת לקליטה, וקליטת רצפה חדשה עליה הייתה נחסמת. תוקן מראש.

**הגדלת ראש — חמש השאלות.**
1. *איפה עוד נוגע:* grep על `delivered_ts` — שלושה צרכנים בממשק (`renderNobleReports`,
   `nobleSummaryData`, `nobleDashCard`) עברו ל-`b49cCompletedTs`; `finLaundryHtml`
   ו-`nobleCreateInvoice` מסננים `internal!=='כן'` ולכן **לא** מושפעים — כביסה פנימית
   לעולם לא נכנסת לחיוב. בשרת: `nobleBoard` (הלוח החי) קיבל את אותו סינון.
2. *מה בכל שלב:* החזרה בלי פריטים → אין קליטה ריקה · משימה בלי `intake_id` → הגשר
   שקט · שתי משימות לאותה הזמנה → הקליטה נסגרת רק אחרי האחרונה · עגלה שכבר משויכת
   לקליטת רצפה אחרת → הגשר לא נוגע בה.
3. *עקביות:* אותה טבלת `laundry_events`, אותו `nobleEvent`, אותם חישובי דוח — אפס
   לוגיקת שלבים חדשה. יומן המשימה (`laundry_task_events`) נשאר במלואו.
4. *מי רואה:* מנהל/משרד — הדוח ולוח הבקרה · מכבסה/רצפה — הלוח החי · נהג — לא נוגע ·
   פורטל/חנות (`DB=null`) — הפונקציות מוקשחות ונבדקו.
5. *מה נשבר:* בלי `setupDatabase` שלוש העמודות לא יישמרו והקליטה תיעלם בין בקשות.
   זה הסיכון היחיד, והוא מודגש בראש המסמך וב-HANDOFF.

**מה נבדק. 71 בדיקות שרת ב-`vm.createContext` + 34 בדיקות ממשק ב-jsdom + 4 הקשרי
ריצה — 109/109 עברו.** סכימה · החזרה פותחת קליטה אחת · החזרה ריקה לא פותחת כלום ·
**רגרסיה על הנתונים האמיתיים: `WASH-MS1PC3KXGC001` בארבעה מעברים → שישה אירועים,
שלושה זוגות סגורים, MACH-002/003/006, העובד אברהם** · יומן המשימה ללא שינוי ·
סגירת קליטה בשתי משימות · ירושת `intake_id` בפיצול · משימה ללא `intake_id` ·
משימה חסרת היסטוריה לא מפילה את הדוח · קליטת רצפה וקליטה ידנית של B49b לא נשברו ·
L3 בכל ארבעת המקרים כולל תאימות לאחור · מצב ריק · `DB=null`. `node --check` על שני
הקבצים — עבר.

**מה לא נבדק.** דפדפן אמיתי · נתוני ייצור אחרי `setupDatabase` בפועל · מסך רצפת
הייצור מול קליטות שנפתחו מהחזרה · התנהגות אחרי צבירה של הרבה עגלות VCART.


### B49b — קליטת מכבסה בלי עגלה פיזית רשומה (26.07.2026)

**רקע.** דוח "ביצועי מכבסה" היה ריק כי `laundry_events` ריקה, כי לא נפתחה אף קליטת
NOBLE, כי `nobleIntake` דרש ברקוד של עגלה קיימת בסטטוס "פנויה". במציאות חלק מהכביסה
מגיעה בשקים או בערימה, בלי עגלה רשומה — ולא היה נתיב לקלוט אותה.

**החלטה ארכיטקטונית מרכזית — עגלה וירטואלית, לא מסלול קוד שני.**
`nobleWeigh` מחשב `net = gross − cart.tare_kg`; `nobleStageStart`, `nobleMarkReady`,
`nobleReleaseAll`, `nobleDeliver`, `nobleBoard`, `nobleCartInfo` ו-`nobleWeighRollup`
כולם בנויים סביב ישות עגלה ומפתח `intake_id|cart_id`. **ביטול דרישת העגלה היה מחייב
מסלול מקביל לכל השרשרת** — סיכון גבוה לסטייה עתידית בין שני המסלולים. במקום זאת:
בקליטה ידנית נוצרת אוטומטית עגלה אחת עם `virtual='כן'` ו-`tare_kg=0`, ומרגע זה
**אף שורת קוד אחרת בצינור לא השתנתה** (`nobleWeigh`/`nobleStageStart`/`nobleDeliver`
לא נגעו בהם כלל).

**מה נבנה — שרת.**
- סכימה: `TABLES.carts` קיבלה עמודה `virtual` **בסוף המערך** (מיגרציה בטוחה של
  `setupDatabase`, שכותב כותרות עמודה-עמודה). **שינוי סכימה — יש להריץ `setupDatabase`.**
- `nobleIntake` קיבלה פרמטר `manual`. כשמסומן: נוצרת עגלה `VCART-###`
  (מונה חדש `vcart_seq`) עם `status='בשימוש'`, `tare_kg=0`, `virtual='כן'`,
  `barcode=id` (אחידות עם כלל B39 — הברקוד נגזר מהמזהה). מוחזר `virtual_cart_id`.
- שילוב `manual='כן'` עם ברקודים סרוקים נחסם בשגיאה עברית מפורשת, במקום להתעלם בשקט.
- `auditNote` מציין במפורש "ידנית ללא עגלה פיזית (VCART-###)" — כדי שיומן הפעולות
  יאפשר לזהות את המקרים האלה בדיעבד.

**מה נבנה — ממשק.**
- "קליטה חדשה" (`floorIntakeTab`): checkbox "אין עגלה פיזית — קליטה ידנית", שמסתיר
  את כל אזור סריקת העגלות (`fl_cartwrap`) ומציג הסבר. `floorSaveIntake` שולחת
  `manual` ו-`cart_barcodes:[]`, וקופצת ישר לטאב "עבודה וסריקה" עם הקליטה פתוחה.
- **הפתרון לבעיית "אין מה לסרוק":** מזהה העגלה הווירטואלית משמש כברקוד
  (`nobleFindCart` כבר תמך בהתאמה לפי `id`, לא רק `barcode` — B39). בטאב "עבודה
  וסריקה" נוסף כרטיס "קליטות ידניות פתוחות" עם כפתור "פתח" לכל אחת
  (`b49bManualIntakes` → `floorOpenManual`) — אותו מסלול בדיוק כמו סריקה.
- הסתרת עגלות וירטואליות (`b49bRealCarts`) בכל ששת אתרי הצריכה שנמצאו ב-grep על
  `DB.carts`: מסך "עגלות ומכונות" (הרשת + באנר "לא תקינות"), בחירת עגלה בלוח הכביסה
  (`laundryAdvanceForm`), פיצול משימה (`laundrySplitForm`), הדפסת ברקודים,
  ובורר הנכס בתקלות/ביקורי טכנאי (B42).
- ניסוח לפי הקשר: בלוח החי ובכרטיס הקליטה מוצג "קליטה ידנית (ללא עגלה)" במקום מזהה
  טכני; תווית השקילה משתנה ל"משקל **נטו**" עם הבהרה שאין טרה להפחית.
- דוח "ביצועי מכבסה": שורה "מתוכן N קליטות ידניות · X ק\"ג" מתחת ל-KPI
  (`b49bManualIntakeSet`, נגזר מהשיוך לעגלה וירטואלית — בלי שדה חדש בקליטה עצמה).

**החלטות נוספות ולמה.**
- **`virtual` נשמר על העגלה ולא על הקליטה** — כך מסכי הציוד מסננים בשאילתה אחת,
  והקליטה נשארת אחידה (`laundry_intakes` לא קיבלה עמודה חדשה).
- **עגלה וירטואלית משוחררת חוזרת לסטטוס "פנויה"** ולא לסטטוס חדש — כדי לא להכניס
  ערך חדש למילון הסטטוסים שכל המסכים מכירים. ההסתרה נעשית לפי `virtual`, לא לפי סטטוס.
- **לא נוצר ניקוי אוטומטי** של עגלות וירטואליות ישנות — `intake_carts` ו-`laundry_events`
  מפנים אליהן, ומחיקה תשבור את המעקב ההיסטורי ואת הדוח.

**הגדלת ראש — חמש השאלות.**
1. *איפה עוד נוגע:* grep מלא על `DB.carts`/`db.carts` — 6 אתרי צריכה בממשק (כולם
   סוננו) ו-16 בשרת (כולם פועלים על מזהה או כותבים את הטבלה; אף אחד לא מונה עגלות
   כנכס). `nobleWeighRollup`/`nobleMachineLoad`/`nobleBoard` — עובדים על `cart_id`
   ולכן אדישים לסוג העגלה.
2. *מה בכל שלב:* ידנית+עגלה סרוקה → שגיאה; ידנית בלי לקוח ובלי "פנימי" → נחסם כקודם;
   שקילה עם נטו 0 → נחסם כקודם; מסירה אחרי שחרור → `nobleLastReleasedIntake` מאתר
   את הקליטה גם דרך העגלה הווירטואלית (נבדק).
3. *עקביות:* אותה טבלת `laundry_events`, אותו `nobleEvent`, אותו חישוב חיוב וכלל
   המע"מ B45 — אפס לוגיקה כספית חדשה.
4. *מי רואה:* עובד רצפה/מכבסה — הקליטה הידנית; מנהל/משרד — גם הדוח. פורטל/חנות
   (`DB=null`) — פונקציות העזר הוקשחו ל-`(DB&&DB.carts)||[]`. נהג — לא נוגע.
5. *מה נשבר:* בלי `setupDatabase` העמודה `virtual` לא תישמר והעגלות הווירטואליות
   **יופיעו** במסך הציוד. זה הסיכון היחיד — לכן ההוראה מודגשת ב-HANDOFF.

**מה נבדק. 27 בדיקות שרת ב-`vm.createContext` + 8 בדיקות ממשק — 35/35 עברו.**
קליטה ידנית יוצרת עגלה `virtual='כן'` עם `tare_kg=0` ורושמת אירוע ביומן · הצינור
המלא על עגלה וירטואלית (התחלת שלב → סיום → שקילה → מוכן) · **נטו = ברוטו** וחיוב
30×8=240 · איתור הקליטה אחרי שחרור העגלה · קליטה רגילה עם עגלה לא נשברה ולא יוצרת
עגלה וירטואלית · קליטה בלי עגלה ובלי `manual` עדיין נחסמת · `virtual` נוספה בסוף
מערך העמודות · פילטרי הממשק מסתירים וירטואליות ומציגים רק קליטות ידניות פתוחות ·
`DB` ריק לא מפיל. `node --check` על שני הקבצים — עבר.

**מה לא נבדק.** דפדפן אמיתי · נתוני ייצור · מדפסת ברקודים · התנהגות אחרי `setupDatabase`
בפועל על הגיליון החי · לוח הבקרה העסקי (`rBi`) עם קליטות ידניות.

## היסטוריה (שורה לאצווה; פירוט מלא בכותרת קובץ השרת)
B49e — איסוף עצמי מול משלוח כשני מצבים עצמאיים · חתימה מופרדת (signatures) · P7 ביטול נסיעה שלא בוצעה · תיקון פד החתימה (הוקדם מ-B51)
C1–C3, D, F1, G1 — תשתיות מוקדמות: עמודות, אימיילים, מדפים/ברקודים, תיוג, טעינה עצלה, קיצוץ טקסטים ארוכים.
NC1–NC4 — מודול מכבסה: רצפה, לוח חי, חשבונית מרוכזת, יתרות.
B1 — חשבוניות/קבלות + מספור + הקצאה (S7) + PDF ל-Drive.
B2 — מנוע תשלום ואשראי (S1/S2/S5, D7/D9/D15), אגורות, idempotency.
B3 — פקדון/אימות אשראי + ערבויות (stub סליקה; credentials ב-ScriptProperties — S4).
B4 — התראות אימייל + טריגר יומי (b4RunNow).
B5 — Driver-Lite: מסלול, הסרת כתובת, תיעוד שיחות, סטטוסים.
B7/B9/B10 — superadmin · דוח גיל חובות + חריגים · מרכז אישורים.
B13 — חתימה דיגיטלית במסירה (Bug 1 פתוח) · B14 — BI · B16b — מסירה בצילום בלי לקוח.
B20 — כרטיס עובד מרוכז (openEmp) · B23 — תפריט מובייל.
B31 — הבטחת חשבונית · B34 — סריקת העמסה · B35 — מדפים ואצוות.
B36–B37 — שעון נוכחות, מספר אישי, בורר עובד למנהל.
B38 — אבטחה: סינון pin/password_hash, אימות PIN בשרת, hard-fail, ymdLocal/dYmd, בלוק "טרם שובץ נהג".
B39–B42 — אייקוני ציוד · פיצול כביסה · סגירת איסופים תקועים · תחזוקה, תקלות, ספקי שירות.
B43 — תזרים, מועדי פירעון, חובות לקוחות (b43ReceivablesFE).
B44–B45a — כלל מע"מ אחיד.
B46 — PDF אמיתי · אריחי עובד ורכב · photo_url.
B47 — תפריט עליון בדסקטופ · B48a — תיקון פתיחת החלוניות (evtInside).
B48 — מקור אמת אחד ליתרות לקוחות (b48BalancesAg) · פנוי-על-המדף להתראת מינימום · מסנן מחסן במלאי.
B49 — מרכז דוחות: קטלוג REPORTS_CATALOG, טופס פרמטרים אחיד, goReport לדריל-דאון.
B49b — קליטת מכבסה ללא עגלה פיזית: עגלה וירטואלית (carts.virtual, tare_kg=0), בלי שינוי בשקילה/שלבים/מסירה.
B49c — גשר לוח הכביסה ← יומן NOBLE: החזרה פותחת קליטה פנימית, קידום שלב כותב לשני היומנים, L3 (ready_ts).

## פתוח לטיפול (לפי עדיפות)
0. ⭐ **B49e — איסוף עצמי בהשכרה + חתימה מכרטיס ההזמנה.** המפרט המלא בסוף המסמך.
   **הבא בתור** (הכרעת אבי 27.07.2026 — הוקדם לפני B49d כי הוא מסיר חיכוך
   שמפריע בכל בדיקה ידנית; אין תלות טכנית בין השניים).
1. ⭐ **B49d — כביסה מלקוח + כפתור ייעודי לפתיחת קליטה.** המפרט המלא בסוף המסמך.
   תלוי ב-B49c (בוצע). אחרי B49e.
1. דוחות חסרים: שעות עבודה, ניצולת השכרות, צי רכב + ייצוא CSV (כולל תזרים B43) — B50.
2. מדד ניצולת מכונות עם חלון קשיח 05:00–17:00 — להחליף לגזירה מהאירוע הראשון ביום
   (הכרעת אבי Y1) — B50.
3. ביצועים: `getAll` שולח את כל 55 הטבלאות במלואן — B51.
4. Bug 1: חתימה דיגיטלית בטאבלט — B51.
5. ניקוי גיליון `audit_log` מסיסמאות שנכתבו לפני B36 — B51.
6. מסך קיוסק — B52 (המפרט נכתב מחדש ב-BUILD_PLAN; החסימה הוסרה).
7. פריסת ניווט משולבת: שורה עליונה + סרגל צד ימני קבוע — B53.
8. כפתור השבתת ספק שירות (השדה `active` קיים, אין כפתור).
9. החלטה סופית: סדר תצוגה בחנות ובפורטל (נטו ראשי מול ברוטו ראשי).
10. דוח "עלות תחזוקה לפי חודש" מ-`fault_events` (מ-B42) — מיועד ל-B50 (סעיף 4 שם).
11. חשבוניות מכבסה מרוכזות (`nobleCreateInvoice`) אינן מפיקות מסמך PDF כלל. דורש
    החלטה: להפיק להן מסמך, ואם כן — מאיזה מקור נתונים (`laundry_intakes`).
12. `customerBalance` בשרת נותרה בלי קוראים אחרי שה-aging עבר ל-`b48BalancesAg`.
    נשמרה בכוונה כנקודת גישה נקייה ללקוח בודד. B49 לא השתמש בה (הסינון של דוח הגיל
    נעשה בממשק, על שורות `b9AgingReport`, לא בשרת) — לשקול הסרה אם B50 גם לא ישתמש.
13. ✅ **בוצע ב-B49c (27.07.2026)** — שני הזרמים חוברו: לוח הכביסה מזין את דוח
    הביצועים דרך `b49cAfterAdvance`. נותר B49d (כביסה מלקוח). התיעוד המקורי לשימור:
    **הוכרע ע"י אבי (26.07.2026).** ההכרעה: **לחבר** את
    שני הזרמים, כך שלוח הכביסה יזין את דוח הביצועים. המפרט המלא בסוף המסמך,
    תחת "מפרטים ממתינים". הרקע שהוביל להכרעה נשמר כלשונו: במערכת קיימים שני יומני אירועים נפרדים
    למכבסה — `laundry_task_events` (לוח כביסה B40, לפי הזמנה/פריט) ו-`laundry_events`
    (סריקת NOBLE ברצפת הייצור, לפי קליטת לקוח + עגלה/מכונה). דוח "ביצועי מכבסה"
    קורא רק מהשני. זה תקין ומכוון (שני זרמי עבודה שונים), אך עלול לבלבל — כדאי
    להחליט אם: (א) להשאיר כך ולהבהיר בממשק לאיזה תהליך כל דוח שייך, (ב) לחייב גם
    סריקת NOBLE בלוח הכביסה כדי שדוח הביצועים ישקף גם אותו, או (ג) לבנות דוח
    ביצועים נוסף שמבוסס על `laundry_task_events` (זמני מעבר בין שלבים בלוח
    הכביסה עצמו). לא בוצע שינוי — ממתין להכרעת אבי.
14. ✅ **בוצע ב-B49b (26.07.2026)** — קליטת מכבסה בלי עגלה פיזית רשומה.
    ההמלצה שנרשמה כאן אומתה מול הקוד החי ומומשה כלשונה (עגלה וירטואלית,
    `carts.virtual`, `tare_kg=0`, הסתרה ממסכי הציוד). התיעוד המקורי לשימור: אבחון מלא: `nobleIntake` (שרת) דורש לפחות ברקוד עגלה אחד
    שקיים בטבלת `carts` ובסטטוס "פנויה" — אין עדיין נתיב "קליטה ידנית" בלי עגלה.
    **חשוב יותר:** `nobleWeigh` (שרת) **גם הוא** תלוי בעגלה — `net = gross − cart.tare_kg`,
    כלומר משקל הטרה הוא שדה על רשומת העגלה עצמה. **מסקנה ארכיטקטונית:** לא ניתן
    סתם "לבטל" את דרישת העגלה — השקילה, מעקב השלבים (`nobleStageStart`) והמסירה
    (`nobleDeliver`) כולם בנויים סביב ישות עגלה. **המלצה לביצוע (להחלטה סופית
    בסשן הייעודי מול הקוד החי):** ליצור "עגלה וירטואלית" אוטומטית בקליטה ידנית
    (`carts` מקבלת שדה `virtual` — **שינוי סכימה**), עם `tare_kg=0`, כדי שכל שאר
    הצינור (שקילה/שלבים/מסירה) ימשיך לעבוד **בלי שכפול לוגיקה** — במקום לבנות
    מסלול קוד מקביל לשקילה/שלבים שלא תלוי בעגלה (סיכון גבוה יותר לסטייה עתידית).
    עגלות וירטואליות צריכות להיות מוסתרות ממסך "צי עגלות"/נכסים הרגיל.
    **סשן זה (B49) לא נגע בשרת ולא בזה** — הוגדר כ-batch נפרד (B49b), ריצה לפני
    B50, ממשק+שרת יחד. אבי אישר את החריגה מהכלל "batch אחד ממוקד" כי זה חוסם
    בדיקת דוחות המכבסה בפועל.
15. **חדש (B49):** מסך `bi` (לוח הבקרה העסקי) עדיין טוען את חמשת הכרטיסים שלו בנפרד
    ולא עבר לדפוס קטלוג — נשאר כך בכוונה (BUILD_PLAN B49 סעיף 3: "מסך BI נשאר מסך
    נפרד — לא נבלע"). לא פריט לטיפול, לתיעוד בלבד.

## מפרטים ממתינים
- `BUILD_PLAN.md` בידע הפרויקט: B50 · B51 · B52 · B53 — כתובים במלואם, כולל הכרעות
  ארכיטקטוניות. (B46, B48, B48a, B49, B49b, B49c בוצעו.)
- **מפרט B49d** — בסוף מסמך זה. הבא בתור.
- ⚠ **לקח מ-B48, אומת מחדש ב-B49:** סעיפי "מצב קיים" ב-`BUILD_PLAN` עלולים לתאר קוד
  שהתיישן. תמיד לאמת את העוגנים מול הקוד החי לפני שבונים.
- ⚠ **תלות מחייבת שנוצרה ב-B48, לא נשברה ב-B49:** `b48BalancesAg` (שרת) ·
  `custBalance` (ממשק) · `b2CreditUsedAg` (מנוע האשראי) — שלושתם חייבים להחזיר אותו
  מספר. B49 לא נגע בשרת ולא בפונקציות האלה.
- כלל בדיקה מחייב שנוסף ב-B48a, יושם גם ב-B49: **כל בדיקת ניווט/קליק מדספאטצ'ת אירוע
  DOM אמיתי (`MouseEvent`/`change` עם `bubbles:true`).** בדיקה שקוראת לפונקציית ה-handler
  ישירות אינה קבילה.
- **חדש (B49) — לב הרחבה עתידית ל-B50:** `REPORTS_CATALOG`/`REPORT_RENDERERS`/
  `repParamsForm` בנויים כך שדוח נוסף = רשומה בקטלוג + פונקציית render + (אופציונלי)
  הגדרת פרמטרים — בלי לגעת ב-`rReports` עצמה. B50 יוסיף שלוש רשומות (`rep_hours`,
  `rep_utilization`, `rep_fleet`) בדיוק בדפוס הזה.
- כיוון עתידי (לא מתוכנן): פיצול לאפליקציות ייעודיות לפי סיווג (הנהלה/נהגים/ייצור).

---

## מפרט B49d — כביסה מלקוח + כפתור ייעודי

נכתב 26.07.2026 אחרי הכרעת אבי. **B49c מתוכו בוצע ב-27.07.2026** — הסעיף שלו נשמר
למטה כתיעוד היסטורי בלבד, אין לבנות אותו שוב.

### הכרעות אבי (מחייבות)
- **L1** — שלוש נקודות כניסה יוצרות קליטת NOBLE: (א) החזרת טקסטיל MAPA מהזמנה,
  (ב) הזמנת לקוח מסוג `כביסה` שנאספת, (ג) כפתור ייעודי לפתיחה ידנית.
- **L3 (27.07.2026, מכריע) — ביטול התלות במסירה.** מפות של MAPA **חוזרות למפעל
  ואינן "נמסרות"**. היום `renderNobleReports` סופר ק"ג, הכנסות וקליטות **רק
  מקליטות עם `delivered_ts`** — ולכן גם אחרי חיבור הזרמים הדוח היה נשאר מאופס.
  **הכלל החדש:** כביסה פנימית (`internal='כן'`) נספרת כשהגיעה ל**"מוכן"**;
  `delivered_ts` נשאר תנאי **רק לכביסת לקוח חיצוני** (שם המסירה היא אירוע אמיתי
  ומחויב). לתעד את ההגדרה בכותרת הדוח.
- **L2** — גרעיניות: **אין כלל קשיח.** ברירת מחדל קליטה אחת לאירוע (החזרה/איסוף),
  עם אפשרות להוסיף לקליטה פתוחה קיימת. **אין תקרה** על מספר עגלות, יחידות או משקל.

### ראיה קשה מייצוא בסיס הנתונים (27.07.2026) — אין לחקור מחדש
אבי ייצא את הגיליון החי. הספירה בפועל:
`laundry_tasks`=3 · `laundry_task_events`=5 · **`laundry_intakes`=0 · `intake_carts`=0 ·
`laundry_events`=0**. כלומר: מסך "רצפת ייצור" (NOBLE) **מעולם לא היה בשימוש**;
כל העבודה נעשית בלוח הכביסה. זו אינה השערה — זו ספירה.

רצף העבודה המתועד למשימה `WASH-MS1PC3KXGC001` (הזמנה `ORDE-MS1LGW4DFQ001`, 10 יח'):
```
14:15:36  נקלט מהחזרה  → התקבל
14:16:46  קידום שלב     → בכביסה         CART-002 · MACH-002 · אברהם
14:17:22  קידום שלב     → בייבוש         CART-002 · MACH-003 · אברהם
14:19:41  קידום שלב     → בגיהוץ וקיפול  CART-002 · MACH-006 · אברהם
14:20:42  סיום          → מוכן           MACH-006 · מדף 2 · אברהם
```
**מסקנה: כל נתוני דוח הביצועים (מכונה · עובד · חותמות זמן) כבר נאספים — ליומן הלא-נכון.**

**שתי מלכודות בנתונים הקיימים:**
- שתי משימות (`WASH-MS19HPB8KU001`, `WASH-MS1B206ZJL001`) נמצאות בשלב "מוכן"
  **בלי אף אירוע ובלי `cart_id`/`machine_id`**. הדוח חייב לסבול משימות חסרות
  היסטוריה בלי ליפול ובלי לחשב מהן זמנים.
- טבלת `carts` בייצוא **עדיין בלי עמודה `virtual`** — כלומר B49b טרם נפרס.
  אין להתחיל B49c לפני שאבי פרס אותו והריץ `setupDatabase`.

### הממצא המכריע (אומת בקוד, אין לחקור מחדש)
`receiveReturn` (שרת, ~3560) הוא **המקור היחיד** ליצירת `laundry_tasks` — אין יצירה
ידנית. `advanceLaundry` (~3607) כותב **רק** ל-`laundry_task_events`; `renderNobleReports`
קורא **רק** מ-`laundry_events`. **זהו כל שורש הבעיה.**
לטובתנו: B40 כבר אוכף עגלה+מכונה בכל מעבר שלב — כלומר כל הנתונים שהדוח צריך
(מי · איזו מכונה · מתי התחיל · מתי סיים) **כבר נרשמים**, רק ליומן הלא-נכון.

### ✅ B49c — החזרות + גשר אירועים — **בוצע 27.07.2026. תיעוד היסטורי, לא לביצוע**

1. **קליטה אוטומטית בהחזרה.** ב-`receiveReturn`, אחרי יצירת ה-`laundry_tasks`:
   קליטה אחת עם `internal='כן'`, `customer_id=''`, מקושרת ל-`order_id`.
   **משתמשים בעגלה הווירטואלית של B49b** (`virtual='כן'`, `tare_kg=0`) — בשלב ההחזרה
   עדיין אין עגלה פיזית משויכת. זו בדיוק הסיבה ש-B49b רץ ראשון.
   שדה קישור חדש על `laundry_tasks`: `intake_id` — **שינוי סכימה, להריץ setupDatabase.**
2. **גשר האירועים (הלב).** `advanceLaundry` תמשיך לכתוב ל-`laundry_task_events`
   כרגיל (אין רגרסיה ביומן המשימה), **ובנוסף** תקרא ל-`nobleEvent` עם
   `intake_id` מהמשימה, `cart_id`/`machine_id` שכבר נבחרו, ו-`event_type`
   'התחלה'/'סיום' לפי המעבר. **חוק ברזל: אין לשכפל את לוגיקת המעבר** — קריאה
   אחת נוספת בסוף הפונקציה הקיימת.
3. **שיוך עגלה אמיתית.** כשמוקצית עגלה למשימה (`assignCart`), אם הקליטה עדיין על
   עגלה וירטואלית — לשייך את האמיתית ולשחרר את הווירטואלית.
4. **סגירת הקליטה** כשכל משימות ההזמנה הגיעו ל'מוכן'.

4b. **יישום L3 בדוח** (`renderNobleReports`, ~5923): הסיכומים עוברים מ-`delivered_ts`
   ל"תאריך ההשלמה" — `delivered_ts` לכביסת לקוח, מעבר ל'מוכן' לכביסה פנימית.
   מצב ריק נשאר רק כשאין **שום** אירוע. משימות בלי אירועים (ראה מלכודות) נספרות
   בכמויות אך לא בחישובי זמן.

**מה אסור לגעת בו:** `receiveReturn` בחלק החוסרים והמלאי · `laundry_task_events`
(היומן הקיים נשאר במלואו) · `nobleWeigh`/`nobleIntake` · מנוע האשראי · `API_URL`.

**בדיקות קבלה:** החזרה יוצרת קליטה פנימית אחת · קידום שלב בלוח כותב לשני היומנים ·
דוח הביצועים מציג זמני עובדים וניצולת מכונות מהלוח · יומן המשימה הקיים ללא שינוי ·
החזרה בלי פריטים לא יוצרת קליטה ריקה · **L3: כביסה פנימית שהגיעה ל"מוכן" בלי מסירה
נספרת בדוח** · fixture עם משימה חסרת אירועים (כמו PKG-1/PKG-2) לא מפיל את הדוח ·
**רגרסיה על הנתונים האמיתיים:** משימה `WASH-MS1PC3KXGC001` מייצרת ארבעה אירועים
ומופיעה בדוח עם MACH-002/003/006 ועם העובד אברהם.

**היקף: גדול | מודל: Sonnet | קבצים: שניהם | שינוי סכימה: יש.**

### B49d — כביסה מלקוח + כפתור ייעודי

1. הזמנה מסוג `כביסה` (קיים; ראה שרת ~590 — `delivery_fee>0` מייצר איסוף/מסירה):
   כשהאיסוף מסומן שבוצע → קליטה עם `customer_id` ו-`price_per_kg` מכרטיס הלקוח.
2. כפתור "פתח קליטה" במקום נוח (לוח כביסה / מסך משלוחים), שפותח את אותו טופס
   של `floorIntakeTab` — **שימוש חוזר, לא טופס שני.**
3. אפשרות הוספה לקליטה פתוחה קיימת (הכרעה L2).

**היקף: בינוני | מודל: Sonnet | תלוי ב-B49c.**

---

## מפרט B49d — כביסה מלקוח + כפתור ייעודי

**הבא בתור.** המפרט המלא נשמר בגרסאות הקודמות של קובץ זה; הכרעות אבי והראיה
מייצוא בסיס הנתונים מופיעות בסעיפים למעלה. B49d תלוי ב-B49c (בוצע).

**⚠ עדכון אחרי B49e:** `b38PlannedDeliveries` כבר אינה נגזרת מ-`delivery_fee`.
כל תכנון של "כביסה מלקוח שנאספת" חייב להישען על `ship_out`/`ship_back`
ולא על מחיר השינוע — ההסקה מהעמלה בוטלה בהכרעת אבי P2.

## מפרט B51 — עדכון היקף אחרי B49e

**חלק 2 (באג החתימה) — בוצע ב-B49e.** אין לבנות אותו שוב.
B51 מצטמצם לשני חלקים: קיצוץ `getAll` (`SLIM_POLICY`) וניקוי `audit_log`
מסיסמאות (`b51ScrubAudit`).

⚠ **תוספת ל-`SLIM_POLICY` של B51:** טבלת `signatures` החדשה של B49e צומחת עם
כל מסירה. היא נצרכת ע"י `b49eProofsOf` בכרטיס ההזמנה בלבד. אם תיחתך ל-90 יום —
הוכחות ישנות ייעלמו מהכרטיס. ההמלצה: לא לחתוך אותה, או להעביר את `b49eProofsOf`
לפעולת שליפה ייעודית.
