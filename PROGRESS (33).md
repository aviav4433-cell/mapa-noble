# MAPA-NOBLE — מצב המערכת

## מצב נוכחי
canary ממשק: **v4.38-B46** | canary שרת: **v4.38-B46** | עדכון אחרון: 26.07.2026
שתי הגרסאות מיושרות — B46 נגע בשני הקבצים.

**מה עובד:** כל הליבה — הזמנות, מלאי רב-מחסני, משלוחים ומסלולי נהג (Driver-Lite B5),
החזרות ושקילה, מודול מכבסה מלא (NC1–NC4), רצפת ייצור, משימות, פגישות, פורטל לקוח,
חנות ציבורית, מנוע תשלום ואשראי (B2), פקדון/ערבויות (B3), התראות אימייל (B4),
חשבוניות עם מספור רציף ומספרי הקצאה (B1/B31), דוח חובות לפי גיל ודשבורד חריגים (B9),
מרכז אישורים (B10), BI (B14), מדפים (B35), שעון נוכחות ושכר (B36–B37), אבטחת שדות
רגישים (B38), תחזוקת ציוד ותקלות (B42), תזרים וחובות עם מועדי פירעון (B43),
סריקת העמסה מרובה (B44), כלל מע"מ אחיד (B45), תפריט עליון בדסקטופ (B47),
**מסמכים נשמרים כ-PDF אמיתי + אריחי עובד ורכב עם תמונות (B46)**.

**מה שבור:**
1. 🟡 Bug 1: חתימה דיגיטלית בטאבלט לא נרשמת כראוי (מ-B13). → B51.

**ממתין לאימות אבי:**
1. ⚠ **הפעלת שירות Drive API בעורך Apps Script** — בלי הצעד הזה תיקון ה-PDF לא פועל
   (הממשק יציג אזהרה במקום להיכשל בשקט).
2. הרצת `setupDatabase` (נוסף `photo_url` ל-`employees` ול-`vehicles`).
3. הרצת "תיקון מסמכים שנשמרו כ-HTML" במסך ההגדרות, להמרת המסמכים הישנים.
4. הפקת חשבונית חדשה ובדיקה ב-Drive שהקובץ הוא PDF.
5. מראה תפריט B47 בדפדפן אמיתי בכמה רוחבי מסך (נבדק רק ב-jsdom).

## שלושת ה-batches האחרונים

### B46 — PDF אמיתי + אריחי עובד ורכב + תמונות (26.07.2026)
**מה נבנה.** (א) `invoiceSaveToDrive` נכתבה מחדש עם זיהוי יכולות: `b46DriveCaps()`
מזהה Drive v3 (`Drive.Files.create`) / v2 (`Drive.Files.insert`) / שירות כבוי, וההמרה
עברה לפונקציה משותפת `b46HtmlToPdf`. ה-fallback ל-HTML נשמר אך **מדווח על עצמו** דרך
`PDF_LAST_FALLBACK` → `pdf_fallback` בתשובה, והממשק מציג אזהרה חוסמת (`b46PdfWarn`)
בשלוש זרימות הפקת המסמכים. (ב) `b46FixPdfDocs` — תיקון רטרואקטיבי באצוות של 30 עם
`more:1`, אידמפוטנטי, כפתור במסך ההגדרות (`b46FixPanelHtml` / `b46RunFix`) שקורא
בלולאה עד סיום. (ג) `photo_url` נוסף ל-`employees` ול-`vehicles`; פעולות
`b46UploadEmpPhoto` / `b46UploadVehPhoto` לפי דפוס `uploadReceipt`. (ד) `rStaff`
ו-`rFleet` הומרו לדפוס האריח האחיד `.eqgrid`/`.eqtile`; נוסף כרטיס רכב `openVeh`;
תמונה נוספה לראש `openEmp`.

**החלטות שהתקבלו ולמה.**
- **חתימת `invoiceSaveToDrive` לא שונתה** (חמישה אתרי קריאה) — דיווח ה-fallback דרך
  דגל מודולרי ולא דרך שינוי טיפוס ההחזרה.
- **התיקון הרטרואקטיבי הורחב גם לקבלות** — מעבר ל-BUILD_PLAN, שהניח שאין להן מקור
  שחזור. בפועל `b2GetReceiptLink` כבר משחזר קבלה מטבלת `payments`, וקבלות הן מחצית
  המסמכים השבורים.
- **חשבוניות מכבסה מרוכזות (`nobleCreateInvoice`, ללא `order_id`) מדולגות במפורש**
  ומדווחות כ-`no_source` — אין להן שורות ב-`orderLines`, ובנייה מחדש הייתה מפיקה
  מסמך ריק. זה הסיכון האמיתי בכל תיקון רטרואקטיבי.
- **האזהרה חוסמת ולא toast** — הכישלון השקט הוא מה שהסתיר את הבאג חודשים.
- **גם רכב מקבל תמונה** (לפי BUILD_PLAN) — אותו דפוס, מונע batch שלישי.
- כפתורי עריכה/איפוס סיסמה **לא שוכפלו על האריח** — הם קיימים בכרטיס העובד.
- הפעולות החדשות ב-`MANAGER_ONLY`, אף אחת לא ב-`READ_ONLY_ACTIONS`.

**מה נבדק.** 94 בדיקות אוטומטיות עברו (37 שרת ב-`vm.createContext` עם Drive מדומה
בשלושת המצבים v3/v2/כבוי; 57 ממשק ב-jsdom על גוף העמוד האמיתי). `node --check` על
שני הקבצים. שלושת הקשרי הריצה נטענים בלי חריגה. אומת ש-`case 'update'` בשרת עושה
**מיזוג** (`Object.keys(p.row).forEach`) ולכן `saveEmp`/`saveVeh`, שאינם שולחים
`photo_url`, אינם מוחקים את התמונה — זה היה הסיכון הגדול ב-batch.

**מה לא נבדק.** התנהגות Drive האמיתי (מדומה בלבד) · המרת HTML→PDF בעברית RTL בפועל ·
מראה האריחים בדפדפן אמיתי · תיקון רטרואקטיבי על היקף אמיתי של מסמכים.

### B47 — תפריט ניווט עליון בדסקטופ (26.07.2026)
סרגל הצד (228px) הוחלף בשורת תפריט אופקית sticky עם חלוניות. 'דוחות' יצא לקטגוריה
עצמאית (הכנה ל-B49). מובייל ונהג — ללא שינוי. הוחלט: `NAV_DD` נפרד מ-`NAV_OPEN`;
פריטי קטגוריה תמיד ב-DOM (`.gmenu`). נבדק: 139 בדיקות jsdom. לא נבדק: פריסה חיה.

### B45 + B45a — כלל מע"מ אחיד (שרת+ממשק, ואז תיקון ממשק)
כל סכום נקלט ומוצג נטו; הכולל בכתב קטן לצידו. חריג: מסמך חשבונית/קבלה.

## היסטוריה (שורה לאצווה; פירוט מלא בכותרת קובץ השרת)
C1–C3, D, F1, G1 — תשתיות מוקדמות: עמודות, אימיילים, מדפים/ברקודים, תיוג, טעינה עצלה, קיצוץ טקסטים ארוכים.
NC1–NC4 — מודול מכבסה: רצפה, לוח חי, חשבונית מרוכזת, יתרות.
B1 — חשבוניות/קבלות + מספור + הקצאה (S7) + PDF ל-Drive.
B2 — מנוע תשלום ואשראי (S1/S2/S5, D7/D9/D15), אגורות, idempotency.
B3 — פקדון/אימות אשראי + ערבויות (stub סליקה; credentials ב-ScriptProperties — S4).
B4 — התראות אימייל + טריגר יומי (b4RunNow).
B5 — Driver-Lite: מסלול, הסרת כתובת, תיעוד שיחות, סטטוסים.
B7/B9/B10 — superadmin · דוח גיל חובות + חריגים · מרכז אישורים.
B13 — חתימה דיגיטלית במסירה (Bug 1 פתוח) · B14 — BI · B16b — מסירה בצילום בלי לקוח.
B20 — כרטיס עובד מרוכז (openEmp) · B23 — תפריט מובייל.
B31 — הבטחת חשבונית · B34 — סריקת העמסה · B35 — מדפים ואצוות.
B36–B37 — שעון נוכחות, מספר אישי, בורר עובד למנהל.
B38 — אבטחה: סינון pin/password_hash, אימות PIN בשרת, hard-fail, ymdLocal/dYmd, בלוק "טרם שובץ נהג".
B39–B42 — אייקוני ציוד · פיצול כביסה · סגירת איסופים תקועים · תחזוקה, תקלות, ספקי שירות.
B43 — תזרים, מועדי פירעון, חובות לקוחות (b43ReceivablesFE).
B44–B45a — ראה למעלה · B47 — ראה למעלה.
B46 — PDF אמיתי (Drive v3/v2 + fallback מדווח + תיקון רטרואקטיבי) · אריחי עובד ורכב · photo_url.

## פתוח לטיפול (לפי עדיפות)
1. איחוד אלגוריתם חובות הלקוחות (שרת מול `b43ReceivablesFE`) — B48.
2. דגל "מתחת למינימום" מחושב כולל−כביסה בלי שריונים; "פנוי" במסך המלאי לא מסונן לפי מחסן — B48.
3. מרכז דוחות (קטגוריה עצמאית כבר מוכנה מ-B47) — B49.
4. דוחות חסרים: שעות עבודה, ניצולת השכרות, צי רכב + ייצוא CSV (כולל תזרים B43) — B50.
5. מדד ניצולת מכונות עם חלון קשיח 05:00–17:00 — להחליף לגזירה מהאירוע הראשון ביום (הכרעת אבי) — B50.
6. ביצועים: `getAll` שולח את כל 55 הטבלאות במלואן (fault_events / date_changes / package_moves / laundry_events / audit_log) — B51.
7. Bug 1: חתימה דיגיטלית בטאבלט — B51.
8. ניקוי גיליון `audit_log` מסיסמאות שנכתבו לפני B36 — B51 (פעולת שרת, לא ידני).
9. מסך קיוסק — B52 (המפרט נכתב מחדש ב-BUILD_PLAN; החסימה הוסרה).
10. כפתור השבתת ספק שירות (השדה `active` קיים, אין כפתור).
11. החלטה סופית: סדר תצוגה בחנות ובפורטל (נטו ראשי מול ברוטו ראשי).
12. דוח "עלות תחזוקה לפי חודש" מ-`fault_events` (מ-B42).
13. חשבוניות מכבסה מרוכזות (`nobleCreateInvoice`) אינן מפיקות מסמך PDF כלל — אין להן
    `pdf_url` ואין להן שורות לשחזור. נמצא ב-B46. דורש החלטה: להפיק להן מסמך, ואם כן —
    מאיזה מקור נתונים (`laundry_intakes`).

## מפרטים ממתינים
- `BUILD_PLAN.md` בידע הפרויקט: B46 · B48 · B49 · B50 · B51 · B52 — כתובים במלואם, כולל הכרעות ארכיטקטוניות.
- כיוון עתידי (לא מתוכנן): פיצול לאפליקציות ייעודיות לפי סיווג (הנהלה/נהגים/ייצור) מטעמי אבטחת מידע — הכרעת אבי 26.07.2026.
