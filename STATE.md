# MAPA-NOBLE — STATE

**קובץ יחיד, מתגלגל, תקרה 400 שורות.** מחליף את `CLAUDE.md` + `PROGRESS.md` +
`TASK_QUEUE.md`. כל תיקון מעדכן אותו ומחזיר אותו **שלם**. פירוט מלא ישן נשאר
בהיסטוריית git (`git log`), לא כאן.

---

## 1. מצב נוכחי

canary ממשק: **v4.84-B84** | canary שרת: **v4.83-B83** | עדכון: 03.09.2026
בדיקות: **785, 0 כשלים** (`node tests.js`, אחרי `npm i jsdom`).

**מה עובד:** כל המערכת עד B83, ובנוסף B84 — מד ההתאמה האמיתי ל-1024px
(`b84ProbeMain`, מכווץ את `main` בפועל ומודד דרישה, במקום למדוד רוחב חלון)
וכיווץ רצועת הצד (61px שחסרו בחלון 650px, בלי לוותר על קטגוריה).

**⛔⛔ פרט לבדיקת `BLD-05` — אין פריט פנוי בתור.** ראו סעיף 5.

**⚠ ממתין לאבי (בדיקה בדפדפן אמיתי, לא קוד):**
- הרצה חדשה של "בדיקה עצמית" ← "התאמה ל-1024px" (הכלי הקודם היה שבור — ראו B84 בסעיף 7).
  ⛔⛔ אם כל המסכים מחזירים אותו מספר בדיוק — הכלי עדיין שבור, לעצור ולהתריע.
- מסך שכר/נוכחות/כרטיס עובד: הבזק "טוען נתוני נוכחות ושכר" בכניסה ראשונה = תקין; נתקע = באג.
- מסכי נוכחות/שכר של **הנהג** חייבים להציג נתונים, לא ריק.
- טאב "הוצאות עתידיות" בכספים: סיכום טיוטות שכר ≠ ₪0.
- דוח ביצועי מכבסה: מדד Y1 זהה לקודם.
- דוח "מלאי ממוספר לפי סטטוס" (דוחות←תפעול): ריק = לא נרשמו יחידות, לא באג.
- אישור הצהרת תשלום יפיק **פחות** מסמכים מעכשיו (DEC-04) — זו התנהגות מוכרעת, לא תקלה.
- בורר "שיוך התשלום" חייב ברירת מחדל "אוטומטי".
- ניקוי `audit_log` מסיסמאות — הכפתור קיים, טרם הופעל בפועל.

---

## 2. ארכיטקטורה במספרים (מהקוד החי, לא מהזיכרון — ספרו שוב אם עבר זמן)

- שרת: Google Apps Script, קובץ יחיד `קוד שרת.txt` (~11,450 שורות). כניסה יחידה `doPost→route→handle`.
- נתונים: Google Sheets, **55 טבלאות** (אובייקט `TABLES` בראש קובץ השרת = מקור האמת היחיד).
  `setupDatabase` הוא נתיב היצירה היחיד; רק אחרי אישור מפורש "מאשר שינוי סכימה".
- ממשק: `index.html` יחיד (~15,700 שורות), RTL, GitHub Pages. **29 מסכים** ב-`VIEWS`.
- 5 תפקידים: מנהל · משרד · מכבסה · עובד רצפה · נהג.
- 4 הקשרי ריצה: צוות · `?portal=1` · `?shop=1` · `?kiosk=1`.
- `tests.js` בשורש הריפו (~11,460 שורות), חלקים `t01`…`t29+`, כל בדיקה מתויגת `[מודד]`/`[שומר]`.

---

## 3. מנגנונים חסינים — אסור לגעת בלי בדיקה נגדית מפורשת

רשימת-על (R4 בפרוטוקול העבודה). **לפני נגיעה בכל אחד — `grep` בקוד החי ובחלק
המתאים ב-`tests.js`; הרשימה כאן היא מפה, לא תחליף לקריאת השומר עצמו.**

| תחום | מנגנון | חלק ב-tests.js |
|---|---|---|
| ליבה | `API_URL` · מנוע האשראי/תשלום · `b54Ledger` · `toAg`/`fromAg` | t03 |
| שכר/נוכחות | `clockCore` · `attEff` · `buildPayrollForMonth` · `b67PayOneRow` (מסלול כתיבה יחיד) · `B67_WRITE_DEFER` | t11 |
| מע"מ | כלל B45 (נטו ראשי) · `b12Gross/Net/PriceInline/PriceStack` (חנות/פורטל בלבד) | t07 |
| ניווט | `allowedViews`/`navGroupOf`/`navQuickKeys` · `DRIVER_TAB_ORDER` · `#shell`/`navIsSide`/`NAV_MODE`/`nav_rail` · `NAV_DD`/`.gmenu` (900–1099px) · אקורדיון מובייל | t06 |
| קיוסק | טוקן `kiosk_token_hash_<n>` · `B52_KIOSK_ACTIONS` (רשימה סגורה) | — |
| ציר סטטוס | **`w24Stat(rec,LIST,dflt)` הוא המנרמל היחיד** לכל טבלה (הזמנות/חשבוניות/משלוחים/כסף/ציוד/יחידות...) · `w25Usable` לשערי "ריק מותר" · `sVal`/`sPick` זהים תו-בתו בשני הקבצים, **אסור על כסף/משקל/מזהים** | t08, t27 |
| כביסה (NOBLE) | `nobleWeighRollup` (מצטברת, לא דורסת) · `laundry_events` **append-only** · `nobleWeighFix` מסלול התיקון היחיד · כסף כביסה **באגורות שלמות** (`w10Cent` וכו') | t16, t17 |
| מלאי יחידות | שער מחיקה: `UNIT_BUSY`+לא-מוכר חוסמים, **ריק לא חוסם** | t27 |
| רכב/מכבסה | הפרדה מלאה בין תקלות רכב לתקלות מכונות (R5) — `carts.condition` ≠ `status` | t27 |
| נתונים כבדים | `SLIM_POLICY.attendance/payroll = {onDemand:true}` — לא `drop`, לא חיתוך לפי ימים · חריג נהג ב-`getDriverFilteredPayload` | t29 |
| אבטחה | `READ_ONLY_ACTIONS` (פעולת כתיבה לעולם לא נכנסת) · `IDLE_LIMIT_MS` | — |
| היסטוריה | `history.back()` **אסור לחלוטין**, סריקת קוד מקור נכשלת אם יחזור | t01/t02 |
| כללי-על | **R1** — אבי אינו מייצא/שומר DB; חסר מידע → בונים פעולת אבחון READ_ONLY בתוך המערכת · **R6** — `b48BalancesAg`≡`custBalance`/`b48BalancesAgFE`≡`b2CreditUsedAg` תמיד שווים · **R8** — אין דפוס שני לנקודת הרחבה קיימת (`REPORTS_CATALOG`, `exportCsv`, `moreMenu`, `B52_KIOSK_ACTIONS`) · **R10** — לצמצם מספר בקשות HTTP, לא רק משקל | t03/t29 |

### 3א. מפת זרימת הכסף — עדכון חובה בכל תיקון שנוגע בכסף (R6 מורחב)

**האמת האדריכלית:** אין טבלת-על אחת. `b54Ledger(db)` היא **פונקציה מחושבת**
שקוראת מחדש בכל הרצה מ-5 טבלאות מקור: `orders`/`orderLines` · `laundryIntakes`
· `charges` · `invoices` · `payments`. שינוי שלא מגיע אליה, או צרכן שלא קורא
ממנה, נשארים לא-מסונכרנים בשקט — זה קרה בפועל ב-B77–B82.

| מקור/צרכן | קורא מ- | מוזן אוטומטית? |
|---|---|---|
| **`b54Ledger`** — מקור האמת לחיוב | 5 הטבלאות למעלה, ישירות | — (זו נקודת הכניסה) |
| `b48BalancesAg`/`b48CustomerBalanceAg` (שרת) | → `b54Ledger` | ✅ עוטף ישיר |
| `custBalance`/`b48BalancesAgFE` (ממשק) | → `b48BalancesAg` | ✅ עוטף ישיר |
| `b2CreditUsedAg` (מנוע אשראי) | **מחושב עצמאית** | ⚠ חייב שוויון ידני — זו בדיקת R6 הליבה |
| `b43Receivables` | → `b54Ledger` + מועדי פירעון | ✅ עוטף ישיר |
| `b9AgingReport` (דוח גיל חובות) | → `b43Receivables` | ✅ |
| `b14Cashflow` (BI, נכנס) | → `b43Receivables` | ✅ |
| `b9ExceptionsDashboard` | לוגיקה/נתונים קיימים בנפרד | ⚠ לבדוק נקודתית לפי סוג החריג |
| `b14Revenue` (BI, הכנסות) | **ישירות מ-`db.invoices`**, לא דרך `b54Ledger` | ⛔ **לא** — הבדל עסקי מכוון (הכנסה מוכרת ≠ יתרה פתוחה), אבל תלוי ב-`w23InvActive` שנשאר עקבי |
| `fexpOpen`/`fexpRemain` (תזרים יוצא) | טבלת `futureExpenses` **נפרדת** | ⛔ **לא מסונכרן עם `b54Ledger` בכלל.** גשר ידני קיים רק לשכר (`syncFutureExpenseForPayroll`, B67/B78). כל מקור הוצאה חדש דורש גשר דומה בפירוש |
| `finPayrollTotal` | → `fexpOpen` (סינון `source==='payroll'`) | ✅ בתוך תזרים בלבד |

**כלל עבודה:** תיקון שמוסיף מקור כסף חדש (טבלה/שדה) חייב לענות: (1) האם
`b54Ledger` צריך לדעת עליו? (2) האם התזרים (`fexpOpen`) צריך לדעת עליו?
(3) האם BI-הכנסות (`b14Revenue`) צריך לדעת עליו? — ולתעד תשובה לשלושתם,
גם אם התשובה "לא רלוונטי במכוון".

---

## 4. באגים פתוחים ידועים

- אין באג פתוח פעיל שלא טופל נכון לרגע זה — ראו סעיף 1 לפריטי אימות תלויי-אבי.

## 5. תור עבודה — מה מותר לפתוח

**⛔⛔ אין לבחור פריט עצמאית ואין להמציא היקף. אין לפתוח פריט סכימה בלי
שאבי כתב במפורש "מאשר שינוי סכימה".**

| פריט | מה | חוסם |
|---|---|---|
| `BLD-05` | הורדת נקודת שבירה מ-1100 ל-1024px | **מספר מהבדיקה העצמית של אבי**, טרם התקבל תקין |
| `BLD-13` | תכנון עומס מכבסה | שינוי סכימה (`orders`+עמודת אומדן משקל) — אבי דחה לעת עתה |
| `BLD-10ב` | ניצולת ברמת יחידה ממוספרת | שינוי סכימה (יומן תנועות יחידה / `unit_id`) |
| `BLD-07` | תחזוקת רכב שלב ב' | שינוי סכימה (שיוך הוצאה לרכב) |
| `BLD-11` | יתרת פתיחה תקופתית ל-`stock_moves` | שינוי סכימה, סיכון גבוה — תמיד אצווה לבד |

**עכשיו: שלב תיקונים נקודתיים.** אבי מריץ את המערכת ומדווח באג אחד בכל פעם.
צ'אט תיקון אינו ממתין לתור למעלה אלא מטפל בדיווח שהובא אליו.

## 6. החלטות עסקיות פתוחות (DEC)

- **DEC-02** — נוכחות היסטורית שגויה משויכת (מבאג B37, לפני B52): אין דרך
  אוטומטית לזהות אילו רשומות; אבי צריך להחליט אם לתקן ידנית ואילו חודשים.
- **DEC-03** — סדר תצוגת מחיר בחנות/פורטל (נטו ראשי מול ברוטו ראשי): הוכרע
  ב-DEC-01 לטובת **כולל מע"מ ראשי** (`BLD-12`, בוצע ב-B64) — לתעד שנסגר אם
  אבי מאשר, אחרת נשאר פתוח כניסוח.
- **DEC-04** — הפקת חשבוניות סלקטיבית על תשלום חלקי: **הוכרעה ובוצעה ב-B82**
  (`b82ParseTarget`/`b82DebtSort`). ממתינה רק לאימות אבי בייצור (סעיף 1).

## 7. 5 התיקונים/אצוות האחרונות (הישן ביותר יורד כשנכנס חדש)

- **B84** (18.08) — `b84ProbeMain`: מדידת התאמה אמיתית ל-1024px (כיווץ סינכרוני
  של `main`, שחזור ב-`finally`) + כיווץ רצועת הצד. גילה שהכלי הקודם (B65) דיווח
  רוחב חלון ולא דרישה אמיתית — פעם שלישית שהכלי עצמו חוסם את `BLD-05`.
- **B83** (18.08) — `BLD-09`+`BLD-10`: נוכחות/שכר לפי דרישה (`SLIM_POLICY.onDemand`)
  עם חריג נהג; דוח "מלאי ממוספר לפי סטטוס". גילה שהנהג היה מקבל מסכים ריקים בשקט.
- **B82** (18.08) — `W23-שאריות`+`BLD-06`: סגירת ציר הסטטוס הכללי; תיקון הפקת-יתר
  של חשבוניות על תשלום חלקי (DEC-04).
- **B81** (18.08) — `WASH-23ג` חלק ג': ציר סטטוס לתשע טבלאות אחרונות; תוקן שער
  מחיקת יחידות שאיפשר מחיקת מלאי אצל לקוח בטעות.
- **B80** (18.08) — `WASH-23ג` חלק ב': ציר סטטוס לטבלאות הכסף והשערים; גילה
  ש-`charges`/`payments` עצמם לא היו מנורמלים.

---

**כללי תחזוקת הקובץ:** כל תיקון מוסיף שורה לסעיף 7 ומוריד את הישנה ביותר
(רשימה נגררת של 5). מעדכן סעיפים 1/4/5/6 לפי הצורך. **אין היסטוריה מצטברת —
זו הסיבה ש-PROGRESS.md הישן תפח ל-3000+ שורות.** הפירוט המלא של כל אצווה
עדיין קיים ב-`git log`/`git show` על קובץ זה בגרסאות קודמות, לא כאן.
